# V2 内容社区与 RAG 详解【知识词典】

> 配套主文档：`V2内容社区与RAG详解.md`
> 收录 V2 涉及的全部概念，与《Spring AI 从零到实战【知识词典】》《V1.5工程基建详解【知识词典】》不重复。

## 目录

- 一、内容社区与 Feed 流
  - UGC / 种草 / 瀑布流 / 游标分页 / 二元组游标 / N+1 问题 / 幂律分布 / 热度公式 / 软删除
- 二、互动与幂等
  - 唯一索引幂等 / DuplicateKeyException / 先查后插的竞态 / 原子自增 / 丢失更新 / 冗余计数
- 三、RAG 核心
  - 语义鸿沟 / Chunking（切片）/ 重叠（overlap）/ Embedding / 余弦相似度 / L2 归一化 / 2-gram 哈希向量 / BM25 / Hybrid 检索 / RRF / Top-K 与 Candidate-K / 幂等索引（upsert）/ 脏语料
- 四、Agent 分流
  - 双工具路由 / 工具描述工程 / 来源标注（citation）/ 温度的三副面孔
- 五、消息队列（审核链路）
  - 异步解耦 / 发布者确认 / 持久化队列 / 死信队列 DLX / 毒消息 / 消费端幂等 / 消息只带 id / 事务外发消息 / prefetch
- 六、内容安全
  - 先审后发 vs 先发后审 / AI 审核提示词 / 宁漏勿杀 / 审核流水留痕 / 人工复核兜底
- 七、MySQL 全文检索
  - FULLTEXT / ngram 解析器 / ngram_token_size / 自然语言模式 / 为什么不上 ES

---

## 一、内容社区与 Feed 流

### UGC（User Generated Content）
**一句话：用户生成的内容——笔记/评论/弹幕，与 PGC（平台/专业生成）相对。**
本项目的笔记即 UGC。UGC 是双刃剑：带来内容生态，也带来审核成本——这正是 AI 审核+MQ 的业务出发点。

### 种草
**一句话：通过真实体验分享激发他人购买欲的内容行为（小红书核心模式）。**
技术映射：`t_note_product`（笔记关联商品）是"种草"的数据化——内容与交易的接缝，点击即跳商品页。

### 瀑布流
**一句话：卡片不等高、按列排布、无限滚动的信息流布局（小红书/ Pinterest 标配）。**
实现：CSS `column-count` + `break-inside: avoid`（本项目 Notes.vue），配合"加载更多"按钮触发游标翻页。

### 游标分页（Cursor-based Pagination）★
**一句话：用"上一页最后一条的排序值"做过滤条件（WHERE x < cursor）翻页，代替 OFFSET 扫描丢弃。**
对比：`LIMIT 100000,20` 要扫并丢 10 万行，越翻越慢；游标靠索引直接定位，深度无关。
代价：不能跳页，只能连续翻——Feed 天然适合，后台管理要跳页仍用 offset。**按场景选型**。

### 二元组游标 ★
**一句话：当排序键可能重复时，游标必须是（排序键, 唯一键）的复合条件，否则漏数据。**
例：按 hot_score DESC 排序，多篇笔记热度相同——只用 hot_score 做游标会漏同分内容；补上 `AND id < #{cursorId}` 才完备。本项目落地：`(hot_score < #{h} OR (hot_score = #{h} AND id < #{cursorId}))`。
这是游标分网最容易讲错/写错的细节。

### N+1 问题 ★
**一句话：列表接口对每条记录再发一次关联查询（1 次列表 + N 次关联）导致 SQL 数爆炸。**
本项目解法：批量 IN 查询 + 内存分组组装（20 篇笔记固定 5 条 SQL）。反例：循环里 `selectById` 查作者。
面试问"列表接口优化"第一答。

### 幂律分布（长尾分布）
**一句话：少数爆款获得极高互动、大多数内容是长尾——社交内容的天然形态。**
模拟数据必须服从它（10% 爆款 + 90% 长尾），均匀的"每篇 300 赞"一眼假，热门榜也没有梯度。

### 热度公式
**一句话：把多维度互动加权合成一个可排序的分数。**
本项目：`like*3 + collect*5 + view*1`——收藏权重最高的理由：收藏=强购买意图，点赞=弱认可，浏览=免费。V3 演进：Redis zset `ZINCRBY` 实时更新 + 定时回写 MySQL。

### 软删除
**一句话：用状态标记（OFFLINE）代替物理 DELETE，数据可追溯可恢复。**
笔记下架用 UPDATE status='OFFLINE'：内容合规追溯、用户误操作可恢复、外键引用（点赞/收藏/审核流水）不断裂。

---

## 二、互动与幂等

### 唯一索引幂等 ★
**一句话：用 UNIQUE KEY 让数据库物理上拒绝重复数据，把并发正确性交给存储层。**
`uk(note_id, user_id)`：同一用户对同一笔记最多一条点赞。重复 INSERT 抛 DuplicateKeyException → 捕获当"已点过"静默处理。
对比"先查后插"：两次并发都查到"没点过"→ 插两条。**唯一索引是并发下最可靠的幂等手段**。

### DuplicateKeyException
**一句话：Spring 对 SQL 唯一键冲突（error 1062）的统一异常封装（DataAccessException 家族）。**
用途：捕获它区分"真的失败"与"重复请求"——幂等实现的信号弹。

### 先查后插的竞态
**一句话：SELECT 判断 + INSERT 两步之间可能被并发插队，是经典的 TOCTOU（check 时对、act 时错）问题。**
凡是"判断+写入"分离的并发逻辑都要问自己：两步之间被插队会怎样？答案要么唯一索引（本场景），要么条件更新（CAS）。

### 原子自增 vs 丢失更新 ★
**一句话：`SET x = x + 1` 在数据库内原子完成；先 SELECT 到 Java 算再 UPDATE 会丢并发更新。**
反例：两请求都读到 count=100 → 各写 101 → 丢一次。凡是计数场景一律 SQL 自增（或 Redis INCR）。
自减配 `AND x > 0` 防负数。

### 冗余计数
**一句话：把 count(*) 聚合结果冗余成列，列表展示免聚合查询。**
代价：写路径要维护一致性（同事务原子自增）。V2 用 DB 自增（简单正确），V3 升 Redis 计数+定时落库（热点写正解）——**按压力演进，不预支复杂度**。

---

## 三、RAG 核心

### 语义鸿沟
**一句话：字面不同但意思相同（"续航久"vs"电池耐用"），关键词匹配跨不过去，这正是向量检索存在的原因。**
反方向的鸿沟：字面相似但意思不同（"苹果手机"vs"苹果好吃"），向量也可能翻车——所以需要 Hybrid 双保险。

### Chunking（切片）★
**一句话：把长文档切成几百字的小片段，检索按片段做、上下文按片段拼。**
为什么：整篇检索粒度太粗、token 成本高、细节问题匹配不上。
本项目策略：固定 300 字 + 50 字重叠（性价比最高的起步方案）；进阶是语义切分/父子切片（知道即可，按需演进）。

### 重叠（overlap）★
**一句话：相邻切片保留一段重复内容，防止关键句被从中间切断导致两边语义残缺。**
步长 = 切片长 - 重叠（300-50=250）。没有重叠时，恰好横跨边界的句子两边都不完整，检索与生成双输。

### Embedding ★
**一句话：把文本映射成高维向量，语义相近的文本在向量空间里距离近。**
工程要点：①同一系统内所有向量必须来自同一模型（不同模型的向量空间不可比）；②检索 query 与语料用同一模型；③维度恒定。
现实约束：DeepSeek 不提供 embedding 接口——所以本项目抽象了 EmbeddingClient，默认本地哈希、可切 spring-ai 真实模型。

### 余弦相似度 ★
**一句话：两向量夹角的余弦（∈[-1,1]），衡量方向相似性而非长度——文本相似度的标准度量。**
技巧：向量先 L2 归一化（模长=1）后，**点积即余弦**，省一次模长计算（本项目 embed 时统一归一化）。

### L2 归一化
**一句话：把向量除以自身模长，变成单位向量——之后点积=余弦。**
本项目在 embed 出口统一归一化，检索时只算点积。注意：必须"同一套"归一化，混用归一/非归一向量结果无意义。

### 2-gram 哈希向量（Hashing Trick）
**一句话：把文本切成二元字符组、哈希到固定维度桶内累加——零依赖的"穷人版 embedding"。**
能捕获字面重叠（"降噪耳机"≈"耳机降噪"），不能捕获语义（"续航久"≠"电池耐用"）。
诚实定位：兜底实现，保证链路可跑可验证；质量升级靠换真实 embedding 模型（接口已抽象）。
与 MySQL ngram 分词器同用 2-gram 粒度——两个通道的"词"口径对齐。

### BM25 / 全文检索
**一句话：基于词频（TF）与逆文档频率（IDF）的经典相关性打分算法，MySQL FULLTEXT / ES 的默认排序依据。**
擅长精确词（型号、专有名词），换种说法就失配——与向量互补。

### Hybrid 检索（混合检索）★
**一句话：关键词（BM25/FULLTEXT）与向量两路并行召回，再融合排序——2026 年 RAG 生产基线。**
为什么必须双路：电商场景"型号词"（BM25 主场）与"体验问法"（向量主场）都高频，单路必有一类问题答不上。
业界共识：Hybrid 比单路召回率普遍高 10~20%，是两小时工作量换双保险的买卖。

### RRF（Reciprocal Rank Fusion）★
**一句话：按各路排名倒数（1/(k+rank)）累加融合，k≈60——只看名次不看分数，天然免疫量纲差异。**
为什么不能分数加权：BM25 可上百、余弦 ∈[0,1]——"厘米加斤"。RRF 的美妙性质：**两路都命中的片段得分自动叠加**（双确认最可信）。
k 的作用：压平第 1 名与第 2 名差距（1/61 vs 1/62），避免单路第一名碾压。

### Top-K 与 Candidate-K
**一句话：每路粗筛召回 Candidate-K 条（如 20），融合后再截断 Top-K（如 5）进 prompt。**
两层过滤的原因：融合需要足够的候选池才能体现"多路确认"；最终上下文预算有限。

### 幂等索引（upsert）
**一句话：同一来源（source_type+ref_id）的索引是替换语义——删旧切片写新切片，重复执行结果不变。**
反面：追加式索引会积累多版本脏切片，下架内容仍被检索到。**脏语料比没语料更糟**。

### 脏语料
**一句话：已失效/被下架/旧版本的内容仍留在检索库里，会生成"引用了不存在内容"的回答。**
治理：下架/编辑触发索引更新（本项目 offline→remove、审核通过→index）；定期全量重建兜底。

---

## 四、Agent 分流

### 双工具路由 ★
**一句话：给模型同时注册 searchProduct（SQL）与 searchDocs（RAG），由模型按问题性质自选——代码不写意图 if-else。**
为什么不用规则路由：自然语言意图无法枚举（"这玩意儿贵吗""预算内有没有替代款"……），规则挂一漏万；工具描述是给模型的"说明书"，模型的理解力就是路由器。这就是 Agentic RAG 的"Agentic"。

### 工具描述工程 ★
**一句话：@Tool 的 description 要写清"返回什么 + 何时该调 + 何时该用另一个工具"，它就是模型的选路依据。**
本项目实践：searchDocs 的描述里明确写"价格库存等实时信息不要用本工具（用 searchProduct）"——**负向引导防止抢跑**（V1 踩过"该调不调"的坑，V2 防"抢着调错"）。

### 来源标注（citation）
**一句话：RAG 回答附带知识片段的来源（官方说明书/用户笔记），用户可验证、可信度可感知。**
工程落地：searchDocs 返回的每条片段带 source 字段 + system prompt 要求"回答注明来源"。RAG 从黑盒变白盒的产品化表达。

### 温度的三副面孔 ★
**一句话：本项目同一模型三个温度——审核 0.1（要确定）、导购 0.7（平衡）、文案 0.8（要灵气）。**
面试聊 temperature 别只会背"0~1 越低越确定"——用自己项目的三处实例讲"按任务性质选温度"。

---

## 五、消息队列（审核链路）

### 异步解耦 ★
**一句话：把"秒级耗时的 AI 审核"从发布请求链路中剥离，发布秒回、审核后台完成。**
判断标准（项目铁律）：MQ 的引入必须能回答"砍掉它会怎样"——本场景答案是"用户点发布要干等 3~10 秒"。

### 发布者确认（publisher confirm）★
**一句话：Broker 收到消息后回执 ACK 给生产者——"消息不丢"的发送端一半。**
配置：`publisher-confirm-type: correlated`。另一半：队列/消息持久化 + 消费端处理成功才算闭环。

### 持久化队列/消息
**一句话：队列 durable + 消息 delivery_mode=2，Broker 重启消息不丢。**
本项目 AuditMqConfig 用 `QueueBuilder.durable(...)` 声明。

### 死信队列（DLX）★
**一句话：消费失败（reject/nack/重试耗尽）的消息被路由到的"收容所"队列，等人工或恢复后处理。**
价值：**失败可被发现**。没有 DLX，失败消息直接蒸发，问题无从排查。本项目审核失败另有 ERROR 流水双保险。

### 毒消息 ★
**一句话：必然失败的消息（如下游服务已挂）在"重入队→消费→失败→重入队"里无限循环，打爆日志与 CPU。**
对策：`default-requeue-rejected: false`（消费异常不重入队）+ 消费端 catch 吞掉 + DLX 收容 + 补偿任务。**"失败可发现"优于"失败无限重试"**——高频进阶考点。

### 消费端幂等 ★
**一句话：MQ 语义是 at-least-once（至少一次），重复投递是常态，消费逻辑必须自带幂等。**
本项目双防线：流水表版本唯一键（INSERT IGNORE）+ 笔记状态机条件更新（WHERE status='AUDITING'）。
通用三方案：唯一键/状态机/version——本项目前两个都用上了。

### 消息只带 id
**一句话：消息体只放标识符（noteId），消费时回查最新数据——消息是"通知"不是"数据载体"。**
好处：消息小；消费时数据总是最新（消息滞留期间内容可能被编辑）；敏感内容不进 Broker。

### 事务外发消息
**一句话：在 @Transactional 方法内发 MQ 有竞态——消费者可能在事务提交前回查到空数据。**
本质：MQ 发送不受数据库事务管辖。正解：事务提交后再发（TransactionSynchronization.afterCommit / 或如本项目把 submit 放在事务边界外调用）。经典分布式一致性考点。

### prefetch（预取数）
**一句话：消费者一次最多预取几条未 ACK 消息——背压控制。**
本项目 prefetch=5：审核是秒级慢任务，预取太多会把消息堆到客户端内存，失去 Broker 端缓冲的意义。

---

## 六、内容安全

### 先审后发 vs 先发后审 ★
**一句话：先审后发（发布即入队审核，通过才公开）安全但慢；先发后审（先公开再巡查）快但风险外溢。**
本项目取"发布秒回+秒级 AI 审"的折中：内容落库为 AUDITING（对外不可见），AI 几秒内出结果——对用户像"先发"，对系统是"先审"。

### AI 审核提示词要点
**一句话：分类标准显式列出 + 强制 JSON 输出 + 低温度 + 拿不准倾向通过。**
"宁漏勿杀"的理由：误杀正常用户的伤害（体验/流失）大于漏过一条软广（可巡查补救）。审核系统**宁可不作为，不可乱作为**。

### 审核流水留痕
**一句话：每次 AI 判定原始结果入库（ai_result_json），可追溯、可翻案、可统计误判率。**
没有流水，AI 误判就是黑箱——用户申诉无据可查。

### 人工复核兜底
**一句话：AI 审核定位是"过滤 99% 明确内容"，灰区交给人工（reviewer 字段已预留）。**
生产系统的 AI 审核从来不是终审——讲清这条边界反而显得懂行。

---

## 七、MySQL 全文检索

### FULLTEXT 索引
**一句话：MySQL 内建的倒排索引，支持 MATCH...AGAINST 全文检索与相关性排序。**
中文场景必须配 ngram，否则形同虚设。

### ngram 解析器 ★
**一句话：MySQL 8 的中文分词方案——把文本切成 n 元字符组（默认 2 字一组）建倒排，中文检索才能命中。**
`FULLTEXT KEY ft_x (col) WITH PARSER ngram`。
局限：纯字面切词无语义（"续航"搜不到"电池耐用"——交给向量通道）；单字查询搜不到（token_size=2）。

### ngram_token_size
**一句话：ngram 的分词粒度参数，默认 2，可全局配置。**
2 对中文够用；调大更精确但索引更胖。本项目用默认值，与本地哈希向量的 2-gram 口径一致。

### 自然语言模式（NATURAL LANGUAGE MODE）
**一句话：MATCH...AGAINST 的默认模式——按相关性打分排序返回，无需布尔运算符。**
另有 BOOLEAN MODE（支持 +/-/* 通配），本项目用自然语言模式即可。

### 为什么不上 ES ★
**一句话：100 商品 + 500 笔记的量级，MySQL FULLTEXT(ngram) 毫秒级返回，上 ES 是用分布式集群解决单机都没碰到的问题。**
ES 的引入时机：数据量百万级/需要复杂聚合与高亮/多表联合搜索——届时也是"检索层换实现"，业务不动。
面试加分句："我知道 ES 是搜索标配，但我的数据量用 MySQL 全文索引足够——**为 500 条数据上 ES 才是过度设计**。"

---

## 返回主文档

[V2内容社区与RAG详解.md](V2内容社区与RAG详解.md)
