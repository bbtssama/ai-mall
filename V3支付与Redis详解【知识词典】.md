# V3 支付与 Redis 详解【知识词典】

> 配套主文档：[V3支付与Redis详解.md](V3支付与Redis详解.md)（按章节学习用主文档，查概念用本词典）
> 与前几本词典（《AI模块详解-SpringAI从零到实战【知识词典】》《V1.5工程基建详解【知识词典】》《V2内容社区与RAG详解【知识词典】》）不重复收录。
> 词条格式：**一句话核心 → 比喻 → 原理展开 → 代码对应 → 常见追问**。每个词条尾部可跳回主文档对应章节。

## 目录

- [一、支付](#一支付)：[支付单](#支付单) / [异步回调](#异步回调) / [验签](#验签) / [哈希函数](#哈希函数) / [HMAC-SHA256](#hmac-sha256) / [RSA2](#rsa2) / [状态机 CAS](#状态机-cas) / [幂等回调](#幂等回调) / [金额核对](#金额核对) / [主动查单对账](#主动查单对账) / [PayChannel](#paychannel) / [MockPayChannel](#mockpaychannel) / [沙箱](#沙箱) / [本地消息表](#本地消息表)
- [二、延迟消息](#二延迟消息)：[TTL](#ttl) / [死信队列再认识](#死信队列再认识) / [队头阻塞](#队头阻塞) / [延迟消息插件](#延迟消息插件)
- [三、Redis 基础](#三redis-基础)：[Redis 为什么快](#redis-为什么快) / [单线程模型](#单线程模型) / [zset](#zset) / [缓存旁路](#缓存旁路) / [RedisTemplate 序列化](#redistemplate-序列化) / [JSON 序列化](#json-序列化) / [连接池参数](#连接池参数)
- [四、缓存三兄弟](#四缓存三兄弟)：[缓存穿透](#缓存穿透) / [空值缓存](#空值缓存) / [布隆过滤器](#布隆过滤器) / [缓存击穿](#缓存击穿) / [互斥锁重建](#互斥锁重建) / [逻辑过期](#逻辑过期) / [缓存雪崩](#缓存雪崩) / [缓存与 DB 一致性](#缓存与-db-一致性)
- [五、分布式协调](#五分布式协调)：[SET NX PX](#set-nx-px) / [锁误删](#锁误删) / [Lua 脚本原子性](#lua-脚本原子性) / [预减库存](#预减库存) / [看门狗](#看门狗)
- [六、限流](#六限流)：[固定窗口](#固定窗口) / [滑动窗口](#滑动窗口) / [令牌桶](#令牌桶) / [降级方向](#降级方向)
- [七、安全](#七安全)：[时序攻击](#时序攻击) / [公网端点的身份模型](#公网端点的身份模型)

---

## 一、支付

### 支付单
**一句话：一次"发起支付"的唯一凭证，独立于订单存在——幂等与对账的锚点。**

比喻：订单是"合同"，支付单是"每一次打款的回执"——合同可以多次尝试打款（超时重付），但每笔打款有且只有一张回执。

原理展开：为什么必须独立于订单——①**幂等粒度**：一次订单可能多次发起支付，"哪一次成功了"这个事实只能锚在支付单号上；②**对账锚点**：第三方交易号（trade_no）、回调原始报文（callback_body）都要留痕，塞订单表会让它膨胀成大杂烩；③**状态解耦**：订单状态机（待支付→已支付→已发货→已完成）与支付状态机（PAYING→PAID/CLOSED）生命周期不同——订单取消了，支付单可能还停在 PAYING 等关闭，甚至已 PAID 等退款。

代码对应：`t_payment` 表（`uk_payment_no` 唯一索引 = 幂等的物理兜底）；冗余的 order_no/user_id 字段让回调"只查一张表"完成归属校验与金额比对。

常见追问：*"为什么不用订单号当支付单号？"*——一次订单多次发起支付时订单号无法区分"哪一次"；支付单号是每次发起独立生成的（`P+时间戳+随机+用户尾号`）。
→ 返回[主文档 4.1-4.2](V3支付与Redis详解.md#41-先撞南墙为什么不在订单上加个已支付标记)

### 异步回调
**一句话：第三方付完款后"主动通知你"的 HTTP 请求——支付结果的主要来源。**

比喻：网购到货后快递员给你打电话——你不能只靠"查物流"（主动查单）活着，但也不能假设电话一定能打通（回调会丢）。

原理展开：为什么是异步而不是同步返回——用户付完款跳回你的页面（同步返回）只适合"体验确认"，**不能作为记账依据**（页面可能被关、参数可被伪造）；真正的资金事实必须由第三方服务器**主动**通知你的后端（服务器对服务器，用户不可见不可篡改）。两条铁律背下来：回调**必然重复**（超时重传/对方重试 8 次以上）；回调**可能丢失**（网络抖动）——所以幂等 + 主动查单是双保险，缺一不可。

代码对应：`PaymentController.callback`（`@PostMapping("/callback/{channel}")`，永不抛异常、返回字符串给机器读）。

常见追问：*"回调返回 success 之外的内容会怎样？"*——对方认为通知失败并按策略重试（间隔递增，支付宝最多 8 次+），所以处理成功必须回字面量 `success`。
→ 返回[主文档 6.1-6.2](V3支付与Redis详解.md#61-全链路时序先把全景刻进脑子)

### 验签
**一句话：用对方公钥/共享密钥验证"报文确实来自第三方且未被篡改"。**

比喻：信封上的火漆印章——拆开信（解析报文）之前先看印章（验签）对不对；印章不对，信写得再动听也不能信。

原理展开：回调接口是公网端点，不验签 = 任何人都能 POST 把订单改成已支付（白嫖/篡改金额/伪造退款）。验签回答的是**完整性与来源**问题（消息是谁发的、有没有被改），与"认证"（你是谁）是两套模型。验签失败必须拒绝——安全红线，不存在"为了成功率放行"。

代码对应：`MockPayChannel.verifyCallback`——重算签名 + `MessageDigest.isEqual`（constant-time，防[时序攻击](#时序攻击)）。

常见追问：*"验签和 HTTPS 不是一回事吗？"*——HTTPS 防"传输途中被窃听/篡改"（链路安全），验签防"发起方本身就是假的"（身份伪造）。HTTPS 之上依然要验签，因为攻击者可以自己对你的 HTTPS 接口发请求。
→ 返回[主文档 5.1](V3支付与Redis详解.md#51-先撞南墙不验签的世界)

### 哈希函数
**一句话：把任意长度的输入压缩成固定长度"指纹"的单向函数——改一个字，指纹面目全非。**

比喻：把整本书碾成一杯"指纹粉"——粉能验证书有没有被改（重新碾一杯对比），但**不可能从粉还原出书**（单向性）。

原理展开：三个性质决定了它能用于完整性校验：①**确定性**（同输入同输出）；②**雪崩效应**（输入微变，输出巨变）；③**不可逆**（无法从指纹反推原文）。SHA-256 输出 256 位（64 个十六进制字符）。但注意：**裸哈希只能检测"意外篡改"，挡不住"蓄意伪造"**——攻击者也知道你的拼接规则，改完重算哈希即可。挡伪造需要引入密钥 → [HMAC](#hmac-sha256)。

代码对应：`MessageDigest.getInstance("SHA-256")`（JDK 内置，本项目的 HMAC 内部也以它为哈希基元）。

→ 返回[主文档 5.2](V3支付与Redis详解.md#52-从哈希到-hmac三步演进0-基础)

### HMAC-SHA256
**一句话：带密钥的哈希（Hash-based Message Authentication Code）——只有持密钥方能生成/验证，防伪造+防篡改二合一。**

比喻：普通哈希是"人人会盖的邮戳"，HMAC 是"只有你和对方有的私章"——盖出来的印记一样，但私章不可复制。

原理展开：为什么不手搓 `SHA256(明文+密钥)`——这种拼接有**长度扩展攻击**弱点（攻击者不知密钥也能构造出新合法哈希）。HMAC 用两轮哈希巧妙嵌套密钥（内层外层各嵌一次），数学上免疫扩展攻击。工程三要点：①签名输入必须**按 key 字典序排序**（HashMap 遍历序不稳定，不排序则签名时对时错）；②sign 字段自身不参与签名（签名不能签自己）；③比较用 constant-time（见[时序攻击](#时序攻击)）。

代码对应：`MockPayChannel.sign/verifyCallback`——`Mac.getInstance("HmacSHA256")` + `SecretKeySpec`；密钥来自 `aimall.pay.mock.secret` 配置（生产用环境变量注入）。

常见追问：*"密钥泄露了怎么办？"*——轮换密钥（换 secret 后所有旧签名立即失效）；这也是支付平台提供"密钥管理/重置"功能的原因。
→ 返回[主文档 5.2-5.3](V3支付与Redis详解.md#52-从哈希到-hmac三步演进0-基础)

### RSA2
**一句话：非对称签名——我方私钥签名、对方公钥验签，密钥不共享，泄露面小（真实支付宝的方案）。**

比喻：HMAC 是"两家共用一把私章"（章丢了双方都能被冒充）；RSA2 是"我刻自己的私章、把能验证印记的'公章拓片'发给全世界"——拓片只能验证不能盖章。

原理展开：为什么多方互信场景要非对称——与支付宝对接的有成千上万家商户，若用共享密钥，任何一家泄露都危及全体；RSA2 里每家只管好自己的私钥，验签用支付宝分发的公钥，**密钥不交叉**。本项目 Mock 渠道收发双方都是自己（对称够用），真实接入时换 `AlipaySandboxChannel` 实现即可——只动签名/验签两个私有方法，接口形状不变。

常见追问：*"RSA2 和 HTTPS 里的 RSA 是一回事吗？"*——同一族数学（公私钥），用途不同：HTTPS 用它**交换对称密钥**（加密），支付签名用它**验证身份**（不可抵赖）。
→ 返回[主文档 5.5](V3支付与Redis详解.md#55-hmac-vs-rsa2对称与非对称)

### 状态机 CAS
**一句话：UPDATE 带 WHERE status=期望状态——数据库原子保证"只有第一个到达者能改状态"，输家拿 0 行。**

比喻：会议室的门禁改成"只能从'空'刷成'占用'"——两个人同时刷卡，门禁系统（数据库行锁）只认第一下，第二个人刷的时候状态已经是"占用"，刷不动了。

原理展开：对比"先查后判"（SELECT 判断再 UPDATE）——两个并发请求可能**都**查到旧状态、**都**往下执行；而条件更新的判断与写入在**一条 SQL 内**由数据库原子完成，并发裁决天然正确。返回值是关键信息：1=我改成功了（我负责后续动作），0=别人抢先（我直接放弃，什么都不做）。本项目四次使用同一模式：扣库存（stock>=?）、审核回写（status='AUDITING'）、订单取消（status='PENDING_PAY'）、支付流转——**面试时"一个模式吃遍天下"是最有说服力的掌握证据**。

代码对应：`PaymentMapper.updateToPaid`（`WHERE payment_no=? AND status='PAYING'`）。

常见追问：*"0 行返回后要不要重试？"*——分场景：支付回调的 0 行说明"别人已处理"，直接成功返回；若业务要求"我必须成功"（如抢锁），则用自旋或放弃策略。
→ 返回[主文档 6.3](V3支付与Redis详解.md#63-handlecallback-逐段精读六步全贴)

### 幂等回调
**一句话：同一笔回调到达 N 次，结果与到达 1 次完全相同——支付系统的入场券。**

比喻：电梯按钮按 1 次和按 10 次，电梯来一趟——按钮设计成"按了就是按了"，重复按不产生额外效果。

原理展开：三层防线（缺一不可）：①**物理层**：`uk(payment_no)` 唯一索引，重复创建直接被数据库拒绝；②**快路径**：已是 PAID → 直接回 success（挡串行重复，最常见）；③**慢路径**：状态机 CAS（挡并发重复——两个回调同时穿过快路径）。为什么快路径挡不住慢路径的场景：两个回调**同时**到达，都查到 PAYING（谁都没提交），都往下走——只有 CAS 让其中一个落空。注意幂等的**方向性**：已支付的重复回调必须回 success（让对方停止重发），只有业务性失败才返回失败让它重发。

常见追问：*"Redis 的 SETNX 也能做幂等，为什么用数据库？"*——支付链路的数据本来就在 MySQL 事务里，用唯一索引/CAS 不引入第二个存储的一致性问题；分布式锁幂等适合"高频重复请求拦截"（如防重复提交），支付这种强一致场景以数据库为准。
→ 返回[主文档 7.1-7.2](V3支付与Redis详解.md#71-先立事实重复回调必然发生)

### 金额核对
**一句话：回调金额必须与我方支付单完全一致——不一致/缺失/非法一律拒绝。**

比喻：收银员收了钱要核对小票金额——"客户说付了 399"不算数，要对账系统里的数字。

原理展开：防的是"篡改报文金额"攻击（0.01 元买 399 的单）。三个分支缺一不可：缺失→拒绝（**最容易写错的一条**：早期代码 `amountStr != null` 才核对，"不带 amount 字段"就绕过了核对——评审揪出的 P0）；非法数字→拒绝（防 `amount=abc` 探测）；不等→拒绝。用 `BigDecimal.compareTo` 而不是 `equals`（`equals` 连 scale 一起比，`399.0` ≠ `399.00` 会误判）。

代码对应：`PaymentService.handleCallback` 第 ④ 步三分支：amount-missing / amount-invalid / amount-mismatch。
→ 返回[主文档 6.3](V3支付与Redis详解.md#63-handlecallback-逐段精读六步全贴)

### 主动查单对账
**一句话：不依赖被动通知，主动向第三方查询交易状态修正本地——回调丢失的兜底。**

比喻：快递电话没接到（回调丢），你打开 App 查物流（主动查单）——两个信息渠道互为备份。

原理展开：两条原则：①**以第三方结果为准**修正本地（query 返回 PAID 才补偿流转）；②**UNKNOWN 不改状态**——查不到 ≠ 没付（可能是查询接口抖动），绝不擅改。生产形态三级：用户侧轮询（本项目前端订单页调 sync）→ 定时任务批量对账 → 日终对账文件（支付平台每天给账单文件，逐笔核对，差异人工处理）。诚实边界：MOCK 渠道 query 恒返 UNKNOWN（没有真第三方），sync 在 MOCK 下是死路径——它为真实渠道而生，接口契约先立好。

代码对应：`PaymentService.sync`（仅 PAID 才补偿，UNKNOWN 直接放弃）。
→ 返回[主文档 7.3](V3支付与Redis详解.md#73-回调会丢主动查单对账)

### PayChannel
**一句话：支付渠道抽象接口（create 下单换收银台 / verifyCallback 验签 / query 查单）——任何支付网关都逃不出这三件事。**

比喻：充电接口标准（USB-C）——手机（业务代码）不关心对面插的是充电器、充电宝还是电脑（支付宝/微信/银联），只认"能供电的 USB-C 形状"（接口契约）。

原理展开：存在理由是一个现实约束——真实沙箱需要商户资质（个人拿不到），硬依赖 SDK 则没有凭证整条链路跑不通。抽象后换渠道只动实现类，验签/幂等/状态机等业务代码零改动。**为什么是这三个方法**：create 对应"下单换收银台"（所有渠道的第一步）；verifyCallback 对应"验签"（每家算法不同但输入输出形状相同）；query 对应"主动查单"（对账兜底的接口基础）。这是本项目可插拔家族的第三次出现（StorageService、EmbeddingClient 在前）——可以讲成你的设计哲学。

代码对应：`com.aimall.pay.channel.PayChannel` 接口 + `PayChannelRouter.resolve`（按 code 路由，找不到降级 MOCK 并告警）。
→ 返回[主文档 4.4](V3支付与Redis详解.md#44-paychannel为什么是这三个方法)

### MockPayChannel
**一句话：本地模拟渠道——"模拟"的只有"第三方服务器不存在"，签名/验签/回调链路全是生产级写法。**

比喻：考驾照用的模拟考场——场地和考试车是真的，只有"社会车辆"是假的。

原理展开：模拟支付端点（mock-pay）**不直接改状态**，而是构造带签名的报文去调 `handleCallback`——验签、幂等、金额核对、CAS 全部真实走过。这样设计的好处：①你可以真实验证整套安全链路（curl 假签名看它拒绝）；②换真实渠道时业务代码零改动（业务代码从头到尾没假设"第三方是真的"）；③面试可现场演示。HMAC 密钥来自配置，改掉 secret 重启，所有旧签名立即失效——顺手演示"密钥轮换"。

代码对应：`MockPayChannel`（sign/verifyCallback/create）+ `PaymentController.mockPay`（归属校验后构造回调）。
→ 返回[主文档 6.4](V3支付与Redis详解.md#64-mock-pay模拟收银台的闭环设计)

### 沙箱
**一句话：支付官方提供的仿真环境——真实协议与验签、假资金，需注册开放平台获取测试 APPID/密钥。**

比喻：飞行员训练舱——仪表、操纵杆与真机完全一样，只有"坠机不会死"是模拟的。

原理展开：沙箱与生产只差一个网关地址与凭证。本项目 Mock 渠道相当于"自建沙箱"：协议链路完整，第三方端点本地化。要不要接真沙箱的决策论证见项目设计文档 §5.3.2（结论：面试近不接，面试远花半天接——接的唯一真实价值是把"换渠道零改动"从主张变成实证）。
→ 返回[主文档 0.3](V3支付与Redis详解.md#03-一个绕不开的现实约束决定第-4-7-章的形态)

### 本地消息表
**一句话：消息与业务数据同事务落库、定时任务扫描投递——事务性发消息的强一致方案。**

比喻：寄快递前先把"要寄的东西+地址"记在自己的台账上（与货一起入库），快递员来不来都台账在——漏寄了翻台账补寄。

原理展开：解决的问题与 afterCommit 相同（事务回滚不该发消息），但更强：消息先与业务**同事务**写进本地表（要么都成功要么都失败），投递由独立任务扫描（失败可重扫、可人工补偿）。代价：一张表 + 扫描任务 + 状态管理。本项目用 afterCommit（轻量够用）；量级/一致性要求上来再上本地消息表——面试聊"分布式事务最终一致"时的标准答案之一。
→ 返回[主文档 8.6](V3支付与Redis详解.md#86-发送侧aftercommit评审修正的反模式)

---

## 二、延迟消息

### TTL
**一句话：消息/队列的存活时间（Time To Live），过期即"死亡"进入死信流程。**

比喻：超市的保鲜期标签——到期的商品不是"消失"，而是被理货员（死信路由）搬到"临期区"（死信队列）另行处理。

原理展开：两种设置位：**队列级**（`x-message-ttl` 队列参数，全队列统一时钟）与**消息级**（每条消息自带 expiration 属性，可各不相同）。选型规则：过期时间统一 → 队列级（简单且免疫[队头阻塞](#队头阻塞)）；差异化 → 消息级或延迟插件（要接受各自的坑）。本项目订单统一 30 分钟 → 队列级。

代码对应：`OrderDelayMqConfig.orderDelayQueue` 的 `args.put("x-message-ttl", 30*60*1000)`。
→ 返回[主文档 8.3](V3支付与Redis详解.md#83-ttl--dlx让消息睡30-分钟)

### 死信队列再认识
**一句话：DLX 不只收容"失败消息"——配合 TTL 就是"延迟投递"引擎（死信队列的第二用途）。**

比喻：酒店的"延迟退房"机制——你不能 14 点前进房（消息不能提前被消费），但到了 14 点（TTL 到期）房卡自动生效（死信路由把它送到能消费的地方）。

原理展开：RabbitMQ 里消息"死亡"的三种方式：被 reject/nack、TTL 过期、队列超长。配了死信参数的队列（`x-dead-letter-exchange` + `x-dead-letter-routing-key`），死掉的消息**不被丢弃而是按参数重新路由**。V2 审核用它收容失败消息（第一用途）；V3 把消息投进一个**没有消费者**的 TTL 队列，过期后经死信路由转运到真正的工作队列——这就是"延迟队列"的标准零插件实现（第二用途）。

代码对应：`order.delay.queue`（无消费者 + TTL + 死信指回业务交换机）→ `order.cancel.queue`（真正被消费）。
→ 返回[主文档 8.3](V3支付与Redis详解.md#83-ttl--dlx让消息睡30-分钟)

### 队头阻塞
**一句话：RabbitMQ 只检查队首消息是否过期——队首未过期，后面已过期的消息也不会被投递。**

比喻：单车道隧道——头车抛锚（队首消息 TTL 长），后面再快的车（短 TTL 消息）也出不去。

原理展开：RabbitMQ 为性能不扫描全队列，只看队首。数字例子：队首 TTL=60min、第二条 TTL=1min → 第二条实际延迟 60 分钟。这是**消息级 TTL** 的固有坑。规避：①统一过期时间用队列级 TTL（本项目）；②差异化超时用延迟插件；③或多条不同 TTL 的固定队列路由。面试时能主动讲出"我知道消息级 TTL 有这个坑所以我选队列级"，比背"我用了延迟消息"高一个段位。
→ 返回[主文档 8.4](V3支付与Redis详解.md#84-队头阻塞为什么用队列级-ttl)

### 延迟消息插件
**一句话：官方 rabbitmq-delayed-message-exchange 插件，x-delayed-message 交换机直接支持每条消息任意延迟。**

比喻：机场的"按登机时间叫号"——每条消息自带"登机时间"（x-delay 属性），交换机按时间投递，不受队头影响。

原理展开：优点：无队头阻塞、任意 TTL、消息不用"睡"在队列里；代价：Broker 必须装插件（`rabbitmq-plugins enable rabbitmq_delayed_message_exchange`）——生产环境不一定装得了，本地 docker-compose 默认镜像也没有。选型口诀：**能装插件且需要差异化延迟 → 插件；环境受限或延迟统一 → TTL+DLX**。
→ 返回[主文档 8.4](V3支付与Redis详解.md#84-队头阻塞为什么用队列级-ttl)

---

## 三、Redis 基础

### Redis 为什么快
**一句话：数据全在内存（比磁盘快 10 万倍）+ 单线程无锁竞争——瓶颈本来就不在 CPU。**

比喻：图书管理员把最常被借的 100 本书放在手边桌上（内存），而不是每次都去仓库（磁盘）搬。

原理展开：内存随机读写 ~100ns vs 磁盘寻道 ~10ms，差 5 个数量级；单线程看似"慢"，实际避免了多线程的锁竞争与上下文切换，且瓶颈在网络 IO 与内存带宽——单核足以打满万兆网卡。**快是手段不是目的**：项目里引入 Redis 的理由永远是"读多写少用内存挡 DB"或"热点写用计数器扛"，而不是"Redis 很快所以我用"。
→ 返回[主文档 1.1](V3支付与Redis详解.md#11-它是什么为什么快)

### 单线程模型
**一句话：Redis 用一个线程处理所有命令——换来的是每个命令天然原子（Lua 脚本原子性的根基）。**

比喻：只有一个窗口的银行——排队看似慢，但每个业务（命令）都不会被插队打断，柜员办完一件再办下一件。

原理展开："单线程"指**命令执行**线程（网络 IO 在新版本是多线程的，但执行仍是串行）。对本项目的意义：①`INCR` 天然原子，无并发问题；②**Lua 脚本执行期间不插入其他命令**——这是"判断+写入"复合操作原子性的来源（预减库存、限流、安全解锁全靠它）；③也带来约束：Lua 里不能写慢操作（会阻塞所有客户端）。
→ 返回[主文档 1.1](V3支付与Redis详解.md#11-它是什么为什么快)

### zset
**一句话：有序集合——每个成员带分数（score），按分数自动排序，范围查询 O(logN)。**

比喻：游戏的积分排行榜——每个玩家（member）有积分（score），实时排名（ZREVRANGE 前 N 名），不用每次现算。

原理展开：底层是"跳表 + 哈希表"双结构（哈希表管 member→score 的点查，跳表管按分数的范围查）。本项目热门榜的用法：互动时 `ZADD hot:notes score noteId`，查榜 `ZREVRANGE hot:notes 0 9` 一次拿前 10——对比 SQL 的 `ORDER BY hot_score DESC LIMIT 10`（每次排序），**榜单是天然适合 zset 的场景**：写入时维护顺序，读取时直接拿。
→ 返回[主文档 1.2](V3支付与Redis详解.md#12-用-redis-cli-手玩一遍强烈建议真做)

### 缓存旁路
**一句话：缓存是 DB 的加速层而非数据源——缓存挂了业务"变慢"而不是"不可用"。**

比喻：眼镜（缓存）之于眼睛（DB）——眼镜丢了（缓存挂）你看得模糊（变慢）但还能看（业务可用）；不能设计成"没眼镜就闭眼"（缓存决定可用性）。

原理展开：工程落地三件套：①所有 Redis 调用包进可降级门面（RedisOps.safe：异常→WARN→兜底值）；②降级默认值**逐方法按业务语义选**（缓存读→空走库；限流→放行 fail-open；见[降级方向](#降级方向)）；③降级要可观测（每次 WARN 带 traceId，可统计降级率）。反面教材：裸调 redisTemplate 的代码，Redis 一断全站 500——缓存成了故障放大器。
→ 返回[主文档 2.3](V3支付与Redis详解.md#23-redisops-门面把降级收敛到一处本章核心)

### RedisTemplate 序列化
**一句话：默认 JDK 序列化三宗罪——redis-cli 乱码（不可读）、字节含类全名（包名一改存量全废）、任意类反序列化（安全漏洞）。**

比喻：把文件存成"只有你家电脑装了对应软件才能打开"的私有格式——换台电脑（换包名/换语言客户端）全废；存 PDF（JSON）谁都能看。

原理展开：三宗罪的现场：①`GET product:1001` 返回 `\xac\xed\x00\x05sr...`——排查只能靠猜；②字节里写死 `com.aimall.goods.dto.ProductVO`，重构改包名后存量缓存全部 `ClassNotFoundException`；③Java 反序列化 RCE 一族事故的经典入口（任意类的构造器/readObject 可能被恶意触发）。对策：key 用 String 序列化（可读、跨客户端兼容），value 用 JSON（带类型信息还原具体类）。这是**跨进程数据不用语言专属二进制**原则在缓存层的应用——与 V2 的 MQ JSON 转换器同一思想。
→ 返回[主文档 2.2](V3支付与Redis详解.md#22-序列化器新手-100-会撞的坑)

### JSON 序列化
**一句话：用 Jackson（带类型信息）替代 JDK 序列化——可读、跨语言、默认关闭任意类型反序列化。**

原理展开：三处定制缺一不可：①`registerModule(new JavaTimeModule())`——不配它 `LocalDateTime` 直接序列化失败（Jackson 默认不认 JSR-310）；②`activateDefaultTyping(NON_FINAL)`——把类型信息写进 JSON（`{"@class":"..."}`），取出才能还原成 ProductVO 而非一坨 LinkedHashMap；③key/value 序列化器分开设——key 必须纯字符串，否则 SCAN/DEL/管理台肉眼排查全废。双模板分工：`StringRedisTemplate`（值本来就是字符串：计数/锁/限流/zset）+ `RedisTemplate<String,Object>`（缓存对象）。
→ 返回[主文档 2.2](V3支付与Redis详解.md#22-序列化器新手-100-会撞的坑)

### 连接池参数
**一句话：max-active/max-idle/min-idle/max-wait——有界+等待上限+拒绝无界堆积，与线程池同一套资源治理哲学。**

比喻：餐厅的餐桌数（max-active）与常开桌（min-idle）——不可能无限加桌（Redis 也扛不住无限连接），客满时客人最多等 2 分钟（max-wait）而不是无限排队。

原理展开：四个参数的语义：`max-active` 最多并发连接数（上限保护 Redis）；`max-idle` 空闲连接保有上限；`min-idle` 常驻连接（预热，避免突发流量现建连接的延迟毛刺）；`max-wait` 拿不到连接的最长等待（快速失败优于无限堆积）。对照 V1.5 线程池与本项目第三次出现的"有界资源"范式——**面试聊线程池/连接池本质是同一道题**。
→ 返回[主文档 2.1](V3支付与Redis详解.md#21-依赖与配置逐行读)

---

## 四、缓存三兄弟

### 缓存穿透
**一句话：查询**不存在**的数据，缓存永远不命中，流量全部打到 DB。**

比喻：有人反复按一栋楼里不存在的门铃号——保安（缓存）每次都要去查住户名册（DB），名册翻烂了。

原理展开：与[击穿](#缓存击穿)的本质区别：穿透是"数据本来就没有"（缓存无从谈起），击穿是"数据有但缓存恰好过期"。攻击/误刷场景下穿透的杀伤力在于**缓存完全失效**——每个请求都落库。对策两档：[空值缓存](#空值缓存)（把"不存在"也缓存起来，短 TTL）挡小规模；[布隆过滤器](#布隆过滤器)（海量 key 前置拦截）挡大规模。
→ 返回[主文档 3.3](V3支付与Redis详解.md#33-老大穿透查一个根本不存在的-id)

### 空值缓存
**一句话：查不到也往缓存写一个特殊标记（如 __NULL__），短 TTL 挡住重复查询。**

比喻：物业在空房间门上贴"此户无人居住"的条子——下次查地址的人看到条子就直接走了，不用敲门（查库）。

原理展开：两个设计细节：①**TTL 必须短**（本项目 2 分钟）——商品可能下一秒上架，空值缓存 30 分钟 = 上架后半小时用户都查不到（脏读）；2 分钟是"挡刷子"与"不脏读"的平衡。②**用特殊串而不是存 null**——很多客户端不存 null 值，且"key 存在但值是标记"与"key 不存在"是两种可区分的明确状态。读路径：先查正常缓存 → 再查空值标记 → 都没有才查库。
→ 返回[主文档 3.3](V3支付与Redis详解.md#33-老大穿透查一个根本不存在的-id)

### 布隆过滤器
**一句话：用极小内存回答"这个元素**一定不存在**吗？"——说不存在就一定不存在，说存在可能误判。**

比喻：一张"黑名单速查卡"——卡上没有的名字一定不是黑名单（可放心放行），卡上有的名字需要再仔细核实（可能记错了）。

原理展开：原理：一个位数组 + k 个哈希函数——添加元素时把 k 个哈希位置全部置 1；查询时任一位置为 0 则**一定不存在**（否则添加时会被置 1），全为 1 则**可能存在**（位置可能被别的元素碰巧占过）。特点：不存元素本身（省内存）、不支持删除（标准版）、误判率随装载上升（需定期重建）。适用：key 海量且绝大多数查询无效的场景（恶意 id 99% 是假的）——本项目 100 商品用[空值缓存](#空值缓存)绰绰有余，布隆过滤器知道原理即可。
→ 返回[主文档 3.3](V3支付与Redis详解.md#33-老大穿透查一个根本不存在的-id)

### 缓存击穿
**一句话：某个**热点 key 过期的瞬间**，海量并发同时未命中、同时查 DB。**

比喻：演唱会开票瞬间售票系统崩溃——票（数据）明明有，但"查询通道"（缓存）恰好断了，所有人涌向人工窗口（DB）。

原理展开：与[穿透](#缓存穿透)（数据不存在）和[雪崩](#缓存雪崩)（大面积失效）区分：击穿是**单个热点 key** 的瞬时尖刺。两条对策路线：[互斥锁重建](#互斥锁重建)（同一 key 只放一个线程查库，其他人等——本项目采用，简单可靠）与[逻辑过期](#逻辑过期)（key 永不过期+异步刷新——无锁但有脏读，适合极致热点）。选型口诀：**一般热点用互斥锁，超热点（QPS 数万+）用逻辑过期**。
→ 返回[主文档 3.4](V3支付与Redis详解.md#34-老二击穿热点-key-过期的瞬间)

### 互斥锁重建
**一句话：同一 key 的缓存重建并发收敛为 1 个线程（SET NX 抢锁，赢家查库回写，输家等待重读）。**

比喻：会议室换投影仪——一个人去拿（赢家），其他人在座位上等 50 秒再抬头看（重读缓存），而不是全员冲向仓库（全查库）。

原理展开：三个必答细节：①**锁必须带 TTL**——赢家查库途中崩溃，无 TTL 的锁会永远卡住所有人（死锁）；代价是"赢家超时没干完，锁过期别人也能进"——最坏退化成无锁，不会错。②**输家等待要有上限**——sleep 50ms 重读，仍读不到就**直接查库兜底**：缓存不能决定可用性，"等锁"不能等成新故障。③释放用 Lua 校验 value（防[锁误删](#锁误删)）。对比逻辑过期：互斥锁有等待（延迟毛刺）但无脏读；逻辑过期无等待但返回旧值——按业务容忍度选。
→ 返回[主文档 3.4](V3支付与Redis详解.md#34-老二击穿热点-key-过期的瞬间)

### 逻辑过期
**一句话：key 永不物理过期，值内嵌"逻辑过期时间"——发现过期后返回旧值 + 单线程异步重建。**

比喻：便利店的"当日鲜食"标签——过了标签时间货架还摆着（返回旧值），店员（异步线程）在后台换新，顾客不用等。

原理展开：实现：缓存值里加一个 `expireAt` 字段；读到"已逻辑过期"时——①返回旧值（用户拿到的是几秒前的数据，多数场景无感）；②抢锁成功者起异步线程查库回写，失败者直接返回旧值。优点：**无锁等待、无瞬时尖刺**（读路径永不查库）；缺点：短暂脏读 + 热点数据常驻内存。适合超热点（秒杀商品详情）；一般场景互斥锁更简单直观。
→ 返回[主文档 3.4](V3支付与Redis详解.md#34-老二击穿热点-key-过期的瞬间)

### 缓存雪崩
**一句话：大量 key 同一时刻集体失效（或 Redis 整体宕机），DB 瞬时被打爆。**

比喻：写字楼的早高峰电梯全停——不是某一层的问题（那是击穿），是整栋楼的人瞬间全部走楼梯（DB）。

原理展开：两种形态与对策：①**集体过期**——批量导入给了相同 TTL → 对策**TTL 随机抖动**（`30min + random(10min)`，失效点打散在窗口内；零成本收益巨大）；②**Redis 宕机**——对策多级缓存（本地 Caffeine 兜一层）+ 限流降级 + Redis 高可用（哨兵/集群，超出本项目）。三板斧里抖动最便宜——**面试先答抖动，再展开另两板**。
→ 返回[主文档 3.5](V3支付与Redis详解.md#35-老三雪崩集体失效)

### 缓存与 DB 一致性
**一句话：先更新 DB 再删除缓存（Cache-Aside 标准序），接受秒级不一致。**

比喻：改了官方公告（DB）后把门口的展板撤掉（删缓存）——路人下次经过会看到重新贴的最新版（读时重建）；不能只改公告不撤展板（脏数据一直挂着）。

原理展开：三个高频问答：①**为什么删不更？**——并发写时"更新缓存"的到达顺序可能颠倒（A 先写库后写缓存，被 B 反超），旧值复活覆盖新值；删除是幂等的，谁删都一样，下次读自然重建。②**为什么先库后缓存？**——反过来（先删缓存再更库）两步之间的读请求会把**旧值**重新载入缓存。③**还有不一致窗口吗？**——有（先更库后删缓存之间，读者可能读到旧缓存），商品详情等场景可接受；**追求强一致就别用缓存**。进阶（知道名字）：延迟双删（更库后延时几百毫秒再删一次）、订阅 binlog 的 Canal（把删缓存挂在 binlog 事件上）。
→ 返回[主文档 3.6](V3支付与Redis详解.md#36-缓存与-db-的一致性先更库再删缓存)

---

## 五、分布式协调

### SET NX PX
**一句话：SET key value NX PX ttl——"不存在才设置 + 毫秒级过期"一条命令完成的加锁原语。**

比喻：储物柜投币占柜——只有空柜（NX）才能放东西，且租约一小时（PX），到点自动腾空，不怕你忘记取。

原理展开：三个参数各司其职：**NX**（Not eXists）保证互斥（已有 key 就拒绝）；**PX** 防死锁（持锁者崩溃后锁自动释放）；**value 存随机 UUID** 标识"锁的主人"（释放时校验用，防[锁误删](#锁误删)）。为什么必须一条命令——`SETNX` + `EXPIRE` 分开写的老写法在两步之间崩溃就产生**永不过期的死锁**（与限流 INCR+EXPIRE 的教训同构）。Java 侧：`opsForValue().setIfAbsent(key, value, ttl, TimeUnit.MILLISECONDS)`。
→ 返回[主文档 2.3](V3支付与Redis详解.md#23-redisops-门面把降级收敛到一处本章核心)

### 锁误删
**一句话：A 的锁超时自动释放后，A 执行到 finally 把 B 刚拿到的锁删了——释放锁比加锁更容易写错。**

比喻：酒店房卡退房超时自动失效，你 tardily 回来拿旧卡把新房客的房门刷开了——错的不是"刷"这个动作，是没核实"这还是不是你的房"。

原理展开：时间线：A 拿锁（TTL 3s）→ A 的业务卡了 5s → 锁已自动释放、B 拿到了锁 → A 的 finally 执行 `DEL lock` → **B 的锁没了**，C 又能拿锁——两个线程同时持锁，互斥失效。对策：释放前**校验 value 是自己的 UUID**——而"GET 比对 + DEL"两步之间仍有竞态（比对通过、DEL 之前锁恰好易主），所以必须用 **Lua** 把"校验+删除"焊成原子操作。这也是"多命令复合语义必须 Lua"的又一实例。
→ 返回[主文档 2.3](V3支付与Redis详解.md#23-redisops-门面把降级收敛到一处本章核心)

### Lua 脚本原子性
**一句话：Redis 单线程执行脚本，期间不插入任何其他命令——"读-判断-写"复合操作的标准实现。**

比喻：银行柜员办一笔转账（验余额+扣款+入账）是"一个业务"，不能办到一半让下一个客户插进来——Redis 把整段脚本当一个"业务"处理。

原理展开：本项目四处使用（一个判别式贯穿）：**多命令的复合语义必须 Lua**——①限量发售预减（判断库存+扣减）；②失败回补（INCRBY）；③限流（INCR+EXPIRE——两步写法在 EXPIRE 失败时产生永不过期的 key=永久限流）；④安全释放锁（校验 value+DEL）。写法要点：`KEYS[1]`/`ARGV[1]` 取参数（不要在脚本里拼接变量，防注入）；脚本要短（执行期间阻塞所有客户端）。**为什么不用分布式锁替代**：简单原子计数用 Lua 一发入魂；锁适合保护复杂临界区（一长段业务逻辑），每次两次网络往返+全局串行化——按场景选，不是谁更高级。
→ 返回[主文档 9.4](V3支付与Redis详解.md#94-第一层lua-逐行讲0-基础lua-是什么)

### 预减库存
**一句话：活动开始前把库存灌入 Redis，抢购时 Lua 原子扣减挡住绝大部分流量，DB 只接少量真实请求。**

比喻：演唱会的"网上抢票+现场兑换"——网上（Redis）抢到资格的人才能去现场（DB）兑票，没抢到的连门都到不了。

原理展开：返回值约定（本项目）：`-1` 库存不足 / `-2` 键不存在（未预热或 Redis 挂了→降级走 DB）/ `≥0` 扣减后剩余。**预减成功 ≠ 抢购成功**——后续 DB CAS 失败/撞限购 uk 时必须回补（INCRBY 对称），否则名额白吃、"看起来抢完了实际有货"（对用户是资损）。与 DB 的一致性：两个存储无事务，**最终一致**——回补可能失败、TTL 重灌可能覆盖，靠活动结束后对账校正。能讲清这个边界的候选人不多。
→ 返回[主文档 9.4-9.6](V3支付与Redis详解.md#94-第一层lua-逐行讲0-基础lua-是什么)

### 看门狗
**一句话：Redisson 给锁自动续期的后台线程——业务没执行完、锁快过期时自动延长。**

比喻：停车费自动续缴——你的会开多久（业务），车位租约（锁）就自动续多久，办完事再退（释放）。

原理展开：手写 SET NX PX 锁没有看门狗，两个选择：①给足 TTL 并接受"业务超时锁先释放"的边界（本项目 3 秒，最坏退化成无锁不会错）；②引入 Redisson，其 `lock()` 不指定 leaseTime 时启动看门狗（默认每 10s 续到 30s，业务线程活着就一直续）。这是"你的锁过期了业务还没执行完怎么办"的标准答案——先答业务边界设计，再引 Redisson。
→ 返回[主文档 3.4](V3支付与Redis详解.md#34-老二击穿热点-key-过期的瞬间)

---

## 六、限流

### 固定窗口
**一句话：每个时间窗一个计数器（INCR+EXPIRE 原子执行），超过阈值拒绝。**

比喻：游乐场"每小时限进 1000 人"——整点清零重新数，简单粗暴。

原理展开：实现三要点：①INCR+EXPIRE 必须 [Lua 原子化](#lua-脚本原子性)——两步写法在 EXPIRE 恰好失败时产生**永不过期的 key**（该用户被永久限流，本项目评审修过的洞）；②只在计数为 1 时设置过期（首个请求开窗）；③Redis 不可用时[放行](#降级方向)。**已知缺陷**：窗口切换瞬间最多放行 2 倍（前窗末尾+后窗开头同时打满）——对"防刷/成本控制"够用；要严格上滑动窗口/令牌桶。**能说出自己方案的缺陷比堆砌高级方案更显功底**。
→ 返回[主文档第 10 章](V3支付与Redis详解.md#第-10-章-ai-限流给烧钱的接口上闸)

### 滑动窗口
**一句话：按"最近 N 秒"精确计数（zset 或分片桶实现），消除固定窗口的边界突变。**

比喻：不看"这个小时进了多少人"，看"过去 60 分钟内进过多少人"——窗口跟着时间滑，没有清零时刻。

原理展开：zset 实现：每个请求把时间戳写进 zset（`ZADD key timestamp member`），判定时 `ZCOUNT key now-window now` 数最近 N 秒——精确但每次请求多一次范围计数（内存/CPU 高于固定窗口）；分片桶实现（把窗口切成 6 个 10s 小桶，各自计数求和）折中。什么时候值得上：固定窗口的 2 倍突刺已造成资损/压测不达标时。
→ 返回[主文档 10.3](V3支付与Redis详解.md#103-固定窗口的边界知道缺陷才敢说会用)

### 令牌桶
**一句话：按固定速率往桶里放令牌，请求拿到令牌才放行——允许受控突发。**

比喻：游戏里的"体力值"系统——每 6 分钟回 1 点体力，攒着可以连续打几关（突发），但连续打的总量受回充速率限制。

原理展开：两个参数（桶容量 b、速率 r）决定行为：空闲时令牌攒到 b 个，突发流量最多连续放行 b 个，长期速率被 r 封顶——**"平滑限速+容忍突发"** 的组合是网关（Sentinel、Nginx limit_req、Guava RateLimiter）的主流选择。对比固定窗口：无边界突变问题，且有"攒额度"语义（AI 接口允许用户偶尔连问，长期不超限即可——其实比固定窗口更贴需求）。
→ 返回[主文档 10.3](V3支付与Redis详解.md#103-固定窗口的边界知道缺陷才敢说会用)

### 降级方向
**一句话：限流依赖的 Redis 挂了怎么办？——放行（fail-open），因为限流是成本保护不是可用性核心。**

比喻：商场的防盗闸机断电——应该敞开让人走（生意继续），而不是把所有人锁在外面（为防盗把店关了）。

原理展开：fail-open vs fail-close 的选型取决于**组件保护的是什么**：限流保护的是"钱"（成本）→ 挂了放行（多花点钱，服务不断）；鉴权保护的是"数据安全" → 挂了**拒绝**（fail-close，宁可服务不可用不能放陌生人进来）；缓存保护的是"性能" → 挂了走库（变慢）。**没有统一的"降级=关掉"，每个组件单独想清楚"它挂了业务该怎么活"**——这是面试区分"背过降级"与"想过降级"的试金石。
→ 返回[主文档 2.3](V3支付与Redis详解.md#23-redisops-门面把降级收敛到一处本章核心)

---

## 七、安全

### 时序攻击
**一句话：通过比较操作的耗时差异逐字节猜出密钥/签名——`equals` 的短路返回泄露了"对了几位"。**

比喻：试密码锁时，转对一位会"咔哒"一声——听声辨位，一位一位试，比暴力全试快几个数量级。

原理展开：`String.equals` 碰到第一个不同字符立即返回 false——耗时 ∝ 正确前缀长度。攻击流程：伪造 256 种首字符的签名逐个提交，响应慢 1μs 的那个就是对的；固定首位再试第二位……256×32 次试出 64 位十六进制签名（对比暴力 16^32 次）。对策：`MessageDigest.isEqual` 做 **constant-time 比较**——无论差异在第几位，耗时恒定，侧信道关闭。属于"安全代码的细节就是魔鬼"：一行代码的差别，攻防两重天。
→ 返回[主文档 5.4](V3支付与Redis详解.md#54-时序攻击为什么不用-equals)

### 公网端点的身份模型
**一句话：业务接口靠"你是谁"（token 认证），回调接口靠"消息是谁发的、有没有被改"（签名完整性）——两套模型不可混用。**

比喻：公司内部刷工卡进门（认证：你是谁）vs 收快递看防伪码（完整性：货是谁寄的、有没有被动过）——快递员没有你的工卡，但你依然能确认货是真发件人寄的。

原理展开：落地两件事：①Sa-Token 白名单放行 `/api/v1/payments/callback/**`（第三方拿不到你的用户 token）；②验签把关（HMAC/RSA2 验证来源与完整性）。常见错误反应是"给回调也加个 token"——没想清楚调用方是机器不是人。**面试能讲清"为什么回调不用登录态"就赢了大多数候选人**。延伸：回调处理还要防重放（同一报文重发——本项目靠幂等天然免疫）、防伪造金额（金额核对）——三件套构成回调安全全景。
→ 返回[主文档 6.2](V3支付与Redis详解.md#62-controller三个反直觉的设计)

---

## 返回主文档

[V3支付与Redis详解.md](V3支付与Redis详解.md)
