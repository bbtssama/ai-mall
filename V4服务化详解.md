# V4 服务化从零到实战：跟着 AI 种草商城第一次"拆"服务

> ai-mall 项目升级讲解文档 · V4（服务拆分 / 服务间通信 / 注册发现 / 网关 / 熔断）
> 配套词典：[V4服务化详解【知识词典】.md](V4服务化详解【知识词典】.md)——正文中每个带链接的概念都在词典里有"一句话核心+比喻+原理展开+代码对应+常见追问"
> 前置阅读：[V1.5工程基建详解.md](V1.5工程基建详解.md)（可降级可插拔原则）、[V2内容社区与RAG详解.md](V2内容社区与RAG详解.md)（AI 能力现状）
> 读法建议：**本章假设你零微服务基础**——前两章是纯概念（用餐厅/快递讲清楚），第 3 章起才碰代码。按章顺序读，每章末尾有动手作业。

---

## 【易错关键点】（先背这个，全文读完回来再背一遍）

1. **微服务不是"多个 Spring Boot 项目"**——是"按业务边界拆分、各自独立部署、通过网络协作"的架构风格。**拆分边界（数据归谁）比拆分动作（建几个模块）重要十倍**。
2. **本项目只拆了 ai-service 一个**——用户/商品/订单事务耦合紧密，拆开要引入分布式事务，当前业务量不划算。**面试能讲"为什么不拆其余"比堆五个服务高级**（[为什么不全拆](V4服务化详解【知识词典】.md#为什么不全拆)）。
3. **拆 AI 的理由是"耗时差异"**——AI 秒级 vs 普通接口毫秒级，放一起 AI 会占满 Tomcat 线程池拖垮下单。不是为了简历好看（[五条理由](V4服务化详解【知识词典】.md#拆-ai-的五条理由)）。
4. **服务间调用必须带鉴权**——"都在内网就裸奔"是错觉：端口误映射/横向渗透后，内部接口就是免费后门。本项目用 [HMAC 内部签名](V4服务化详解【知识词典】.md#hmac-内部签名) + [5 分钟时间戳防重放](V4服务化详解【知识词典】.md#防重放)。
5. **`lb://服务名` 不是魔法**——是"按名字找实例"的地址格式：网关/客户端拿服务名问[注册中心](V4服务化详解【知识词典】.md#注册中心)"mall-app 在哪台机器"，拿到实例列表后[负载均衡](V4服务化详解【知识词典】.md#负载均衡)挑一个。
6. **熔断三态机必背**：Closed（正常统计失败率）→ 超阈值 Open（快速失败走 fallback）→ 等 30s Half-Open（放 3 个试探）→ 恢复闭合。**熔断是"快速失败"，不是"重试"**（[熔断器](V4服务化详解【知识词典】.md#熔断器)）。
7. **Resilience4j 配置必须顶层 `resilience4j:`**——挂 `spring:` 下**静默失效**（不报错、参数全默认）。本项目真实踩过（[评审复盘](#第-9-章-评审复盘本轮踩的坑)）。
8. **写好的抽象必须有调用方**——本项目第三次踩"写了≠生效"（V3 缓存不接线 / V3 榜单不接线 / V4 双模式无调用方）。**评审第一动作是 grep 调用点**。
9. **网关是安全边界**——`/internal/**` 必须在网关层拒绝（404 而非 403，不向探测者确认端点存在），否则外部直连 8081 就绕过了所有设计（[网关](V4服务化详解【知识词典】.md#网关)）。
10. **多网卡机器注册 IP 必须显式指定**——Nacos 自动选择会选中 VMware 虚拟网卡（192.168.49.1），注册上去的地址跨机器不可达。真实踩坑（[多网卡陷阱](V4服务化详解【知识词典】.md#多网卡陷阱)）。
11. **默认 local 模式**——不配 `aimall.ai.mode` 时一切照旧（V3 形态）：**面试官克隆仓库不必起 ai-service/Nacos/gateway，单跑 backend 就能体验全功能**（零依赖铁律）。
12. **API Key 只存在于 ai-service**——密钥隔离是拆分的收益之一；本项目的密钥放 `application-local.yml`（gitignore 忽略、默认激活）。

---

## 全书地图

```
第 0 章  破除恐惧：你其实天天在用"微服务"（心智模型）
第 1 章  单体 vs 微服务：一家餐厅的进化史（零基础）
第 2 章  拆分边界：为什么只拆 AI、为什么不全拆（判断标准）
第 3 章  服务怎么"说话"：HTTP + 声明式客户端
第 4 章  服务间鉴权：HMAC 签名 + 防重放
第 5 章  注册发现：从电话簿到 Nacos
第 6 章  网关：大门、保安与前台
第 7 章  熔断：给故障装一根保险丝
第 8 章  双模式落地与故障隔离的兑现
第 9 章  评审复盘：本轮踩的坑（真实学习素材）
第 10 章 验收清单 + 面试速查 + 报错速查
```

---

# 第 0 章 破除恐惧：你其实天天在用"微服务"

## 0.1 先别管定义，看三个你熟悉的场景

**场景一：你点外卖。** 商家（厨房）、平台（美团 App）、骑手（配送）是三个独立团队/系统——你下单时，美团调用商家的菜单接口、再叫骑手。**没有任何一个系统"包含"另一个**，它们通过网络协作。这就是微服务的日常。

**场景二：你刷视频。** 点赞系统挂了，你还能看视频（点赞按钮转圈而已）；推荐系统挂了，你还能搜。**一个大 App 内部是几十个独立小服务**——坏一个不塌全部。

**场景三：本项目 V1~V3。** 一个 Spring Boot 进程（backend:8080）装下了用户/商品/订单/内容/AI——这就是[单体架构](V4服务化详解【知识词典】.md#单体架构)。它没有任何问题（后面会讲单体被"妖魔化"的真相），但有一个结构性隐患：**AI 调用要 5~30 秒，占着 Tomcat 线程不放——AI 一慢，全站排队**。

> 💡 **心智模型①（一句话）**：微服务 = 把"一个大进程里的模块"变成"几个小进程"，它们用 HTTP 互相调用——**拆的是进程边界，本质是隔离故障与独立伸缩**。

## 0.2 V4 一张图（先刻进脑子，后面逐章展开）

```
                          ┌──────────────────┐
        用户/浏览器  ────► │   gateway:9000    │  ← 大门/保安（第 6 章）
                          │  只放行 /api/**   │
                          └───────┬──────────┘
                                  │ lb://mall-app（按名字找，第 5 章）
                                  ▼
                          ┌──────────────────┐
                          │  mall-app:8080    │  ← 单体主体：用户/商品/订单/内容
                          │ (Sa-Token鉴权/    │
                          │  会话/落库)       │
                          └───────┬──────────┘
                                  │ HTTP+HMAC签名（第 3、4 章）
                                  ▼
                          ┌──────────────────┐
                    ┌───► │ ai-service:8081   │  ← 唯一拆出来的：AI+RAG+API Key
                    │     │ (ChatClient/语料) │
                    │     └───────┬──────────┘
                    │             │ 需要商品数据时回调
                    └─────────────┘（AI 查商品要"回家问"，第 3 章）
```

三个进程、三种角色：**大门（gateway）→ 业务主体（mall-app）→ AI 能力（ai-service）**。V4 全部内容就是把这张图变成代码并回答四个问题：怎么调（第 3 章）、怎么证明是自己人（第 4 章）、怎么找到对方（第 5 章）、挂了怎么办（第 7 章）。

## 0.3 高频术语速查表（全书通用）

| 术语 | 一句话 | 详见词典 |
|---|---|---|
| 单体 | 全部功能在一个进程里 | [单体架构](V4服务化详解【知识词典】.md#单体架构) |
| 服务拆分 | 按业务边界切成独立部署的进程 | [服务拆分](V4服务化详解【知识词典】.md#服务拆分) |
| RPC | 服务间调用（泛指，HTTP 是一种实现） | [RPC](V4服务化详解【知识词典】.md#rpc) |
| 注册中心 | 服务电话簿：名字→地址列表 | [注册中心](V4服务化详解【知识词典】.md#注册中心) |
| 服务发现 | 从电话簿查地址的动作 | [服务发现](V4服务化详解【知识词典】.md#服务发现) |
| 网关 | 统一入口：路由/过滤/安全 | [网关](V4服务化详解【知识词典】.md#网关) |
| 负载均衡 | 多实例挑一个（轮询等） | [负载均衡](V4服务化详解【知识词典】.md#负载均衡) |
| 熔断 | 故障时快速失败防拖垮 | [熔断器](V4服务化详解【知识词典】.md#熔断器) |
| 降级 | 失败时返回兜底结果 | [服务降级](V4服务化详解【知识词典】.md#服务降级) |
| 级联故障 | 一个服务挂拖垮一串 | [级联故障](V4服务化详解【知识词典】.md#级联故障) |

## 本章动手作业

不写代码，回答三个问题：
1. 用外卖场景说出"三个系统各自独立部署、网络协作"对应微服务的哪三个特征。
2. V1~V3 的单体里，为什么 AI 是"最危险的住户"？（提示：耗时与线程池。）
3. 用户能直接访问 8081 吗？为什么？（答：不能也不应该——8081 只接带签名的内部调用；网关层还挡了 /internal/**。）

---

# 第 1 章 单体 vs 微服务：一家餐厅的进化史（零基础）

> 核心机制：[单体架构](V4服务化详解【知识词典】.md#单体架构) · [微服务](V4服务化详解【知识词典】.md#微服务) · [服务雪崩](V4服务化详解【知识词典】.md#级联故障) · [单体被妖魔化](V4服务化详解【知识词典】.md#单体被妖魔化)

## 1.1 一家餐厅的单体时代

你开了一家餐厅，前厅（点餐）、后厨（做菜）、收银（结账）**都在一个铺子里**——这就是单体：一个进程、一次部署、一个数据库。

**优点（先说好话，面试要敢说）**：
- **没有网络开销**——前厅喊一嗓子后厨就听到了（进程内方法调用，纳秒级）；
- **事务简单**——下单扣库存，一个 `@Transactional` 全搞定；
- **部署/调试简单**——一个 jar、一个断点跟到底。

**什么时候开始疼**：
- 一个模块出事全店停业——后厨着火（AI 线程池被占满），前厅点餐（商品接口）也得等；
- 想给后厨换设备（换 AI 模型），得全店歇业装修（全量重新部署）；
- 收银台招的人只该碰钱，却能看到全部菜谱秘方（API Key、全部数据库权限在一个进程里）。

## 1.2 进化：把"最重的活"外包出去

注意！**不是**把餐厅拆成三家公司（那是过度拆分），而是**只把一件事外包**：中央厨房（AI）。

- 后厨（AI）单独建厂：它做一道菜要 30 分钟（模型推理），在自己厂里慢慢做，不堵前厅；
- 前厅通过"传菜系统"（HTTP）下单、取菜；
- 中央厨房着火？前厅照常营业，只是暂时没有那道菜（**故障隔离**——V4 核心收益）；
- 生意火了想多开一条中央厨房产线？独立扩容，餐厅一根毫毛不动（**独立伸缩**）。

> 💡 **心智模型②**：微服务的价值不在"拆得多"，在"**拆得对**"——把"耗时/故障/伸缩/安全属性都与众不同"的那块拆出去。本项目的 AI 恰好四条全占。

## 1.3 反面：拆得太碎的代价（面试讲出来碾压背书者）

假如有 5 个服务，一次下单要跨 3 个服务扣库存+建订单+清购物车：
- **分布式事务**：本地 `@Transactional` 失效了！要么 Seata、要么本地消息表（V2 词典讲过）——复杂度暴涨；
- **网络开销与故障面**：每次内部调用都是一次网络往返，多一倍可挂的点；
- **排障复杂**：一个请求穿 3 个进程，没有链路追踪就是灾难（还好 V1.5 的 traceId 预留了钩子）。

所以业界的共识早已从"拆越细越好"回到"**按需拆**"。你的项目正是这个共识的正确示范。

## 本章动手作业

1. 列出单体的三个优点——敢说优点才证明你懂取舍而不是跟风。
2. 用"中央厨房"比喻复述拆 AI 的四条理由（耗时/故障/伸缩/密钥）。
3. 思考：如果把"订单"也拆出去，下单流程会多出哪两样复杂度？（分布式事务 + 跨服务排障。）

---

# 第 2 章 拆分边界：为什么只拆 AI、为什么不全拆

> 核心机制：[服务拆分](V4服务化详解【知识词典】.md#服务拆分) · [拆 AI 的五条理由](V4服务化详解【知识词典】.md#拆-ai-的五条理由) · [为什么不全拆](V4服务化详解【知识词典】.md#为什么不全拆) · [数据归属](V4服务化详解【知识词典】.md#数据归属) · [康威定律](V4服务化详解【知识词典】.md#康威定律)

## 2.1 判断标准（三问宪法在架构上的应用）

每个候选服务都要过三问，**有一条站不住就不拆**：

| 候选 | 砍掉它会怎样（不拆） | 结论 |
|---|---|---|
| **AI 服务** | AI 慢→占满线程池→**全站排队**；模型挂→**全站挂**；API Key 与业务同进程泄露面大 | ✅ 拆 |
| 用户服务 | 用户表被所有业务 join——拆开后每次查用户多一次网络往返 | ❌ 不拆 |
| 商品+订单 | 下单=同一事务里锁库存+建订单+清购物车——拆开要分布式事务 | ❌ 不拆 |

## 2.2 拆 AI 的五条理由（面试逐条背）

| 理由 | 现场说明 |
|---|---|
| **耗时差异** | AI 调用秒级~几十秒，普通接口毫秒级——放一起 AI 请求占满 200 个 Tomcat 线程，下单接口排队 |
| **故障隔离** | 模型超时/限流/供应商挂了 → 只有"AI 助手"不可用，下单/浏览/支付照常 |
| **独立扩容** | AI 是成本密集（token 计费），需要独立扩缩与独立限流（V3 已做） |
| **密钥隔离** | API Key 只存在于 ai-service，泄露半径最小 |
| **模型可替换** | 换模型/供应商只改这一个服务，业务零感知 |

## 2.3 拆分的实质：数据归谁（比建模块重要十倍）

建一个新 Spring Boot 工程只是"动作"；**划清数据归属才是"设计"**：

| 数据 | 归属 | 另一方怎么访问 |
|---|---|---|
| t_knowledge_*（RAG 语料） | **ai-service** | mall-app 通过 `/internal/v1/rag/index` **请求**索引 |
| 商品/订单/用户/笔记/支付 | **mall-app** | ai-service 通过 `/internal/v1/products/search` **回调**查询 |
| API Key | **ai-service 独占** | 业务服务永不接触 |

**注意"请求"这个词**：语料表物理上还在同一个 MySQL，但 mall-app 从"直接写表"变成"调 ai-service 的接口"——**归属权与访问权分离**，这叫"逻辑边界先于物理边界"（面试金句）。当哪天语料真的搬家到独立库，业务代码一行不用改——因为它们本来就没碰过这张表。

## 2.4 反向论证（面试加分项原话）

> "用户/商品/订单事务耦合紧密——下单要在同一事务里锁库存、建订单、清购物车。拆开就要引入分布式事务（Seata/本地消息表），当前业务量不划算。**这正是我把它们留在单体里的原因**——能讲出'不做什么'和'为什么不做'，比堆五个服务高级得多。"

## 本章动手作业

1. 默写拆 AI 五条理由（耗时/故障/扩容/密钥/替换）。
2. 说出三张数据归属行——特别是"语料归 ai-service，mall-app 只能请求不能直写"。
3. 思考：如果未来笔记量涨到语料需要独立库，要改哪些代码？（答：只改 ai-service 的 datasource——业务侧本来就没碰过语料表，这就是归属清晰的回报。）

---

# 第 3 章 服务怎么"说话"：HTTP + 声明式客户端

> 核心机制：[RPC](V4服务化详解【知识词典】.md#rpc) · [声明式客户端](V4服务化详解【知识词典】.md#声明式客户端) · [动态代理](V4服务化详解【知识词典】.md#动态代理) · [@HttpExchange vs Feign](V4服务化详解【知识词典】.md#httpexchange-vs-feign) · [超时策略](V4服务化详解【知识词典】.md#超时策略)

## 3.1 零基础：服务间调用就是"你写过的 HTTP 客户端"

mall-app 要问 ai-service"这句话怎么答"，本质是 **mall-app 作为 HTTP 客户端，POST 了一个请求**——和你前端 fetch 后端、V3 支付回调被支付宝 POST，是同一件事：

```
mall-app（客户端）                     ai-service（服务端）
    │  POST /internal/v1/ai/chat           │
    │  Header: X-Internal-Sign: xxx        │
    │  Body: {"message":"耳机多少钱"}        │
    │ ──────────────────────────────────► │
    │                                      │ 校验签名→调模型→组织答案
    │  200 OK {"answer":"AirSound Pro..."} │
    │ ◄────────────────────────────────── │
```

唯一的"新东西"：以前你写 `fetch`/`RestTemplate` 是**命令式**（拼 URL、设头、发请求、解响应），现在换**声明式**——见 3.2。

## 3.2 声明式客户端：像调用本地方法一样调远程

`AiServiceClient`（mall-app 侧）逐行精读：

```java
@HttpExchange(url = "${aimall.ai-service.url}")     // ① 基地址从配置读（不写死）
public interface AiServiceClient {

    @PostExchange("/internal/v1/ai/chat")           // ② 方法=一个端点
    Map<String, Object> chat(@RequestBody Map<String, Object> body);  // ③ 参数=请求体
}
```

使用方完全看不到 HTTP 细节：

```java
// ChatServiceImpl / RemoteAiService 里：
Map<String, Object> resp = client.chat(body);       // 看起来像本地方法！
String answer = (String) resp.get("answer");
```

**它怎么做到的**——[动态代理](V4服务化详解【知识词典】.md#动态代理)（面试常问）：Spring 启动时扫描 `@HttpExchange` 接口，**在内存里生成一个实现类**（字节码级别），把"方法调用"翻译成"HTTP 请求"：

```
你调 client.chat(body)
  → 代理拦截：查注解得知 POST /internal/v1/ai/chat
  → 序列化 body 为 JSON，带上签名头（见第 4 章）
  → RestClient 真正发 HTTP
  → 收到响应 JSON，反序列化为 Map 返回
```

**为什么不直接用 RestClient 裸写**：三处调用×（拼URL+设头+解析）的样板代码，且每个调用点都要记得带签名头——漏一处就是安全洞。声明式把"怎么调"收敛到一个接口定义，**调用点零样板、签名自动带**。

## 3.3 为什么选 @HttpExchange 而不是 OpenFeign（选型论证）

| | Spring 6 `@HttpExchange` | OpenFeign |
|---|---|---|
| 依赖 | **零额外**（spring-web 自带） | spring-cloud-openfeign + BOM 版本对齐 |
| 编程模型 | 接口+注解+动态代理 | 接口+注解+动态代理（**几乎一样**） |
| 生态整合 | 与 RestClient/WebClient 一体 | 与注册中心/负载均衡深度整合 |

**选型理由**：本项目只做基础微服务，引入 Spring Cloud 全家桶的版本对齐风险不划算；**接口写法与 Feign 几乎一致，将来真要 Feign 生态时迁移成本极低**——"按需演进"在依赖选型上的体现。注意 gateway 模块**用了** Spring Cloud（Gateway 本身就是 SC 家族），所以这不是"排斥 Spring Cloud"，是"**每个依赖都要有当下成立的理由**"。

## 3.4 双向调用与超时（两张不同的超时表）

V4 有**两条**服务间链路，超时策略完全不同（这个对比是面试亮点）：

```java
// 链路 A：mall-app → ai-service（AI 本来就慢）
f.setConnectTimeout(Duration.ofSeconds(2));    // 连不上快速失败
f.setReadTimeout(Duration.ofSeconds(120));     // 读取给足（长回答；本项目 remote 模式下为整段返回）

// 链路 B：ai-service → mall-app（查商品是毫秒级小事）
f.setConnectTimeout(Duration.ofSeconds(2));
f.setReadTimeout(Duration.ofSeconds(5));       // ★ AI 链路已经很慢，回调必须快进快出
```

> ⚠️ **诚实边界：remote 模式下是"伪流式"**。`RemoteAiService.stream()` 的实现是 `Flux.just(chat(message, null))`——**先拿到完整结果，再包装成 Flux 返回**，不是逐 token 透传。所以：**local 模式（`aimall.ai.mode` 默认）是真流式，remote 模式是伪流式**。
> 面试若被追问"拆服务后流式怎么做"：**答"当前 remote 是整段返回，真流式透传需要把 SSE 帧跨进程转发（WebClient 的 `bodyToFlux` 或 RSocket），这是已知的下一步"**——不要硬说成 token 级流式。

> 💡 **心智模型③**：超时的本质是"**我愿意为这一步等多久**"——等待预算按整条链路分配：AI 请求总预算 120s，其中留给商品回调的只有 5s。**全链路统一超时 = 有人在白白等死**。

## 3.5 工具层降级：AI 查商品失败怎么办

ai-service 的 `ProductSearchTool`（真实代码精读）：

```java
try {
    MallClient.ProductSearchResult result = mallClient.searchProduct(query);
    if (result == null || result.items() == null || result.items().isEmpty()) {
        return "{\"total\":0,\"items\":[]}";          // 没查到：如实说
    }
    return objectMapper.writeValueAsString(...);
} catch (Exception e) {
    // 跨服务调用失败：降级为"查不到"，让模型如实说——绝不编造商品
    log.warn("回调 mall-app 搜索商品失败（工具降级）: {}", e.getMessage());
    return "{\"total\":0,\"items\":[],\"error\":\"service-unavailable\"}";
}
```

**工具异常绝不外抛**（外抛会让整个对话失败）——返回结构化"查不到"，模型据此回答"暂时查不到商品信息"。与 V2 检索降级同一思路：**每个跨服务点都要想"对面挂了我怎么办"**。

## 本章动手作业

1. 画出 client.chat(body) 到 ai-service 收到请求之间发生的 5 件事（代理拦截→查注解→序列化+签名→HTTP→反序列化）。
2. 思考：为什么链路 B 的读超时是 5 秒而不是 30 秒？（AI 总预算 120s 不能被慢回调吃掉；且商品查询正常毫秒级完成，5s 已是 50 倍冗余。）
3. 查mall-app 和 ai-service 的 InternalSignature/Filter 两个文件，确认两侧"client=" + "&ts=" 拼接串一字不差——签名一致性的亲手验证。

---

# 第 4 章 服务间鉴权：HMAC 签名 + 防重放

> 核心机制：[HMAC 内部签名](V4服务化详解【知识词典】.md#hmac-内部签名) · [防重放](V4服务化详解【知识词典】.md#防重放) · [constant-time 比较](V4服务化详解【知识词典】.md#时序攻击-v3-词典) · [为什么内网也要鉴权](V4服务化详解【知识词典】.md#内网裸奔错觉)

## 4.1 先撞南墙："内网就安全"是错觉

ai-service 的 `/internal/v1/ai/chat` 若不加鉴权：任何能到达 8081 端口的人，**免费使用你的 DeepSeek 额度**。风险路径并不遥远：
- docker 端口误映射（`-p 8081:8081` 写进 compose 又部署到公网机器）；
- 办公网/容器网被横向渗透后，内部接口裸奔。

**"内网"是一个会变的假设，鉴权不该建立在假设上。**

## 4.2 协议：三个头 + 一个时间窗

```java
// 客户端每次请求自动带（AiClientConfig 的 defaultRequest）：
X-Internal-Client: ai-service          // 我是谁
X-Internal-Ts: 1788765432109           // 当前毫秒时间戳
X-Internal-Sign: HMAC-SHA256("client=ai-service&ts=1788765432109", secret)
                                       // 用双方共享密钥对"身份+时间"签名
```

服务端 `InternalAuthFilter` 校验三步（真实代码骨架）：

```java
// ① 时间戳新鲜度：|now - ts| ≤ 5 分钟，否则拒绝（防重放）
if (Math.abs(System.currentTimeMillis() - timestamp) > MAX_SKEW) { reject("timestamp expired"); }
// ② 用同一算法+同一密钥重算签名
String expected = sign(client, ts);
// ③ constant-time 比较（防时序攻击，与 V3 支付验签同一行代码）
if (!MessageDigest.isEqual(expected.getBytes(), sign.getBytes())) { reject("sign error"); }
```

## 4.3 为什么是 HMAC + 时间戳（与 V3 支付验签对照着记）

这套协议你在 V3 已经学过——**同一个算法家族的第三次复用**：

| 场景 | 密钥 | 防什么 | 额外机制 |
|---|---|---|---|
| V3 支付回调 | 与第三方约定 secret | 伪造回调/篡改金额 | 金额核对+状态机 CAS |
| V3 MockPayChannel | 同上 | 同上 | —— |
| **V4 内部调用** | 服务间共享 secret | 伪造内部请求 | **时间戳防重放** |

**为什么内部调用要加防重放而支付不用**：支付回调幂等天然免疫重放（重复回调=已处理）；而内部 AI 接口没有幂等语义——截获一次合法请求原样重发 1 万次 = 烧光额度。**同一个威胁，不同的免疫机制**——这个对比面试讲出来非常加分。

## 4.4 双侧实现的对称性（容易错的地方）

- mall-app（客户端侧）：`InternalSignature.sign()` 静态工具
- ai-service（服务端侧）：`InternalAuthFilter` 内的 sign()

**两侧的 plain 拼接串必须一字不差**（`"client=" + client + "&ts=" + ts`），secret 也必须同值（都用 `${AIMALL_INTERNAL_SECRET:aimall-internal-secret}`）。改一侧不改另一侧 = 全部 401——这就是"协议"的含义：**约定先于实现，双侧同步演进**。

## 本章动手作业

1. curl 不带三个头打 `http://localhost:8081/internal/v1/ai/chat` → 401 missing headers。
2. 用正确 secret 但把 ts 改成 10 分钟前 → 401 timestamp expired（重放被拒实证）。
3. 思考：为什么时间窗是 5 分钟而不是 5 秒？（两侧机器时钟可能有偏差，窗口太小会误杀正常请求；5 分钟在"容忍时钟漂移"与"限制重放窗口"间取平衡。）

---

# 第 5 章 注册发现：从电话簿到 Nacos

> 核心机制：[注册中心](V4服务化详解【知识词典】.md#注册中心) · [服务发现](V4服务化详解【知识词典】.md#服务发现) · [心跳](V4服务化详解【知识词典】.md#心跳) · [DiscoveryClient 抽象](V4服务化详解【知识词典】.md#discoveryclient-抽象) · [SimpleDiscoveryClient](V4服务化详解【知识词典】.md#simplediscoveryclient) · [多网卡陷阱](V4服务化详解【知识词典】.md#多网卡陷阱)

## 5.1 零基础：没有注册中心的痛点

gateway 想转发到 mall-app，地址哪来？写死 `http://localhost:8080`——单机没问题，但当 mall-app 部署 3 个实例（192.168.6.1:8080 / 8082 / 8083）呢？**谁记得住、谁感知实例上下线**？

注册中心 = **服务电话簿**（+ 自动更新）：

```
mall-app 启动 → 向 Nacos 登记："我叫 mall-app，地址 192.168.6.1:8080"
mall-app 挂了   → Nacos 5 秒没收到心跳，划掉这条记录
gateway 转发    → 问 Nacos："mall-app 有哪些号？" → 拿到列表 → 挑一个转发
```

## 5.2 `lb://` 的完整含义（拆开讲）

路由里写的 `lb://mall-app` 不是 URL，是一个**指令**：

```
lb://  =  "Load Balanced 地解析后面的服务名"
   ① 向 DiscoveryClient 要 mall-app 的实例列表
   ② 负载均衡器（轮询）从列表挑一个
   ③ 用真实地址（http://192.168.6.1:8080）发请求
```

**服务名是逻辑地址，实例是物理地址**——注册发现就是把"逻辑→物理"的翻译自动化。

## 5.3 本项目的三层实现（面试最有讲究的部分）

```
抽象层：DiscoveryClient（Spring Cloud 标准）
  ├── 实现 1：SimpleDiscoveryClient —— 静态配置的实例表（本项目默认，零依赖）
  └── 实现 2：NacosDiscoveryClient —— 真注册中心（已接入 192.168.6.102:8848）
```

**为什么两个并存**：
- **Nacos 主**：三服务启动即注册（`nacos.discovery.enabled=true` + `fail-fast=false`）——真实体验注册发现；
- **Simple 兜底**：Nacos 挂了，`lb://mall-app` 仍有静态实例可解析——**注册中心自身故障也不能拖垮转发**（可降级铁律延伸到基础设施）。

**为什么敢说"这就是 Nacos 的抽象"**：两者实现的是**同一个接口** `DiscoveryClient.getInstances(serviceName)`。切换只改 starter 和 server-addr，`lb://mall-app` 与全部业务代码零变动——与项目里 storage/embedding/pay 的可插拔是**同一套设计哲学第四次出现**。面试原话："注册发现我用的是 DiscoveryClient 抽象——演示环境静态实例零依赖跑，生产换 Nacos 只改配置。"

## 5.4 真实踩坑：多网卡机器的注册 IP（本机实测）

启动后 Nacos 控制台看到 mall-app 注册地址是 `192.168.49.1`——**VMware 虚拟网卡**！其他机器照这个地址访问必然失败。

根因：Nacos 客户端"自动选 IP"的策略是**取第一块非回环网卡**，而 Windows 装了 VMware/VirtualBox 后虚拟网卡往往排前。修复（三服务的 yml 都有）：

```yaml
spring:
  cloud:
    nacos:
      discovery:
        ip: ${NACOS_REGISTER_IP:192.168.6.1}   # ★ 显式指定注册 IP
```

> 💡 教训（比知识点值钱）：**"自动检测"在复杂环境下是隐形炸弹**——注册 IP、时区、编码，凡是环境相关的东西，生产都值得显式声明。

## 本章动手作业

1. 起三个服务，开 Nacos 控制台（192.168.6.102:8848/nacos）→ 服务列表应有 gateway/mall-app/ai-service，实例 IP 应为 192.168.6.1。
2. 停掉 mall-app，等 15 秒刷新控制台——实例被剔除（心跳失效实证）。
3. 思考：为什么 gateway 敢在 Nacos 挂时继续转发？（simple 静态实例兜底——转发依赖"抽象"，任一实现可用即可。）

---

# 第 6 章 网关：大门、保安与前台

> 核心机制：[网关](V4服务化详解【知识词典】.md#网关) · [路由/断言/过滤器](V4服务化详解【知识词典】.md#路由断言过滤器) · [GlobalFilter](V4服务化详解【知识词典】.md#globalfilter) · [横切关注点](V4服务化详解【知识词典】.md#横切关注点)

## 6.1 零基础：为什么需要"再前面一层"

没有网关：前端要记 mall-app 的地址；用户理论上能直接打 8081 的内部端点（如果端口开着）；每个服务都要自己配跨域。有了网关（9000）：

- **一个门牌**：前端只认识 9000，后端怎么拆都无所谓；
- **一个保安**：`/internal/**` 在门口就拒绝；
- **一个前台**：跨域/traceId/访问日志统一处理，业务服务不操心。

## 6.2 路由表（本项目的全部路由，面试画这张图）

```yaml
routes:
  - id: mall_api        # ① 业务接口
    uri: lb://mall-app
    predicates: [ Path=/api/** ]          # 断言：路径匹配才进这条路由
  - id: mall_files      # ② 静态资源
    uri: lb://mall-app
    predicates: [ Path=/files/** ]
  - id: mall_actuator   # ③ 运维探针
    uri: lb://mall-app
    predicates: [ Path=/actuator/** ]
  - id: deny_internal   # ④ ★ 安全边界
    uri: no://op                          # "不转发"占位符
    predicates: [ Path=/internal/** ]
    filters: [ SetStatus=404 ]            # 返回 404——不向探测者确认端点存在
```

> ⚠️ **注意是 4 条不是 3 条**（`mall_actuator` 容易被漏，但它正好体现了"探针要经网关统一暴露"的运维考虑）。另外——**这张表里没有 ai-service 的路由**：内部调用走 HMAC 签名直连（见 6.5），不经网关，这正是"外部只能从 9000 进来"的边界设计。

**三个组件各司其职**（Gateway 的核心概念）：**断言**（Predicate）判断"这个请求归哪条路由"；**过滤器**（Filter）在转发前后做加工（改头/改状态/记日志）；**路由**（Route）= 断言 + 目标 + 过滤器的组合。

**为什么 404 不是 403**：403 等于告诉探测者"端点存在但你没权限"——引导继续挖；404 是"没这东西"。安全上这叫**最小信息披露**（与 V3 验签失败不透露细节同一思路）。

## 6.3 TraceIdGlobalFilter：V1.5 伏笔的兑现（代码精读）

```java
@Bean
@Order(-1)   // 最先执行：后续所有过滤器/路由都能拿到 traceId
public GlobalFilter traceIdFilter() {
    return (exchange, chain) -> {
        String incoming = request.getHeaders().getFirst("X-Trace-Id");
        String traceId = (incoming != null && !incoming.isBlank())
                ? incoming
                : "gw-" + UUID.randomUUID().toString().substring(0, 8);  // ① 没有→生成

        ServerHttpRequest mutated = request.mutate()
                .header("X-Trace-Id", traceId)      // ② 加到转发请求头（透传给 mall-app）
                .build();
        exchange.getResponse().getHeaders().set("X-Trace-Id", traceId);  // ③ 响应也带回

        long start = System.currentTimeMillis();
        return chain.filter(exchange.mutate().request(mutated).build())
                .then(Mono.fromRunnable(() -> log.info("[{}] {} {} -> {} ({}ms)", ...)));  // ④ 统一访问日志
    };
}
```

**为什么说"伏笔兑现"**：V1.5 的 TraceIdFilter 写了"上游传了就沿用，否则自己生成——为 V4 预留"。现在上游真的存在了：

```
gateway(生成 gw-ab12cd34) → mall-app(沿用) → ai-service(签名调用透传)
```

`grep gw-ab12cd34` 能跨三个进程捞出同一次请求的全部日志——**微服务排障的救命稻草**。这就是"设计时想清楚，演进时零成本"。

## 6.4 网关没做什么（诚实边界）

- **没做鉴权**：用户 token 校验仍在 mall-app（Sa-Token）。生产可上移到网关统一做（Spring Security + JWT）——**知道位置即可**；
- **没做限流**：V3 的限流在业务侧（Redis）；网关层限流（RequestRateLimiter + Redis）是规模化选项。

## 本章动手作业

1. `curl http://localhost:9000/api/v1/products` → 正常（经网关到 mall-app）。
2. `curl http://localhost:9000/internal/v1/anything` → 404（安全边界实证）。
3. 响应头里有 X-Trace-Id，且 mall-app 日志同一 traceId（透传实证）。
4. `GET :9000/actuator/gateway/routes` → 看到 3 条路由的 JSON（调试利器）。

---

# 第 7 章 熔断：给故障装一根保险丝

> 核心机制：[熔断器](V4服务化详解【知识词典】.md#熔断器) · [三态机](V4服务化详解【知识词典】.md#三态机) · [服务降级](V4服务化详解【知识词典】.md#服务降级) · [级联故障](V4服务化详解【知识词典】.md#级联故障) · [舱壁隔离](V4服务化详解【知识词典】.md#舱壁隔离)

## 7.1 零基础：家里的保险丝

电器短路时保险丝熔断——**牺牲一个保险丝，保住整栋房子**。没有熔断器：短路电流持续通过，电线发热，全屋烧毁。软件的同款事故：

```
ai-service 挂了（30 秒超时才失败）
  → mall-app 每个请求都等 30 秒
  → Tomcat 200 个线程全部在"等死"
  → 商品/订单接口没有线程可用 → 全站瘫痪
  → 这叫【级联故障】/ 服务雪崩
```

**熔断器跳闸后**：请求不再发往 ai-service，**毫秒级**直接走降级——线程立刻释放，商城照常。

## 7.2 三态机（面试必背，能画出来）

```
        失败率≥50%（最近10次里）
 ┌──────────────────────┐
 │                      ▼
[CLOSED 正常]      [OPEN 跳闸]
 │  ▲    统计失败率      │  快速失败（直接 fallback）
 │  │                      │ 等 30 秒
 │  │ 试探成功             ▼
 │  └───────────── [HALF-OPEN 半开]
 │                  放 3 个请求试探
 └──────────────────────┘ 试探失败 → 回 OPEN
```

- **CLOSED**：正常放行，滑动窗口统计失败率（窗口 10 次、至少 5 次才开始统计——防冷启动误判）；
- **OPEN**：一律快速失败走 fallback，**根本不发请求**；
- **HALF-OPEN**：等 30 秒后放 3 个试探请求——全成功→CLOSED（恢复），任一失败→OPEN（继续歇）。

**关键理解**：熔断是**自动的**——跳闸与恢复都不需要人干预，服务恢复后它自己探测回来。"快速失败"与"重试"是反义词：重试是"再试试"（加重对端压力），熔断是"先不试"（保护双方）。

## 7.3 代码接入（两行注解 + 一个降级方法）

```java
@CircuitBreaker(name = "aiService", fallbackMethod = "chatFallback")
public String chat(String message, String imageBase64) {
    // ... 正常调 ai-service ...
}

// 降级方法：签名=原方法 + 末尾加 Throwable
private String chatFallback(String message, String imageBase64, Throwable t) {
    log.warn("AI 调用熔断/失败，走降级文案: {}", t.toString());
    return "AI 助手正在休息中（服务暂时不可用），请稍后再试～";   // 友好文案，不是异常
}
```

三个细节：
1. **fallback 返回文案而不是抛异常**——用户看到"AI 在休息"，而不是红色报错；商城其它功能照常（**故障隔离的最终呈现**）；
2. **`name = "aiService"` 对应 yml 里 `resilience4j.circuitbreaker.instances.aiService`**——名字对不上就又是"配置不生效"；
3. **AOP 生效前提**：`@CircuitBreaker` 靠代理拦截——**自调用（同类内 this.chat()）不走代理，注解失效**（与 @Transactional 自调用同源，本项目第二次提醒）。

## 7.4 配置（以及那个真实踩过的坑）

```yaml
# ★★ 必须是【顶层】 resilience4j: ——挂到 spring: 下会【静默失效】！
resilience4j:
  circuitbreaker:
    instances:
      aiService:
        sliding-window-size: 10             # 统计最近 10 次
        failure-rate-threshold: 50          # 失败率 ≥50% 跳闸
        wait-duration-in-open-state: 30s    # 跳闸 30 秒后半开
        permitted-number-of-calls-in-half-open-state: 3
        minimum-number-of-calls: 5          # 至少 5 次才开始统计
        automatic-transition-from-open-to-half-open-enabled: true
  timelimiter:
    instances:
      aiService:
        timeout-duration: 60s               # 单次调用上限（读超时外的第二道闸）
```

**错层级的可怕之处**：不报错、应用照常起、熔断器照常工作——但全用默认参数（窗口 100 而非 10）。你演示"6 次失败就跳闸"时会发现跳不动（100 次窗口才统计）。**验证方法**：启动日志里 Resilience4j 打印的实例参数，或 `/actuator/circuitbreakers` 端点。

## 本章动手作业

1. 停掉 ai-service（remote 模式），连问 6 次——前 5 次慢失败，第 6 次**瞬间**返回降级文案（跳闸实证）。
2. 重启 ai-service，等 30 秒再问——半开试探成功，恢复正常（自动恢复实证）。
3. 启动日志 grep `aiService`——确认打印的窗口/阈值是 10/50 而非默认值（配置生效实证，防错层级坑）。
4. 思考：如果 fallback 里抛异常会怎样？（fallback 失败会继续向上抛——降级方法必须"稳如老狗"，绝不再调任何可能失败的东西。）

---

# 第 8 章 双模式落地与故障隔离的兑现

> 核心机制：[策略模式](V4服务化详解【知识词典】.md#策略模式) · [可插拔](V1.5工程基建详解【知识词典】.md#可插拔)

## 8.1 接线：ChatServiceImpl 显式构造器（评审修复后的最终形态）

```java
public ChatServiceImpl(...,
                       @Qualifier("localAiService") AiService localAi,      // 两个实现都注入
                       @Qualifier("remoteAiService") AiService remoteAi,
                       @Value("${aimall.ai.mode:local}") String aiMode) {   // 配置决定用哪个
    this.aiExecution = "remote".equalsIgnoreCase(aiMode) ? remoteAi : localAi;
    this.aiModeFlag = aiMode.toLowerCase();
    log.info("AI 执行模式 = {}（{}）", aiModeFlag, aiExecution.getClass().getSimpleName());
}

public String chat(ChatRequest req) {
    ...
    if ("remote".equals(aiModeFlag)) {
        // remote：整个模型调用交给 ai-service（含熔断）；本地只保留业务职责
        answer = aiExecution.chat(req.getMessage(), req.getImage());
    } else if (req.hasImage()) {
        answer = visionChatClient...   // local：原有双链路
    } else {
        answer = chatClient...
    }
    // 落库/自动命名两模式共用 —— 业务与 AI 能力分离
    saveMessage(...); autoRenameIfDefault(...);
}
```

**三个要点**：
1. **构造时定格**（`aiModeFlag` 是 final）——不每次请求读配置，模式在启动时就定死（可预测性）；
2. **为什么不用 @RequiredArgsConstructor**——Lombok 按类型注入，两个 AiService 实现会冲突；显式构造器 + @Qualifier 才能表达"按名二选一"；
3. **职责分离**：会话管理/鉴权/落库永远在 mall-app（业务侧），模型调用按模式本地或远程（AI 侧）——**换的是执行，不变的是业务**。

## 8.2 降级矩阵（V4 全景，面试可整表背）

| 场景 | 表现 | 机制 |
|---|---|---|
| 什么都不配 | AI 走本地链路，行为=V3，**零依赖全功能** | 默认 local 模式 |
| remote + ai-service 挂 | 前 5 次慢失败→跳闸→**毫秒级**降级文案；商城照常 | 熔断三态机 |
| remote + mall-app 挂（ai-service 视角） | searchProduct 工具返回"查不到"，模型如实说 | 工具层 try-catch |
| Nacos 挂 | 三服务照常启动（WARN），lb:// 走静态兜底 | fail-fast=false + Simple |
| 网关挂 | 前端不可用；直连 8080 后端仍可工作（降级路径） | 网关无状态可快速重启 |

**这张表就是"故障隔离"从架构图到现实的全部兑现**——每一行都有对应机制，而不是口号。

## 本章动手作业

1. 不配任何环境变量启动 backend → 日志"AI 执行模式 = local"。
2. `AIMALL_AI_MODE=remote` 重启（ai-service 在跑）→ 日志变 remote；问答走 8081。
3. 对照 8.2 表逐行说出"机制"那一列——说得出机制才算掌握，只背现象是八股。

---

# 第 9 章 评审复盘：本轮踩的坑（真实学习素材）

| # | 级别 | 问题 | 后果（如果不修） | 修复 |
|---|---|---|---|---|
| 1 | **P0** | resilience4j 配置挂在 spring 块内 | 熔断**静默失效**（不报错、参数全默认、演示跳闸跳不动） | 移到顶层；yml 解析校验；文档 §7.4 立碑 |
| 2 | **P0** | AiService 双模式抽象**无任何调用方** | 配 remote 模式毫无作用——所有请求仍走本地 | ChatServiceImpl 显式构造器接线 |
| 3 | P1 | ai-service 的 Filter 双重注册（@Component + @Bean 同名） | BeanDefinitionOverrideException 启动失败 | 去 @Component，FilterRegistrationBean 显式注册（只保护 /internal/*） |
| 4 | P1 | 多网卡机器 Nacos 注册了 VMware IP | 控制台显示 192.168.49.1，跨机器不可达 | 显式 `nacos.discovery.ip` |
| 5 | P1 | yml 多轮 Edit 锚点漂移 | 键落错层级（正是坑 1 的成因） | 每次 Edit 后 parse 校验 |
| 6 | P2 | target 产物差点入库（gitignore 只盖了 backend/target） | 仓库膨胀+构建机污染 | `**/target/` 通配 |

**三条元教训**：
1. **"写了≠生效"已是第三次**（V3 缓存、V3 榜单、V4 双模式）——评审第一动作永远是 **grep 调用点**；
2. **配置前缀意识**：Spring 生态里"非 spring 前缀"的配置（resilience4j/sa-token/management）挂错层级**静默失效**——改完 yml 必须 parse 校验 + 启动日志验证参数；
3. **自动检测是隐形炸弹**：注册 IP/时区/编码，环境相关的东西生产值得显式声明。

---

# 第 10 章 验收清单 + 面试速查 + 报错速查

## 10.1 全链路验收清单

```bash
# 0. 环境：虚拟机 Nacos(8848) 在跑；MySQL/Redis/RabbitMQ 同 V3；DEEPSEEK key 已放 local yml

# 1. 单体模式（零依赖兜底）
[ ] 只启动 backend → 日志"AI 执行模式 = local"，全功能可用（V3 形态）

# 2. 三服务 + 注册发现
[ ] 启动 ai-service(8081) → mall-app(8080, AIMALL_AI_MODE=remote) → gateway(9000)
[ ] Nacos 控制台：三个服务，实例 IP=192.168.6.1（不是 192.168.49.1！）
[ ] backend 启动日志 "AI 执行模式 = remote（RemoteAiService）"

# 3. 网关
[ ] curl :9000/api/v1/products → 200（经网关）
[ ] curl :9000/internal/v1/xxx → 404（安全边界）
[ ] 响应头 X-Trace-Id；三服务日志同一 traceId

# 4. 内部鉴权
[ ] 无签名头打 8081 /internal/** → 401
[ ] 旧时间戳签名 → 401（防重放）

# 5. 跨服务闭环
[ ] 问 AI"有没有降噪耳机" → 模型调 searchProduct → ai-service 回调 mall-app → 答案含真实商品
[ ] 问"戴久了耳朵疼吗" → searchDocs（本地语料）

# 6. 熔断
[ ] 停 ai-service → 连问 6 次：前 5 慢失败，第 6 瞬间降级文案
[ ] 启动日志确认熔断参数=配置值（10/50，防错层级坑）
[ ] 重启 ai-service 等 30s → 自动恢复

# 7. 隔离总验收
[ ] ai-service 挂掉期间：登录/浏览/下单/支付全部正常 ← V4 的核心承诺
```

## 10.2 面试速查表（30 秒一答）

| 问题 | 一句话答案 |
|---|---|
| 为什么做微服务 | 只拆 AI：耗时差异/故障隔离/独立扩容/密钥隔离/模型可替换——五条业务理由 |
| 为什么不全拆 | 用户/商品/订单事务耦合，拆开要分布式事务，当前量级不划算；能讲"不做什么"更高级 |
| 服务间怎么调 | Spring 6 声明式 HTTP（@HttpExchange）——零额外依赖，写法与 Feign 几乎一致，按需迁移 |
| 服务间怎么鉴权 | HMAC 签名（client+ts+secret）+ 5 分钟时间窗防重放 + constant-time 比较 |
| 为什么内网也要鉴权 | 端口误映射/横向渗透即裸奔；"内网"是会变的假设 |
| 注册发现怎么做的 | DiscoveryClient 抽象：Nacos 主（启动即注册）+ Simple 静态兜底——换实现零改代码 |
| lb:// 是什么 | 按服务名查实例列表→负载均衡挑一个→真实地址转发；逻辑地址与物理地址解耦 |
| 网关做了什么 | 统一入口 + 路由 + /internal/** 拒 404（安全边界）+ traceId 生成透传 + 访问日志 |
| 熔断三态 | Closed 统计→超 50% 跳闸 Open 快速失败→30s 后 Half-Open 放 3 个试探→恢复闭合 |
| 熔断和重试区别 | 重试是"再试试"（加压对端），熔断是"先不试"（保护双方）；恢复靠半开自动探测 |
| AI 服务挂了怎样 | 前 5 次慢失败→跳闸→毫秒级降级文案；下单/浏览照常——故障隔离兑现 |
| traceId 怎么跨服务 | 网关生成→请求头透传→各服务 Filter 沿用——V1.5 设计时预留的钩子 |
| 遇到过什么坑 | 熔断配置错层级静默失效 / 双模式抽象无人调用 / 多网卡注册错 IP / Filter 双重注册——能讲四个 |

## 10.3 报错速查表

| 症状 | 原因 | 处置 |
|---|---|---|
| `BeanDefinitionOverrideException: internalAuthFilter` | Filter 上 @Component + WebConfig @Bean 同名 | 去 @Component，只用 FilterRegistrationBean 注册 |
| Nacos 控制台实例 IP 是 192.168.49.x | 多网卡自动选错（VMware 网卡） | yml 显式 `nacos.discovery.ip` |
| 网关 503 SERVICE UNAVAILABLE | lb:// 解析不到实例（服务没起/Nacos 挂且无静态兜底） | 起服务；检查 simple.instances 配置 |
| 内部调用 401 sign error | 两侧 secret 不一致 / 拼接串不一致 / 客户端没带签名头 | 三点逐一核对（协议双侧同步） |
| 401 timestamp expired | 机器时钟偏差 >5 分钟 或 ts 用了旧值 | 校时；确认 ts 是"当前"毫秒 |
| 熔断演示跳闸跳不动 | **配置错层级（spring.resilience4j）静默失效** | 移到顶层；启动日志核对参数 |
| 配了 remote 但 AI 还走本地 | （已修的）双模式无调用方；或环境变量没生效 | 确认启动日志"AI 执行模式 = remote" |
| ai-service 起不来：api key 报错 | DEEPSEEK_API_KEY 未设且无 local yml | 建 application-local.yml（参考 backend 同名文件） |
| ai-service 中文乱码/模型不回答 | base-url 指向中转但 key 不匹配 | 核对 local yml 的 base-url 与 key 成对 |

---

> 读完建议回到顶部把【易错关键点】再背一遍，然后做 10.1 验收清单——**动手过的才是你的**。
> 配套词典：[V4服务化详解【知识词典】.md](V4服务化详解【知识词典】.md)
> 延伸阅读：[V3支付与Redis详解.md](V3支付与Redis详解.md)（HMAC 首次登场在支付验签）、[V1.5工程基建详解.md](V1.5工程基建详解.md)（traceId 与可降级哲学的起点）
