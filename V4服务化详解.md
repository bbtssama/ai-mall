# V4 服务化详解：为什么只拆 AI 服务（以及怎么拆的）

> ai-mall 项目升级讲解文档 · V4（服务化：ai-service 独立 / 声明式 HTTP 客户端 / 服务间鉴权）
> 前置阅读：[V1.5工程基建详解.md](V1.5工程基建详解.md)（可降级可插拔原则）、[V2内容社区与RAG详解.md](V2内容社区与RAG详解.md)（AI 能力现状）

---

## 【易错关键点】

1. **只拆 ai-service，其余留在单体**——用户/商品/订单事务耦合紧密，拆开要引入分布式事务，当前业务量不划算。**能讲"为什么不拆其余"比堆五个服务高级**。
2. **拆分的唯一站得住的理由是"耗时差异 + 故障隔离 + 密钥隔离"**——不是"微服务更高级"。
3. **服务间不共享代码库**——契约 DTO 各自持有（共享 common 会导致"改一处全服务重编译"，是微服务反模式）。
4. **默认 local 模式**——不配 `aimall.ai.mode=remote` 时一切照旧（V3 形态），零依赖能启动的铁律不破。
5. **没上 Nacos 也能跑**——静态 URL 直连（`@HttpExchange(url="${...}")`，注册中心是可选升级项。
6. **内部接口不是"内网裸奔"**——`/internal/**` 虽被 Sa-Token 放行，仍需校验 HMAC 签名 + 时间戳防重放。
7. **声明式 HTTP 客户端用 Spring 6 `@HttpExchange`** 而非 OpenFeign——零额外依赖，接口写法与 Feign 几乎一致，迁移成本低。
8. **数据归属要划清**——t_knowledge_*（语料）归 ai-service；商品/订单归 mall-app，AI 要查只能跨服务问。
9. **降级方向：AI 挂了不影响下单**——远程调用失败转成友好提示，商城主体照常。
10. **`ConditionalOnProperty` 是"可降级"在装配层的表达**——不配置就不创建 Bean，零影响。

---

## 第 0 章 为什么 V4 只拆 AI（先立判断标准）

### 0.1 反面教材：原设计的"拆 5 个服务"

早期项目（包括很多练手项目）的微服务方案是：用户服务、商品服务、订单服务、AI 服务、网关——五个服务齐上阵。问题在于**每一个拆分都要能回答"不拆会怎样"**，而多数回答不上来：

| 拆分 | 不拆会怎样 | 答案站得住吗 |
|---|---|---|
| 用户服务 | 用户逻辑与业务耦合 | ❌ 用户表几乎被所有业务 join，拆开每查一次要多一次 RPC |
| 商品服务 | —— | ❌ 下单要读商品、扣库存，与订单强事务 |
| **AI 服务** | **AI 秒级耗时拖垮 Tomcat 线程池；模型/供应商故障连累下单** | ✅ **站得住** |

### 0.2 拆 AI 的五条理由（面试逐条讲）

| 理由 | 现场说明 |
|---|---|
| **耗时差异** | AI 调用秒级~几十秒，普通接口毫秒级。放一起：AI 请求占满 200 个 Tomcat 线程 → 下单接口排队 → 整个商城慢 |
| **故障隔离** | 模型超时/限流/供应商挂了 → 只有"AI 助手"不可用，下单/浏览/支付照常 |
| **独立扩容** | AI 是成本密集（token 按量计费），需要独立扩缩与独立限流（V3 已做） |
| **密钥隔离** | API Key 只存在于 ai-service，泄露半径最小（业务服务完全不接触） |
| **模型可替换** | 换模型/供应商只改这一个服务，业务零感知 |

### 0.3 ★ 必须能讲"为什么不拆其余"

> "用户/商品/订单事务耦合紧密——下单要在同一事务里锁库存、建订单、清购物车。拆开就要引入分布式事务（Seata/本地消息表/TCC），当前业务量（日订单量级）不划算。**能讲出'不做什么'和'为什么不做'，比堆五个服务高级得多。**"
> —— 这句话本身就是面试加分项：它证明你看过复杂度，而不只是跟过风。

---

## 第 1 章 架构与边界

### 1.1 结构

```
ai-mall/
├── backend/       mall-app（单体，8080）：user / content / goods / order / pay
│   └── 新增：AiService 抽象（local|remote）+ AiServiceClient + InternalAuthFilter
│               + /internal/v1/products/search（供 ai-service 回调）
├── ai-service/    ★ 唯一独立的服务（8081）：ChatClient 双链路 / RAG / API Key
│   ├── AiInternalController   /internal/v1/ai/chat、/rag/index
│   ├── HybridRagService       关键词+向量+RRF（语料归本服务）
│   ├── ProductSearchTool      ★ 跨服务回调 mall-app 查商品
│   ├── KnowledgeSearchTool    本地语料检索
│   ├── MallClient             Spring 6 声明式 HTTP 客户端
│   └── InternalAuthFilter     HMAC 内部鉴权（防重放）
└── gateway/       （可选，见第 4 章）
```

### 1.2 数据归属（★ 拆分的实质是"数据归谁"）

| 数据 | 归属 | 另一方如何访问 |
|---|---|---|
| t_knowledge_doc / t_knowledge_chunk（语料） | **ai-service** | mall-app 通过 `/internal/v1/rag/index` 请求索引 |
| 商品/订单/用户/笔记/支付 | **mall-app** | ai-service 通过 `/internal/v1/products/search` 回调 |
| API Key | **ai-service 独占** | 业务服务永不接触 |

**注意**：物理上两个服务可以连同一个 MySQL 实例（本项目如此，省运维），但**归属权与访问权**已分离——只有 ai-service 写语料表，mall-app 只能"请求"不能"直接写"。这是"逻辑边界先于物理边界"的实践。

### 1.3 调用链

```
用户 → mall-app(8080, Sa-Token 鉴权) 
         └─(remote 模式)─► ai-service(8081, HMAC 内部鉴权)
                              ├─ 本地 RAG（语料）
                              └─(需商品数据)─► mall-app /internal/v1/products/search（HMAC 签名）
```

用户**永不直接接触** ai-service——避免额度被滥用，也让会话/鉴权/限流留在业务侧。

---

## 第 2 章 关键实现（逐段讲）

### 2.1 声明式 HTTP 客户端：为什么不用 OpenFeign

```java
@HttpExchange(url = "${aimall.ai-service.url}")
public interface AiServiceClient {
    @PostExchange("/internal/v1/ai/chat")
    Map<String, Object> chat(@RequestBody Map<String, Object> body);
}
```

| | Spring 6 `@HttpExchange` | OpenFeign |
|---|---|---|
| 依赖 | **零额外**（spring-web 自带） | spring-cloud-openfeign + BOM 版本对齐 |
| 编程模型 | 接口+注解，动态代理 | 接口+注解，动态代理 |
| 生态 | 与 RestClient 一体 | 与注册中心/负载均衡/熔断深度整合 |

**选型理由**：本项目只做基础微服务（一个拆分），引入 Spring Cloud 全家桶（含版本对齐风险）不划算；真需要注册发现/负载均衡时再迁移——**接口写法几乎一样，迁移成本极低**。这是"按需演进"在依赖选型上的体现。

### 2.2 服务间鉴权：与 V3 支付验签同一套 HMAC

```java
// 客户端（生成）
String ts = String.valueOf(System.currentTimeMillis());
req.header("X-Internal-Client", clientId);
req.header("X-Internal-Ts", ts);
req.header("X-Internal-Sign", InternalSignature.sign(clientId, ts, secret));

// 服务端（校验）：① 时间戳 5 分钟窗口防重放 ② constant-time 比对签名
```

**"内网就安全"是错觉**：容器网络被横向渗透、端口误映射到公网，内部接口就成了免费后门（本项目里就是"免费 AI 额度"）。所以内部调用也要"证明你是谁 + 报文没被改 + 不是重放"——三要素与公网回调一致。

### 2.3 双模式切换：让调用点零感知

```java
public interface AiService { String chat(String message, String imageBase64); Flux<String> stream(String message); }

@Service("localAiService")  class LocalAiService  implements AiService { /* 委托原 ChatService */ }
@Service("remoteAiService") class RemoteAiService implements AiService { /* HTTP 调 ai-service */ }
```

配置：`aimall.ai.mode = local（默认）| remote`。
远程客户端 Bean 用 `@ConditionalOnProperty(havingValue="remote")`——**不配置就不创建，零依赖零影响**（可降级原则在 Spring 装配层的表达）。

### 2.4 超时：连接要快失败，读取要给足

```java
f.setConnectTimeout(Duration.ofSeconds(2));   // 连不上快速失败，别拖住用户线程
f.setReadTimeout(Duration.ofSeconds(120));    // AI 本来就慢，读取要给足
```
ai-service 回调商品则是 `2s / 5s`——**AI 链路已经很慢，回调必须快进快出**，不能让慢回调拖垮 AI 请求（故障隔离的微观实践）。

---

## 第 3 章 降级与故障隔离（本版核心收益）

| 场景 | 表现 | 机制 |
|---|---|---|
| ai-service 未启动 | 默认 local 模式 → **完全正常**（AI 走本地） | 模式开关 |
| remote 模式下 ai-service 挂了 | AI 问答提示"AI 服务暂时不可用"；**下单/浏览/支付不受影响** | 异常转友好提示，故障隔离 |
| mall-app 挂了（ai-service 视角） | searchProduct 工具返回"查不到"，模型如实回答 | 工具层 try-catch 降级 |
| 语料为空 | searchDocs 返回空，模型说"不知道" | 检索降级 |

**这就是独立拆分的收益兑现**：AI 故障被限制在 AI 链路内，商城主体照常——V1 时"AI 慢→全站慢"的问题从架构上解决。

---

## 第 4 章 Gateway / 注册发现 / 熔断：做，但每个都要有业务理由

> 这三个是微服务面试必问词（注册发现 / 网关 / 熔断）。但本项目不做"因为标配所以加"——
> **每个都要能回答"砍掉它会怎样"**。

### 4.1 Gateway：安全边界，不是装饰

**砍掉会怎样**：外部只要知道 8081 端口就能直接打 ai-service 的 `/internal/**` 端点——
V4 承诺的"用户永不直接接触 ai-service"就只是句口号，没有强制力。

实现（`gateway/`，端口 9000，外部唯一入口）：

```yaml
routes:
  - id: mall_api        uri: lb://mall-app   predicates: [Path=/api/**]
  - id: deny_internal   uri: no://op         predicates: [Path=/internal/**]
                        filters: [SetStatus=404]   # ★ 用 404 而非 403：不确认端点存在
```

- 用户态接口、静态资源、运维探针 → mall-app；**内部端点一律拒绝**；
- 全局过滤器生成 traceId 并透传（**V1.5 埋的伏笔在这里兑现**：网关生成 → mall-app 沿用 → ai-service 再透传，`grep <traceId>` 跨三进程串起全链路）；
- 前端只认识 9000（未来加服务不改前端）。

**没做的部分**：网关鉴权（JWT）仍在 mall-app（Sa-Token）。生产可上移到网关——**知道它在演进路线上的位置即可**。

### 4.2 注册发现：抽象优先，静态实现先跑通

**这是本项目最有讲究的一处**。项目有"零依赖能启动"铁律，强依赖 Nacos 会违背它（Nacos 没起 = 应用起不来）。

解法：**用 Spring Cloud 官方的 `SimpleDiscoveryClient`（静态服务发现）**——它与 Nacos 用**同一个 `DiscoveryClient` 抽象**、同样的 `lb://服务名` 调用写法，但零额外服务：

```yaml
spring.cloud.discovery.client.simple.instances:
  mall-app:    [{ uri: http://localhost:8080 }]
  ai-service:  [{ uri: http://localhost:8081 }]
```

切 Nacos 时**只换 starter + server-addr，路由里的 `lb://mall-app` 与所有调用代码零改动**（compose 里已备好 Nacos 条目与配置模板）。

面试口径："注册发现我用的是 Spring Cloud 的 DiscoveryClient 抽象——演示环境用静态实例（零依赖可跑），生产换 Nacos 只改配置。这和我项目里 storage/embedding/pay 的可插拔是同一套思路。"

> 这比硬上一个 Nacos 高级：**它证明你理解"抽象与实现分离"，而不只是会配一个组件。**

### 4.3 熔断：故障隔离的兑现（业务理由最实在）

**砍掉会怎样**：ai-service 挂了 → 每个 AI 请求都要**等满 120 秒读超时**才失败 → 请求线程被长时间占住 → 正是 V4 拆 AI 服务要解决的"拖垮线程池"问题没解决。

```java
@CircuitBreaker(name = "aiService", fallbackMethod = "chatFallback")
public String chat(String message, String imageBase64) { ... }

private String chatFallback(String message, String imageBase64, Throwable t) {
    return "AI 助手正在休息中（服务暂时不可用），请稍后再试～";
}
```

三态机（面试必背）：**Closed**（正常）→ 失败率超阈值 → **Open**（快速失败，直接 fallback）→ 等待 30s → **Half-Open**（放 3 个请求试探）→ 成功则闭合。

配置：`sliding-window-size=10`（最近 10 次）、`failure-rate-threshold=50%`、`minimum-number-of-calls=5`（避免冷启动误判）。

**降级返回的是友好文案而不是异常**——用户看到"AI 助手在休息"，商城的浏览/下单/支付完全不受影响。

### 4.4 仍未做的（诚实边界）

| 组件 | 状态 | 理由 |
|---|---|---|
| 分布式链路追踪（SkyWalking/OTel） | 预留 | traceId 已跨三进程透传，接 OTel 时天然串起来 |
| 分布式事务（Seata） | 不做 | **正因为不拆用户/商品/订单，才不需要它**（这是"不拆"的收益） |
| 配置中心（Nacos Config） | 可选 | 与注册中心同一处接入，当前配置量小 |

---

## 第 5 章 验收清单

```bash
# 默认模式（零依赖，什么都不起也能跑）
[ ] backend 照常启动（aimall.ai.mode 未配置 = local），AI 问答走本地链路，行为同 V3

# 远程模式
[ ] 启动 ai-service：cd ai-service && mvn spring-boot:run（8081）
[ ] 启动 mall-app（AIMALL_AI_MODE=remote）：mvn spring-boot:run（8080）
[ ] 问答：/api/v1/chat → mall-app 转发到 ai-service，日志两侧可见
[ ] 鉴权：curl 不带签名打 ai-service 的 /internal/v1/ai/chat → 401
[ ] 重放：用 10 分钟前的 ts 签名请求 → 401（timestamp expired）
[ ] 回调：问"有没有降噪耳机" → AI 调 searchProduct → ai-service 回调 mall-app → 有结果
[ ] 隔离：停掉 ai-service（remote 模式）→ AI 提示不可用，下单/浏览照常

# 熔断（V4 第二阶段）
[ ] 停掉 ai-service 后连问 6 次 → 前几次慢失败，达到阈值后**立即**返回降级文案（快速失败实证）
[ ] 重启 ai-service，等 30s → 半开放行试探 → 恢复正常回答
[ ] 观察日志：Resilience4j 的 CLOSED → OPEN → HALF_OPEN → CLOSED 状态迁移

# 网关（V4 第二阶段）
[ ] 启动 gateway：cd gateway && mvn spring-boot:run（9000）
[ ] curl http://localhost:9000/api/v1/products → 正常（路由到 mall-app:8080）
[ ] curl http://localhost:9000/internal/v1/anything → 404（内部端点被拒）
[ ] 响应头有 X-Trace-Id（网关生成），且 mall-app 日志里的 traceId 与之一致（跨进程透传）
[ ] GET http://localhost:9000/actuator/gateway/routes → 查看路由表

# 注册发现
[ ] 当前用 SimpleDiscoveryClient（静态实例），gb://mall-app 正常解析
[ ] 想真连 Nacos：docker compose up -d nacos（compose 里已备注释模板），改两行配置即可切换
```

---

## 附：面试速查

| 问题 | 一句话答案 |
|---|---|
| 为什么只拆 AI | 耗时差异（秒级 vs 毫秒）、故障隔离、独立扩容、密钥隔离、模型可替换——五条业务理由 |
| 为什么不全拆 | 用户/商品/订单事务耦合，拆开要分布式事务，当前量级不划算；能讲"不做什么"更高级 |
| 服务间怎么调 | Spring 6 声明式 HTTP 接口（@HttpExchange），等价于 Feign 但零额外依赖 |
| 怎么保证内部调用安全 | HMAC 签名（client+ts+secret）+ 5 分钟时间窗防重放 + constant-time 比较 |
| 数据归谁 | 语料归 ai-service；商品订单归 mall-app；API Key 只在 ai-service |
| 为什么不上 Nacos | 不是不上——**用了它的抽象（DiscoveryClient）**：演示用静态实例零依赖跑，换 Nacos 只改配置 |
| 熔断怎么配的三态 | Closed（统计失败率）→ 超 50% 跳闸 Open（快速失败走 fallback）→ 等 30s 半开试探 → 恢复闭合 |
| 网关做了什么 | 统一入口 9000 + 路由 + **拒绝外部访问 /internal/**（安全边界）+ traceId 生成透传 |
| 服务挂了怎么办 | AI 故障只影响 AI 功能（降级文案），商城主体照常——这就是拆分的收益 |
| 本地模式还有意义吗 | 有：默认 local 保证零依赖可启动（面试官克隆仓库不必起 ai-service/Nacos） |
