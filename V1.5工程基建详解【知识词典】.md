# V1.5 工程基建详解【知识词典】

> 配套主文档：`V1.5工程基建详解.md`（按章节学习用主文档，查概念用本词典）
> 收录范围：V1.5 涉及的全部概念。与前两本词典（Spring AI 词典）不重复收录。

## 使用说明

- 按"主题分区"组织，每个词条独立自包含（不依赖其他词条也能读懂）。
- 格式：**一句话核心概念置顶** → 直观比喻 → 与本项目的对应 → 常见追问。
- 加 ★ 的是面试高频词。

## 目录

- 一、数据库版本管理
  - Flyway / flyway_schema_history / baseline-on-migrate / checksum 校验 / 迁移命名规范
- 二、日志与链路追踪
  - MDC / ThreadLocal / InheritableThreadLocal 的局限 / TaskDecorator / traceId / Filter vs Interceptor / 预签名 URL 之外的日志滚动策略
- 三、可观测性
  - Actuator / 健康检查 / 存活探针与就绪探针 / Micrometer
- 四、对象存储与文件安全
  - 对象存储 / MinIO / S3 兼容协议 / 预签名 URL / 路径穿越 / 存储型 XSS / MIME 白名单
- 五、测试
  - JUnit5 / Mockito / MockedStatic（mock 静态方法）/ @TempDir / ArgumentCaptor / BDDMockito 风格 / Testcontainers
- 六、并发与线程池
  - 线程池七参数 / 有界 vs 无界队列 / 拒绝策略四兄弟 / CallerRuns 与背压 / 优雅停机 / 双重检查锁
- 七、工程化与交付
  - Docker Compose / healthcheck / 数据卷 / GitHub Actions / CI 与 CD 的区别 / 十二要素应用

---

## 一、数据库版本管理

### Flyway
**一句话：把"改数据库结构"从人肉执行 SQL 变成"带版本号的脚本放进代码仓库，应用启动时自动补执行缺失版本"的工具。**
比喻：给数据库请了一个"账房先生"，每次改表都记一笔账（版本号+校验和），谁来对账都能说清"现在执行到哪了、还欠哪些没执行"。
本项目：`backend/src/main/resources/db/migration/`，启动时自动比对 `flyway_schema_history` 表。
追问"和 Liquibase 比呢"：Liquibase 用 XML/YAML/JSON 描述变更（跨数据库方言），Flyway 直接写 SQL（对 DBA 直观）。国内团队 Flyway 更多，选型理由说"SQL 直观、够用"即可。

### flyway_schema_history
**一句话：Flyway 的"账本"表，记录每个迁移脚本的版本、描述、checksum、执行时间、成功与否。**
第一次运行自动创建。启动时"扫目录 vs 查账本"，缺的按序补执行。
为什么必须有它：没有账本，Flyway 就无法知道哪些脚本执行过；靠查表结构反推既不可靠也不可行（"加一列又删一列"后无法区分）。

### baseline-on-migrate ★
**一句话：让 Flyway 接管"已经有表"的存量库——自动打一条基线记录声明"版本 N 及以前的状态我认了"，从 N+1 开始纳管。**
比喻：接手一本已经记了一半的账，先抄一页"期初余额"，从这之后按新规矩记。
本项目：`baseline-on-migrate: true` + `baseline-version: 1`——开发库已有 10 张表，基线化后跳过 V1，V2 起纳管。
追问"为什么不用它会导致什么"：非空库 + 无历史表 → Flyway 试图从 V1 跑起 → 建表语句和现有表冲突（`IF NOT EXISTS` 能躲过建表，但历史差异被掩盖成暗雷）。
**这是"存量项目接 Flyway"的标准姿势，大部分人只会新项目从零用。**

### checksum 校验
**一句话：Flyway 给每个已执行脚本算的校验和，内容一变 checksum 就变，启动时比对不一致直接报错拒绝启动。**
为什么这么"轴"：已执行的脚本是"历史"，改历史 = 你现在的库和你队友的库又不一样了（回到了人肉时代）。强制"只增不改"。
改错了怎么办：如果你只是改注释想绕过，用 `flyway repair` 重置 checksum；如果真要改表结构——新增 Vn 脚本，别动旧的。

### 迁移命名规范
**一句话：`V<版本号>__<描述>.sql`，双下划线，版本号全局递增。**
例：`V1__init.sql`、`V2__add_note_tables.sql`。除了 V 前缀还有 `R__`（Repeatable，可重复执行，如视图/存储过程，每次内容变了就重跑）和 `U__`（Undo，社区版不可用）。

---

## 二、日志与链路追踪

### MDC（Mapped Diagnostic Context）★
**一句话：日志框架给每个线程配的一个"随身 Map"，放进去的键值会在打日志时被 pattern 自动带出，业务代码零侵入。**
比喻：医院给每个病人发的手环——护士（日志语句）不需要认识病人（请求），扫手环（`%X{traceId}`）就知道是谁。
本项目：`TraceIdContext.set/get/clear` 包装 `MDC.put/get/remove`，pattern 用 `[%X{traceId:-N/A}]`。
追问"MDC 是什么实现的"：底层 ThreadLocal（Logback），所以**换线程取不到**——引出 TaskDecorator。

### ThreadLocal
**一句话：每个线程各有一份副本的变量，线程之间互相看不见。**
比喻：每个线程自己的储物柜。A 线程往柜子里放的东西，B 线程打不开 A 的柜子。
价值与代价：换来"无侵入传递上下文"（不用改方法签名），代价是线程池复用时**不清理就串号**——必须 finally clear。
同族：`InheritableThreadLocal`（子线程创建时拷贝父线程的值）——见下条。

### InheritableThreadLocal 的局限 ★
**一句话：ITL 只在"子线程被创建的那一刻"继承父线程的值，而线程池的线程是提前创建、反复复用的，创建那一刻还没有请求上下文，所以线程池场景 ITL 失效。**
这是高频面试题："为什么 MDC 换了线程就丢？用 InheritableThreadLocal 不行吗？"——答"创建时 vs 使用时"的时间差即可。

### TaskDecorator ★
**一句话：Spring 提供的线程池装饰器接口，在"任务提交时"抓拍主线程的 MDC，在"任务执行时"还原到工作线程，finally 再清理。**
类比：把"上下文"装进信封随任务一起投递。
本项目：`MdcTaskDecorator` 十几行代码，配在 `ThreadPoolTaskExecutor.setTaskDecorator(...)`。
三个时间点：提交（拍照）→ 执行（还原）→ 结束（clear 防串号）。有专门单测钉死。

### traceId（链路追踪 ID）★
**一句话：一次请求的全生命周期唯一 ID，所有日志带上它，就能把交错并发下的日志按请求归组。**
生产级演进路线（面试加分）：手动 MDC → Micrometer Tracing（Boot 官方，桥接 OTel）→ SkyWalking/Jaeger（可视化调用链）。原理都一样：**入口生成、沿途透传、集中收集**。分布式场景靠请求头传递（本项目用 `X-Trace-Id` 头，已预留网关透传）。

### Filter vs Interceptor ★
**一句话：Filter 在 Servlet 容器层（DispatcherServlet 之前），Interceptor 在 Spring MVC 层——Filter 更早、覆盖更广。**
本项目选 Filter 生成 traceId 的理由：被拦截器挡掉的 401、根本没进 MVC 的错误也该有 traceId——排查鉴权问题恰恰最需要它们。
对比表：Filter 属 Servlet 规范（拿得到原始 Request/Response，拿不到 Handler 信息）；Interceptor 属 Spring MVC（知道要执行哪个 Controller 方法）。执行顺序：Filter → DispatcherServlet → Interceptor → Controller。

### 日志滚动策略（RollingFileAppender）
**一句话：日志文件按规则切分（按天/按大小）、压缩归档、限量保留，防止日志把磁盘写满。**
本项目：`SizeAndTimeBasedRollingPolicy`，50MB/文件、30 天、总量 2GB 封顶，旧文件 `.log.gz` 压缩。
"线上磁盘被日志写满导致服务全挂"是真实高频事故——这三个参数就是防它的。

---

## 三、可观测性

### Actuator ★
**一句话：Spring Boot 的生产运维端点包，一个依赖即可暴露健康检查、指标、环境信息等 HTTP 端点。**
本项目：`/actuator/health`、`/actuator/metrics`、`/actuator/beans`（仅本地，生产必须收窄暴露面）。
价值：运维从"进程在不在"（ping）升级到"活着还是假死、依赖健不健康"（health 里带 db/redis 组件状态）。

### 存活探针与就绪探针（liveness / readiness）★
**一句话：K8s 两类健康检查——liveness 回答"要不要重启我"，readiness 回答"要不要给我导流量"。**
区别是面试常客：程序死锁假死 → liveness 失败 → K8s 重启它；启动中/依赖未就绪 → readiness 失败 → K8s 暂时不把流量打过来（但不重启）。
本项目：`management.endpoint.health.probes.enabled: true` 暴露 `/actuator/health/liveness` 和 `/readiness`。

### Micrometer
**一句话：Java 生态的"指标门面"（类比 SLF4J 之于日志），Actuator 的指标底层，可导出 Prometheus/Grafana。**
知道定位即可：`/actuator/metrics` 里看到的 JVM 内存、HTTP 耗时就是它采集的。V4 后可接 Prometheus + Grafana 做完整监控面板。

---

## 四、对象存储与文件安全

### 对象存储 ★
**一句话：以"对象（文件+元数据）"为单位、通过 HTTP API 读写、天然支持海量文件和多节点共享的存储服务。**
比喻：无限大的停车场，每辆车（文件）有唯一车位号（objectKey），任何门岗（应用实例）都能按号找到车——这解决了"本地磁盘在多实例下 404"的死穴。
本项目：`StorageService` 接口抽象掉具体实现。

### MinIO
**一句话：开源、S3 兼容、单二进制可跑的对象存储，本地/私有化场景的主流选择。**
本项目：docker compose 里一个服务跑起来（9000 API / 9001 控制台），代码里 `MinioStorageService` 懒初始化（不阻断启动）。
追问"为什么不直接用云 OSS"：本项目要"离线也能跑全流程"；且 MinIO 的 API 与 S3/OSS 同族，会一个等于都会。

### S3 兼容协议
**一句话：Amazon S3 定下的对象存储 HTTP API 事实标准（PUT/GET/DELETE + bucket/object 模型），各家存储都兼容它。**
价值：换存储厂商基本只改 endpoint/密钥，代码不动——这就是本项目的抽象层之外的第二重保险。

### 预签名 URL（Presigned URL）★
**一句话：用密钥对"GET 某对象"签出的临时授权链接（带有效期），私有桶也能安全地让外部临时访问。**
比喻：酒店的临时房卡——只在有效期内能开这一间房，而不是把万能钥匙（AK/SK）给人。
本项目：`url-prefix` 留空时自动走预签名（7 天）。对比公开桶：公开桶=门永远开着，被遍历拖库是经典事故。

### 路径穿越（Path Traversal）★
**一句话：攻击者把 `../../` 写进文件名，让你的程序把文件写到预期目录之外（如覆盖系统文件）。**
例：上传文件名叫 `../../etc/passwd.png`，若直接拼接路径就写到系统目录了。
本项目防线：objectKey 由"业务目录/日期/UUID"生成，**原始文件名完全不参与存储路径**（有单测断言 objectKey 不含 `..`）。

### 存储型 XSS ★
**一句话：攻击者上传恶意 HTML/JS 文件到你的域名下，诱导受害者访问，脚本在你域名的信任上下文里执行（偷 Cookie 等）。**
比喻：往你家信箱塞一封"你写的"假信——收信人（浏览器）以为是你发的。
防线：**MIME 白名单**（只允许 jpeg/png/webp/gif）而非黑名单——黑名单永远列不全，白名单默认拒绝未知。

### MIME 白名单 vs 黑名单 ★
**一句话：白名单=只放行明确安全的少数类型；黑名单=禁止已知危险的一大堆——安全设计永远选前者。**
黑名单失效案例：挡了 `.jsp` 没挡 `.jspx`，挡了 `.html` 没挡 `.svg`（SVG 可内嵌 JS）。白名单漏放一个新类型顶多是功能缺失，黑名单漏禁一个就是漏洞。**默认拒绝（deny by default）**是安全第一原则。

---

## 五、测试

### JUnit 5
**一句话：Java 事实标准的单元测试框架，注解驱动（@Test/@DisplayName/@BeforeEach），比 JUnit4 多了嵌套测试、参数化、lambda 风格断言。**
本项目：`@DisplayName` 中文描述每个用例守护什么事故，读测试名就知道安全底线在哪。

### Mockito ★
**一句话：Java 主流 mock 框架——把依赖"换成假货"（指定输入返回指定输出），让被测逻辑被孤立地验证。**
三类最常用 API：`when(...).thenReturn(...)` 打桩；`verify(..., never())` 断言"绝没发生"；`@InjectMocks + @Mock` 自动装配被测对象。
本项目 `OrderServiceImplTest`：五个 Mapper 全 mock，纯内存跑，毫秒级。

### MockedStatic（mock 静态方法）★
**一句话：Mockito 3.4+ 的能力，用 `mockStatic(X.class)` 在 try-with-resources 作用域内替换静态方法行为。**
本项目为什么需要：`StpUtil.getLoginIdAsLong()` 是 Sa-Token 的静态方法，单测没有登录上下文。`mockStatic` 之后"假装已登录 userId=1"。
注意：作用域结束自动恢复，必须包在 try-with-resources 里（static mock 是全局的，泄漏会污染其他测试）。

### @TempDir
**一句话：JUnit5 给测试自动创建一次性临时目录、测试结束自动删除的机制。**
本项目 `LocalStorageServiceTest` 用它真实落盘再断言文件存在，既验真又不留垃圾。

### ArgumentCaptor
**一句话：捕获传给 mock 的真实参数对象，事后断言其内容（而不是只断言"被调过"）。**
本项目：捕获 `orderMapper.insert(order)` 里的 Order，断言金额=798.00、状态=PENDING_PAY——**验证的是"存进去的数据对不对"，不是"方法被调了"**。

### Testcontainers
**一句话：用 Docker 在测试期拉起真实数据库/中间件做集成测试的库，测完自动销毁。**
解决"mock mapper 测不到 SQL 本身对不对"的盲区。本项目 V1.5 未引入（无 Docker 环境也能跑测试是硬约束），已列入 V2 可选项——知道这个权衡本身就能讲。

---

## 六、并发与线程池

### 线程池七参数 ★
**一句话：corePoolSize、maximumPoolSize、keepAliveTime、workQueue、threadFactory、rejectedExecutionHandler，外加一个 unit。**
执行顺序常识（易错）：**核心线程 → 队列 → 扩容到 max → 拒绝**（是"先排队再扩容"，不是"先扩容再排队"）。
本项目：core=4, max=16, queue=200(有界), prefix=aimall-async-, reject=CallerRuns。

### 有界 vs 无界队列 ★
**一句话：无界队列（默认 `Integer.MAX_VALUE`）永远不触发拒绝策略，流量洪峰时任务无限堆积直到 OOM；有界队列满了会触发拒绝策略，把故障变成"快速失败"。**
比喻：无界=不设等候人数上限的银行，人挤到墙塌；有界=排到门外就发"今日号满"牌子。
这是"为什么不能裸用默认线程池"的核心论据。

### 拒绝策略四兄弟 ★
**一句话：AbortPolicy 抛异常（默认）/ DiscardPolicy 静默丢 / DiscardOldestPolicy 丢最老 / CallerRunsPolicy 提交线程自己执行。**
本项目选 **CallerRuns** 的理由：天然**背压**——提交方线程被占用就不能继续提交，压力向源头传导，系统像弹簧一样自我保护；且任务不丢。
Discard 几乎不能用于业务（丢了都不知道）；Abort 适合"必须显式处理失败"的场景。

### 背压（Backpressure）★
**一句话：当下游处理不过来时，把"压力"反向传导给上游减速的机制，避免下游被压垮。**
CallerRunsPolicy 是线程池侧最朴素的背压实现。响应式编程（Reactor）的背压是同一思想（`request(n)` 控制流速）。面试串起这两个例子很加分。

### 优雅停机（Graceful Shutdown）
**一句话：应用收到关闭信号后，先停止接新请求、把手头的任务跑完（限期），再退出。**
反面：kill -9 式硬停 → 正在写的订单/正在发的 MQ 消息直接断掉，数据状态不一致。
本项目：`waitForTasksToCompleteOnShutdown(true)` + `awaitTerminationSeconds(10)`。

### 双重检查锁（DCL, Double-Checked Locking）★
**一句话：两判一锁的懒加载模式——第一次判空免锁快速路径，空则加锁后再判一次才创建，配合 volatile 保证发布安全。**
本项目 `MinioStorageService.client()`：并发首次上传时只建一个 MinioClient。
追问"为什么要判两次"：第一次判空是为了已初始化后不加锁（性能）；加锁后第二次判空是为了挡住"等锁期间别人已建好"的竞态。
追问"为什么必须 volatile"：防止指令重排导致"对象未构造完就被发布"（别的线程拿到半成品引用）。

---

## 七、工程化与交付

### Docker Compose
**一句话：用一个 yml 声明多容器应用（服务、端口、卷、网络、健康检查），一条命令全起全停。**
本项目：mysql/redis/rabbitmq/minio 四件套，`docker compose up -d` 一键。
价值："我本地跑不起来"是协作第一杀手；Compose 把环境变成"可版本化、可重现"的代码。

### healthcheck
**一句话：在 Compose/K8s 里声明"怎么判断这个容器真正可用"，让依赖方能等到"可用"而不只是"启动"。**
本项目 MySQL 用 `mysqladmin ping`：容器起来 ≠ MySQL 可用，中间差着初始化的十几秒——不等它，第一次连接必失败。

### 数据卷（Volume）
**一句话：把容器内的数据目录挂到宿主机管理的卷上，容器删了数据还在。**
本项目：`mysql-data:/var/lib/mysql` 等四个卷。`docker compose down` 保留卷，`down -v` 连卷删（清空数据，慎用）。

### GitHub Actions / CI ★
**一句话：GitHub 内置的 CI 工具，在 yml 里声明"什么时机（push/PR）在什么环境（ubuntu+JDK17）跑什么命令（mvn verify）"。**
CI 与 CD 的区别（高频）：CI=持续集成（编译+测试+打包，保证每次合入是好的）；CD=持续交付/部署（自动发到环境）。
本项目先只有 CI；知道区别即可。
细节：`cache: maven` 缓存依赖提速；失败时 `upload-artifact` 把 surefire 报告传上来方便看失败原因。

### 十二要素应用（12-Factor App）
**一句话：SaaS 应用工程化的十二条最佳实践清单，最核心的几条：配置放环境变量（不入代码库）、日志当事件流（打到 stdout）、依赖显式声明、无状态进程。**
本项目对照：API Key 走环境变量 ✅、日志 stdout+文件 ✅、可降级可插拔 ✅。面试提一句"基本对齐 12-factor"再举两例，比背全十二条有效得多。

---

## 返回主文档

[V1.5工程基建详解.md](V1.5工程基建详解.md)
