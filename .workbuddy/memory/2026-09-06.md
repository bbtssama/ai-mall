# 2026-09-06 工作日志

## 全量扫描 ai-mall 项目(理解现状 + 核对文档过时说法)

读取 README.md + 项目设计文档.md,并派遣 3 个子代理扫描 backend / frontend / (sql+scripts+voice-tts+docs)。

### 现状确认(V1 单体,MVP 闭环已跑通)
- 后端:Spring Boot 3.4.5 / Java17 / 原生 MyBatis(XML 手写,确认无 MyBatis-Plus) / Sa-Token / Spring AI 1.0。按域分包 common/config/user/goods/order/ai/voice,wrapper 接口 84 个类、11 个 Mapper XML。
- 前端:Vue3.5 + Vite6 + ElementPlus2.9 + Pinia + axios;8 页(login/home/product/cart/orders/chat/mine/addresses);SSE 双通道(流式+非流式);Live2D 派蒙舞台 + TTS 播放队列。
- 表:实际 10 张(t_user/t_user_address/t_product/t_product_sku/t_product_image/t_cart/t_order/t_order_item/t_conversation/t_message)。V2+ 规划表(支付/秒杀/RAG/内容域)均未实现。
- 冒烟脚本 9 项(register/login/products/详情/cart加/查/orders下单/查/chat)。

### 【关键矛盾】文档说法 vs 实际代码(需修正)
1. **"乐观扣库存" 表述错误**:实际是 InnoDB 行排他锁 + `WHERE stock>=?`(CAS 式悲观锁),`t_product_sku.version` 仅预留字段、注释明言未用作乐观锁。README/设计文档需改为"行锁 CAS 式扣减"。
2. **voice 注释残留过时描述**:`VoiceChatController` 注释仍写"调派蒙后端 /api/tts 或 PaimonLiveWeb5 :18781",实际 `VitsLifecycle`/`PaimonBackendTtsClient` 已实现**自拉起本机 VITS(:9944)**,默认不再依赖外部 18781。另注释写 `scripts/vits_server.py`,实际脚本在 `voice-tts/`。
3. **前端注释与现状不符(资产已就位)**:`index.html`/`voiceStage.vue`/`api/index.js` 多处声明"公开仓库不含 Live2D 派蒙资产 / 无资产自动降级",但本仓库 `public/assets/model/` 与 `public/vendor/` **已包含完整派蒙模型 + Cubism Core**。这些降级注释/文案误导,应按"私有/已授权仓库"更新。
4. **前端能力注释偏差**:`Chat.vue` 实际 `act` 恒传 ''、`emo` 仅前端启发式 pickEmo 推断,后端未下发音色/动作元数据。
5. AI 模型写死 `deepseek-v4-flash-vision-exp`(README 写 deepseek-v4-pro,需核对以配置文件为准)。
6. 数据库名 `ai_mall`,`t_user_address`(非文档写的 t_address),`t_product_sku` 有 image 列、t_product 有 sub_title/detail(文档漏列)。

### 待办
- 项目设计文档/README 中"乐观扣库存"、"voice 外部 18781"等表述需对照代码修正。
- 前端"无资产降级"注释文案需按"资产已就位"更新。

---

## 【已完成】修正过时说法(共 6 处,均以代码为准)

1. **README** AI 通道写反 → 改为「官方 DeepSeek 直连(api.deepseek.com),OpenCode Go 中转已被注释为备用」;模型 `deepseek-v4-pro` → **`deepseek-v4-flash-vision-exp`**;并注明文本/视觉**共用同一模型**(AiConfig 装配成 chatClient/visionChatClient 两条链路)。
2. **README + 项目设计文档**「乐观扣库存/乐观锁」→ **「InnoDB 行排他锁 + `stock>=?` 条件更新(CAS 思想)」**;`t_product_sku.version` 标注为**预留字段、未参与版本比对**;§8.1 补充术语修正框(面试口径:讲"行锁+条件更新",别讲"乐观锁",否则会被追问版本号在哪比对)。
3. **项目设计文档** 新增「★V1 事实核对表」(扣库存/模型/表数量10张/t_user_address/派蒙资产本机已就位/TTS 自包含 9944)。
4. **application.yml** 注释自相矛盾(把官方直连写成未生效备选 + 模型名错) → 已改写。
5. **backend voice 包** 4 个文件注释残留"外部 PaimonLiveWeb5:18781""scripts/vits_server.py" → 改为「本机 127.0.0.1:9944,由 VitsLifecycle 自动拉起 voice-tts/vits_server.py,不依赖外部服务」。涉及:`VoiceChatController` `VoiceEngine` `EdgeTtsEngine` `VoiceEngineImpl`。
6. **frontend voiceStage.vue** "公开仓库不提供资产" 表述 → 改为「运行时/Core 缺失时降级」的技术性描述(本机资产已就位)。

## 【新增交付】项目设计评审与优化.md(核心成果)

用户深层需求:项目用于求职,必须是"真实需要驱动的演进"而非技术堆砌,要求完整度比得上市面求职项目。
已产出 `项目设计评审与优化.md`,含评审方法论(三问筛伪需求)+ 5 个致命问题诊断 + 2026 时效体检 + 完整度6块拼图 + 重构演进路线 + 面试口径速查表。

**识破的 5 处拼接痕迹**:
1. **RAG 定位混乱(最严重)** —— 设计把"规格/价格"塞进 RAG 语料 = 原理性错误。结构化数据走 RAG 是反模式(价格变次要重新向量化、库存不实时、无法做范围过滤)。
   - **正确定位**:RAG 语料 = 商品说明书(商家上传) + **种草笔记(UGC)** 双源。关键洞察:**种草笔记本身就是最优质 RAG 语料**(用户问"戴久耳朵疼吗"说明书答不了,UGC 笔记有真实体验),与"种草"定位天然契合,非硬塞。
   - **分工**:Agent 判断意图 → 价格/库存走 `searchProduct`(SQL);体验/说明书走 RAG。
   - **2026 升级**:朴素 RAG 已过时 → **Hybrid RAG(向量+BM25+RRF+Rerank)**;商品查询多型号规格词("蓝牙5.3""IPX7"),精确词是 BM25 主场。向量库用 pgvector/Redis Stack。必须建评测集。
   - **必背追问**:"文档才几十份为何不全塞长上下文?" → 语料动态增长/C端首字延迟/权限隔离/成本(2026 业界决策树)。
2. **秒杀 = 八股堆砌** —— 种草商城对标小红书+得物,没有秒杀场景。→ 改为**限量发售(Limited Drop)**(得物/潮玩真实场景),防超卖技术含量不变但业务说得通。
3. **NL2SQL = 高危伪需求** → 降级为可选 + 加 5 层护栏(仅商家端/白名单表+行级过滤/仅SELECT+AST校验/只读账号+超时限行/失败降级预设看板)。
4. **推荐系统 = 无数据支撑** → 改为**基于内容的相似推荐**(同品类同价格带+笔记关联商品)+ Redis zset 热门榜,可验证可讲解。
5. **微服务缺乏业务驱动** → **只拆 AI 服务 + Gateway**(Nacos+OpenFeign),正合用户"只做基础微服务"。**唯一站得住的理由:AI 调用秒级 vs 下单毫秒级会抢线程池 + 故障隔离 + 独立扩容 + API Key 隔离 + 模型可替换**。要能讲清"为什么不拆其余"(事务耦合紧,拆了要引分布式事务,当前业务量不划算)。

**逻辑断裂(结构性)**:项目叫"种草商城"但 V1 无任何种草内容;V3 的 MQ 审核异步、Redis 点赞/排行榜**全部依赖内容社区**却把内容放 V2 → 内容社区是地基不是可选项。

**2026 时效体检**:Spring Boot 3.4.5+Java17 不算过时(稳/资料多,不建议求职前大改);Java 21 是标配(虚拟线程加分);Sa-Token 需备好"为何不用 Spring Security"答案;Sentinel(阿里系)与 Resilience4j(官方标准)要都知道;ES 先用 MySQL 全文,真做搜索再上。

**完整度 6 块拼图(V1.5 建议一次性补齐,不学新技术但消除玩具感)**:Flyway 数据库版本管理 / traceId+结构化日志+Actuator 可观测 / MinIO 对象存储 / JUnit5+Mockito+Testcontainers 测试 / Docker Compose 一键环境 / GitHub Actions CI。

**重构后路线**:V1(已完成) → **V1.5 工程基建(1周,最高性价比)** → V2 内容社区+AI审核异步+Hybrid RAG(3-4周) → V3 支付闭环+Redis+MQ延迟消息+限量发售防超卖+AI成本治理(3-4周) → V4 只拆 AI 服务(2-3周) → V5 可选(商家看板+护栏NL2SQL+相似推荐)。

---

## 【已完成】设计文档合并 + V1.5 工程基建全量落地（第二轮，01:40-02:10）

用户指示：①合并评审进主文档 ②接受"AI 生成模拟数据(100商品/500笔记)"前提 ③直接开工下一版升级 ④配套讲解文档。

### 1. 设计文档合并
- `项目设计文档.md` 全文重写：并入评审全部内容(三问宪法/数据规模前提2.1/RAG定位5.4.1/限量发售5.3.1/ItemCF对比5.5/只拆AI服务8.6/护栏NL2SQL 9.6/2026体检10/六块拼图11/面试口径13)。
- 新增【模拟数据前提下的重估】：100×500 矩阵稀疏 → 主方案基于内容+热门榜,ItemCF 做离线对比实验(讲得出取舍)。
- 独立评审文件已删除(内容全部并入)。

### 2. V1.5 全量实现(代码)
- **pom.xml**: +flyway-core/flyway-mysql/actuator/minio 8.5.17(Boot BOM 管版本);顺手修正 tts-edge-java 过时注释。
- **Flyway**: `db/migration/V1__init.sql`(10表+种子,CREATE TABLE IF NOT EXISTS+INSERT IGNORE;version 列注释改为"预留字段,未用作乐观锁");`application.yml` 配 baseline-on-migrate=true+baseline-version=1(存量库标准接法);`sql/init.sql` 加"权威来源已迁移"头注释。
- **traceId 三件套** `common/trace/`: TraceIdContext(MDC包装)、TraceIdFilter(Filter层入口生成/X-Trace-Id透传与回写/finally clear防串号)、MdcTaskDecorator(快照拍照-还原-clear)。
- **AsyncConfig**: 有界队列200+CallerRuns背压+MDC传递+优雅停机;bean名taskExecutor(@Async默认查找名)。
- **对象存储** `common/storage/` 7类: StorageService接口(UUID命名防穿越/覆盖)、StorageProperties(local/minio双配置)、LocalStorageService(默认零依赖+joinUrl规整)、MinioStorageService(懒初始化DCL不阻断启动+预签名URL兜底)、StorageConfig(工厂选择+/files/**静态映射+7天缓存)、FileRestController(/api/v1/files/upload+delete,MIME白名单+大小限制)、StoredObject(record)。
- **logback-spring.xml**: CONSOLE(%clr+%X{traceId:-N/A})+FILE(无clr,按天+50MB滚动,30天/2GB封顶);日志级别统一走yml。
- **application.yml**: +flyway/servlet.multipart(10MB/20MB)/aimall.storage(默认local)/aimall.logging.dir/management(actuator health+probes+metrics)。
- **测试 4 类 15 用例** `src/test/`: OrderServiceImplTest(mockStatic钉StpUtil;守护:扣库存0→never insert/金额状态/下架never扣/状态机CAS未命中never回补/越权404不泄露存在性)、LocalStorageServiceTest(@TempDir;恶意文件名/不覆盖/删除幂等/joinUrl)、MdcTaskDecoratorTest(异步传递/无残留防串号)、PageQueryTest(offset差一)。
- **docker-compose.yml**: mysql8(healthcheck mysqladmin ping)/redis7/rabbitmq3.13-management/minio + 四数据卷。
- **.github/workflows/ci.yml**: push/PR→JDK17+mvn clean verify(纯单测无需DB,DEEPSEEK_API_KEY占位)。
- **.gitignore**: +uploads/。
- 顺手修正: VoiceChatController 里"SaInterceptor默认不强制"错误注释(实际拦截器统一强制,checkLogin是显式兜底)。

### 3. 讲解文档(仿 AI模块详解 章节风格)
- `V1.5工程基建详解.md`: 易错关键点8条前置 + 8章(0为什么/1 Flyway/2 traceId/3 对象存储/4 测试/5 线程池/6 Compose+CI/7 before-after/8 验收清单) + 每章"面试怎么讲"。
- `V1.5工程基建详解【知识词典】.md`: 7大分区约45词条(一句话核心+比喻+项目对应+追问),与前两本词典不重复。
- README 更新: 技术栈表+目录树+快速启动+V1.5 专节+演进预告(V2-V5 对齐新设计)。

### 4. 校验
- 3个 YAML 全部语法校验通过(conda py313);pom.xml XML 良构;voice 配置完好。
- Java 代码逐一对照真实签名(R/ResultCode/Mapper.updateStatus四参/ProductSku字段),无编译环境(MVN_NOT_FOUND)是已知风险——首次 mvn 编译若有小错属正常,测试与实现均已保守编写。

### 5. 遗留(下一步)
- V2 内容社区+Hybrid RAG:新增 t_note 等7表走 Flyway V2__content.sql;模拟数据生成脚本(100商品/500笔记);RabbitMQ 审核异步。
- 首次编译验证:环境无 Maven,需用户 IDEA 打开或装 Maven 后 mvn test 跑一遍 15 用例。

---

## 【已完成】V2 全量落地（第三轮，09:11-10:30）

用户指示"按原有计划完成 V2 + 生成详解文件"。V1.5 编译错误（addHandler→addResourceHandler）已修复，用户确认 mvn 编译通过。

### V2 交付清单（后端 34 个新文件 + 前端 3 页 + 迁移 + 脚本 + 双文档）

**1. Flyway V2__content_and_rag.sql**：10 张新表——t_note(状态机+ngram全文索引)/t_note_image/tag/product/t_note_like(uk幂等)/t_note_collect/t_user_follow/t_audit_record(uk biz+version幂等)/t_knowledge_doc(uk source+ref)/t_knowledge_chunk(embedding JSON+ngram)。

**2. content 包（社区后端）**：bean×6(Note状态机/KnowledgeDoc/Chunk含300字+50重叠常量/AuditRecord)、dto×4(NoteCreateRequest/NoteQuery含二元组游标/NoteVO/NoteGenerateRequest)、mapper×6+XML(NoteMapper游标分页+MATCH AGAINST ngram+条件updateStatus+原子计数)、NoteService/AuditService接口、NoteServiceImpl(N+1批量组装5条SQL/点赞DuplicateKey幂等/热度like*3+collect*5+view)、AuditServiceImpl(MQ优先/降级@Async/幂等三防线)、AuditAiClient(低温度0.1/JSON防御解析)、NoteGenerateService(RAG参与创作/温度0.8/只出草稿)、RagIndexBuildService(@Async批量建索引/游标分批)、NoteRestController+AiContentController(note-draft/index-product/index-all)、mq×3(AuditMqConfig Direct+DLQ/AuditMessage只带id/AuditConsumer吞异常防毒消息)。

**3. ai.rag 包（Hybrid RAG）**：EmbeddingClient接口+LocalHashEmbeddingClient(2-gram哈希256维/L2归一化/诚实定位:字面非语义)+SpringAiEmbeddingClient、RagProperties(local默认/topK5/candidateK20/rrfK60)、RagConfig、RagIndexService+Impl(删旧写新upsert语义)、RagRetrievalService+HybridRetriever(双通道+RRF排名融合+safe()单通道降级+docCache)、RetrievedChunk(record带来源)、KnowledgeSearchTool(@Tool与searchProduct分流)。AiConfig改双工具defaultTools两连调；ChatServiceImpl system prompt改"两只手"分工。

**4. 配置**：pom+spring-boot-starter-amqp；application.yml：spring.rabbitmq(host默认localhost可降级/publisher-confirm/不重入队/prefetch5)+aimall.rag(embedding local默认)。

**5. 前端**：api(noteApi/aiContentApi/fileApi)、Notes.vue(瀑布流column-count+游标加载更多+热门最新切换)、NoteDetail.vue(驳回提示重提/点赞收藏/文中好物侧栏)、NoteCreate.vue(AI起草卡+图片上传+标签/关联商品)、路由3条、App.vue导航"种草"。

**6. 模拟数据**：scripts/gen_mock_data.py(种子20260906可复现)→sql/mock_data.sql(13955行)：100商品(8品类不等量)+179SKU+30用户+500笔记(55%含缺点段/10%爆款幂律/三角时间分布)+标签+关联商品+点赞抽样+PASS流水。

**7. 双文档**：V2内容社区与RAG详解.md(易错点10条+8章+面试口径速查9问)、V2内容社区与RAG详解【知识词典】.md(7分区约50词条)。

### 校验
16个Mapper XML良构全过；yml结构正确(rabbitmq在spring下/rag在aimall下)；132个java文件。**未编译验证**（无Maven），需用户 mvn compile 首验。

### V2 核心设计口径（面试速记）
- 双工具分流：searchProduct(SQL实时)/searchDocs(RAG语料)，模型按工具描述自选，不写if-else——Agentic。
- RRF用排名不用分数(量纲免疫+双路命中叠加)；k=60。
- 审核幂等三防线：流水uk+INSERT IGNORE / 状态机条件更新 / AI失败只记ERROR不误杀。
- 毒消息：吞异常ACK不重入队，失败可发现优于无限重试。
- 事务外发消息：submit在事务边界后调用，防消费者先于commit回查。
- embedding默认local哈希(诚实:字面非语义,DeepSeek无embedding接口是现实约束),可切spring-ai零改业务。

---

## 【已修复】V2 首次启动失败(16:48,exit code 1)——三个雷

用户 mvn spring-boot:run 失败。排查定位三个问题并修复:

1. **主因:6 个 content Mapper 全缺 @Mapper**——项目是逐接口 @Mapper 注册(无 @MapperScan,AimallApplication 注释明确),新 mapper 无注解=不生成 bean → NoteServiceImpl/AuditServiceImpl 注入失败。已批量补 @Mapper+import。
2. **循环依赖**:AuditServiceImpl 注入 NoteService(死字段,方法体从未用) ↔ NoteServiceImpl 注入 AuditService,Boot3 默认禁循环直接崩。已删 noteService 字段(审核只需 NoteMapper 回查)。
3. **@Async 自调用失效**:submit 内 this.auditAsync() 不走代理,注解失效变同步。已改为手动 taskExecutor.execute() 提交(AsyncConfig 的 ThreadPoolTaskExecutor,bean 名 taskExecutor 唯一按类型注入无歧义)。
4. **附带发现(MQ 永不生效的暗雷)**:AuditMessage 是 record 不实现 Serializable,默认 SimpleMessageConverter 发送必抛 MessageConversionException(extends AmqpException)被 catch 静默降级——MQ 路径永远走不通且无报错。已配 Jackson2JsonMessageConverter bean(Boot 自动应用到 template+监听工厂,收发两侧生效)+ DefaultJackson2JavaTypeMapper setTrustedPackages("*")(默认只信 java.util/java.lang,自定义包被拒)。

预检:6 个新 XML namespace 与接口全对齐;全项目无缺 @Mapper;AuditServiceImpl 花括号配对。

**教训(面试可讲)**:@Mapper 逐接口注册 vs @MapperScan;@Async/@Transactional 自调用失效同源;record 默认不 Serializable;SimpleMessageConverter 的限制;循环依赖靠"删多余依赖"解决优于 @Lazy。
**待确认**:若下次启动报 Flyway "Detected failed migration" → 需对 flyway_schema_history 清 failed 行(DELETE WHERE success=0)后重跑。

---

## 【已完成】V3 全量落地（09-07 上午，09:00-10:30 续）

**设计定位**：能收钱+扛得住。支付闭环/Redis/延迟消息/限量发售/AI限流。

**支付（pay 包，约束：无沙箱凭证 → PayChannel 抽象）**：
- PayChannel 接口(create/verifyCallback/query) + MockPayChannel(默认,HMAC-SHA256 真签名+TreeMap 字典序拼接+constant-time 比较) —— "模拟"的只是第三方不存在,工程链路 100% 真实
- PaymentService 六步回调：验签→定位→已付幂等回success→金额核对→支付单CAS→订单CAS；sync 主动查单对账(UNKNOWN 不改状态)
- PaymentMapper.xml：updateToPaid/updateToClosed 全带状态机条件(WHERE status=期望)
- PaymentController：mock-pay 构造带签名参数走真实回调链路;callback 永不抛异常返回明确报文
- **SaTokenConfig 白名单放行 /api/v1/payments/callback/**（第三方无 token,身份靠验签——认证 vs 完整性两套模型）
- PaymentServiceTest 5 用例(真实 HMAC 验签)：篡改签名拒/已付幂等/金额不符拒/正常流转/并发 CAS 落空

**Redis（common.redis.RedisOps 可降级门面 + RedisConfig）**：
- 门面 safe() 包装：异常→WARN→兜底值→业务走 DB（缓存旁路,Redis 不决定可用性）
- RedisConfig：JSON 序列化(JDK 序列化三宗罪:乱码/类名耦合/反序列化漏洞)
- ProductCacheService 缓存三兄弟：穿透=空值缓存 __NULL__(2min)/击穿=SET NX 互斥重建+输家重读+查库兜底/雪崩=TTL 随机抖动 30-40min
- 分布式锁：SET NX PX + Lua 校验 value 再删(防误删)
- RedisOps.eval 通用 Lua；allow() 固定窗口限流(降级=放行,fail-open)

**延迟消息取消订单（OrderDelayMqConfig + OrderCancelConsumer）**：
- TTL+DLX 零插件方案：order.delay.queue(x-message-ttl=30min,无消费者)→过期→DLX 重路由→order.cancel.queue
- 队列级 TTL 避开消息级队头阻塞；消费端状态机 CAS 幂等(手动取消/支付回调/超时取消三方竞争同一订单)——只有取消成功者回补库存
- OrderServiceImpl.create 事务后投递(MQ 缺席降级不投)；手动取消联动 closeIfPaying

**限量发售（t_drop_activity/t_drop_record + DropService）**：
- 三层防护：Redis Lua 原子预减(-1 没了/-2 未预热降级)→DB 行锁 CAS→uk(activity,user) 限购幂等(DuplicateKey 必须让业务感知,非静默)
- 懒预热 warmUpIfAbsent(不覆盖已有值)；eval(script,keys,args) 通用化

**AI 限流**：ChatRestController.checkRateLimit 每用户 10 次/分钟(Redis 固定窗口,降级放行)

**前端**：payApi/dropApi、MockCashier.vue(倒计时+模拟支付)、Orders.vue 去支付按钮、路由 mock-cashier

**yml**：spring.data.redis(localhost:6379, lettuce pool)+aimall.pay(default-channel MOCK/mock.secret/cashier-url)——注意 redis 在 spring 下 pay 在 aimall 下(曾插错位置已修)

**文档**：V3支付与Redis详解.md(易错点12条+7章+验收7节)+词典(7分区~45词条)

**校验**：2 新 XML 良构;PaymentService 构造器顺序与测试匹配;namespace 对齐;未用 import 清理。**未编译验证**。
**V3 收尾未提交 git(等用户验证启动后再提交)**。

---

## 【评审+修复+文档重写】V3 code review 复盘（09-07 上午，用户委托第三方评审视角）

用户要求以挑剔视角审查 V3 并指出"文档写得少、没有超链接、不符合之前标准"。审查揪出 12 个问题并全部修复：

**P0（4个）**：
1. ProductCacheService 写了没接线（缓存三兄弟完全不生效）→ ProductRestController.detail 改调 detailWithCache（Controller 层接线避开 Service 循环依赖）
2. DropService.buy 失败不回补 Redis 预减（名额白吃=用户资损）→ catch 里 INCRBY_LUA 对称回补，doBuyInDb 拆方法统一回补
3. mock-pay 不校验支付单归属（能替别人付款）→ 加 userId 归属校验
4. 金额核对 null 跳过（无 amount 字段可绕过）→ 缺失 amount-missing/非法 amount-invalid/不等 amount-mismatch 三分支全拒

**P1（7个）**：热门榜 zset 未接线（refreshHotScore 同步 zAdd + hotRank 接口 + /notes/hot 端点）/事务内发 MQ（改 TransactionSynchronizationManager.afterCommit）/cancelIfStillPending @Transactional 自调用失效（删注解+注释说明，与 V2 @Async 同源第二次犯）/限流 INCR+EXPIRE 非原子（RATE_LIMIT_LUA）/复用支付单不检查过期（关旧建新）/orderNo 纯毫秒碰撞面大（复用 V1 风格）/warmUp 总量覆盖（warmUpIfAbsent 语义已在）
**P2**：per_limit 与 uk 语义矛盾（注释澄清：uk=一人一次，per_limit=单次上限）

**文档按 AI模块详解标准全面重写**（用户主诉：写得少、没有超链接）：
- 正文所有概念处加词典 Markdown 超链接（[验签](词典#验签) 形式，约 40 处）
- 词典标题去★保锚点干净、每词条尾部加"返回主文档第X章"链接、目录加锚点跳转
- 每章新增"本章动手作业"（AI模块详解传统）
- 新增第 7 章"评审复盘"：12 个问题的表格+三条元教训（写了≠生效了/失败路径是主要路径/同类坑反复出现）
- 主文档从 ~350 行扩到 ~470 行，词条从 ~45 扩到 ~50 并加交叉引用

**元教训**（写入长期记忆）：评审必须 grep 调用点（写了≠生效）；专挑失败路径问；框架代理自调用陷阱（@Async/@Transactional 同源）已犯两次。

---

## 【文档补强】支付渠道选型论证写入设计文档（09-07 10:46）

用户问"个人接入支付宝有没有意义/是否与公司路径不相干"，回答后要求精炼进设计文档。

新增 §5.3.2「★ 支付渠道：个人沙箱要不要接（选型论证）」：
- 三层拆解表：资质开通（不考）/接入工程（全考，个人与公司同一套）/渠道决策（闲谈）→ "路径不相干"不成立
- 决策标准：接的唯一真实理由=把"换渠道业务零改动"从主张变实证（抽象设计能力）；Mock 已覆盖可考内容 ~95%
- 按时间预算决策：面试近不接（写"渠道抽象+Mock全真实现"）/远则半天接沙箱（RSA2+AlipaySandboxChannel 三方法）
- 千万别做：真实收款/微信/退款分账全流程/当主卖点
- 面试口径对比：❌"我接入了支付宝沙箱" ✅"渠道抽象下两个平行实现，切换只改配置——验证了抽象本身"
- §5.3 与 §8.4 加了指向 5.3.2 的指针

**用户级价值锚点**（求职决策可复用）：支付价值锚点=工程正确性（已具备），沙箱=锦上添花不是雪中送炭。

---

## 【排障】派蒙 VITS 没拉起走兜底（09-07 11:02）

现象：mvn spring-boot:run 后 TTS 走 Edge-TTS 兜底，9944 无监听。
**根因**（vits-server.log 实锤）：VitsLifecycle 用裸 `python`（PATH 解析）拉起 VITS——之前 PATH 上的 python 恰好是 G:/tts/env（装好 torch/soundfile 的 venv），最近 PATH 漂移成无依赖的 3.13 → spawn 进程 ModuleNotFoundError: soundfile 秒死 → 静默降级。
**修复**：
1. yml：`vits.python` 显式指向 `G:/tts/env/python.exe`（保留 AIMALL_VITS_PYTHON 环境变量覆盖；正斜杠避免 YAML 转义）——已验证该解释器 import soundfile/torch 成功(3.10.20)
2. VitsLifecycle：健康轮询加"进程死亡快速失败"——spawn 出的 Python 缺依赖会秒退，原逻辑要干等 90s 超时才告警；现在立即 ERROR 并指路 voice-tts/vits-server.log
**教训**：依赖环境变量/PATH 的 spawn 是隐性配置漂移源；外部进程秒死要快速失败而非等超时。

## 【README 补强】TTS 请求链路概述（09-07 11:30）

用户指出"该结构应该在 README 里有概述"——README 派蒙章节原有资产合规/恢复步骤/降级行为，但缺链路图。已补"请求链路（谁调用谁）"ASCII 图：前端 fetch('/api/v1/voice/tts') → Vite 代理 → 8080 门面 → 主路径 127.0.0.1:9944（内嵌 Python 子进程） / 兜底 Edge-TTS；定性 9944=内嵌子进程非独立服务（只绑回环、前端无感、故障不穿透、生命周期归后端）；附 PATH 漂移教训提醒（vits.python 必须显式配置）。

---

## 【文档重构·1/3】V3 两件套按《AI模块详解》标准重写（09-07 12:01）

用户主诉：V1.5/V2/V3 详解文档太潦草，不符合 0 基础手把手教学原则（对照标杆 = AI模块详解：109KB 主文档+231KB 词典、逐段代码精读、概念链接词典、动手作业、报错速查）。

**V3 主文档**（32KB→65KB，12 章）按范文配方重写：
- 每章配方：先撞南墙→类比同构（与已会概念）→0基础入门（如"Redis 5分钟"“HMAC三步演进"）→贴真实项目代码逐段精读→逐个回答疑问→踩坑复盘→本章动手作业
- 新增：第0章心智模型（支付=订单续集/Redis=远程HashMap）、高频术语速查表、报错速查表（Detected failed migration/假签名排查/delay队列无参重建等）
- 代码全贴精读：RedisOps.safe/三兄弟完整逻辑/handleCallback六步全贴/sign逐行/Lua逐行/三方竞争消费端/afterCommit反模式
- 链接密度对齐范文（每章头部"核心机制"链接行+正文40+处词典锚点）

**V3 词典**（19KB→45KB，50词条）：每词条五段式（一句话核心→比喻→原理展开→代码对应→常见追问）+尾部返回主文档链接。新词条：哈希函数/Redis为什么快/单线程模型/zset/JSON序列化/连接池参数/幂等回调。

**剩余排队**：V2（22KB→目标~70KB+词典扩）、V1.5（33KB→目标~80KB+词典扩）——等用户说"继续"按同模板逐套重构。token 物理限制一轮只能做透一套。

---

## 【文档重构·2/3】V2 两件套按《AI模块详解》标准重写完成（09-07 12:31）

主文档 22KB→52KB（12 章），词典 17KB→42KB（约 60 词条）。与 V3 模板完全一致：
- 每章配方：先撞南墙→比喻→贴真实代码逐段精读（assembleList 五条批量SQL/like幂等四要点/split逐行/LocalHashEmbedding逐段/retrieve三设计点/auditOnce三防线表/generate素材组装）→逐个回答疑问→动手作业
- 第 0 章心智模型："你已经是内容社区资深用户"（刷到的每个元素=需求清单）+ 内容飞轮五环
- 新增第 11 章模拟数据（幂律/55%缺点段/固定种子）；12 章验收+面试速查 12 问+报错速查 7 条（含 ngram 单字搜不到/索引空 AI 不走 searchDocs/翻页重复=游标漏 hotScore）
- 链接密度：每章头部"核心机制"链接行 + 正文 40+ 处词典锚点 + 跨文档链接（AI模块详解的 FC 词条/V3 的 DLX 与 afterCommit）

词典五段式（一句话→比喻→原理→代码→追问）全覆盖，新词条补齐主文档引用：UGC语料/种草/瀑布流/TOCTOU/丢失更新/余弦相似度/L2归一化/哈希技巧碰撞追问/TopK与CandidateK/向量存MySQL/RRF三为什么/语义鸿沟/at-least-once/prefetch/发布者确认/宁漏勿杀/人工复核/AI不直写/自然语言模式/为什么不上ES 等。

**剩余**：V1.5（33KB→目标~80KB+词典扩）——等"继续"。

---

## 【文档重构·3/3 完成】V1.5 两件套按《AI模块详解》标准重写完成（09-07 12:45）

主文档 33KB→47KB（7 章），词典→46KB（约 40 词条，全部五段式）。三套重构收官。

V1.5 特有的处理：
- 第 0 章三个事故场景（凌晨翻日志/新机器克隆建表失败/磁盘写满）→ 六块拼图表，每块回指"V2/V3 谁复用了它"（基建投资回报叙事）
- MdcTaskDecorator 三时点逐段精读（提交快照/执行设置/完毕恢复），TTL 跨池边界认知
- 线程池参数逐个讲依据（4/16=IO密集核数×2~4、200=有界、CallerRuns=反压）+ Boot 默认无界队列 OOM 陷阱
- 上传安全三件套独立小节（白名单 vs 黑名单哲学反转/UUID 防穿越/大小限制）+ 存储型 XSS 纵深防御
- mockStatic try-with-resources 作用域军规 + 纯内存原则与 CI 的因果
- 7.2 面试速查 10 问 + 7.3 报错速查 8 条（checksum mismatch/baseline/traceId NONE/CI 红本地绿等）

词典新增亮点词条：线程串号/TaskDecorator快递比喻/TTL/懒初始化fail半径/S3兼容双保险/拒绝策略四种餐厅比喻/CallerRuns陷阱(借用Web线程)/反压TCP同族/IO vs CPU密集服务员厨师/优雅停机三次出现/测试金字塔冰淇淋筒反模式/@TempDir酒店钟点房/纯内存测试与CI因果/可插拔家族三棒总表。

**三套全部完成**：V1.5(47+46KB) V2(59+55KB) V3(82+46KB)，总 335KB（原 152KB），链接互通成网（含跨文档：V1.5→V3词典魔法字节、V2→V3 afterCommit、V3→V2 FC）。
