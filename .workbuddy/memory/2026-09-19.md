# 2026-09-19 工作日志

## 简历C 定稿：5 项修改 + 全副文件同步 + 重出成品

### 触发
用户要求把「简历C 评估」发现的 5 项问题**落实到所有文件**（不只主文件），明确点名要改
`后端求职/简历/简历C-超链学习目录/`。

### 具体改动

| # | 问题 | 改法 | 涉及文件 |
|---|---|---|---|
| 1 | Feed 流游标写成 `WHERE id < cursorId`（对热度排序是**事实错误**） | 改为「最新排序单列游标 + 热度排序 `(hot_score, id)` 二元组游标」 | `简历B/C.md`、`简历B/C.html`、`P07-Feed流分页.md`(6处) |
| 2 | 削峰没讲 MQ 投递失败的库存黑洞 | 补「Lua 脚本对称回补（`incrby` + `del` 判重标记）」 | `简历B/C.md`、`简历B/C.html` |
| 3 | 缺失「热门榜」「接口限流」两条可讲项 | 各补 1 条 bullet | 同上 + 新建 `P13-热门榜与限流.md` |
| 4 | Redis 技能写「掌握并深入理解高可用」与自评不符 | 降为「了解 Redis 高可用方案（哨兵主从切换与集群分片原理）」 | `简历C.md/html`、`简历C-超链化.md` |
| 5 | 「幂等四式」是自造名词，面试会被追问 | 全局改「幂等设计」；「四式」仅作目录内部速记并加命名纪律注 | `简历B/C.md/html`、`README.md`、`P03`、`08`、`简历C-超链化.md`、`简历/README.md` |

另：RAG 口径补边界（向量通道 = MySQL 存储 + 内存余弦，受语料规模约束，接口已抽象可替换专用向量库）
并标「（了解层）」。

### ★ 两个关键踩坑（已写入 dsh.memory）

1. **改文字会撑爆 C 版一页**。新增 2 条 bullet 后 C 从 1 页变 2 页（溢出 53.6pt）。
   修法是**调 CSS 不改文字**：`line-height 1.34→1.22` + `.section 5px→3.5px` + `.project 4px→2.5px`
   → 回到 1 页，底部余量 18.7pt。对照：`lh1.28` 仍溢出 20.6pt；`lh1.24/mb4/2.5` 只剩 7.4pt（太贴边）。
   **规则：C 版每次新增文字后必须重新量余量，目标 ≥15pt。**

2. **本机 HTML→PDF 通路**（推翻此前"必须起 http 服务"的认知）：
   `C:/Users/user/AppData/Local/ms-playwright/chromium-1234/chrome-win64/chrome.exe`
   支持 `file://`，`--headless --disable-gpu --no-sandbox --no-pdf-header-footer --virtual-time-budget=8000 --print-to-pdf=<Windows绝对路径>`。
   ⚠️ `--print-to-pdf` 必须传 **Windows 路径**（`C:/...`），传 `/c/...` 报 0x3。
   ⚠️ 页数校验用 `C:/Users/user/.conda/envs/py313/python.exe`（有 PyMuPDF），managed python 没有。

### 顺带修的真 bug
`_校验三文件.py` 原来是窄范围（只查 P02/P03/P04），已重写为**全量 28 文件 / 1180 链接**严格校验，
并当场抓到 `P01-限量发售削峰.md:249` 一处真断链：`[..](</../../../03-...md>)`（多打的 `<` 使路径成了 `</../../../`），已修。
现两个校验脚本均 EXIT=0。

### 成品状态
- `简历B-王欣-Java后端.{html,pdf}` = 1 页（余量 33.6pt）
- `简历C-王欣-Java后端.{html,pdf}` = 1 页（余量 18.7pt）
- `预览-简历B/C.png` 已更新

### 未做的后续选项（等用户决定）
1. 批量修改 `项目设计文档.md` 里 22 处 🔴「规模借口」
2. 生成「代码 ↔ 文档」对照表，标注过时段落

---

## 追加：复习优先级定调（用户两次澄清后的最终口径）

**第一次原话**：手写代码题 / 初级岗机考 / 白板题「现在**完全不用管**这个，AI 时代它变得越来越不重要，
关键点在于**如何把简历写的技术点的原理讲清楚**即可，这也是我创建简历超链版的初衷。」

**第二次原话（关键修正）**：「我说错了，是【"这段为什么这么写" · 项目非手敲，被指到某一行会卡 ·
**得挑 20 处核心代码逐行过一遍**】这里。**代码题我自己刷就行了**。我是说**手敲代码以及面试官问某一行
代码，这种情况根本不存在你懂吗**。」

### 两件事必须拆开（我此前混为一谈）

| | 内容 | 归谁 | 我的动作 |
|---|---|---|---|
| **①** | 手写代码题 / 算法 / 白板 | **用户自己刷** | ❌ 不管、不提、不进风险清单 |
| **②** | 面试官问「这**段**为什么这么写」 | **我的主战场** | ✅ 唯一真风险 |

### ★ 最重要的澄清

用户指出：**"面试官问某一行代码"这种情况根本不存在**。
→ "被指到某一行会卡" 是**原清单的措辞错误**，我误信并沿用。
→ 因此「挑 20 处核心代码逐行过一遍」**不需要做**。
→ 真实场景 = 问**设计决策与取舍动机**（"为什么这么做 / 为什么不那么做"），**不是逐行考代码**。

### 结论

- **唯一主线**：简历每条 bullet 都能讲清原理 + 取舍动机。
- **工作台** = `简历C-超链学习目录/`；新增/优化笔记都服务于此，**不扩张知识面**。
- **我的定位**：陪练（把原理讲到能开口），不是监考（提醒哪里会考）。
- 已写入 `后端求职/dsh.memory` §2 判断 5 + §5.1d + §9 会话日志。

---

## 全项目文档审计（3 子代理并行 + 主代理代码基线）

**产出**：`docs/00-项目文档地图与存废裁决.md`（唯一权威报告）

### 方法
- 主代理做**代码事实基线**（14 条实测事实，作为判定标尺）
- 3 个子代理并行分域：① 根目录 16 文档 ② docs/+工程附属 ③ 后端求职/
- 文件集互不重叠，全部给行号级证据

### 代码基线（实测，非文档宣称）
24 张表 / 191 Java 文件 / 14 Controller / 26 单测 / 4 个 Flyway 迁移 / 67 commit。
`t_user_follow` 零代码、`AlipaySandboxChannel` 不存在（仅 2 处注释）、商品搜索 `LIKE`、
gateway **4 条**路由（文档漏了 `mall_actuator`）、`RemoteAiService.java:76` 伪流式。

### 核心发现（最危险的 3 个点）
1. **`sql/init.sql` 必须删**：只建 8 张表（缺 16），用 `DROP TABLE IF EXISTS`（Flyway 明令禁用），
   且 **L75 注释说 version 是"乐观锁"** —— 与 Flyway `V1:71` 的口径**正好相反**。
2. **`03-现存退化方案与技术债清单.md:124`**：话术模板把"量级"排在理由第一位，
   而 **:126 自己警告"不能滥用"** → 背出去就先说量级。
3. **`超链目录/06-Redis.md:3/10/360/405`**：仍写"掌握并深入理解 Redis 高可用"，
   简历 C 已降为"**了解**" → 复习投入会翻倍。

### 文档分层洞察
五层结构：AGENTS（元层）→ 项目设计文档（蓝图）→ V1.5~V4 详解+词典（8份）
→ AI 专题教程（3份 8245 行）→ 后端求职/（当前在用）→ 工程附属。
**同一技术点被写 4 遍，越早的层越残留错误口径。**

**比例失调**：简历 C 里 AI 只占 1 条 bullet（了解层），却对应 8245 行 AI 文档。

### 处置分组
- 🗑️ 删：`hs_err_pid28388.log`（IDE 语言服务器崩溃，非项目）、`agent操作日志.log`、
  `_imgtest_stream/`、**`sql/init.sql`**
- 📦 归档：简历 A/B 组、`技术栈一百问`、`简历裁决表`、AI 词典、RAG 零基础（含版本事实错误）、
  `docs/learning/` 15 个、PaimonLiveWeb 跨项目文档
- 🔧 修订 10 项（🔴3 项建议立即做）
- ✏️ 改名：`READEME.md`→`README.md`、`P03-幂等四式.md`→`P03-幂等设计.md`（12 处引用）

### 注意
`简历/` 整个目录在 `.gitignore:56` → **删除不可通过 git 恢复**，故建议归档而非删除。



