# 2026-09-03

## 项目全貌扫描（子代理 Explore 完成，未改任何文件）

### ai-mall 现状
- 定位：面向初级 Java 求职的实战项目，AI 种草电商。README 明确「完整设计文档：求职2026-8/项目设计文档.md」。
- 技术栈：Spring Boot 3.4.5 + Java 17 + MyBatis(XML) + MySQL 8 + Sa-Token + Spring AI 1.0(DeepSeek) / Vue 3.5 + Vite 6 + Element Plus + Pinia。Maven 单模块，**无 Redis/RabbitMQ/Nacos**。
- 包结构按域分包：`com.aimall.{common,config,user,goods,order,ai}`，68 个 Java 文件 / 3393 行 + 9 个 Mapper XML。
- 前端 src/ 仅 16 文件，路由 `/login / /product/:id /cart /orders /chat /mine /mine/addresses`，**无管理端**。
- DB 10 表：t_user、t_user_address、t_product(_sku/_image)、t_cart、t_order(_item)、t_conversation、t_message。
- 完成度：0 TODO、0 空方法，V1 闭环 9/9 冒烟通过（scripts/smoke-test.ps1）。

### 已发现的断点 / 待确认
1. **AiConfig.visionChatClient()** 注释说「视觉链路不带工具」，但代码第 65 行实际 `defaultTools(searchTool)` —— 注释与实现矛盾，疑似重构漏删，需确认是有意还是 bug。
2. **支付完全缺失**：t_order 有 pay_type/pay_time 但无支付接口，订单恒为 PENDING_PAY，PAID/SHIPPED 状态不可达。
3. **内容社区名不副实**：README 说内容社区是流量入口，但后端无 content/note 模块、无对应表。
4. README「V1 验收清单」过时（仍写"预置商品知识注入"，实际已是 Function Calling）；演进预告把 Function Calling 列到 V5，与代码差 4 个版本。
5. 可清理：根目录 hs_err_pid28388.log（JVM 物理内存不足 OOM 遗留）、_imgtest_stream/（空目录，.gitignore 只挡了 `.imgtest/` 没挡住它）。

### 设计蓝图（求职2026-8/项目设计文档.md，401 行）
- 性质：架构设计 + roadmap + 面试剧本三位一体，活的文档（有两处修订批注），无版本号/日期。
- 阶段：V1 电商主干(2-3周) → V2 内容社区+RAG(3-4周) → V3 Redis/MQ/秒杀(3周) → V4 微服务(3-4周) → V5 Agent/推荐/NL2SQL(3-4周)。合计 14-18 周。
- **V1~V3 = 简历底线，V4 视时间，V5 加分项**。当前代码只到 V1。
- §4.5「衔接层」是项目灵魂 + 面试深挖点，铁律：**AI 永不直写交易数据，只走受控接口**。
- §9 防膨胀守则，明确不做：直播、复杂优惠券/拼团、多级分销、推荐算法调参、ES 全文检索。
- 同目录另外三本：AI模块详解-SpringAI从零到实战.md（11章主教程）、【知识词典】（5464行/120词条）、SpringAI_RAG_零基础详解（V2 RAG 专项）。三本与主教程是同一套教材，无八股题、无算法、无面经。

### 关键判断
- 求职2026-8 目录整体是「围绕单一项目深度展开的求职冲刺资料库」，不是泛八股仓库。
- V2 的 RAG 有专项笔记可补，V3 的 Redis/MQ/秒杀目前零代码 —— 面试被追问须诚实界定为「规划中」，不能说成已实现。
