# V1.5 工程基建详解【知识词典】

> 配套主文档：[V1.5工程基建详解.md](V1.5工程基建详解.md)（按章节学习用主文档，查概念用本词典）
> 与前几本词典（《AI模块详解-SpringAI从零到实战【知识词典】》《V2内容社区与RAG详解【知识词典】》《V3支付与Redis详解【知识词典】》）不重复收录。
> 词条格式：**一句话核心 → 比喻 → 原理展开 → 代码对应 → 常见追问**。每个词条尾部可跳回主文档对应章节。

## 目录

- [一、数据库版本管理](#一数据库版本管理)：[Flyway](#flyway) / [迁移（Migration）](#迁移migration) / [baseline-on-migrate](#baseline-on-migrate) / [checksum](#checksum)
- [二、可观测性](#二可观测性)：[traceId](#traceid) / [MDC](#mdc) / [ThreadLocal](#threadlocal) / [线程串号](#线程串号) / [TaskDecorator](#taskdecorator) / [TransmittableThreadLocal](#transmittablethreadlocal) / [logback pattern](#logback-pattern) / [日志滚动](#日志滚动) / [Actuator](#actuator) / [可观测性](#可观测性)
- [三、对象存储与上传安全](#三对象存储与上传安全)：[对象存储抽象](#对象存储抽象) / [上传安全三件套](#上传安全三件套) / [MIME 白名单](#mime-白名单) / [路径穿越](#路径穿越) / [存储型 XSS](#存储型-xss) / [Content-Disposition](#content-disposition) / [预签名 URL](#预签名-url) / [懒初始化](#懒初始化) / [S3 兼容](#s3-兼容)
- [四、线程池](#四线程池)：[线程池](#线程池) / [有界队列](#有界队列) / [拒绝策略](#拒绝策略) / [CallerRuns](#callerruns) / [反压](#反压) / [IO 密集 vs CPU 密集](#io-密集-vs-cpu-密集) / [优雅停机](#优雅停机)
- [五、测试](#五测试)：[测试金字塔](#测试金字塔) / [mockStatic](#mockstatic) / [@TempDir](#tempdir) / [纯内存测试](#纯内存测试)
- [六、部署与协作](#六部署与协作)：[docker-compose](#docker-compose) / [CI 门禁](#ci-门禁) / [可降级设计](#可降级设计) / [可插拔](#可插拔)

---

## 一、数据库版本管理

### Flyway
**一句话：数据库结构版本管理工具——迁移 SQL 按版本号有序执行并记录，启动即校验，代码与结构永不脱节。**

比喻：游戏的存档系统——每过一关自动存档（执行迁移+记录版本），下次读档只补"没打的关"（对比 history 表与迁移目录的差异）。

原理展开：工作机制四步：①启动时查 `flyway_schema_history` 表（首次自动创建）；②扫描 `db/migration` 目录；③对比出未执行的迁移，**按版本号顺序**执行；④每条记录版本/描述/耗时/[checksum](#checksum)。核心价值：数据库的"真相"从"某个人的手工操作"变成"仓库里的迁移文件序列"——新人克隆即得同构库，结构变更有了历史与评审入口。

代码对应：`db/migration/V1__init.sql` / `V2__content_and_rag.sql` / `V3__pay_and_drop.sql`。

常见追问：*"和 Liquibase 比呢？"*——Liquibase 用 XML/YAML 抽象 DSL（跨数据库方言），Flyway 用原生 SQL（学习成本零、能力全集）；单体单数据库项目 Flyway 是主流选择。
→ 返回[主文档 1.2](V1.5工程基建详解.md#12-flyway-怎么工作5-分钟原理)

### 迁移（Migration）
**一句话：一个版本号对应一个 SQL 文件（V2__content_and_rag.sql），只执行一次、执行后不可修改。**

比喻：历史书的章节——已经出版（执行）的章节不能偷偷改写（checksum 会发现）；要修订就出"第 4 章"（新迁移）。

原理展开：命名规则 `V<版本>__<描述>.sql`（**双下划线**，版本号全局递增）。两条纪律：①**只增不改**——改已执行的文件 → checksum 校验失败 → 启动被拒（保护历史不可篡改，这是 Flyway 的哲学不是麻烦）；②**失败处理**——迁移半途失败留下 `success=0` 记录，下次启动报 `Detected failed migration`；因 DDL 多数数据库不支持回滚，处置是人工修复后 DELETE 失败行重跑（迁移要尽量小、幂等，降低失败影响面）。
→ 返回[主文档 1.4](V1.5工程基建详解.md#14-迁移纪律团队约定)

### baseline-on-migrate
**一句话：对已有表的存量库首次接入 Flyway 时自动打"基线"（版本 0），从基线之后开始执行迁移。**

比喻：接手一个装修到一半的房子——先拍照存档（baseline）记录"现状从今天算第 0 版"，之后的改动（迁移）才开始记版本；不从毛坯重建。

原理展开：没有它：非空库+无 history 记录 → Flyway 拒绝执行（不知道库处于什么状态，乱跑可能重复建表）。有了它：自动插入 baseline=0 一行，V1 起的迁移开始比对执行。**配套决策**：V1__init.sql 全部 `CREATE TABLE IF NOT EXISTS`（幂等）——存量库重放 V1 无害、全新库正常建表，**新旧库双兼容**。这个"baseline+幂等首个迁移"的组合拳是存量项目接 Flyway 的标准姿势。
→ 返回[主文档 1.3](V1.5工程基建详解.md#13-本项目的接入一个关键决策面试高频)

### checksum
**一句话：Flyway 给每个已执行迁移文件算的校验和——文件被篡改时启动报错，保护"历史不可篡改"。**

比喻：试卷的骑缝章——交卷（执行）后文件任何一字改动，与存档章（checksum）对不上，直接暴露。

原理展开：执行迁移时把文件内容的校验和写进 history；每次启动重算比对。触发 mismatch 的场景：手滑改了老迁移、IDE 格式化/换行符变化（CRLF↔LF）。处置：还原文件内容；**真的需要改结构=写新迁移**，永远不要改历史。也有 `flyway repair` 命令可刷新 checksum（慎用——它治"文件合法变更后想追认"的场景，不是给"改历史"开后门）。
→ 返回[主文档 1.4](V1.5工程基建详解.md#14-迁移纪律团队约定)

---

## 二、可观测性

### traceId
**一句话：贯穿一个请求全生命周期的唯一标识——日志、响应头、跨线程、跨服务（未来）都带着它。**

比喻：医院的就诊条码——挂号生成，抽血/检查/取药每个窗口都扫它；出问题时凭条码调出你今天的全部记录。

原理展开：生成规则（本项目）：日期时间+随机数（短、可读、够唯一）。三个落点：MDC（日志自动带）、响应头 X-Trace-Id（用户报障时手里有凭证）、透传上游（请求头带进来就沿用——V4 微服务链路的预留）。价值：把"一团乱麻的并发日志"变成"按 traceId 一根线抽出"——`grep <traceId> app.log` 就是事故现场完整回放。

常见追问：*"分布式下怎么传？"*——HTTP 头显式传递（网关→服务→服务，每个入口 Filter 沿用上游的 traceId）；业界标准是 W3C Trace Context / B3 header，OpenTelemetry 是事实标准实现——本项目 Filter 已预留"有 incoming 就沿用"的钩子。
→ 返回[主文档 2.2](V1.5工程基建详解.md#22-三件套总览先背结构)

### MDC
**一句话：Mapped Diagnostic Context——日志框架自带的"线程储物柜"，存进去的值能在同线程所有日志的 pattern 里自动打出。**

比喻：银行的"叫号屏"——你取号（set）后，之后任何窗口叫到你都自动显示你的号（pattern %X）；不需要你每次开口报号（改方法签名）。

原理展开：底层是 [ThreadLocal](#threadlocal)（线程隔离）：`MDC.put("traceId", x)` 后，同线程任何 `log.info(...)` 的 pattern 里 `%X{traceId}` 都能取到——**零侵入**是它对比"给每个方法加 traceId 参数"的本质优势。两条铁律：①线程隔离=换线程就丢（[TaskDecorator](#taskdecorator) 补）；②线程池复用=不清理会[串号](#线程串号)（finally clear 补）。

代码对应：`TraceIdContext.set/clear`（对 MDC 的薄封装）。
→ 返回[主文档 2.1](V1.5工程基建详解.md#21-问题驱动日志为什么串不起来)

### ThreadLocal
**一句话：线程隔离的变量——每个线程看到的是自己那份副本，互不可见。**

比喻：酒店房间的保险箱——每个房间（线程）一个，你放的东西只有刷你的房卡能取；隔壁房间的人既看不见也拿不到。

原理展开：实现：每个 Thread 内部有一个 ThreadLocalMap，key 是 ThreadLocal 实例、value 是存的值——"隔离"来自"map 挂在线程自己身上"。两个经典坑：①**线程池下不清理**=下个任务读到上个任务的残留（[串号](#线程串号)）；②** Entry 的 key 是弱引用、value 强引用**=不 remove 时 value 可能内存泄漏（线程池线程长活，Entry 挂在它身上出不去）——**用完必 remove** 是 ThreadLocal 的军规。本项目 MDC 的 set/clear 配对就是这条军规的执行。
→ 返回[主文档 2.4](V1.5工程基建详解.md#24-坑一异步线程里-traceid-丢了本章最重)

### 线程串号
**一句话：线程池复用线程时，上一个请求的 MDC 残留被下一个请求读到——日志张冠李戴。**

比喻：酒店房间没打扫就入住下一个客人——抽屉里还有上一位的房卡和票据，你以为是自己的。

原理展开：时间线：请求 A 在 Tomcat 线程 #7 存 traceId=A → A 结束（没 clear）→ 线程 #7 归还池子 → 请求 B 拿到线程 #7 → B 的日志全带 traceId=A → 排查 B 的事故时按 A 的 traceId grep，**看到的全是错误现场**。比"没有 traceId"更糟——没有是不知道，串号是**被误导**。防御：Filter 的 finally clear + TaskDecorator 的恢复 previous——**凡是池化线程，离开时还一个干净的储物柜**。
→ 返回[主文档 2.3](V1.5工程基建详解.md#23-入口traceidfilter-逐行精读)

### TaskDecorator
**一句话：Spring 线程池的"任务包装器"钩子——在提交/执行/完成三个时刻插入 MDC 复制逻辑，实现 traceId 跨线程传递。**

比喻：快递代寄点——你（Web 线程）把包裹（任务）连同你的地址信息（MDC 快照）一起交给店员；快递员（池线程）送货时贴上你的地址（设置 MDC），送完撕掉（恢复），车（线程）还给店里时是干净的。

原理展开：三个时刻缺一不可：①**提交时**（还在 Web 线程）`MDC.getCopyOfContextMap()` 快照——此刻还能拿到 traceId；②**执行时**（池线程）`setContextMap(快照)` 设置；③**完成时**恢复 previous（池线程自己也复用——与 Filter 的 finally 同理）。边界认知：**单层池有效，跨池传递失效**（A 池 decorate 时 A 的 MDC 已无值）——那种场景上 [TTL](#transmittablethreadlocal)。

代码对应：`MdcTaskDecorator`（15 行）+ AsyncConfig 的 setTaskDecorator。
→ 返回[主文档 2.4](V1.5工程基建详解.md#24-坑一异步线程里-traceid-丢了本章最重)

### TransmittableThreadLocal
**一句话：阿里的 TTL 库——增强版 ThreadLocal，解决"跨线程池传递"（A 池提交给 B 池）时上下文丢失。**

比喻：普通快递（TaskDecorator）只能一站直达；多级中转（跨池）的包裹要靠全程物流追踪系统（TTL：代理 Runnable + 注册中心记录各级快照）。

原理展开：原理：TtlRunnable 包装任务时**抓取当前线程的全部 TTL 变量**（不只是显式传的那个），任何一层池执行时都恢复——"传递"不依赖调用链上每层都记得 decorate。代价：要用 TtlExecutors 包装线程池+（可选）Agent 字节码增强。**本项目单层池用 TaskDecorator 够用**——知道"什么时候不够"（出现第二层池/CompletableFuture 链）比背 API 重要。
→ 返回[主文档 2.4](V1.5工程基建详解.md#24-坑一异步线程里-traceid-丢了本章最重)

### logback pattern
**一句话：日志格式模板——%X{key} 占位符从 MDC 取值，traceId 就此打进每行日志。**

原理展开：关键占位符：`%d` 时间、`%thread` 线程名、`%X{traceId:-NONE}`（MDC 取 traceId，缺省 NONE）、`%-5level`、`%logger{36}`、`%msg`。`:-NONE` 的细节：没值时打 NONE 而不是空白——**列对齐，肉眼扫日志不串行**。本项目 pattern：`%d{HH:mm:ss.SSS} [%thread] [%X{traceId:-NONE}] %-5level %logger{36} - %msg%n`——线程名前缀（aimall-async-）让你一眼区分 Web 线程与池线程。
→ 返回[主文档 2.5](V1.5工程基建详解.md#25-logback-pattern让-traceid-真的打出来)

### 日志滚动
**一句话：按大小/时间切分日志文件并限量保留——不治理的日志会写满磁盘拖垮应用。**

比喻：家里的垃圾分类清运——每天定时收走（滚动）、桶有容量上限（单文件大小）、旧报纸只留一个月（保留天数）、储物间总量有限（总容量上限）。

原理展开：本项目策略：50MB/文件、30 天、总量 2GB 上限（超限删最老）。三段式配置（rollingPolicy + maxHistory + totalSizeCap）。**事故视角**：没滚动的日志=磁盘定时炸弹——磁盘满的连锁反应：日志写不进（丢现场）、数据库写不进、应用挂。运维三板斧（日志滚动/磁盘告警/单独磁盘分区）里滚动是最便宜的一板。
→ 返回[主文档 2.5](V1.5工程基建详解.md#25-logback-pattern让-traceid-真的打出来)

### Actuator
**一句话：Spring Boot 的运维端点——/health 健康检查、/info 元信息，容器编排与监控的对接点。**

比喻：汽车的仪表盘与 OBD 检修口——日常看仪表（health），检修插诊断口（metrics/env 等端点）。

原理展开：liveness/readiness 探针的判据就是 `/actuator/health`——K8s 据此决定重启/引流。健康信息自动聚合：V3 接入 Redis 后 health 里自动多出 redis 组件状态（down 时整体 DOWN——**依赖的健康会传染**，这也是"可降级"设计要在代码层兜住、而不是让 health 决定生死的理由之一）。安全注意：生产暴露端点要精选（health/info 公开，env/heapdump 严禁公网）。
→ 返回[主文档 2.6](V1.5工程基建详解.md#26-actuator给运维一双眼睛)

### 可观测性
**一句话：系统"可被观察"的能力——日志（traceId 串起来）、指标（QPS/耗时）、链路（请求经过哪些环节）三支柱。**

比喻：医院的监护仪——心率血氧（指标）、病历时间线（日志）、查房路线（链路）；没有监护仪的病人出事只能靠猜。

原理展开：三支柱分工：日志答"发生了什么"（排查）、指标答"系统现在怎么样"（告警）、链路答"请求卡在哪一环"（定位）。本项目 V1.5 落了日志支柱（traceId 三件套+滚动）+健康端点；指标/链路的业界标准是 Prometheus+Grafana / OpenTelemetry——单体阶段够用，V4 微服务化的第一件事就是把另两支柱补上（面试聊"上微服务最先准备什么"的高分答案）。
→ 返回[主文档第 2 章](V1.5工程基建详解.md#第-2-章-traceid-全链路日志从一团乱麻到一根线)

---

## 三、对象存储与上传安全

### 对象存储抽象
**一句话：文件存取的统一接口（store/url/newObjectKey），local 与 MinIO 两个平行实现按配置切换——可插拔家族第一棒。**

比喻：家里的插座标准——电器（业务代码）只认插头形状（接口），墙后是市电还是发电机（实现）与电器无关。

原理展开：接口三方法覆盖存取全流程：store（存+返回对象键）、url（键→可访问 URL）、newObjectKey（生成存储键）。两个实现：LocalStorageService（文件落本地、`/files/**` 静态映射出 URL——零依赖默认）与 MinioStorageService（S3 对象存储、[预签名 URL](#预签名-url)）。切换=改一行配置，**20 处 `new File` 硬编码的迁移噩梦从根上消除**。后续 V2 EmbeddingClient、V3 PayChannel 复用同一设计——面试可把三次串成你的设计哲学。
→ 返回[主文档 3.2](V1.5工程基建详解.md#32-抽象层设计)

### 上传安全三件套
**一句话：MIME 白名单 + UUID 重命名 + 大小限制——公网写入点的三条红线，缺一不可。**

比喻：小区门禁三关卡——访客登记白名单（类型）、发临时卡不能用别人名字（UUID 键）、大件限重（大小）；三关都过才放行进库。

原理展开：①[白名单](#mime-白名单)挡非图片文件（html/svg 可携带脚本的"图片"）；②UUID 键挡[路径穿越](#路径穿越)与覆盖（原始文件名完全不被采信）；③大小限制挡"10GB 文件写满磁盘"的 DoS。总纲：**上传的每个文件都是不可信输入**——文件名、声明类型、内容，三者都可能是谎言。纵深防御：三件套之上再加 [Content-Disposition](#content-disposition)/独立域名（见存储型 XSS）。

代码对应：`FileRestController` 上传端点 + spring.servlet.multipart 配置。
→ 返回[主文档 3.3](V1.5工程基建详解.md#33-上传安全三件套红线)

### MIME 白名单
**一句话：只允许已知安全的类型（png/jpeg/gif/webp）——"不在名单里=拒绝"，天然完备；黑名单永远列不全。**

比喻：夜店的会员制（白名单：在册的才能进）vs 黑名单（列了 100 个禁止入内的，第 101 个捣乱者还是能进）。

原理展开：黑名单列不全的原因：变体无穷——.svg（XML 可嵌脚本）、.html、双扩展名（a.jpg.html）、大小写（.JPG）、Content-Type 伪造（curl 随便写）。白名单的哲学反转：**证明"安全"（在名单里）才能过，而不是证明"不安全"（撞上名单）才拒绝**。工程细节：扩展名与 Content-Type **双校验**（两个都能伪造，交叉验证提高成本）；更硬核的是读文件头[魔法字节](V3支付与Redis详解.md#54-时序攻击为什么不用-equals)（png 的 \x89PNG——本项目 AI 识图模块的 detectImageMimeType 就是这套思路的复用）。
→ 返回[主文档 3.3](V1.5工程基建详解.md#33-上传安全三件套红线)

### 路径穿越
**一句话：恶意文件名（../../etc/evil）拼接路径时逃出目标目录，写到任意位置。**

比喻：快递地址栏写"收件地址：往前走绕过门卫直入仓库三号货架"——地址本身成了通行证。

原理展开：攻击面：`new File(uploadDir + "/" + filename)` 里 filename 含 `../`——`uploads/../../app.jar` 覆盖应用本体。防御阶梯：①UUID 键（本项目）：存储键完全由服务端生成，原始文件名只取扩展名（且扩展名过白名单）——穿越无从谈起；②`Path.normalize()` 后校验仍以目标目录开头（startsWith）——允许保留原名的场景用；③框架层面（Spring 的 MultipartFile.transferTo 拒绝部分穿越）——不能依赖，自己兜底。**原则：不信任的输入永远不直接参与路径拼接**。
→ 返回[主文档 3.3](V1.5工程基建详解.md#33-上传安全三件套红线)

### 存储型 XSS
**一句话：攻击内容被存储在服务器上（上传的"图片"实为含脚本的 HTML），其他用户访问时在站点域名下执行。**

比喻：公告栏里贴了张"告示"（实为钓鱼便条）——路过的同事都会读到；因为贴在了公司公告栏（同域），大家天然信任。

原理展开：攻击链：上传 evil.html（绕过类型检查）→ 存为 `/files/evil.html` → 受害者点击链接 → 浏览器在同域直接渲染执行脚本（偷 cookie/localStorage）。防御分层：①MIME 白名单从源头挡掉 html/svg（主防线）；②[Content-Disposition: attachment](#content-disposition) 强制下载不渲染（附件纵深）；③独立静态域名（浏览器同源策略隔离——脚本在本域执行也没权限）。**上传接口的安全不是"存得下"，是"存的东西不能变成武器"**。
→ 返回[主文档 3.3](V1.5工程基建详解.md#33-上传安全三件套红线)

### Content-Disposition
**一句话：HTTP 响应头——attachment 强制浏览器下载而非内联渲染，断掉"上传的 HTML 被当页面执行"的路。**

比喻：海关把包裹一律"拆件检验转交"（下载后由本地程序处理），而不是"直接在你面前打开"（内联渲染）。

原理展开：两个值：inline（默认，浏览器能渲染就渲染——html/pdf/图片直接展示）、attachment（弹保存框，内容不进 DOM）。对用户上传的文件用 attachment 是纵深防御第二层：即使白名单漏了某个可执行内容，也只被下载不被渲染。图片这种确定安全的可 inline（要正确设置 Content-Type 且经过了白名单）。配套技巧：filename 参数同时防乱码与注入。
→ 返回[主文档 3.3](V1.5工程基建详解.md#33-上传安全三件套红线)

### 预签名 URL
**一句话：带签名的临时授权 URL——客户端凭它直接上传/下载对象存储，不经过应用服务器中转流量。**

比喻：酒店的临时房卡——限你的房间（指定对象）、限时（过期失效）、限权限（只能开不能退房）；前台（应用服务器）发卡但不陪你去房间。

原理展开：解决的问题：图片走应用服务器中转=双倍带宽+应用线程被 IO 占死。预签名：应用用密钥生成"限时+限对象+限操作"的 URL 交给前端，前端直传 MinIO/S3——**流量不过应用**。安全边界：URL 泄露只在有效期内有效（本项目默认短时效）；签名验证在存储服务端。V1.5 的 MinioStorageService 实现了该语义——即使当前 local 实现用不上，**接口契约已为"流量卸载"预留**。
→ 返回[主文档 3.2](V1.5工程基建详解.md#32-抽象层设计)

### 懒初始化
**一句话：资源（MinIO client）不在启动时创建，首次使用时才建——配置了但依赖缺席不阻断启动。**

比喻：备用发电机不常转——主电（local 存储）一直在用，真要用发电机（MinIO）的那一刻才点火；点火失败也只影响这一件事，不是全楼断电。

原理展开：对立面（饿汉式）：构造 Bean 时就连 MinIO——容器没起=启动失败=**一个可选功能绑架整个应用**。懒初始化把失败推迟到"用到那一刻"，失败半径从"全应用"缩小到"该功能"。与可降级原则配合：懒初始化（不阻断）+ RedisOps 式门面（用时降级）= 可选依赖的完整处理。适用判别：**强依赖早连（fail-fast 暴露配置错）、弱依赖懒连（缺席可容忍）**。
→ 返回[主文档 3.2](V1.5工程基建详解.md#32-抽象层设计)

### S3 兼容
**一句话：对象存储的事实 API 标准（亚马逊 S3 定义），MinIO/OSS/COS 都兼容——换厂商只改 endpoint 与密钥。**

比喻：USB 接口标准——键盘鼠标硬盘（各厂商对象存储）都造 USB 口（S3 API），电脑（应用）不用管对面是什么牌子。

原理展开：为什么选 MinIO 而不是直接学云厂商：MinIO 自部署（学习项目免费零依赖）且 S3 兼容——**今天连 MinIO、明天换阿里 OSS，SDK 与代码几乎不动**（改 endpoint）。这是"抽象 + 事实标准"的组合拳：自家接口（StorageService）防框架锁定，S3 兼容防厂商锁定。面试聊"对象存储选型"时这层双保险是加分点。
→ 返回[主文档 3.2](V1.5工程基建详解.md#32-抽象层设计)

---

## 四、线程池

### 线程池
**一句话：预建线程复用执行任务的机制——省去逐任务建/销线程的开销，并提供队列、拒绝策略等治理手段。**

比喻：出租车车队——乘客（任务）来了派车（线程），车不去销毁而是回场等下一单；高峰期排队（队列），爆满时拒载或调度室自己跑一趟（拒绝策略）。

原理展开：核心参数四件套：corePoolSize（常驻车队）、maxPoolSize（高峰扩编上限）、queueCapacity（候客排队位）、rejectedExecutionHandler（爆满对策）。执行顺序语义（**易错**）：先占 core → **满了进队列** → 队列满了才扩线程到 max → 再满触发拒绝——不是"先扩线程再排队"。Boot 默认池的陷阱：queueCapacity=Integer.MAX_VALUE——队列永远不满 → 永不扩线程、**永不拒绝** → 无声堆积到 OOM。生产代码必须显式有界（[有界队列](#有界队列)）。

代码对应：`AsyncConfig.taskExecutor`（4/16/200/CallerRuns）。
→ 返回[主文档 4.2](V1.5工程基建详解.md#42-asyncconfig-逐参数精读)

### 有界队列
**一句话：容量有限的任务队列——超出即触发拒绝策略；宁可快速降速也不要无限堆积。**

比喻：火锅店取号上限 200 号——满了就"暂停取号"（拒绝策略），而不是让号无限排到明天早上（无界队列），顾客饿着等 8 小时比被拒更糟。

原理展开：无界的数学死刑：提交速率 > 消费速率时队列单调增长，堆内存同步增长，OOM 只是时间问题；**阴险在于全程无报错**。有界的哲学：拒绝是有信息的（"我过载了"），可触发告警/降级/反压；无限等待是无信息的（所有人都在等，没人知道等多久）。与 [CallerRuns](#callerruns) 配对=有界+反压的完整方案；与 V3 Redis 连接池的 max-wait 同一哲学（有界+快速失败）。
→ 返回[主文档 4.1](V1.5工程基建详解.md#41-先撞南墙boot-默认线程池是颗地雷)

### 拒绝策略
**一句话：队列满+线程满时的处置——Abort 抛异常/CallerRuns 提交线程自己跑/Discard 丢弃/DiscardOldest 丢最老。**

比喻：餐厅满座四种对策：劝退（Abort：明说没位了）、老板亲自端菜加速翻台（CallerRuns：自己上）、默默不管新客（Discard）、赶走等最久的换新客（DiscardOldest）。

原理展开：四种语义：**Abort**（默认，抛 RejectedExecutionException——快速失败，适合"失败可感知可重试"的场景）；**CallerRuns**（[反压](#反压)，提交线程执行——适合"可延迟不可丢"）；**Discard/DiscardOldest**（静默丢——**几乎总是错的**：丢任务无日志无感知，出事查无此据；要丢也要先记日志的自定义 handler）。选型口诀：能感知失败的 Abort、能减速的 CallerRuns、自定义 handler 兜其余——**静默丢弃永不选**。
→ 返回[主文档 4.2](V1.5工程基建详解.md#42-asyncconfig-逐参数精读)

### CallerRuns
**一句话：CallerRunsPolicy——队列满时让提交任务的线程自己执行任务，用"拖慢上游"实现自动限速。**

比喻：早高峰的地铁限流——站台满了就关闭进站口几分钟（提交线程被占住），人流（请求）在站外自然减速；不是把人扔掉（Abort），而是让他等。

原理展开：机制的妙处：提交线程（如 Tomcat 的 Web 线程）被任务占住 → 它无法处理新请求 → **上游速率自然下降**——无需任何协调组件，压力沿调用链反向传导。适用判别：任务"可延迟、不可丢"（审核、日志落盘）选它；任务"用户在等结果"（同步导出）选 Abort+明确提示。陷阱认知：CallerRuns 期间提交线程长时间被占（任务很慢时）等于**该 Web 线程被借用**——极端时整个 Web 池都被拖进执行，请求排队；所以它是"减速带"不是"免死金牌"，容量规划仍要做。
→ 返回[主文档 4.2](V1.5工程基建详解.md#42-asyncconfig-逐参数精读)

### 反压
**一句话：backpressure——当下游过载，把压力信号反向传导给上游使其自然减速，而不是独自死扛或丢弃。**

比喻：流水线的停线绳——任何工位跟不上就拉绳整线降速；而不是让半成品在传送带上无限堆积压垮车间。

原理展开：对比三种过载处置：硬扛（无界队列→OOM）、丢弃（用户请求莫名失败）、**反压**（上游减速——过载是暂时的，减速等消化）。实现载体不止 CallerRuns：TCP 滑动窗口（协议层）、Reactor 的 limitRate（响应式）、MQ 的 prefetch（消费端限流——V2 用了）。反压的思想价值：**把"速率匹配"看作系统设计问题而非运气问题**——面试聊高并发时主动提反压，比堆"加缓存加集群"高一个层次。
→ 返回[主文档 4.2](V1.5工程基建详解.md#42-asyncconfig-逐参数精读)

### IO 密集 vs CPU 密集
**一句话：任务等外部 IO（查库/调 API/写文件）线程多设也无妨（核数×2~4）；任务纯计算线程数≈核数再多反而切换亏损。**

比喻：餐厅服务员（IO 密集）：点完单菜在厨房做（等 IO），服务员可以去招呼别桌——多雇几个提升吞吐；厨师（CPU 密集）：灶台就 4 个（4 核），雇 8 个厨师也只得 4 个在炒，多出的在换班（上下文切换）纯亏。

原理展开：判断依据是**线程大部分时间在等还是在算**：等待时 CPU 空闲，多线程=用切换成本买吞吐；计算时 CPU 饱和，多线程=纯切换开销。本项目审核任务（调 AI 接口秒级等待）典型 IO 密集——4/16 的池；若是本地向量化计算（CPU 密集）则应压到核数。经验公式只是起点（CPU 密集 N+1、IO 密集 N×2~4），**真实答案靠压测**——参数是调出来的不是算出来的。
→ 返回[主文档 4.2](V1.5工程基建详解.md#42-asyncconfig-逐参数精读)

### 优雅停机
**一句话：收到关闭信号后不再接新任务、等存量任务执行完再退出——防止"任务跑到一半进程消失"。**

比喻：打烊流程——门口挂"暂停接待"（不再收新任务），等店里客人吃完（存量任务），关灯锁门（退出）；而不是直接熄灯赶人。

原理展开：Spring 实现：`setWaitForTasksToCompleteOnShutdown(true)` + `setAwaitTerminationSeconds(30)`——等最多 30 秒，超时强制退（防一个死任务拖死整个停机）。不优雅的后果：K8s 滚动更新时任务（如支付回调处理到一半）被杀——**更新=事故窗口**。同类思想的三次出现：本池的优雅停机、voice 的 VITS 进程 destroy 等待、MQ 消费者的 ACK 语义——**"退出也要退得干净"是资源生命周期的最后一课**。
→ 返回[主文档 4.2](V1.5工程基建详解.md#42-asyncconfig-逐参数精读)

---

## 五、测试

### 测试金字塔
**一句话：大量快的单元测试（毫秒）+ 少量关键链路集成测试（秒）+ 极少的端到端测试（分钟）——快慢与数量成反比。**

比喻：体检体系——日常自测血压血糖（单测，便宜高频）、季度体检（集成，中频）、全年深度住院检查（E2E，低频昂贵）；只靠住院检查查不出日常问题。

原理展开：为什么底座是单测：快（秒级全量）→ 每次提交都跑得起 → 反馈闭环短 → 缺陷早暴露早修（修复成本随发现时间指数上升）。集成测试慢在真环境（起容器/连库），只留给"连接处"（事务边界、SQL 真实执行）；E2E 只验用户主路径。反模式："冰淇淋筒"（大量 E2E、单测稀少）——慢、脆（任何一处改动全红）、定位难（红了我不知道哪层错）。本项目：15+5 单测为主力，冒烟测试（9 项手工清单）当 E2E——量级匹配的项目选择。
→ 返回[主文档 5.1](V1.5工程基建详解.md#51-为什么第一批测试是它们)

### mockStatic
**一句话：Mockito.mockStatic——给静态方法打桩的机制，try-with-resources 限定作用域；钉住 StpUtil 让单测无需登录态。**

比喻：临时换掉门禁系统的读卡器（静态调用）——在你练习的那几分钟（作用域内）刷什么卡都显示"42 号员工"；练完换回原装（出域自动卸载）。

原理展开：为什么普通 @Mock 不行：依赖注入 mock 的前提是"调用经过对象引用"，而 `StpUtil.getLoginIdAsLong()` 是**类名直接调用**——没有引用可替换。mockStatic 在字节码层拦截该类的静态调用。**必须 try-with-resources**：静态 mock 改的是全局状态（类级别），不限作用域会污染同 JVM 的其它测试（并行测试下随机挂）。本项目用法：钉"当前用户=42"→ 测越权（42 访问 43 的订单应 404）、测归属（42 的订单才有状态流转权）——**登录态是单测里最常被钉死的边界**。

代码对应：`OrderServiceImplTest` 的 MockedStatic 段。
→ 返回[主文档 5.2](V1.5工程基建详解.md#52-关键技巧mockstatic-钉住-stputil)

### @TempDir
**一句话：JUnit 自动创建临时目录、测试结束自动删除——文件类测试不留垃圾也不互相污染。**

比喻：酒店的钟点房——入住（测试开始）给你一间干净的房（临时目录），退房（测试结束）自动清理，下一个客人（下一个测试）又是全新房间。

原理展开：解决的问题：文件测试写死路径 → ①测试残留垃圾文件污染仓库；②并行测试写同一路径互相覆盖（随机挂）。@TempDir 每次注入独立目录（JUnit 保证唯一），测完删——**测试的隔离性原则**（每个测试不依赖、不破坏其它测试的环境）。本项目 LocalStorageServiceTest 用它测上传落盘与路径穿越防御。同族思路：测试数据用随机后缀、数据库测试用事务回滚——都是"测完还原世界"。
→ 返回[主文档 5.2](V1.5工程基建详解.md#52-关键技巧mockstatic-钉住-stputil)

### 纯内存测试
**一句话：单测不依赖任何外部服务（DB/Redis/MQ/文件系统）——Mapper 全 mock、登录态 mockStatic、文件用 TempDir。**

比喻：模拟考——不占用真实考场（外部环境），试卷自包含（内存造数据）；模拟考挂了是知识问题（代码 bug），不是考场问题（环境故障）。

原理展开：价值两连：①**可靠性**——测试红=代码错，不是环境抖（依赖环境的测试"偶尔红"，团队很快学会忽略红灯——测试体系信用破产）；②**CI 友好**——CI 容器里没 MySQL，纯内存照样全绿，`mvn test` 即门禁。边界认知：纯内存的代价是不测真实 SQL 与事务——所以关键 SQL 路径要有少量集成测试（连测试库）兜底；**单测守逻辑、集成测守连接处**，分工明确。

代码对应：全项目 20 个测试用例零外部依赖，`mvn test` <10s。
→ 返回[主文档 5.2](V1.5工程基建详解.md#52-关键技巧mockstatic-钉住-stputil)

---

## 六、部署与协作

### docker-compose
**一句话：一条命令编排多个容器（mysql/redis/rabbitmq/minio）——依赖环境从"文档里的安装步骤"变成"可执行的代码"。**

比喻：宜家的组装说明书+全套零件包——不是给你一张零件清单让你自己找（文档装环境），而是整箱递给你照着拧（compose up）。

原理展开：价值：**环境即代码**——版本（mysql:8.0）显式钉死、依赖关系（depends_on）声明式表达、一键起停；"我机器上能跑"从借口变成 `docker compose up -d` 可复现。本项目的 compose 四件套对应四个版本引入的依赖（V1 mysql / V1.5 minio / V2 rabbitmq / V3 redis）——依赖图即 compose 文件，**一眼看清系统外部依赖全貌**（这本身也是架构文档）。注意 compose 与虚拟机的分工：compose 是"全新环境模板"，你的虚拟机（6381/5673 非默认端口）是"长期运行环境"——两套配置靠环境变量统一（REDIS_HOST 等）。
→ 返回[主文档 6.1](V1.5工程基建详解.md#61-docker-compose一台电脑讲清整个依赖图)

### CI 门禁
**一句话：每次 push 自动构建+测试，"红则不得合并"——质量从个人自觉变成流程强制。**

比喻：机场安检——不是靠乘客自觉"我没带违禁品"（人肉保证），而是人人过闸（CI 检查），拦下就登不了机（合不了并）。

原理展开：门禁的意义大于动作本身：本地测试会跳过（赶时间、侥幸），**流程化的检查才不可绕过**。本项目的 workflow：checkout → setup JDK 17 → `mvn test`（20 用例纯内存秒级）→ package。设计呼应：测试的[纯内存原则](#纯内存测试)是 CI 的前提——**测试体系的设计决定 CI 的成本**（要起数据库的测试，CI 要服务编排，慢且脆）。进阶（知道即可）：分支保护规则+必需状态检查+PR 触发——小团队 push 门禁够用。
→ 返回[主文档 6.2](V1.5工程基建详解.md#62-github-actions-ci)

### 可降级设计
**一句话：任何中间件都不是应用启动的先决条件——依赖缺席时自动退到备用路径，功能降级但服务不断。**

比喻：家里的备用钥匙——钥匙（主依赖）锁屋里了走备用（降级路径）进门；而不是没钥匙就永远进不了家（强依赖宕机即全站挂）。

原理展开：本项目的降级矩阵（面试可整表背出）：

| 依赖 | 缺席时 | 落点 |
|---|---|---|
| MinIO | local 文件存储 | StorageConfig 按配置装配 |
| RabbitMQ | 线程池异步审核 | AuditServiceImpl catch AmqpException |
| Redis | 全部走 DB | RedisOps.safe 门面 |
| VITS | Edge-TTS | VitsLifecycle 健康检测 |

设计要义：①**降级方向逐个选**（V3 词典"降级方向"详述——限流放行/鉴权拒绝/缓存走库，没有统一答案）；②**降级要可观测**（WARN 日志+traceId，"静默降级"=无人知道系统正在带伤运行）；③**降级路径也要被测试**（不然真降级时才发现备用路也是断的）。这一条是本项目"零依赖能启动"硬约束的完整表达。
→ 返回[主文档 0.3](V1.5工程基建详解.md#03-设计总原则可降级可插拔)

### 可插拔
**一句话：面向接口编程+按配置装配实现——换实现零改业务代码；与可降级是一体两面。**

比喻：灯座与灯泡的接口标准——换 LED（实现升级）不用动灯座（业务代码）；没灯泡时点蜡烛（降级）也照亮同一张桌子。

原理展开：本项目的可插拔家族（**面试串成线讲**）：

| 抽象 | 实现 A（默认） | 实现 B（升级） | 引入版本 |
|---|---|---|---|
| StorageService | local | MinIO（S3） | V1.5 |
| EmbeddingClient | local 哈希 | spring-ai 真实模型 | V2 |
| PayChannel | MOCK | 支付宝沙箱 | V3 |

设计模式本体：策略模式+工厂（Router/Config 按配置选实现）。价值：①新环境零依赖可跑（默认实现）；②升级是"加实现"不是"改代码"（开闭原则的实践版本）；③**抽象先于需求**——V1.5 立接口时未必知道 V3 的 PayChannel 会复用同一思想，但"现实约束逼出抽象"的方法论是可复制的。
→ 返回[主文档 0.3](V1.5工程基建详解.md#03-设计总原则可降级可插拔)

---

## 返回主文档

[V1.5工程基建详解.md](V1.5工程基建详解.md)
