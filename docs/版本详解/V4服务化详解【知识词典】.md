# V4 服务化详解【知识词典】

> 配套主文档：[V4服务化详解.md](V4服务化详解.md)（按章节学习用主文档，查概念用本词典）
> 与前几本词典（《AI模块详解》《V1.5工程基建详解》《V2内容社区与RAG详解》《V3支付与Redis详解》）不重复收录。
> 词条格式：**一句话核心 → 比喻 → 原理展开 → 代码对应 → 常见追问**。每个词条尾部可跳回主文档对应章节。

## 目录

- [一、架构概念](#一架构概念)：[单体架构](#单体架构) / [微服务](#微服务) / [服务拆分](#服务拆分) / [拆 AI 的五条理由](#拆-ai-的五条理由) / [为什么不全拆](#为什么不全拆) / [单体被妖魔化](#单体被妖魔化) / [数据归属](#数据归属) / [服务边界](#服务边界) / [康威定律](#康威定律) / [进程内调用 vs 网络调用](#进程内调用-vs-网络调用)
- [二、服务间通信](#二服务间通信)：[RPC](#rpc) / [声明式客户端](#声明式客户端) / [动态代理](#动态代理) / [@HttpExchange vs Feign](#httpexchange-vs-feign) / [超时策略](#超时策略) / [契约](#契约)
- [三、注册发现](#三注册发现)：[注册中心](#注册中心) / [服务发现](#服务发现) / [心跳](#心跳) / [负载均衡](#负载均衡) / [DiscoveryClient 抽象](#discoveryclient-抽象) / [SimpleDiscoveryClient](#simplediscoveryclient) / [Nacos](#nacos) / [多网卡陷阱](#多网卡陷阱) / [fail-fast](#fail-fast)
- [四、网关](#四网关)：[网关](#网关) / [路由断言过滤器](#路由断言过滤器) / [GlobalFilter](#globalfilter) / [横切关注点](#横切关注点) / [最小信息披露](#最小信息披露)
- [五、熔断与容错](#五熔断与容错)：[熔断器](#熔断器) / [三态机](#三态机) / [服务降级](#服务降级) / [级联故障](#级联故障) / [舱壁隔离](#舱壁隔离) / [滑动窗口](#滑动窗口) / [重试 vs 熔断](#重试-vs-熔断) / [雪崩三件套](#雪崩三件套)
- [六、安全与演进](#六安全与演进)：[HMAC 内部签名](#hmac-内部签名) / [防重放](#防重放) / [内网裸奔错觉](#内网裸奔错觉) / [策略模式](#策略模式) / [双注册陷阱](#双注册陷阱) / [微服务演进路线](#微服务演进路线) / [分布式事务为什么不做](#分布式事务为什么不做)

---

## 一、架构概念

### 单体架构
**一句话：全部功能在一个进程里——一次构建、一次部署、一个数据库。**

比喻：一家前厅后厨收银都在一个铺子的餐厅——喊一嗓子就传菜（进程内调用，纳秒级）、一本账本管所有钱（单库事务）、歇业一次全店升级（单次部署）。

原理展开：优点三件套：无网络开销、本地事务简单（一个 @Transactional 全搞定）、部署调试直接。痛点三件套：一个模块出事全进程陪葬、无法按模块独立扩缩、全部密钥/权限集中在一个进程（泄露面大）。**判断单体好不好不看流行度，看"最重的那个模块"是否拖垮其它**——本项目 AI 恰好是这种模块，所以拆它。
→ 返回[主文档 1.1](V4服务化详解.md#11-一家餐厅的单体时代)

### 微服务
**一句话：按业务边界拆成多个独立部署的小进程，通过网络协作的架构风格。**

比喻：中央厨房模式——重活（AI）外包独立建厂，餐厅前厅（业务）照旧；着火互不连累、扩产各自独立。

原理展开：拆的是**进程边界**，收获的是四种隔离：故障隔离（挂一个不塌全部）、伸缩隔离（按各自压力扩容）、安全隔离（密钥各归各）、演进隔离（换模型不动业务）。代价也真实：网络调用有开销有失败、跨进程排障要链路追踪、数据分家后一致性变难——**微服务是用开发复杂度换运维隔离性**，值不值取决于规模（所以本项目只拆一个）。
→ 返回[主文档 0.1](V4服务化详解.md#01-先别管定义看三个你熟悉的场景)

### 服务拆分
**一句话：把模块变成独立进程的动作——但"边界划在哪"比"拆出多少个"重要十倍。**

比喻：搬家不是把家具扔到街上就完了——谁住哪个房间（数据归属）、共用哪些设施（契约）、门牌怎么登记（注册发现）都要先想清楚。

原理展开：拆分判断三问（本项目宪法应用）：这个模块砍掉独立会怎样（耗时差异）？它挂了要不要连累别人（故障隔离）？它要不要独立换件（模型可替换）？——三条都"是"才值得拆。反面：按技术层拆（controller 一个服务/service 一个服务）是最常见的错误——边界应沿**业务能力**划，不沿技术分层划。
→ 返回[主文档 2.1](V4服务化详解.md#21-判断标准三问宪法在架构上的应用)

### 拆 AI 的五条理由
**一句话：耗时差异 / 故障隔离 / 独立扩容 / 密钥隔离 / 模型可替换——AI 占全了五条。**

比喻：餐厅里那位"做一道菜要半小时的大厨"——占了灶台（线程池）、他的状态决定全店生死（故障）、想给他配助手只能全店改编制（无法独立扩容）、他的秘方全店人都看得到（密钥暴露）——把他的厨房独立出去，四件事一次解决。

原理展开：逐条的业务现场：①耗时——AI 秒级 vs 普通接口毫秒级，200 个 Tomcat 线程会被 AI 请求占满；②故障——模型超时/限流/供应商挂，不能让下单不可用；③扩容——token 计费成本密集，要独立扩缩与限流；④密钥——API Key 只在 ai-service 存在；⑤替换——换 DeepSeek/GLM 只改一个服务。**面试要能逐条配"现场"**，背清单等于没背。
→ 返回[主文档 2.2](V4服务化详解.md#22-拆-ai-的五条理由面试逐条背)

### 为什么不全拆
**一句话：用户/商品/订单事务耦合紧密——拆开要引入分布式事务，当前业务量不划算。**

比喻：一家三口分三个房子住——做饭要打车送菜（网络开销）、交电费要三方对账（分布式事务）、忘带钥匙要跑三个小区（排障成本）——人口没到那个规模，分家纯属自找。

原理展开：具体到下单链路：同一事务里锁库存（t_product_sku）+ 建订单（t_order）+ 清购物车（t_cart）——拆开 = 要么 Seata（引入 TC 协调者，运维复杂度+性能损耗）、要么本地消息表最终一致（开发复杂度）——日订单量两位数的演示项目付不起。**"能讲出不做什么"是架构能力的更高证明**——跟风拆五个服务的简历，面试官一句"为什么拆"就现形。
→ 返回[主文档 2.4](V4服务化详解.md#24-反向论证面试加分项原话)

### 单体被妖魔化
**一句话：单体不是落后——大量成功产品长期跑单体；它只是在"模块属性差异巨大"时才开始吃亏。**

比喻：小家庭用一辆七座车完全合理——没必要因为 F1 车队换车了就给自家也配维修站。

原理展开：业界 2020 年代的声音（Martin Fowler 的 MonolithFirst 等）：**先单体、按痛点拆**——因为你最初猜不准边界在哪，过早拆分大概率划错线（改边界比写代码贵）。你的项目路线正是范本：V1~V3 单体快速闭环，V4 才因 AI 的五条理由拆一个。面试聊"你怎么看微服务"时，这个立场比"微服务先进"高一个段位。
→ 返回[主文档 1.3](V4服务化详解.md#13-反面拆得太碎的代价面试讲出来碾压背书者)

### 数据归属
**一句话：每张表（每份数据）必须只有一个服务拥有——别方只能通过它的接口访问。**

比喻：档案柜分部门——人事档案只有人事部能登记修改，其他部门要信息只能发函调档（接口），不能自己翻柜子（直连写库）。

原理展开：本项目的归属表：语料（t_knowledge_*）归 ai-service / 业务数据归 mall-app / API Key 归 ai-service 独占。关键认知：**归属是逻辑概念，先于物理**——当前两个服务连同一个 MySQL，但 mall-app 已不再直写语料表（改为调 /rag/index 接口）；将来语料搬独立库，业务代码零改动。反模式：多个服务读写同一张表（"共享数据库集成"）——表结构一改全员崩，耦合比单体还狠。
→ 返回[主文档 2.3](V4服务化详解.md#23-拆分的实质数据归谁比建模块重要十倍)

### 服务边界
**一句话：一个服务对外暴露的接口集合 = 它的边界；边界内的实现细节别人无权知道。**

比喻：奶茶店的点单窗口——顾客只知道"菜单+价格"（接口契约），后厨配方、供应商、设备（实现）一概不透明；换设备不影响顾客点单。

原理展开：边界设计的检验法：**把服务内部重写一遍，其它服务要不要改？** 不要=边界好。本项目：ai-service 内部换成 GLM、RAG 换算法，mall-app 零感知（只认 /internal/v1/ai/chat 契约）。边界也决定了"什么不该暴露"——/internal/** 前缀本身就是边界声明："这是给服务用的，不是给用户的"。
→ 返回[主文档 2.3](V4服务化详解.md#23-拆分的实质数据归谁比建模块重要十倍)

### 康威定律
**一句话：系统架构会长得像组织架构——沟通边界决定代码边界。**

比喻：两个团队之间改个需求要开三次会——于是系统自然在两团队之间裂出一条 API 缝；不是技术选的，是组织"选"的。

原理展开：对本项目的应用（一句话知道即可）：单人项目的"组织"就是你自己脑内的模块分工——所以按你脑中"AI 是独立技能域"的认知来划服务，天然贴合康威定律；反例是按简历关键词划服务（假装有五个团队）——架构与"组织"不符，维护时会持续别扭。
→ 返回[主文档第 2 章](V4服务化详解.md#第-2-章-拆分边界为什么只拆-ai为什么不全拆)

### 进程内调用 vs 网络调用
**一句话：方法调用纳秒级且不会"失败"；网络调用毫秒级且随时可能失败——这是单体与微服务一切差异的源头。**

比喻：同桌递纸条（进程内：快、必达）vs 发微信（网络：要等已读、可能没网、对方可能拉黑你）。

原理展开：网络调用的三个"不可能消失"：**延迟**（往返时间）、**失败**（超时/拒绝/网络分区）、**部分失败**（请求到了对端、响应没回来——你不知道它到底执行没执行）。所以服务间调用的代码必须永远处理：超时（等多久）、重试/熔断（失败怎么办）、幂等（重复/不确定怎么办）——V4 的第 3、4、7 章分别回应这三件事。**理解了这一条，微服务的所有组件就都有了存在的理由**。
→ 返回[主文档 3.1](V4服务化详解.md#31-零基础服务间调用就是你写过的-http-客户端)

---

## 二、服务间通信

### RPC
**一句话：Remote Procedure Call——像调本地方法一样调远程服务（泛指这个概念，HTTP+JSON 是它的一种实现）。**

比喻：打电话订位 vs 亲自跑一趟餐厅——效果一样（座位订上），体验完全不同；RPC 框架就是那部"电话机"。

原理展开：两个层次分清（面试常被绕晕）：**狭义 RPC**（Dubbo/gRPC）：自定义二进制协议，性能高，但客户端要配 SDK/IDL；**HTTP RPC**（REST + JSON / Spring 6 HTTP Interface）：通用文本协议，性能略低但零协议学习成本、任何语言可调。本项目选 HTTP：内网调用量小（QPS 两位数），性能差 1ms 无感，换来的通用性值钱。**选型逻辑又是"按当前量级，不按简历"**。
→ 返回[主文档 3.3](V4服务化详解.md#33-为什么选-httpexchange-而不是-openfeign选型论证)

### 声明式客户端
**一句话：只声明"接口长什么样"（注解），代理自动把方法调用变成 HTTP 请求——你写的是接口，不是 HTTP 代码。**

比喻：导航 App——你只输入目的地（方法名+参数），它负责规划路线/开车/停车（建连/序列化/发送/解析）；手挡开车（RestTemplate 裸写）当然也行，但每趟都重复全部动作。

原理展开：三件"自动"：URL 拼接（注解声明）、序列化/反序列化（框架按类型转）、**公共头注入**（本项目把 HMAC 签名头放 defaultRequest——每个调用点自动带，漏不掉）。价值：**把"怎么调"从每个调用点收敛到一个接口定义**——改动集中、安全语义一致。同类思路：MyBatis 的 Mapper 接口（XML 声明 SQL）、Spring Data JPA（方法名推导）——**"声明接口、框架实现"是 Spring 生态的通用范式**，你已经用第四次了。
→ 返回[主文档 3.2](V4服务化详解.md#32-声明式客户端像调用本地方法一样调远程)

### 动态代理
**一句话：运行时在内存里生成接口的"假实现"，拦截方法调用转成真实操作——声明式客户端的底层机制。**

比喻：代购——你把购物清单（接口方法）给代购（代理），他跑腿（HTTP/反射/SQL）把结果给你；你没有仓库没有车（无实现类源码），但体验像自己买了。

原理展开：链条：启动时扫描 @HttpExchange 接口 → HttpServiceProxyFactory.createClient() 用字节码技术（JDK 动态代理）生成实现 → 注入为 Bean → 调用方法时 InvocationHandler 拦截：读注解（方法→端点）、序列化参数、发请求、反序列化返回。**AOP 也是同一机制**（拦截加通知逻辑）——所以 @CircuitBreaker 自调用失效与声明式客户端"必须走接口"是同一个原理的两面。面试被问"Feign/@HttpExchange 原理"，答"动态代理+注解元数据"就到点上了。
→ 返回[主文档 3.2](V4服务化详解.md#32-声明式客户端像调用本地方法一样调远程)

### @HttpExchange vs Feign
**一句话：同一个编程模型（接口+注解+代理）的两个实现——Spring 官方内置 vs Netflix 系开源，前者零依赖后者生态深。**

比喻：手机自带的相机 App vs 专业相机 App——日常拍摄（本项目量级）自带款足够；要 RAW/连拍/外接镜头（注册中心深度整合/熔断注解一体的生态）再上专业款。

原理展开：对比表（选型必背）：依赖（零 vs spring-cloud-openfeign+BOM 对齐）/ 编程模型（几乎一样）/ 生态（RestClient 家族 vs SC 家族）。迁移成本：接口写法几乎一致，@FeignClient 替换 @HttpExchange 即可。**本项目的立场**：gateway 模块用了 Spring Cloud（Gateway 是 SC 家族）而服务间调用用原生 @HttpExchange——不是排斥 SC，是**每个依赖都要有当下成立的理由**。
→ 返回[主文档 3.3](V4服务化详解.md#33-为什么选-httpexchange-而不是-openfeign选型论证)

### 超时策略
**一句话：超时是"我愿意为这一步等多久"的预算分配——全链路统一超时等于有人在白白等死。**

比喻：等外卖——主干预算 40 分钟（AI 链路 120s），其中"商家出餐"（商品回调）只配等 5 分钟：商家超时直接取消，不能让骑手陪它耗完整点。

原理展开：两条链路两种预算（本项目实证）：mall-app→ai-service 连接 2s/读取 120s（AI 慢是常态）；ai-service→mall-app 连接 2s/读取 5s（毫秒级小事，5s 已是 50 倍冗余）。两个反模式：①不设超时（默认无限等——线程被无限占用）；②超时大于上游超时（下游还在忙，上游已经放弃了——纯浪费）。经验：**连接超时秒级、读超时按对端 P99 定**。
→ 返回[主文档 3.4](V4服务化详解.md#34-双向调用与超时两张不同的超时表)

### 契约
**一句话：服务间接口的约定（路径/方法/参数/返回结构）——契约稳定，双方独立演进；契约一改，双方都得动。**

比喻：插座标准——两孔三孔的形状尺寸（契约）定了，电器厂和发电厂各自随便升级内部，但谁也不能改插座形状。

原理展开：本项目的两份契约：mall-app 暴露 /internal/v1/products/search（精简字段省 token——契约按消费者裁剪），ai-service 暴露 /internal/v1/ai/chat 与 /rag/index。**为什么微服务不共享 DTO 代码库**：共享=一处改动全体重新编译部署（假独立）；各自持有同名契约类=真正的解耦（代价是两份要人肉同步——用 review 把关）。契约演进纪律：只加字段不删改（向后兼容），要破坏性变更就上新路径（/v2）。
→ 返回[主文档 2.3](V4服务化详解.md#23-拆分的实质数据归谁比建模块重要十倍)

---

## 三、注册发现

### 注册中心
**一句话：服务电话簿——服务上线登记地址、下线自动销号，调用方按名字查号。**

比喻：114 查号台+自动销号——新店开业报号码（注册），倒闭了查号台自动不再转接（心跳失效剔除）。

原理展开：三个动作：**注册**（服务启动把自己的 IP:port 写进注册中心）、**心跳**（每隔 5 秒报一次平安）、**查询**（调用方按服务名要实例列表）。没有它：多实例地址写死在调用方配置里，实例上下线要人工改配置重启——规模一大必错。主流实现：Nacos（阿里，注册+配置二合一）、Eureka（Netflix，停更）、Consul、K8s Service（云原生内建——**K8s 环境下常不需要额外注册中心**，知道即可）。
→ 返回[主文档 5.1](V4服务化详解.md#51-零基础没有注册中心的痛点)

### 服务发现
**一句话：从"服务名"解析到"可用实例列表"的动作——逻辑地址到物理地址的自动翻译。**

比喻：外卖平台搜"麦当劳"——不关心哪家店做（哪个实例），平台给你派一家能做的（负载均衡挑一个）。

原理展开：两种发现模式（面试常问）：**客户端发现**（调用方自己查表+自己挑——本项目模式，少一跳网络）vs **服务端发现**（经过中间层/网关代查——K8s Service/网关模式）。`lb://mall-app` 里就藏着一次客户端发现：Gateway 问 DiscoveryClient 要列表 → LoadBalancer 挑实例 → 用真实地址转发。服务名是**逻辑地址**（稳定），实例是**物理地址**（多变）——这对解耦是微服务弹性的地基。
→ 返回[主文档 5.2](V4服务化详解.md#52-lb-的完整含义拆开讲)

### 心跳
**一句话：服务定期向注册中心报平安（默认 5 秒一次），超时未报即剔除——注册中心的"活性检测"。**

比喻：值班打卡——保安每 5 分钟按一次铃；10 分钟没按，中控认为岗位空了。

原理展开：参数链（Nacos 默认）：心跳 5s / 不健康 15s（3 次没报标记） / 剔除 30s（6 次没报删除）。**心跳 ≠ 业务健康**：进程活着（心跳在）但数据库连不上/线程池满（业务已死）——所以生产还要配 actuator health 的**主动健康检查**。演示项目用心跳够了，知道差异即可——面试聊"心跳的局限"是加分点。
→ 返回[主文档 5.1](V4服务化详解.md#51-零基础没有注册中心的痛点)

### 负载均衡
**一句话：目标实例有多个时挑一个的算法——轮询/随机/权重/最少连接。**

比喻：银行叫号——多个窗口（实例）压力均衡地叫号（轮询），不让 1 号窗口排长队其它空着。

原理展开：Spring Cloud LoadBalancer 默认轮询（Round Robin）。**重要认知**：负载均衡的前提是**服务无状态**——任何请求打到任何实例结果都一样。本项目 mall-app 若开两个实例，用户 token 怎么共享？Sa-Token 存的是内存/Redis……（本项目单实例，问题不暴露；但面试被追问"多实例怎么办"：状态外置到 Redis——V3 的 Redis 基建恰好是这个答案的地基）。
→ 返回[主文档 5.2](V4服务化详解.md#52-lb-的完整含义拆开讲)

### DiscoveryClient 抽象
**一句话：Spring Cloud 的服务发现标准接口——getInstances(服务名)；Nacos/静态/Eureka 都只是它的实现。**

比喻：USB 接口标准——电脑（调用方）只认 USB 形状，对面是 U 盘还是硬盘还是手机（实现）随便换。

原理展开：本项目最讲究的设计：同一套 `lb://mall-app` 写法，实现可在 NacosDiscoveryClient 与 SimpleDiscoveryClient 间切换——**与 storage/embedding/pay 同族的可插拔哲学第四次出现**。面试价值：它证明你理解"抽象与实现分离"，而不是"会配 Nacos"。切换成本：换 starter + server-addr 两行，业务零改动——这套话术把 V1.5 以来的设计哲学串成一条线。
→ 返回[主文档 5.3](V4服务化详解.md#53-本项目的三层实现面试最有讲究的部分)

### SimpleDiscoveryClient
**一句话：Spring Cloud 自带的静态服务发现——实例列表写在 yml 里，零额外服务。**

比喻：贴在墙上的分机号表——小公司（三个服务）够用，不用雇前台（Nacos）；公司大了（实例动态增减）再雇。

原理展开：配置即列表：`spring.cloud.discovery.client.simple.instances.mall-app[0].uri=http://localhost:8080`。本项目的双保险用法：Nacos 主（真实注册发现体验）+ simple 兜底（Nacos 挂了 lb:// 仍可解析）——**注册中心自身故障不拖垮转发**。注意它是"死表"：实例上下线不会自动反映——所以只配兜底场景，不配生产主力。
→ 返回[主文档 5.3](V4服务化详解.md#53-本项目的三层实现面试最有讲究的部分)

### Nacos
**一句话：阿里的注册中心+配置中心二合一组件（Naming and Configuration Service）——国内微服务事实标准之一。**

比喻：物业前台——既管"谁家在几楼"（注册发现）又管"小区公告"（配置下发：改了配置推送到每家）。

原理展开：本项目只用发现（配置中心未用——配置量小，yml+环境变量够）。部署形态：docker standalone 单机模式（学习够；生产要集群 3 节点）。关键参数：`server-addr`（注册中心地址）、`fail-fast=false`（连不上不阻断启动——可降级）、`ip`（显式注册 IP——见[多网卡陷阱](#多网卡陷阱)）。控制台 ：8848/nacos 看服务列表——**验收时第一眼就看实例 IP 对不对**。
→ 返回[主文档 5.3](V4服务化详解.md#53-本项目的三层实现面试最有讲究的部分)

### 多网卡陷阱
**一句话：装了 VMware/VirtualBox 的 Windows，Nacos 自动选 IP 会选中虚拟网卡——注册上去的地址跨机器不可达。**

比喻：快递地址自动填充成"车库"（虚拟网卡）而不是"家门"（物理网卡）——快递员照地址送，永远送不到。

原理展开：Nacos 客户端选 IP 策略：第一块非回环网卡——Windows 上 VMware 的 192.168.49.x 常排最前。症状：控制台实例 IP 是 192.168.49.1（本机 VMware 网段），跨机器调用超时。修复：`nacos.discovery.ip=192.168.6.1` 显式指定。**元教训**：自动检测（IP/时区/编码/工作目录）在复杂环境是隐形炸弹——环境相关的东西生产值得显式声明。同类亲戚：JVM 的 user.dir、Docker 的时区（UTC vs CST）。
→ 返回[主文档 5.4](V4服务化详解.md#54-真实踩坑多网卡机器的注册-ip本机实测)

### fail-fast
**一句话：注册中心连不上时"快速失败退出"还是"打 WARN 继续跑"——本项目的选择是后者（可降级铁律）。**

比喻：打卡机坏了公司停摆吗？——考勤敏感的公司停（fail-fast=true），务实的公司先上班后补卡（false+自动重连）。

原理展开：fail-fast=true 的场景：**强依赖**注册中心的服务（每个请求都要查询发现）——早起快死好过带病运行。false 的场景：注册只是"锦上添花"（本项目：发现失败走 simple 静态兜底，转发不断）。配合机制：Nacos 客户端自动后台重连——恢复后自动补注册，全程无需人工。**每个中间件都要做这道选择题**，与 V3 的"Redis 挂了限流放行"同一思考框架。
→ 返回[主文档 5.3](V4服务化详解.md#53-本项目的三层实现面试最有讲究的部分)

---

## 四、网关

### 网关
**一句话：所有外部流量的统一入口——路由转发、统一安全、横切逻辑集中地。**

比喻：园区大门+保安+前台——一个门牌（外部只记一个地址）、进门先过保安（过滤/拦截）、前台统一接待转接（路由）、访客登记（traceId/日志）。

原理展开：三个存在理由（本项目全部兑现）：**一个入口**（前端只认 9000，后端随便拆）；**安全边界**（/internal/** 在门口拒绝——没网关时外部直连 8081 绕过一切设计）；**横切集中**（跨域/traceId/访问日志写一处，业务服务不重复）。主流实现：Spring Cloud Gateway（本项目，WebFlux 响应式）、Nginx（更底层，纯流量层）、Kong/APISIX（插件生态）。**网关自己必须高可用**——它是单点，挂了全站入口没了（生产多实例+keepalived）。
→ 返回[主文档 6.1](V4服务化详解.md#61-零基础为什么需要再前面一层)

### 路由断言过滤器
**一句话：Gateway 三组件——断言判断"归不归我"，路由决定"转发给谁"，过滤器负责"转发前后加工"。**

比喻：机场分拣系统——条码扫描判断目的地（断言），传送带决定去哪个登机口（路由），安检/贴标在前后加工（过滤器）。

原理展开：本项目的三条路由：/api/** 与 /files/** 断言命中→lb://mall-app（路由）+ traceId 头加工（过滤器）；/internal/** 断言命中→no://op+SetStatus=404（不转发，伪装不存在）。常见断言：Path/Method/Header/Query；常见过滤器：AddRequestHeader/SetStatus/StripPrefix/RewritePath。**理解三组件 = 会读任何 Gateway 配置**——面试给你一段路由 yml 能当场讲出行为。
→ 返回[主文档 6.2](V4服务化详解.md#62-路由表本项目的全部路由面试画这张图)

### GlobalFilter
**一句话：作用于所有路由的过滤器（区别于路由级 filter）——日志/traceId/鉴权这类"全局横切"写在这里。**

比喻：机场的防爆安检——不管你去哪个登机口都要过（全局），而"免税店折扣券"只对特定航班发（路由级）。

原理展开：本项目的 TraceIdGlobalFilter：@Order(-1) 保证最先执行；职责三件——生成/沿用 traceId、加到转发请求头、回写响应头+访问日志。写法是 WebFlux 的（返回 Mono/Flux）——网关是独立进程，技术栈与后端 Servlet 并存不冲突。**@Order 的意义**：过滤器有顺序，traceId 必须最先（后续过滤器也能拿到）——与 V1.5 TraceIdFilter "越早设置覆盖越全"同一逻辑。
→ 返回[主文档 6.3](V4服务化详解.md#63-traceidglobalfilterv15-伏笔的兑现代码精读)

### 横切关注点
**一句话：所有请求都要、但跟业务无关的逻辑（日志/鉴权/traceId/跨域/限流）——网关存在的最大理由之一。**

比喻：大楼的消防系统——每一层都要（横切），但不能让每家租户自己装（重复且不齐）；物业统一装（网关集中）。

原理展开：横切的演进史：V1.5 的 TraceIdFilter（单体层横切）→ V4 的 GlobalFilter（流量入口横切）——**服务越拆越多，横切越要上收**（写 20 个服务各自的日志过滤器是灾难）。判断"该放哪层"：与业务相关的下沉（Sa-Token 鉴权在 mall-app），与流量相关的上收（traceId/跨域/访问日志在网关）。这也是"网关鉴权要不要上移"讨论的本质：token 校验是纯流量逻辑（可上移），权限校验是业务逻辑（必须下沉）。
→ 返回[主文档 6.1](V4服务化详解.md#61-零基础为什么需要再前面一层)

### 最小信息披露
**一句话：拒绝请求时别透露"这里有什么"——404 优于 403，报错不回显内部细节。**

比喻：陌生人敲门问"你家保险柜密码对不对"——正确回答是"没有保险柜"（404），而不是"对是对的但你无权知道"（403，确认了保险柜存在）。

原理展开：本项目的两处应用：网关对 /internal/** 返回 404（不确认端点存在）；ai-service 验签失败统一回 "internal auth failed"（不区分"签名错了"还是"密钥不对"——不给攻击者校准信息）。与 V3 支付验签的 constant-time 比较同一思想族：**安全的每个细节都不要给对手免费信息**。
→ 返回[主文档 6.2](V4服务化详解.md#62-路由表本项目的全部路由面试画这张图)

---

## 五、熔断与容错

### 熔断器
**一句话：统计下游调用的失败率，超阈值就"跳闸"——一段时间内直接快速失败，不再发请求；过一会儿自动试探恢复。**

比喻：家里保险丝——电器短路（下游故障）时熔断，保住整条线路（本服务线程池）；换好电器（下游恢复）后合闸（半开试探）送电。

原理展开：解决的问题（数字推演）：ai-service 挂 → 每请求等 30s 超时 → Tomcat 200 线程全在等死 → 商城全瘫（[级联故障](#级联故障)）。跳闸后：请求毫秒级走 fallback，线程立刻释放。与三个近义词的分界（面试常混）：**超时**（单次请求的上限）、**重试**（失败再试，对端压力+1）、**熔断**（连续失败后的整体熔断，对端压力归零）——三者是互补不是替代。实现：Resilience4j（本项目，Spring 官方推荐）/ Sentinel（阿里，规则更丰富有控制台）/ Hystrix（Netflix，已停更，老面试题里还会出现）。
→ 返回[主文档 7.1-7.2](V4服务化详解.md#71-零基础家里的保险丝)

### 三态机
**一句话：熔断器的三个状态——CLOSED（正常+统计）/ OPEN（跳闸快速失败）/ HALF-OPEN（放行试探），自动往返。**

比喻： cauteloso 的老板——平时正常营业顺便记差评率（CLOSED）；差评率太高挂"歇业"牌（OPEN）；歇业三天后开门放三桌客人试试（HALF-OPEN）——好评就继续营业，差评继续歇。

原理展开：状态迁移条件（本项目配置）：CLOSED→OPEN：最近 10 次中失败率≥50%（且至少 5 次才开始统计——防冷启动误判）；OPEN→HALF-OPEN：等 30s（自动迁移）；HALF-OPEN→CLOSED：3 个试探全成功；HALF-OPEN→OPEN：任一失败。**全部自动**——跳闸与恢复无人值守，这是熔断区别于"人工摘除节点"的核心。面试画图题：把这张状态机画在白板上，标上本项目参数——瞬间从"背概念"变"真做过"。
→ 返回[主文档 7.2](V4服务化详解.md#72-三态机面试必背能画出来)

### 服务降级
**一句话：失败（或熔断）时返回"兜底结果"而不是报错——给用户一个可接受的退而求其次。**

比喻：餐厅招牌菜卖完——服务员推荐替代菜（降级），而不是把顾客轰走（异常抛给用户）。

原理展开：降级的三种返回形态（按业务选）：**友好文案**（本项目 AI："AI 助手在休息"）、**缓存旧值**（推荐系统挂了返回昨天榜单）、**功能收缩**（支付渠道挂了只留一个）。降级方法铁律：**必须稳如老狗**——绝不再调任何可能失败的东西（fallback 里再调远程=降了个寂寞）；本项目 fallback 只 return 一个字符串+log。**降级与熔断的配合**：熔断是"决策"（现在要不要放弃），降级是"放弃后给什么"。
→ 返回[主文档 7.3](V4服务化详解.md#73-代码接入两行注解-一个降级方法)

### 级联故障
**一句话：一个服务的故障沿调用链向上蔓延，最终拖垮全链路——微服务最经典的死法（服务雪崩）。**

比喻：多米诺——上游等下游、上上游等上游；第一张牌（ai-service）倒下，每层都在"等超时"中被拖倒。

原理展开：蔓延机制（本项目数字版）：ai-service 挂 → mall-app 请求堆积等超时 → mall-app 线程耗尽 → 网关请求 mall-app 也超时 → 网关连接堆积 → 全站不可用。**雪崩三件套**防线的分工：超时（限制单次等待）、舱壁（限制占用资源）、熔断（批量快速失败）——本项目用超时+熔断，舱壁知道原理（见下条）。面试问"微服务最大的风险"：答级联故障并讲出蔓延链，比答"分布式事务"更体现理解（事务是数据问题，雪崩是可用性问题——后者先死）。
→ 返回[主文档 7.1](V4服务化详解.md#71-零基础家里的保险丝)

### 舱壁隔离
**一句话：给每类调用划分独立资源池（线程/信号量），一类调用出事只淹自己的舱——船不沉。**

比喻：轮船水密舱——进水只进一个舱，整船不沉；没有水密舱的船，一处进水全船漫灌。

原理展开：Tomcat 线程池就是"无舱壁"的现场：200 个线程被 AI 调用占满，商品接口跟着完蛋。舱壁方案：给 AI 调用单独划 20 个线程的池（Hystrix 线程池隔离 / Resilience4j ThreadPoolBulkhead），AI 再慢最多淹自己的 20 个。本项目没做的诚实说明：AI 调用已经独立成服务+熔断兜底（隔离已由"进程级"实现了大半——拆分本身就是最粗粒度的舱壁），线程池舱壁留给将来 mall-app 内多类远程调用并存时。**知道"拆分即舱壁"这个视角，比会用 Bulkhead 更显架构理解**。
→ 返回[主文档 7.1](V4服务化详解.md#71-零基础家里的保险丝)

### 滑动窗口
**一句话：只统计"最近 N 次"的滚动区间——旧的失败会滚出窗口，不永久记仇。**

比喻：信用分看"最近 6 个月"的记录——五年前的逾期不该影响今天；窗口外的历史自动翻篇。

原理展开：与"计数窗口"（从启动累计）的差别：累计式会被历史失败污染（服务早挂过一次，之后健康运行也跳闸）；滑动式只看最近 10 次（本项目），恢复后失败滚出窗口、失败率下降、自动闭合。参数取舍：窗口大（100）→ 平滑但反应慢；窗口小（10）→ 灵敏但易被偶发抖动误跳——**minimum-number-of-calls=5 就是为防误跳的护栏**（样本太少不统计）。
→ 返回[主文档 7.2](V4服务化详解.md#72-三态机面试必背能画出来)

### 重试 vs 熔断
**一句话：重试是"再试一次"（适合偶发抖动，但会给故障中的对端加压）；熔断是"先别试"（保护双方）；方向相反，配合使用。**

比喻：对方手机占线——重试=再拨一次（可能只是占线）；连续 50 次占线还拨=骚扰（重试风暴）；熔断=停一小时再拨（给对方喘息）。

原理展开：选型判断：**偶发瞬时故障**（网络抖动、死锁超时）→ 重试（带退避：1s/2s/4s）；**持续性故障**（服务挂了）→ 重试只会雪上加霜（每次都占满超时时长），必须熔断。危险场景：**无脑重试×多层**——网关重试 3 次×mall-app 重试 3 次=对 ai-service 瞬间 9 倍流量（重试风暴）。本项目立场：熔断做在 mall-app→ai-service；重试不做（AI 生成类请求重试有幂等成本），知道分层重试的坑即可。
→ 返回[主文档 7.2](V4服务化详解.md#72-三态机面试必背能画出来)

### 雪崩三件套
**一句话：超时、舱壁、熔断——防级联故障的三层递进防线。**

比喻：防洪三道堤——超时是"每户沙袋"（单点保护）、舱壁是"分区堤坝"（资源隔离）、熔断是"泄洪闸"（整体熔断）。

原理展开：三者的层次关系：超时保护"单次请求"（最细粒度）→ 舱壁保护"本服务的资源池"（中粒度）→ 熔断保护"调用链整体"（最粗粒度）。每层解决上一层解决不了的问题：超时了线程还是被占了一段时间→舱壁限量；舱壁内的请求还是全失败→熔断直接不发。本项目实现：超时（RestClient 配置）+熔断（Resilience4j）+进程级舱壁（拆分本身）——**能讲清"三件套各挡哪一层"，容错面试题基本通关**。
→ 返回[主文档 7.1](V4服务化详解.md#71-零基础家里的保险丝)

---

## 六、安全与演进

### HMAC 内部签名
**一句话：用服务间共享密钥对"身份+时间戳"算 HMAC-SHA256——证明"这是自己人发的、没被改过"。**

比喻：公司内部的暗号——对上暗号（签名对）才开门；暗号只有自己人知道（共享密钥）。

原理展开：三件套请求头：X-Internal-Client（我是谁）+ X-Internal-Ts（何时）+ X-Internal-Sign（HMAC(client+ts, secret)）。与 V3 支付验签同算法家族第三次复用——差别只在输入（支付签参数、内部签身份时间）。**为什么不用 JWT**：JWT 要签名方持有私钥+验证方公钥（适合跨组织多方信任）；内部服务同属一个部署体，共享对称密钥更简单——**按信任结构选认证方案**，不是哪个更新用哪个。
→ 返回[主文档 4.2](V4服务化详解.md#42-协议三个头-一个时间窗)

### 防重放
**一句话：截获的合法请求被原样重发——签名全对但不是"现在"发的；对策=时间戳参与签名+校验时间窗。**

比喻：演唱会门票 photocopy——假票做得跟真票一样（签名对），但票根上有日期（时间戳），过期的票不放行。

原理展开：机制：ts 参与签名（改 ts 签名就失效）+ 服务端校验 |now-ts|≤5 分钟。为什么支付回调不用而内部调用要（本项目对比）：支付回调有幂等兜底（重复=已处理，无害）；AI 接口无幂等语义（重复=重复烧钱）——**同一威胁在不同语义下要不同强度的防御**。5 分钟窗口的权衡：太小误杀（时钟漂移），太大重放面大——两台自己机器之间 5 分钟绰绰有余。
→ 返回[主文档 4.3](V4服务化详解.md#43-为什么是-hmac-时间戳与-v3-支付验签对照着记)

### 内网裸奔错觉
**一句话："服务都在内网，内部接口不用鉴权"——错；内网是会变的假设（端口误映射/横向渗透/员工网络）。**

比喻：小区内部不用锁门？——物业换了、租客转租、快递员进出的那一天，"内部"就不再是只有自己人。

原理展开：真实风险路径：①docker -p 误把 8081 暴露公网；②跳板机被攻破后横向移动，内网接口全裸（本项目的 ai-service 裸奔=免费 AI 额度+语料泄露）；③办公室 wifi 被蹭。安全原则：**鉴权不建立在环境假设上**——这也是云原生零信任架构（Zero Trust）的核心思想，知道这个名词能接住追问。
→ 返回[主文档 4.1](V4服务化详解.md#41-先撞南墙内网就安全是错觉)

### 策略模式
**一句话：同一接口的多个实现按条件选用——ChatServiceImpl 构造器按配置在 local/remote 间二选一。**

比喻：出行方式策略——同是"去公司"（接口），雨天选地铁、晴天骑车（实现），出发前定一次（构造时定格）。

原理展开：本项目的落地姿势（**比 @Autowired+@Profile 更值得讲**）：显式构造器注入两个 @Qualifier 实现 + @Value 配置在构造时定格——运行期零判断（aiModeFlag 是 final），模式切换=重启。与项目可插拔家族的关系：策略模式是**机制**（怎么选实现），可插拔是**理念**（为什么要有多个实现）——storage/embedding/pay/AiService 四连击后，这就是你简历上的设计哲学主线。
→ 返回[主文档 8.1](V4服务化详解.md#81-接线chatserviceimpl-显式构造器评审修复后的最终形态)

### 双注册陷阱
**一句话：一个 Filter 同时被 @Component 扫描和 @Bean 注册——同名 Bean 冲突（Boot 3 默认禁止覆盖，启动失败）。**

比喻：同一个人在前台登记了两次名字不同的入住——系统检测到重名直接拒绝办手续。

原理展开：本项目真实事故：InternalAuthFilter 类上 @Component + WebConfig 里 @Bean 同名 → BeanDefinitionOverrideException。修复：去 @Component，改 FilterRegistrationBean 显式 new+注册——附带好处是能指定 URL 模式（只保护 /internal/*，@Component 的 Filter 会被 Boot 自动映射到 /* 全拦）。**通用法则**：Filter/Listener 这类"既可注解又可 Bean"的组件，**选一种注册方式**——要 URL 模式控制就选 FilterRegistrationBean。
→ 返回[主文档第 9 章](V4服务化详解.md#第-9-章-评审复盘本轮踩的坑真实学习素材)

### 微服务演进路线
**一句话：拆分→通信→发现→网关→熔断→链路追踪→服务网格——每一步都有"什么时候值得做"的规模门槛。**

比喻：孩子长大路线——先上学（拆分）、再学说话（通信）、记电话簿（发现）、配手机（网关）、学急救（熔断）……不是一生下来全副武装。

原理展开：本项目的位置（诚实讲法）：已完成前五步的最小版；**没做**链路追踪（SkyWalking/OTel——traceId 已透传，接入是锦上添花）、配置中心（Nacos Config——配置量小）、服务网格（Istio——把熔断/重试下沉到 sidecar，语言无关，超大规模才值得）。面试话术："我知道完整路线图每一步解决什么问题，也知道**当前规模下做到哪一步停**——过度的基建和缺失的基建一样有害。"
→ 返回[主文档 10.2](V4服务化详解.md#102-面试速查表30-秒一答)

### 分布式事务为什么不做
**一句话：因为没拆订单/商品/用户——本地事务还在，所以不需要 Seata/消息最终一致；这是"不拆"的直接收益。**

比喻：没分家的家庭不存在"跨家转账对账"问题——分了家才需要；我们的选择是"不值得为分家付对账成本"。

原理展开：万一将来要拆（面试必追问"以后订单量大了呢"）：三档预案——①本地消息表最终一致（V2 已实践过同款思想：审核流水+状态机；成本最低，首选）；②Seata AT 模式（自动补偿，性能损耗一档）；③TCC（业务侵入大，资金强一致才上）。**回答"没做"但"知道怎么做、什么条件下做"**——这是所有"没做"类问题的满分模板。
→ 返回[主文档 2.4](V4服务化详解.md#24-反向论证面试加分项原话)

---

## 返回主文档

[V4服务化详解.md](V4服务化详解.md)
