> 📖 **与主笔记的对照方式**：每条语句节上方都有锚点（#A1、#A2 … #G1），主笔记的语句卡片标题处有"深挖入口"链接直接跳转到这里；正文里出现的概念名词也可直接点主笔记中的内链跳来本节查证。本节内部每条 10 问（#### A1-1 ~ A1-10），阅读顺序即面试深挖顺序。
# 简历语句技术深挖笔记：每条 10 问（面试逐题过关）

> 配套《简历项目经历与面试准备.md》——那边是"挑菜+30秒答案"，这本是"每道菜的后厨全流程"。
> 用法：每条技术语句 = 一组 10 个扩展点。**别背，先读通再用自己的话复述**；每个扩展点末尾的"一句话口径"是面试当场能甩出去的浓缩答案。
> 覆盖范围：主文件 A~G 全部 30 条语句。已按 ★★★/★★ 顺序编排——**你标 ★★★ 的（A/C/D 大部分）要逐题读通，★★ 的至少读透前 5 问**。

---

## A. 数据库与事务域

<a id="A1"></a>
### A1. 行锁 CAS 扣库存

#### A1-1 一句话直觉再深化
扣库存的本质不是"减数字"，是**把"检查有货"和"扣减"合并成一次数据库原子操作**。SQL 层只有一条 UPDATE；"库存不足"不是 Java 判断出来的，是数据库让 UPDATE 返回 0 行判出来的。为什么这个语义天然正确：MySQL 的 UPDATE 对命中行会**先加行锁再修改**——并发下两条 UPDATE 到同一行，第二个**必须等第一个提交或回滚**才能执行，等它执行时 stock 已经被第一个改小了，`stock>=?` 不成立→0 行。
一句话口径：扣库存 = `UPDATE sku SET stock=stock-? WHERE id=? AND stock>=?`；"够不够"由数据库在行锁内裁决，0 行即失败。

#### A1-2 "先查后改"到底错在哪（TOCTOU 完整推演）
时间线：库存 1 件，线程 A、B 并发下单：
```
t1 A: SELECT stock → 1
t2 B: SELECT stock → 1        ← B 读到的是 A 改之前的旧值（A 还没改）
t3 A: UPDATE stock=0 提交
t4 B: UPDATE stock=0 提交      ← 两个订单扣了同一件库存 → 超卖
```
"检查"（SELECT）和"使用"（UPDATE）之间被插队——[TOCTOU](V2内容社区与RAG详解.md#4.1%20先撞南墙：狂点红心会发生什么)（Time-Of-Check-To-Time-Of-Use）。条件更新把"检查"挪进 UPDATE 的 WHERE，与修改同处一行锁内，**插队的窗口物理消失**。
一句话口径：两步逻辑之间永远有并发窗口；把判断焊进原子操作里窗口才消失。

#### A1-3 条件更新 vs 乐观锁（version）——本项目口径红线
- 条件更新（本方案）：`WHERE stock>=?`，比较的是**业务字段当前值**，失败=库存真不足，无需重试。
- 乐观锁（version）：`WHERE version=?`，比较的是**版本号**，失败=有别人改过，通常要重试读最新版本。
本项目 version 字段建表时留了但**从未参与比对**——扣库存失败是"真没货"，重试没意义，所以用业务条件而不用版本号。**面试不说"乐观锁扣库存"**（会被追问 version 细节，而你的现场是条件更新）。若被问"你了解乐观锁吗"——答：了解，扣库存用条件更新是刻意的，重试无意义；乐观锁更适合"更新基于旧值计算"的场景（如更新用户资料）。
一句话口径：条件更新=一次判定；乐观锁=版本判定+可能重试；扣库存选前者因为失败即终局。

#### A1-4 行锁的代价与热点问题（进阶必会）
`WHERE id=?` 命中的是**主键行锁**（InnoDB 锁粒度是索引记录）。痛点：爆款商品=单行热点，所有下单串行在该行的锁上——DB 层扣库存的吞吐上限约等于单行更新的 TPS（几千/秒级别已经吃紧）。这不是 bug 是特性：**行锁保证了正确性，代价是同一行的并发被串行化**。解法不是去掉锁，是把流量挡在 DB 之前（Redis 预减，见 B2）或用队列把下单请求削峰串行化（MQ）。
一句话口径：行锁的正确性由串行化换来；热点行的出路是"前置拦截 + 削峰"，不是绕开锁。

#### A1-5 减成负数怎么办
`stock>=?` 已经保证扣减后 ≥0（扣 N 件要求 stock≥N）。但库存**回补**（取消订单）时呢？`SET stock=stock+?` 上不封顶没问题。真正的坑是**回补与扣减并发**：取消订单回补 + 新订单扣减同时发生，顺序不影响最终一致性（加法和减法满足交换律）——只要每次减都有 `stock>=?` 守卫，库存永远不会负数。
一句话口径：守门员在扣减侧（>=?），回补侧只需原子 +N，不会出负数。

#### A1-6 返回 0 行时的幂等语义（易错）
同一条 UPDATE 在"库存真不足"和"请求重复到达"两种情况下都返回 0 行——**业务层必须能区分**。本项目：下单接口里扣库存 0 行=抛"库存不足"；超时取消/支付回调里 CAS 0 行=说明已被别人处理（静默或返回成功）。同一个 0 行，两种语义，**按调用场景选**——这是本项目反复出现的统一思路。
一句话口径：0 行只是信号，语义由调用方定：买=失败，回写=已被处理。

#### A1-7 为什么事务要包住"扣库存+建订单"
扣库存成功但建订单失败（比如订单号撞唯一键），库存就凭空少了——所以两件事必须在**同一个数据库事务**里：要么都成功（扣了库存+订单存在），要么都回滚（库存没扣+订单不存在）。@Transactional 保证的正是这个。注意：事务内**别做网络调用**（调 AI/发 MQ），长事务占连接，而且 MQ 不参与 DB 事务（见 C3-8）。
一句话口径：跨表一致性靠本地事务；事务内禁网络调用。

#### A1-8 MySQL 默认隔离级别下的行为（连环追问）
- 追问"在 RR（可重复读）下条件更新会不会有问题？"——答：`UPDATE ... WHERE stock>=?` 用的是**当前读**（加锁读，读最新已提交版本），不受 RR 快照读影响；即使事务开始后别人提交了扣减，当前读也看得到最新值。
- 追问"gap 锁/next-key 锁"——答：`WHERE id=?` 走主键等值查询，命中唯一索引记录，只加**记录锁**（record lock），无 gap 锁问题；若 WHERE 走普通索引范围，RR 下会有 gap 锁（知道名字即可）。
一句话口径：条件更新是当前读，天然读最新；唯一键等值只锁行不锁区间。

#### A1-9 面试连环追问脚本（自测用）
① 扣库存 SQL 怎么写？→ ② 为什么能防超卖？（行锁内判断）→ ③ 高并发热点怎么办？（前置 Redis）→ ④ Redis 和 DB 不一致怎么办？（最终一致+对账）→ ⑤ 事务范围？→ ⑥ 为什么不用乐观锁？（失败即终局无重试价值）。这六问全部答顺，A1 就算过关。
一句话口径：六问链路=SQL→原理→性能→一致性→事务→选型，每个都有一句现场。

#### A1-10 一句话口径（浓缩背诵版）
"库存扣减我用条件更新：`UPDATE ... WHERE stock>=?`，并发裁决交给数据库行锁内的原子判断，返回 0 行即失败。热点场景我在这层之前用 Redis Lua 预减挡流量，DB 只接少量真实请求。之所以不叫乐观锁——version 字段在我们项目里从未参与比对，扣减失败是终局不需要重试。"

---

<a id="A2"></a>
### A2. 唯一索引幂等

#### A2-1 为什么"直接插+撞键"优于"先查再插"
唯一索引把**幂等判断下沉到数据库物理层**：第二次 INSERT 根本执行不到数据页就抛 1062（Duplicate entry）。对比先查后插：两次 SELECT 都返回"没有"，两次 INSERT 都成功→重复数据入库。本质与 A1 相同：**检查+动作分两步必有并发窗口，唯一索引把拒绝焊进 INSERT 的物理操作**。
一句话口径：幂等最便宜的实现=让数据库替你拒绝第二次。

#### A2-2 DuplicateKeyException 的异常体系
它是 Spring 的 DataAccessException（运行时异常）家族成员，翻译自 MySQL 的 1062 错误码。**catch 它的位置**：必须是撞唯一键的那条 insert 语句外层；若 Service 方法上有 @Transactional，catch 后事务**还能正常提交**（运行时异常被 catch 就不会触发回滚）——所以"插关系表失败→静默→但计数已经 incr"的场景必须把 insert 和 incr 放同一个事务里才能原子。
一句话口径：catch DuplicateKey 不触发回滚的前提是你真的 catch 住了，且同事务内的其它写已保证一致。

#### A2-3 点赞幂等 vs 限购幂等：同一异常两种语义
| | 点赞 | 限量发售 |
|---|---|---|
| 撞键原因 | 已点过 | 已抢过 |
| catch 后 | **静默**（返回成功，前端红心已亮） | **抛业务异常**（"每人限购一次"） |
| 为什么 | 幂等=重复即已达成，报错反而体验差 | 重复=业务不允许，必须让用户知道 |
这是一个主动讲能加分的对比："同一个异常，我按业务语义决定是幂等静默还是业务失败——点赞是状态确认型，抢购是资格限制型。"
一句话口径：幂等静默 vs 业务失败，取决于"重复到达"是否被业务允许。

#### A2-4 唯一键的字段选择：什么不该进唯一键
`uk(note_id, user_id)` 两个业务键，不把 id 放进去——为什么：①id 本来就唯一，唯一键等于主键没意义；②业务唯一性是"同一用户对同一笔记一条记录"，要表达的是**业务含义**而不是行标识。再注意：唯一键列**不能有 null 语义歧义**——MySQL 唯一索引允许多个 NULL（NULL≠NULL），如果 user_id 可空，空值用户能插多条——所以唯一键列必须 NOT NULL。
一句话口径：唯一键表达业务唯一语义；列必须 NOT NULL（NULL 在唯一索引里不互斥）。

#### A2-5 与 A1 的关系：幂等家族图谱
本项目"幂等"共**四种做法**，面试要能一次说全：
1. **唯一索引**（点赞/收藏/限购/流水表）：物理拒绝重复插入；
2. **状态机条件更新（CAS）**（支付/订单/审核）：逻辑拒绝重复流转；
3. **一次性 token**（下单防双击）：DEL 原子消费；
4. **消费端回查**（消息只带 id，消费时回查最新态）：以最新状态判断该不该执行。

> ⚠️ **命名纪律**：简历上写的是「**幂等设计**」，**不要说"幂等四式"**——那是自造名词，面试官会追问"哪四式？谁定义的？"。直接说"我们用了四种手段做幂等，分别解决……"即可。

判别式：数据要"唯一存在"→唯一索引；状态要"只流转一次"→状态机 CAS。
一句话口径：**幂等设计我用了几种手段叠加**——建表时用索引物理兜底，流转时用 CAS 逻辑兜底。

#### A2-6 面试连环追问：插入失败但计数已加怎么办
场景：like 的 `try { insert; incr } catch (DuplicateKey) {}`——如果 insert 抛的是**别的异常**（如 DB 断连），incr 不会执行（同一事务异常即回滚）。真正要防的是反着写：先 incr 再 insert，insert 撞键被 catch 但 incr 已提交——所以**顺序必须是 insert 在前**，且同事务。追问"不加事务会怎样"→ 答：insert 成功、incr 前进程崩溃 → 计数与关系表不一致（永久漂移）；所以冗余计数必须与关系表操作同事务（这是 V2 设计里反复强调的）。

> **★ 2026-09-09 修缮更新（计数已改走 Redis 增量桶）**：V3 后期计数不再直写 DB（每次互动 3 次 DB 写的写放大），改为 `HINCRBY aimall:cnt:like {noteId} 1` 攒**增量**、60s 定时任务 Lua 原子取走后 `like_count += delta` 加法回写。事务故事升级为两层：关系表仍在 DB 事务里保"一人一条"；计数是 Redis 加法语义——**两边都是加法，天然可重试可对账**（存全量才有覆盖丢更新窗口）。insert 与 HINCRBY 的顺序仍要求"insert 在前"，撞键静默时 HINCRBY 不执行（同一 try 块）。Redis 挂了降级回 DB 直写（本节原方案自动成为降级路径——老代码没白写）。
一句话口径：先插关系再更计数，且必须在同一事务——顺序与原子性缺一不可；计数上 Redis 后靠"加法+对账"保最终一致。

#### A2-7 压力下唯一索引的性能（基本盘认知）
唯一索引比普通索引多一道**唯一性检查**，但检查走索引（O(logN)），并发插入同一唯一键时：第一个插入成功，后续的会**阻塞等前一个提交**（唯一性检查要看到最新），然后抛 1062——所以"抢同一资格"的并发 N 个请求：1 个成功，N-1 个排队后撞键失败，**天然不超卖也不重复**。这就是限量发售 uk 那层的真相。
一句话口径：唯一索引的并发语义=先到者胜、后到者撞键，数据库替你完成资格仲裁。

#### A2-8 取消点赞的 rows>0 判断（易漏细节）
取消是 DELETE，返回影响行数：0=本来就没点过（不减计数）；>0=真删了（减计数）。漏掉 rows 判断直接无条件 decr，会把计数减成负数（本没点过也减）。与扣库存的 0 行语义同族——**写操作的影响行数必须被消费**。
一句话口径：DELETE 的 rows 是"是否真的发生"的信号；没发生就不该有副作用。

#### A2-9 扩展：INSERT IGNORE / ON DUPLICATE KEY UPDATE 的区别
- `INSERT IGNORE`：撞键静默跳过（返回影响行数 0）——审核流水表用它（重复消息直接忽略，靠 rows 判断是否首次）；
- `ON DUPLICATE KEY UPDATE`：撞键则更新（upsert）——索引文档表用类似思路（同来源替换）；
- 纯 `INSERT + catch`：撞键抛异常由代码处理——点赞用它。
三者选型：**想拿"是否首次"的信号→INSERT IGNORE 或 catch；想幂等更新→upsert**。
一句话口径：撞键三种处置=忽略/更新/抛异常，按"要不要知道是重复"来选。

#### A2-10 一句话口径（浓缩背诵版）
"点赞收藏这些'一人一条'的关系，我用唯一索引 `uk(note_id,user_id)` 做幂等：直接 INSERT，撞键抛 DuplicateKeyException，catch 后当已点过静默返回。没有用先查后插——两个并发请求都会通过检查插出两条。同一招用在限量发售上就是一人一单，但那边撞键要报业务错误而不是静默。收尾注意顺序：先插关系表再原子更新计数，同一事务保一致。"

---

<a id="A3"></a>
### A3. 游标分页

#### A3-1 深分页的真相：OFFSET 在做什么
`LIMIT 100000, 20`：MySQL 先按 ORDER BY 排好序（可能用 filesort），然后**数 100000 行并丢弃**，只返回最后 20。复杂度 O(offset+size)——翻越深越慢，且这个慢**没有上限**（翻到百万页等于全表扫描+排序）。
一句话口径：OFFSET 是"数到第 N 条再跳过"，深度=开销。

#### A3-2 游标分页的 SQL 本质
`WHERE id < cursor ORDER BY id DESC LIMIT 20`：走主键索引二分定位到 cursor，从那里向下取 20 行——**翻页成本恒定 O(logN + 20)**，与页深无关。cursor 不是页码是"锚点"：客户端把上一页最后一条的 id 传回来。
一句话口径：游标=上一页的终点，下一页从这里继续；成本恒定。

#### A3-3 为什么必须有排序键配套（二元组的必要性推导）
按 `ORDER BY hot_score DESC, id DESC` 排序时：游标若只传 hot_score → `WHERE hot_score < 999` **漏掉所有 999 分的其它笔记**；游标只传 id（热度排序下 id 无序）→ 漏掉"热度更高但 id 更小"的。正确游标是二元组条件：
```sql
WHERE hot_score < 999 OR (hot_score = 999 AND id < 10537)
```
先比主排序键，**并列时用唯一键破平局**——通用规律：排序键 N 列，游标就是 N 元组，最后一列必须是唯一键。
一句话口径：会并列的排序键都要补唯一键做游标尾巴，否则翻页漏数据。

#### A3-4 漏数据的现场还原（面试演示）
第一页返回 hot=999 的 A（id 100）、B（id 99）；第二页如果只用 `hot_score < 999`，C（hot=999, id 98）**永远翻不到**。用户感受：明明有 3 条 999 分的，只看到 2 条，还以为是没数据。Feed 场景漏数据比重复数据更难发现（用户不记得看过啥）——所以排序字段变化时，前端必须同时带全游标字段（本项目 Notes.vue 把 cursorId + cursorHot 一起存）。
一句话口径：热度并列是常态，漏数据是隐性 bug，[二元组游标](V2内容社区与RAG详解.md#2.3%20二元组游标：本项目最容易讲错/写错的细节)是唯一解。

#### A3-5 翻页过程中数据变化会怎样（一致性话题）
[游标分页](V2内容社区与RAG详解.md#2.2%20游标分页：把"第几页"换成"从哪继续")对"翻页期间新数据插入"的表现：按 id DESC 翻，新插入的 id 更大，永远在第一页之前——**后续页不会因为新数据而错位**（这是游标优于 OFFSET 的另一面；OFFSET 翻页时新数据插入会让整页内容后移，出现重复/漏看）。按 hot 排序若有人点赞分数变了，可能轻微错位——可接受。
一句话口径：游标分页天然免疫"翻页时新增数据"的错位问题。

#### A3-6 游标的代价（主动说边界加分）
不能跳页：没有"第 N 页"的概念，只有"从锚点继续"——Feed 天然连续往下滑没问题；后台管理要跳到第 37 页 → 老老实实用 OFFSET。**两种方案并存是常态，按场景选**——能主动说出这条边界的候选人比只说"游标更好"的成熟。
一句话口径：Feed 用游标、后台表格用 offset，我按"要不要跳页"选。

#### A3-7 分页与 count(*) 的关系
游标解决取数，但总数（total）还得 count——`SELECT COUNT(*)` 大表也慢。本项目列表接口返回 total 用于前端"共 N 条"展示；若 total 不是硬需求，可省略或走近似统计（EXPLAIN 的行数估算）。面试被问"百万行分页+count 怎么办"→ 答：①业务上弱化 total 展示（无限流 UI 本来就不需要总数）；②count 走二级索引覆盖统计。
一句话口径：取数用游标、总数单独权衡——无限流的 UI 根本不需要精确 total。

#### A3-8 索引要怎么写才能支持游标 SQL
`WHERE hot_score <= ? ORDER BY hot_score DESC, id DESC`：最理想是 `(hot_score, id)` 联合索引——排序的两列都在索引里，避免 filesort，且范围定位走索引。若只有单列 hot_score 索引，MySQL 仍要 filesort 排序 id——**排序键与索引列不一致是分页慢的隐形元凶**（与 A4 呼应）。
一句话口径：游标 SQL 的性能依赖"排序键恰好是联合索引"。

#### A3-9 面试连环追问脚本
① LIMIT 百万行为什么慢 → ② 游标怎么写的 → ③ 为什么还要 id 尾巴 → ④ 数据一直在加会错位吗 → ⑤ 能跳页吗 → ⑥ 翻页和 count 怎么配合。六问通顺即过关。
一句话口径：六问=原理→写法→并列→动态数据→边界→工程配套。

#### A3-10 一句话口径（浓缩背诵版）
"Feed 流用游标分页替代 OFFSET：`WHERE id < cursor ORDER BY id DESC LIMIT 20` 走索引定位，翻多深成本都不变。按热度排的时候我用了二元组游标（hot_score,id）——因为热度会并列，只用 score 会漏掉同分笔记。代价是没法跳页，所以后台管理的分页我保留 OFFSET——按场景选型。"

---

<a id="A4"></a>
### A4. 索引设计

#### A4-1 联合索引与最左前缀（复习你的强项）
`(user_id, status, created_at)`：查询用得上它的条件是**从第一列起连续匹配**——`user_id` 可用、`user_id+status` 可用、`user_id+status+created_at` 可用；**跳过 user_id 直接用 status 则索引失效**。等价于你把三列拼成一个"复合字符串"做前缀匹配：`(1,2,'')`、`(1,2,3)` 都是它的前缀，`(2,3,...)` 不是。
一句话口径：联合索引=按声明顺序拼前缀，跳列即失效。

#### A4-2 列序的设计依据（为什么这个顺序）
列序规则：①**等值查询列放前**（user_id=? 高频且等值）；②范围列放后（created_at BETWEEN）——范围列之后的列**进不了索引**（B+树按前缀有序，范围是断点）；③区分度大的优先（可选）。本项目订单列表查询模式：查某用户订单 → 过滤状态 → 按时间排序 → `(user_id, status, created_at)` 让"过滤+排序"都在索引内完成，**排序不用 filesort**。
一句话口径：等值在前、范围在后、排序蹭索引——三句话定列序。

#### A4-3 覆盖索引（避免回表）
`(user_id, status, created_at)` 若查询只要这三列（如 SELECT user_id,status,created_at），索引树本身就有数据 → **无需回表**（extra 显示 Using index）。列表接口"只取列表需要的列"是对索引友好的写法——这解释了为什么业务要控制 SELECT 的列而不是 `SELECT *`。
一句话口径：让查询要的列尽量在索引里，省一次随机 IO 回表。

#### A4-4 索引失效的常见场景（八股+现场）
①**对列做函数/运算**：`WHERE DATE(created_at)='2026-01-01'` → 索引失效（改写成范围 `created_at >= ... AND < ...`）；②**隐式类型转换**：varchar 列 `WHERE phone=138...`（数字）→ 索引失效；③**前导通配**：`LIKE '%xx%'` 失效（`xx%` 可用）；④**OR 连接非索引列**；⑤**NOT IN / !=**（视情况）。本项目真实用到的反例：**笔记**搜索早期写法 `LIKE '%关键词%'` 走不了 B+ → 才引入全文索引（V2 的 [ngram](V2内容社区与RAG详解.md#3.2%20中文全文检索：ngram%20一站讲透)）。
⚠️ **注意**：**商品搜索至今仍是 `LIKE '%关键词%'`，即这条索引失效反例在项目里依然活着**。被问到"你自己的项目里有这个反例吗"，要诚实说"有，商品搜索就是，这是已知技术债，解法是 ES，触发条件是十万级或 P99>200ms"——**别把已修的笔记和未修的商品混成一个"我都优化好了"的故事，面试官一查代码就崩。**
一句话口径：函数、隐式转换、前导通配——索引三杀手，现场都有对应反例。

#### A4-5 全文索引 ngram（V2 专属细节，被问到中文搜索就靠它）
中文没有空格分词，MySQL 默认全文解析器按空格切词 → 中文整句算一个词 → 搜任何子串都 0 结果。解决：`FULLTEXT KEY (title,content) WITH PARSER ngram`——按 2 字一组切："主动降噪"→"主动|动降|降噪"。局限：**单字搜不到**（token 最小 2 字）、纯字面无语义（"续航"≠"电池耐用"——语义缺口交给 RAG）。
一句话口径：中文全文=ngram 二元切词；字面检索的天花板正是向量检索存在的理由。

#### A4-6 为什么不上 ES（低频但必被问）
**❌ 别说**："数据才 500 篇，上 ES 是装航空母舰"——规模借口，被问"那 50 万篇呢"就崩，且等于承认没见过规模。
**✅ 正确口径**："ES 的成本我清楚（部署/监控、MySQL→ES 双写一致性链路、运维复杂度），它的收益兑现在百万级文档、复杂聚合、高亮、拼音同义词 analyzer。**我的触发条件是 MySQL 全文检索 P99 > 200ms 或数据到十万级**；检索层已抽象（HybridRetriever），到点换实现业务不动。**是还没触发，不是判断它不够用。**"
⚠️ **这套话只适用于笔记检索**（那里有完整 ngram 方案）。**商品搜索的 LIKE 是"没做"，不能套这套话术**——被问到要诚实认这是技术债。
一句话口径：讲"触发条件 + 已铺退路"，别讲"数据少所以不用"。

#### A4-7 商品搜索为什么是 LIKE（诚实认债版）
商品搜索至今是 `LIKE CONCAT('%',kw,'%')` 匹配名称/副标题——**前缀通配符使 B+ 树索引失效，必然全表扫**。这是**已知技术债**：商品搜索需要的是**多维筛选**（关键词+分类+价格区间+品牌+销量排序+筛选面板聚合），`LIKE` 连"关键词命中"这一环都做不好，更别说其他。**正解是 ES**（倒排索引 + filter 上下文 + 聚合）。
触发条件：商品十万级 或 查询 P99 > 200ms。
一句话口径：**说"还没做成熟的搜索"，不说"数据少所以够用"**——前者是工程判断，后者是自贬。

#### A4-7 慢查询怎么排查（工程方法，加分）
①开慢查询日志（long_query_time=1s）；②`EXPLAIN` 看 type（ALL=全表扫描、range=范围、ref=等值索引、const=主键）、key（用没用到）、rows（扫了多少行）、Extra（Using filesort/Using temporary 是警报）；③针对性加/改索引并对比 rows 下降。本项目优化链路里用 EXPLAIN 对比过 `LIKE '%xx%'`（type=ALL）与全文检索（Using FULLTEXT scan）。
一句话口径：排查三板斧=慢日志→EXPLAIN→改索引对比 rows。

#### A4-8 冗余索引与维护成本
索引不是免费的：每次写要同步维护 B+ 树（写放大）、占磁盘。所以：①删掉冗余索引（`(a)` 与 `(a,b)` 同时存在时 `(a)` 冗余）；②别为低频查询加索引（查询一年跑一次 vs 写入天天付钱）。面试被问"索引越多越好？"——答：写多读少的表要克制，读多写少的表可以大方。
一句话口径：索引是读写之间的权衡——为高频读服务，向每次写收费。

#### A4-9 面试连环追问脚本
①联合索引列序怎么定 → ②最左前缀什么情况失效 → ③范围列放哪 → ④覆盖索引是什么 → ⑤哪些写法让索引失效 → ⑥怎么发现慢查询。六问即 A4 全貌。
一句话口径：定序→失效→回表→写法→排查，索引面试就这五块。

#### A4-10 一句话口径（浓缩背诵版）
"项目里的索引我按查询模式设计：等值列在前、范围列在后，让过滤和排序都落在联合索引内，比如订单列表 (user_id,status,created_at)。我知道最左前缀的失效条件，也踩过 LIKE '%xx%' 走不了索引——所以中文搜索用了 MySQL 的 ngram 全文索引而不是硬扛 B+。量级没到 ES 就先用 MySQL 全文，检索层保持可替换。"

---

<a id="A5"></a>
### A5. 状态机 CAS 状态流转

#### A5-1 状态机的本质：一张合法的"状态转移表"
订单不是"随便一个字符串字段"——它有合法转移：
```
PENDING_PAY → PAID / CANCELLED / CLOSED
PAID → SHIPPED → COMPLETED
PENDING_PAY → (支付回调) PAID
PENDING_PAY → (超时/手动) CANCELLED
```
**终态不可回退**（PAID 不会变回 PENDING_PAY）。状态机的好处：①非法转移在 SQL 层被拒绝（WHERE status=期望态）；②并发竞争由"谁能把状态从 X 改成 Y"裁决。
一句话口径：状态机=给状态的改变立法，CAS 是执法者。

#### A5-2 "支付 vs 超时取消"竞争（本项目最精彩的并发现场）
同一订单同时被支付回调（要 PENDING→PAID）和延迟消息（要 PENDING→CANCELLED）命中：
```
UPDATE t_order SET status='PAID' WHERE id=? AND status='PENDING_PAY'   -- A
UPDATE t_order SET status='CANCELLED' WHERE id=? AND status='PENDING_PAY' -- B
```
行锁保证二者串行：**谁先提交谁赢，后到的 WHERE status='PENDING_PAY' 找不到行→0 行**→输家不做任何副作用。这正是"闸门"的含义：PENDING_PAY 只放一个人过。
一句话口径：并发改同一状态=数据库行锁内先到先得，输家拿 0 行自动放弃。

#### A5-3 输家拿了 0 行之后做什么（每步都要对）
支付输给取消：支付回调 0 行 → 但钱**已经收了**（第三方扣款成功）→ 支付单保持 PAID（资金事实），订单是 CANCELLED（业务事实）→ 记 ERROR 日志+走退款兜底。**资金状态与业务状态分开记账、差异显式化**——这是支付系统"对账思维"的起点，主动讲很加分。
一句话口径：0 行≠无事发生——资金与业务分账，差异留给对账/退款处理。

#### A5-4 为什么 PAID 是终态不回退
用户付了钱，状态绝不能"退回待支付"（会造成"钱收了订单还能取消"的资损错觉）。所以支付状态机 PAYING→PAID/CLOSED，PAID 无出边。若发生"支付成功后要取消"——不是改回 PAYING，而是走**退款流程**（t_refund 表，V3 已建），订单与资金各自终态，靠退款记录关联。
一句话口径：错误的状态不修改，用新流程对冲——退款是终态世界的修正手段。

#### A5-5 状态机 vs 冗余字段（为什么不用 flag）
订单同时有"用户是否取消"和"是否支付成功"等布尔字段 → 组合状态爆炸（取消+已支付？= 矛盾态）。单一 status 字段+合法转移表让**矛盾态不存在**：支付成功那刻如果订单已 CANCELLED，状态机不允许 PAID，矛盾被结构排除，剩下的交给退款流程显式处理。
一句话口径：一个 status 胜过五个 flag——状态机让矛盾态在结构上不可能。

#### A5-6 状态机 CAS 在项目里的四处处刑（模式复用的面试价值）
①扣库存（stock>=?）；②支付单流转（WHERE status='PAYING'）；③订单流转（PENDING_PAY 闸门）；④审核回写（WHERE status='AUDITING'）。四类并发问题用同一招解决——**"一个模式吃遍天下"比会背四种框架更有说服力**。
一句话口径：并发状态类问题在我项目里全部收敛为同一条 SQL 模式。

#### A5-7 与唯一索引幂等的关系（A2 呼应）
- 唯一索引防**数据重复**（插两次）；状态机 CAS 防**流转重复**（改两次）。二者互补：审核链路先插流水（唯一索引防重复消息）再改笔记状态（CAS 防并发改）。
一句话口径：防"重复存在"用索引、防"重复流转"用 CAS——一个系统两招都要。

#### A5-8 状态机在代码里的落点（别用魔法值散落）
本项目：状态常量类（Order.STATUS_PENDING_PAY 等）+ Mapper XML 里的条件 UPDATE——状态名是字符串常量集中管理。演进方向：真正的状态机框架（Spring Statemachine）适合状态多、事件驱动强的场景，本项目四态两事件用常量+CAS 足够——知道何时不需要框架也是判断力。
一句话口径：状态少就常量+CAS，状态成网才上框架——按复杂度演进。

#### A5-9 面试连环追问脚本
①为什么状态流转要带 WHERE 旧状态 → ②支付和取消同时来怎么办 → ③输家怎么处理 → ④为什么 PAID 不回退 → ⑤这个模式项目里用了几处 → ⑥和唯一索引什么区别。六问闭环即过关。
一句话口径：闸门语义→输家补偿→终态哲学→模式复用→与索引分工。

#### A5-10 一句话口径（浓缩背诵版）
"项目的状态流转统一是条件更新：`UPDATE ... SET status='新' WHERE id=? AND status='旧'`。最典型是支付回调和超时取消抢同一个待支付订单——行锁先到先得，输家拿 0 行；如果取消赢了支付才到，钱已经收了，我就让支付单保持已支付（资金事实）、订单保持已取消（业务事实），差异走退款兜底并记日志。四个并发场景我都是这一个模式，所以叫它看家套路。"

---

<a id="A6"></a>
### A6. Flyway

#### A6-1 解决的问题：数据库结构的"真相漂移"
没有版本管理时：A 同事手工改了表加字段、B 不知道；新机器从 init.sql 建库却发现和线上库对不上。**[Flyway](V1.5工程基建详解.md#1.2%20Flyway%20怎么工作（5%20分钟原理）) 让数据库结构像代码一样有版本历史**：每次变更是一个带版本号的 SQL 文件，谁执行都一样，启动即校验。
一句话口径：结构变更从"口头/文档"变成"代码"，真相唯一在仓库。

#### A6-2 执行原理（启动时发生了什么）
①查 `flyway_schema_history`（无则自建）；②扫描 `classpath:db/migration/V*.sql`；③对比出未执行的迁移，**按版本号升序执行**；④成功后写 history（版本/描述/[checksum](V1.5工程基建详解.md#1.4%20迁移纪律（团队约定）)/耗时）；⑤全部通过才放行应用启动。V1__init.sql → V2__content_and_rag.sql → V3__pay_and_drop.sql →（V4 无迁移）……应用每次启动结构都与代码一致。
一句话口径：启动即"补齐没跑过的迁移"——代码与结构永不脱节。

#### A6-3 checksum 机制（为什么改历史文件会报错）
执行时对 SQL 文件内容算校验和存 history；下次启动重算比对，不一致 → `Migration checksum mismatch` 拒绝启动。意义：**历史不可篡改**——"生产库是从 V1 一步步走来的"这个事实必须可信。真想改表 → 新增 V4/V5 迁移，而不是改 V2 的文件。
一句话口径：checksum 是结构的"完整性校验"——改历史=破坏证据链，被拒是保护不是找茬。

#### A6-4 存量库接入：baseline-on-migrate（本项目真实决策）
项目已有 V1 的表（手工建的），首次上 Flyway：非空库+无 history → Flyway 拒绝（不知道库处于什么状态）。解法 `[baseline-on-migrate](V1.5工程基建详解.md#1.3%20本项目的接入：一个关键决策（面试高频）): true`：先打 baseline=0 记录"库从 0 开始由我接管"，然后 V1~V3 顺序执行。**关键配套**：V1__init.sql 全部 `CREATE TABLE IF NOT EXISTS`（幂等）——存量库重放无害、空库正常建表，新旧环境双兼容。
一句话口径：存量库=baseline 打桩+幂等首迁移，新旧库一把接管。

#### A6-5 迁移执行一半失败怎么办（生产事故预案）
DDL 大多不支持事务回滚（MySQL 的 DDL 是隐式提交），失败会留 `success=0` 记录，下次启动报 `Detected failed migration`。处置：**人工检查库现状 → 手动补齐/修复 → DELETE 掉失败记录** → 重启。纪律：迁移要小（一次一件事）、尽量幂等，降低失败面。
一句话口径：DDL 失败没有自动回滚——修好后清掉失败记录重启，迁移要小而幂等。

#### A6-6 迁移命名与团队纪律
格式 `V<版本>__<描述>.sql`（双下划线）；版本号全局递增不分支（分支合并会撞版本）；描述用过去式或名词（V4__add_refund_reason.sql）。CI 里跑 `mvn flyway:migrate` 或启动时自动——**任何环境（dev/test/prod）都用同一套迁移**，这就是环境一致性的来源。
一句话口径：一个版本号一个 SQL，全局递增、只增不改——团队的结构宪法。

#### A6-7 ★「结构」与「数据」两种载体的分工（本项目已收敛为一种）
**正确的心智模型**：**结构变更走迁移（Flyway，唯一权威）**，**种子/演示数据走独立的数据脚本**——两件事两种载体，别混。

本项目的真实演进：早期确实存在一份 `sql/init.sql`（自称"便利脚本"，兼建表 + 灌 mock 数据）。**但它滑后了**——只建出 8/24 张表，用了 Flyway 明令禁用的 `DROP TABLE IF EXISTS`（重跑即毁库），而且 `version` 字段注释写的是"乐观锁版本号"，与 Flyway V1 的"★预留字段，未用作乐观锁，防超卖靠行锁 CAS"**口径正好相反**。留着它等于在仓库里放一份既会毁数据、又会把面试口径带错的"权威建表脚本"，**已于 2026-09-19 删除**。

> 面试如果被翻旧账问"你以前有没有 init.sql" → **不要否认，直接讲这个演进**：*"早期有一份手工建表脚本，后来发现它跟 Flyway 迁移脱节了（只建了部分表、还用 DROP TABLE 不安全、注释口径也过时），我把它删了，结构来源收敛成 db/migration 一处。"* —— 这比"我一开始就做对了"可信得多，而且是**真实发生过**的。

一句话口径：**结构只有一处权威（迁移目录）；一旦发现"第二份建表脚本"，它的结局要么被删、要么被降级成纯数据脚本。**

#### A6-8 与 CI/CD 的配合（加分细节）
CI 里每个 PR 后跑一次迁移测试（连测试库 migrate+清库）——早发现 SQL 写错（表已存在/语法错）。本项目 CI 的 mvn test 纯内存不连库，迁移测试是可选增强；面试说"迁移应该在 CI 里被验证"能体现工程观。
一句话口径：迁移代码化后，CI 验证迁移=结构变更也有自动化门禁。

#### A6-9 面试连环追问脚本
①为什么需要它 → ②原理是什么（history 表） → ③改了已执行文件会怎样 → ④存量库怎么接 → ⑤跑一半失败呢 → ⑥和 Liquibase 区别。六问即 A6 全貌。
一句话口径：漂移→版本化→checksum→baseline→失败预案→竞品对比。

#### A6-10 一句话口径（浓缩背诵版）
"数据库结构我用 Flyway 管：每次变更是 `V{n}__描述.sql`，应用启动自动按版本号补齐没执行的迁移，跑完记录 checksum。规则是只增不改——改历史文件会触发校验失败。存量库我用 baseline-on-migrate 打桩接入，配套把 V1 迁移写成幂等（IF NOT EXISTS）让新旧环境都能跑。跑一半失败的迁移先人工修库再清失败记录重启。"

## B. Redis 域

<a id="B1"></a>
### B1. 缓存三兄弟

#### B1-1 为什么读多写少的场景值得加缓存（先算账）
商品详情读 100 次改 1 次：无缓存时 100 次全部打到 MySQL（每次 SQL 解析+索引+磁盘 IO 约 0.5~2ms）。加缓存后：99 次命中走 Redis（内存 GET 约 0.1ms，吞吐 10 万 QPS），MySQL 只接 1 次写和偶尔的读。**命中率 95% = MySQL 压力降为 1/20**——这是加缓存的全部 ROI 依据。
一句话口径：缓存是拿"一致性复杂度"换"读吞吐"——读多写少才划算。

#### B1-2 缓存三兄弟的区别一句话判型（防混淆）
- **穿透**：数据**根本不存在**（id=-1 的刷子），缓存没有可命中的东西；
- **击穿**：数据**存在但缓存刚过期**，同一瞬间大量请求同时去查库；
- **雪崩**：**大量 key 同一时刻过期**（或 Redis 整体宕机），DB 被打爆。
判型口诀：不存在=穿透、单点过期=击穿、成片过期/宕机=雪崩。
一句话口径：存在性、数量、时间三个维度各占一个兄弟。

#### B1-3 穿透：空值缓存为什么必须短 TTL
查不到也写标记（2 分钟）：挡住重复刷不存在的 id。TTL 为什么短：**商品可能下一秒上架**——[空值缓存](V3支付与Redis详解.md#3.3%20老大·穿透：查一个根本不存在的%20id) 30 分钟 = 上架后半小时内用户都搜不到（假阴性比穿透更伤体验）。2 分钟是"挡刷子"与"不脏读"的平衡。为什么用特殊串 `__NULL__` 而非 null：很多客户端不存 null，且"key 存在但值是标记"与"key 不存在"是两种可明确区分的状态（前者=确认不存在，后者=可以查库）。
一句话口径：空值缓存=T 短到不脏读、标记串到可区分。

#### B1-4 穿透进阶：布隆过滤器（知道原理即可）
位数组+k 个哈希：元素添加时把 k 个位置置 1；查询任一位置为 0 → **一定不存在**（直接拦）；全为 1 → 可能存在（放过查库）。优点省内存（不存元素本身）、缺点不能删、误判率随装载上升需重建。选型：恶意 id 占比极高的海量 key 场景才值得（百万级商品），本项目 100 商品空值缓存绰绰有余。
一句话口径：布隆=内存换"确定不存在"的判断，规模到了才上。

#### B1-5 击穿：互斥锁重建的三个必答细节
①锁必须带 TTL（[SET NX PX](V3支付与Redis详解.md#3.4%20老二·击穿：热点%20key%20过期的瞬间) 3 秒）：赢家查库途中崩溃，无 TTL 的锁永久卡死所有人（死锁）；TTL 到了锁自动释放，最坏退化成"无锁"（多几个人查库），不会错。②输家等待有上限：sleep 50ms 重读一次，仍读不到就**直接查库兜底**——不能让"等锁"变成新的故障点。③释放用 Lua 校验 value（防误删别人刚拿的锁，详见 B3）。
一句话口径：击穿对策=重建并发收敛为 1；输家等 50ms 后兜底查库，等待永不无限。

#### B1-6 击穿另一条路：逻辑过期（对比后二选一）
key 永不物理过期、值里带 expireAt；读到"[逻辑过期](V3支付与Redis详解.md#3.4%20老二·击穿：热点%20key%20过期的瞬间)"→ ①返回旧值（用户拿几秒前的数据）②后台异步重建。优点无锁无等待无尖刺；缺点短暂脏读+热点常驻内存。与互斥锁的选型：一般热点（QPS 几千）互斥锁；超热点（数万+）逻辑过期——**知道另一条路且能说出取舍**，比只会一条更显功底。
一句话口径：互斥锁有等待无脏读、逻辑过期无等待有脏读——按热点烈度选。

#### B1-7 雪崩：TTL 随机抖动的数学直觉
批量导入 100 个商品全给 TTL=1800s → 第 1800 秒整片失效 → 那一秒 DB 被打出尖刺。抖动：`TTL = 1800 + random(0,600)` 秒 → 失效点均匀散在 10 分钟窗口，每时刻只有 1/10 的量级失效——**零成本把峰值削平**。三兄弟里雪崩最便宜的一板，面试先答抖动再答高可用（哨兵/集群）。
一句话口径：抖动=把"同一秒"变成"10 分钟窗口"，成本一行代码收益巨大。

#### B1-8 穿透+击穿+雪崩的"防"之外：可降级门面（本项目独有答案）
三兄弟所有对策都依赖 Redis 活着——**Redis 挂了怎么办**？本项目把全部 Redis 调用收敛进 RedisOps.safe()：异常→WARN→兜底值（空/0/false）→业务自然走 DB。**挂了变慢、不挂掉**。这是"缓存是旁路不是数据源"的代码化——面试被问"Redis 挂了缓存策略全废？"时，别人答"加哨兵高可用"，你可以加一句"我在代码层就做了降级，Redis 缺席时缓存路径直接失效走 DB，这是设计内路径不是故障"。
一句话口径：缓存对策防的是"Redis 在时的问题"；降级门面防的是"Redis 不在时的问题"——两层都要。

#### B1-9 面试连环追问脚本（三兄弟是重灾区）
①三个分别是什么，举项目例子 → ②穿透空值为什么短 TTL → ③互斥锁几个细节（TTL/输家/释放） → ④逻辑过期对比 → ⑤雪崩三板斧 → ⑥Redis 挂了怎么办。六问链是 B1 完整防线。
一句话口径：判型→对策→细节→备选→雪崩→降级，一套六问背下来。

#### B1-10 一句话口径（浓缩背诵版）
"商品详情缓存我把三个经典问题都落了地：穿透用空值缓存（TTL 只给 2 分钟，怕商品上架后脏读）；击穿用 SET NX [互斥锁重建](V3支付与Redis详解.md#3.4%20老二·击穿：热点%20key%20过期的瞬间)（锁 3 秒防死锁、输家 50ms 后兜底查库）；雪崩用 TTL 随机抖动把失效点打散。缓存外面还包了一层可降级门面——Redis 挂了每个方法返回兜底值，业务自然走 DB，缓存绝不当可用性开关。"

<a id="B2"></a>
### B2. Lua 原子预减库存

#### B2-1 为什么 Redis 命令天然不原子（拆开看）
Redis 单线程执行**单条命令**原子，但业务往往是**多条命令**："读库存 → 判断够不够 → 扣减"是三个命令——单线程也救不了（三个命令之间能插入别人的命令）。Lua 的价值：把多条命令**包进一个脚本整体执行**，执行期间 Redis 不处理任何其它命令 → 复合操作的原子性。
一句话口径：单命令原子是 Redis 给的，复合操作原子是自己用 Lua 写的。

#### B2-2 项目预减 Lua 逐行解读
```lua
local stock = tonumber(redis.call('get', KEYS[1]) or '-1')  -- 取当前库存；键不存在给 -1
if stock < 0 then return -2 end                             -- 键不存在(未预热/Redis空)→ 降级信号
if stock < tonumber(ARGV[1]) then return -1 end             -- 库存不足
return redis.call('decrby', KEYS[1], ARGV[1])               -- 原子扣减，返回剩余
```
返回值语义（本项目约定，面试可画）：-2=未预热（降级走 DB）；-1=售罄；≥0=扣减后剩余。
一句话口径：一个脚本三态返回，Java 侧按返回值分路——原子判断+扣减+状态码一次往返。

#### B2-3 为什么不用 Redis 分布式锁包住扣减
锁方案：加锁→读库存→判断→扣减→解锁，每次 2 次网络往返+锁本身是串行点。Lua：一次往返、天然串行、无锁可超时无锁可误删。**锁适合保护"一长段业务逻辑"**（如跨多 key 的复杂事务）；**简单原子计数用 Lua**——本项目限量发售、限流、安全解锁三处都印证这个判断。
一句话口径：计数原子性 Lua 一发入魂，锁留给复杂临界区——按场景不是按档次。

#### B2-4 预减≠成交：失败的补偿链（P0 教训）
预减成功只代表"Redis 侧扣了名额"，后面还有 DB 层 CAS、限购 uk——任一失败，**Redis 已扣的必须 INCRBY 还回去**，否则每次失败白吃名额 → "看起来抢完了实际有货"= 用户侧资损。补偿写法：catch 里判断"我确实预减过"（布尔标记）再[对称回补](V3支付与Redis详解.md#9.5%20第二、三层与失败回补（评审揪出的%20P0）)。
一句话口径：预减是"预订资格"不是"成交"——失败路径必须对称回补，这是 P0 级事故换来的。

#### B2-5 补偿失败怎么办（最终一致的诚实边界）
回补那一下 Redis 恰好也挂了 → 名额漂移。兜底设计：①预热是 warmUpIfAbsent（只在无键时灌）不覆盖进行中的值；②活动结束后对账（Redis 剩余 vs DB 应余）人工/脚本校正。**这是最终一致不是强一致**——能主动说"我知道边界在哪、怎么补"的候选人极少。
一句话口径：双存储无事务=最终一致；补偿失败靠重灌与对账，不装强一致。

#### B2-6 预热与 TTL 的关系（进行中别重置库存）
活动开始前把库存灌进 Redis 并给 TTL（>活动时长）。进行中若键过期被 warmUp 重灌 → **以总量覆盖已售数量**（重置了！）。本项目 warmUpIfAbsent 只在"键不存在"时灌——进行中键还在就不动；若键真没了（TTL 到），宁可降级走 DB 也不重灌污染计数。面试被问"预热脚本怎么防覆盖"→ 答 IfAbsent 语义。
一句话口径：预热=无键才灌（IfAbsent），进行中重灌是资损级 bug。

#### B2-7 Redis 挂了整条链路怎么办（降级矩阵）
预减返回 null（Redis 不可用）→ 本层放弃，**直接交给 DB 层行锁 CAS 兜底**（正确性不丢，只是少了个流量盾）。这就是三层防护的意义：第 1 层是性能优化（挡流量），第 2、3 层是正确性兜底——**Redis 只是加速器，挂了功能退化不丢失**。
一句话口径：每层降级方向都预演过——预减挂了走 DB，功能完整只是少盾牌。

#### B2-8 Lua 的工程约束（写脚本的三个红线）
①**别在脚本里拼接 key**（用 KEYS[i] 参数传）——防注入且是 Redis Cluster 路由要求；②脚本要短——执行期间阻塞所有客户端，慢脚本=全局卡顿；③返回值类型要定（本项目全返回整数，避免转类型歧义）。面试被问"Lua 缺点"→ 答：阻塞性+无调试器+Cluster 下 key 必须同槽。
一句话口径：Lua 是原子性利器也是阻塞源——短、参数化、定返回。

#### B2-9 与支付/审核的呼应（可插拔家族）
限量发售"Redis 预减 + DB 兜底"与 V3 缓存的"Redis 加速 + DB 兜底"是同一个架构母题：**Redis 层永远不承诺正确性，正确性永远在 DB 层**。面试把这句话说出来，整个项目就串成了一条设计哲学。
一句话口径：Redis 层出性能，DB 层保正确——本项目所有 Redis 使用都遵这条。

#### B2-10 一句话口径（浓缩背诵版，2026-09-09 削峰重构后）
"限量发售是经典「Redis 预减 + MQ 异步下单削峰」：HTTP 线程只做 Lua 原子预减（判重+扣减同一脚本，一次网络往返）然后投递 MQ、毫秒级返回『已受理』，前端轮询 result 接口拿结果；DB 建单由消费者匀速完成——**预减挡的是'量'（准入闸门，放行消息数≤库存），MQ 摊的是'峰'（节奏器，DB 行锁不被万级请求争抢），两件事缺一不可**。消费端事务建单（行锁 CAS + uk 一人一单），失败三分流：撞 uk 幂等跳过、业务失败回补 Redis+写失败标记、未知异常进死信队列；秒杀订单同样挂 30 分钟超时取消。Redis 与 DB 是最终一致，消息丢失由定时补偿任务兜底。"

#### B2-11 削峰链路的接口形态（异步化的必然）
异步建单后 **buy 不可能返回订单号**（订单还不存在）——返回 `{status:"QUEUED"}`，前端每 1~2 秒轮询 `GET /drops/{id}/result`（QUEUED/SUCCESS/FAILED/NONE，SUCCESS 带 orderId）。这是秒杀类系统的标准交互，面试被问"为什么你的秒杀接口不返回订单号"就是送分题。判定优先级：DB 记录（真值）> 失败标记 > 受理标记（处理中）。
一句话口径：异步化的代价是接口形态改变——受理与结果分离，轮询收敛。

<a id="B3"></a>
### B3. 分布式锁

#### B3-1 SET NX PX 是什么（逐参数拆）
`SET lock:key value NX PX 3000`：NX=仅当 key 不存在才设置（互斥）；PX=毫秒过期（防持锁者崩溃死锁）；value=随机 UUID（标识"锁是我拿的"，释放时校验）。**一条命令**完成"检查不存在+写入+过期"——因为拆成 SETNX+EXPIRE 两步的话，两步之间崩溃 → key 永不过期=永久死锁（与限流 INCR+EXPIRE 同构的坑）。
一句话口径：加锁=一条命令三语义（NX 互斥/PX 防死/值标识持有者）；拆两步=死锁温床。

#### B3-2 为什么 value 必须校验才释放（锁误删推演）
A 拿锁（TTL 3s）→ A 业务卡了 5s → 锁自动过期，B 拿到锁 → A finally 里直接 `DEL key` → **删掉了 B 的锁** → C 又拿到锁 → A、B 同时持锁，互斥失效。所以释放前必须"校验 value 是我的"——但 GET 比对+DEL 仍是两步（中间锁可能易主），必须 Lua 原子：`if get==myValue then del end`。
一句话口径：释放比加锁容易错——校验+删除必须 Lua 原子，防删掉别人的锁。

#### B3-3 锁的 TTL 怎么定（业务超时 vs 锁过期的博弈）
TTL 给短：业务没跑完锁先没 → 并发进来（互斥失效，最坏多算一次）；TTL 给长：持锁者真崩了要等很久锁才释放。本项目缓存重建给 3 秒（业务<50ms，10 倍冗余）——**给足业务但别给到"崩溃恢复慢"**。进阶：Redisson [看门狗](V3支付与Redis详解【知识词典】.md#看门狗)（默认每 10s 续到 30s，业务线程活着就续）——"业务还没完锁先过期"的标准解法，知道名字和原理即可。
一句话口径：TTL 是"崩溃恢复时间"与"业务超时保护"的权衡；要自动续期上看门狗。

#### B3-4 分布式锁 vs 本地锁（synchronized 为什么不够）
synchronized/ReentrantLock 锁的是**单个 JVM 内**的锁——多实例部署时 A 实例的锁管不住 B 实例的并发。分布式锁把互斥状态放到所有实例共享的存储（Redis）上。面试被问"你这项目单实例为什么要分布式锁"→ 答：[缓存击穿](V3支付与Redis详解.md#3.4%20老二·击穿：热点%20key%20过期的瞬间)锁在单实例也能用本地锁，但写分布式锁的语义是为**多实例**准备的（V4 后 mall-app 可多实例），且 Redis 锁顺带解决跨实例问题——诚实说清单实例下本地锁够用，更显懂边界。
一句话口径：本地锁管单机、分布式锁管集群；本项目为可多实例提前用 Redis 锁。

#### B3-5 锁的"不可重入"问题（Redis 锁的已知短板）
SET NX 锁**不可重入**：同一线程拿锁后再进需要同锁的代码会死等自己（Redis 锁不记录持有线程）。要可重入需用 Redisson（内部计数器+线程记录）。本项目锁都是"一次性临界区"（重建缓存），不需要重入——**知道 Redis 原生锁不可重入**是被追问时的保命知识点。
一句话口径：原生 SET NX 不可重入；需要重入就上 Redisson 的 Lock（带计数器）。

#### B3-6 锁误删之外：时钟跳跃的影响
锁依赖过期时间（PX）——若 Redis 服务器时钟回拨，可能提前过期（锁失效窗口）。Redisson 看门狗基于实例时间也有类似边界。极端可靠要"红锁（RedLock）"或多节点投票——**知道名字+一句话**即可："我们业务上单 Redis 锁+看门狗足够，红锁的复杂度和它解决的问题在我们量级不匹配。"（又一次"不做 X 的理由"话术）。
一句话口径：时钟/过期边界真实存在；量级不到不背红锁的复杂度。

#### B3-7 加锁失败的处理（别死等）
tryLock 返回 null=没抢到（**是正常分支不是异常**）→ 本项目：输家 sleep 50ms 重读缓存，读不到直接查库兜底。反面教材：抢锁失败循环自旋重试=把 CPU 烧在空等上；阻塞等待（Redisson lock()）=可但要有超时。面试被问"没抢到锁怎么办"→ 答：等待有上限+兜底路径，绝不无界自旋。
一句话口径：抢锁失败是常态——等待有上限，兜底要存在。

#### B3-8 与 Lua 预减的分工（B2 呼应）
同样是"防并发"，什么时候用锁、什么时候用 Lua：**锁=保护一段多语句业务逻辑**（读A改B删C，期间不许别人动）；**Lua=保护一次原子计数**（库存减一）。本项目：扣减用 Lua（快），缓存重建用锁（临界区是"查库+回写"多步）——各归其位。
一句话口径：锁圈住代码块、Lua 焊住单操作——复杂度与需求匹配。

#### B3-9 面试连环追问脚本
①SET NX PX 各是什么 → ②为什么 value 用 UUID → ③为什么释放要 Lua → ④业务超时锁先过期怎么办 → ⑤单实例需要吗 → ⑥不可重入知道吗。六问是分布式锁全量考法。
一句话口径：加锁→标识→原子释放→续期→必要性→重入，六问闭环。

#### B3-10 一句话口径（浓缩背诵版）
"分布式锁我用来保护缓存重建这类临界区：SET key value NX PX 一条命令完成加锁（NX 保证互斥、PX 防持锁崩溃死锁、value 存 UUID 标识持有者）；释放时用 Lua 先校验 value 是自己的再删——直接 DEL 会在锁过期后误删别人刚拿的锁。业务可能超过锁 TTL 的边界我知道，真要自动续期就换 Redisson 看门狗；单 Redis 足够，没到需要红锁的量级。"

<a id="B4"></a>
### B4. 限流（固定窗口）

#### B4-1 为什么 AI 接口必须限流而普通接口可以松（差异化）
普通列表接口多查一次成本≈0；AI 接口**按 token 计费**（被刷一晚=真金白银）+ **秒级占用线程池**（刷接口把 Tomcat 线程占满，正常用户全被连坐）。限流不是炫技是**成本治理+防连坐**——判断"哪类接口要限流"看两个指标：单次成本、单次耗时。
一句话口径：限流只给"贵且慢"的接口——AI 恰好两个都占。

#### B4-2 固定窗口的直觉与代码
每分钟每用户 10 次：key=`rate:chat:{userId}`，INCR 计数，首次（n==1）设 EXPIRE 60s——窗口内计数>10 拒绝，窗口滑过自动清零。**必须 Lua 原子**：INCR+EXPIRE 两步在 EXPIRE 失败时 key 永不过期 → 该用户被永久限流——本项目真实踩过修过（评审洞 8）。
一句话口径：[固定窗口](V3支付与Redis详解.md#10.2%20实现：固定窗口%20+%20Lua%20原子化)=每窗一个计数器；INCR+EXPIRE 不 Lua 就有永久限流的洞。

#### B4-3 固定窗口的边界缺陷（知道才能答）
窗口切换瞬间最多放行 2 倍：第 59 秒打满 10 次 + 第 60 秒再打 10 次 = 2 秒内 20 次穿过。对"防刷/防成本失控"够用（用户不可能 2 秒问 20 个问题）；要严格上滑动窗口（zset 按时间戳精确统计）或令牌桶（允许攒额度突发，网关常用）。**能主动说出自己方案的缺陷**比堆高级算法可信。
一句话口径：固定窗口边界可 2 倍突刺——按"防刷够用"标准选型，缺陷说得出。

#### B4-4 限流的降级方向：fail-open 与 fail-close（哲学题）
Redis 挂了：放行（[fail-open](V3支付与Redis详解.md#2.3%20RedisOps%20门面：把"降级"收敛到一处（本章核心）)）还是拒绝（fail-close）？本项目放行——**限流是"省钱"，不是"保命"**：拒绝所有用户=自己制造全站故障，比多花点 token 钱严重得多。对照：鉴权挂了必须拒绝（fail-close，宁可不可用不能放陌生人）——**降级方向没有统一答案，按组件保护的是什么选**。
一句话口径：限流保护钱→挂了放行；鉴权保护安全→挂了拒绝——降级方向=被保护物的价值。

#### B4-5 限流粒度设计（为什么按用户）
key 含 userId：每用户 10 次/分。粒度选择：全局（防总量 DoS）/按用户（防单个滥用）/按 IP（防未登录刷子）——本项目 AI 接口要登录（Sa-Token），所以按用户最精确；登录接口本身可另加 IP 限流。面试被问"刷子换账号怎么办"→ 答：换账号成本低，生产会叠加设备指纹/风控——知道这个纵深即可。
一句话口径：粒度跟着身份走——有登录按用户，无登录按 IP，总量另设。

#### B4-6 限流后的用户体验（别把用户赶走）
拒绝信息要友好可行动："问得太频繁啦，休息一下再来～（每分钟 10 次）"——用户知道规则、知道多久恢复。配合前端：连续发送按钮节流（本地防抖）+ 429 后的倒计时提示。**限流的目的是让合理用户用得舒服，不是制造栅栏**。
一句话口径：被限的用户要"知道规则+知道何时恢复"，拒绝文案也是产品。

#### B4-7 限流与熔断/降级的分工（V4 呼应）
三者别混：限流=防"来太多"（前置闸门）；熔断=防"下游已挂我还打"（后置保险丝）；降级=失败后给什么（兜底内容）。本项目：AI 入口限流（防成本）+ 远程 AI 调用熔断（防拖线程池）——**限流管入口流量，熔断管出口故障**，各管一头。
一句话口径：入口限流、出口熔断、失败降级——三个闸门管三段，面试一次说清。

#### B4-8 固定窗口 vs 令牌桶 vs 滑动窗口选型表（背下来）
| 方案 | 实现 | 特点 | 场景 |
|---|---|---|---|
| 固定窗口 | INCR+EXPIRE | 简单、边界 2 倍突刺 | 防刷/成本控制（本项目） |
| 滑动窗口 | zset 时间戳/分片桶 | 精确、贵一点 | 严格要求无突刺 |
| 令牌桶 | 定时放令牌 | 允许受控突发、平滑 | 网关/允许峰值 |
一句话口径：简单够用选固定窗口，要严格选滑动，要平滑突发选令牌桶——本项目用量级换简单。

#### B4-9 面试连环追问脚本
①为什么 AI 要限流 → ②固定窗口实现 → ③为什么 Lua → ④缺陷是什么 → ⑤Redis 挂了怎么办 → ⑥和熔断区别。六问即 B4 全貌。
一句话口径：必要性→实现→原子性→缺陷→降级→分工，六问闭环。

#### B4-10 一句话口径（浓缩背诵版）
"AI 接口按 token 计费又占线程，我用 Redis 固定窗口给每用户每分钟 10 次的闸：INCR 计数、首次设过期，这两个动作必须 Lua 原子——我踩过 EXPIRE 失败导致 key 永不过期的坑，用户被永久限流。固定窗口的边界突刺我知道，但防刷场景够用。Redis 挂了我就放行而不是拒绝——限流是省钱不是保命，不能让成本组件变成可用性单点。"

<a id="B5"></a>
### B5. 缓存一致性（先更库再删缓存）

#### B5-1 问题本质：两个存储没有事务
DB 和 Redis 是两套系统，没有[分布式事务](V4服务化详解【知识词典】.md#分布式事务为什么不做)——"更新 DB + 更新缓存"不可能原子。所以目标不是强一致，是**把不一致窗口缩到业务可接受**，最终一致。方案必须回答：先更哪个、后动哪个、失败怎么办。
一句话口径：缓存一致性没有"都对"只有"窗口多小"——选窗口最小的顺序。

#### B5-2 为什么"先更库再删缓存"而不是"先更缓存"
时序陷阱：A 请求更新 DB 为价格 100，同时 B 读到缓存旧价 99 并**回写缓存**（读时重建）——A 的删除被 B 的重建覆盖，旧值复活。先删缓存再更库：删→更之间读者把**旧值**载入缓存=更糟。相对最优：先更 DB 再删缓存——不一致窗口只有"更库后、删缓存前"那一瞬（毫秒级），且删除是幂等的（删两次没坏处）。
一句话口径：删是幂等的、更是会被覆盖的；先库后缓存让窗口最小且可自愈。

#### B5-3 为什么"删"而不是"更"（更新缓存的并发陷阱）
A、B 并发写同一商品：A 先到库（价 100）后到缓存，B 后到库（价 90）**先**到缓存——缓存=90 覆盖了本来该是 100 的？不一定……真正的问题：到达顺序不可控，后到库的可能先到缓存，缓存最终值是"乱序"的。删除则不然：无论谁删，下次读都从 DB 取最新——**删除把顺序问题变成无顺序问题**。
一句话口径：更新缓存是写写竞争（乱序覆盖），删除缓存是幂等清场——删省心。

#### B5-4 删除失败怎么办（本项目与生产的差距）
删缓存那一下 Redis 挂了/超时 → DB 已是新值、缓存还是旧值，不一致**长期存在**（TTL 兜底前一直脏）。本项目量级：TTL 兜底（最坏脏到 TTL 过期）。生产三板斧：①重试（把删除消息投 MQ 异步重试）；②延迟双删（更库后先删、等几百毫秒再删一次，覆盖"删与更之间的读回写"窗口）；③订阅 binlog 的 Canal（把删除挂在 DB 变更事件上，最可靠）。**知道三档、说自己用了 TTL 兜底**，比假装全做了可信。
一句话口径：删失败=脏到 TTL；可靠度不够就上重试/双删/binlog——按量级选档。

#### B5-5 读时重建为什么能"自愈"
[Cache-Aside](V3支付与Redis详解.md#3.6%20缓存与%20DB%20的一致性：先更库再删缓存)（旁路缓存）的读路径：命中返回；未命中→查 DB→回写缓存→返回。删除缓存后**下一次读自动重建**——不需要任何"同步"动作，这就是"删"优于"更"的深层原因：系统具有自愈性。
一句话口径：Cache-Aside 的删除=下次读自愈，无需同步逻辑。

#### B5-6 什么场景不应该用缓存（边界）
写多读少、数据强一致要求（金额/库存外的核心账）、单条数据被频繁更新 → 缓存要么不划算（命中率低）要么是累赘（一直删）。**追求强一致就别用缓存**——本项目价格走 SQL、详情这种弱一致场景才缓存，这是"什么进缓存"的判断示范。
一句话口径：缓存只接待"读多写少+容忍秒级旧值"的数据——先判资格再谈方案。

#### B5-7 与"本地缓存"的两级差异（Caffeine 版图）
本项目 Redis 是分布式缓存（多实例共享）；面试常问的 Caffeine 本地缓存是 JVM 内（快但每实例一份、一致性更差）。两级缓存架构：Caffeine 挡超高热点、Redis 挡次级热点、DB 兜底——**但两级缓存的一致性难题翻倍**（两级都各有删除时机问题）。本项目没做本地缓存：单实例场景 Redis 已够，不背两级一致性的复杂度——按需演进话术再次使用。
一句话口径：本地缓存更快但多实例不一致；单实例量级 Redis 足够，两级缓存的复杂度留给将来。

#### B5-8 面试连环追问脚本
①为什么先库后缓存 → ②为什么删不更 → ③删除失败怎么办 → ④窗口多大可接受 → ⑤什么数据不该缓存 → ⑥终极方案知道吗（Canal/binlog）。六问是缓存一致性全量考法。
一句话口径：顺序→幂等→失败处理→容忍度→资格→终极，六问闭环。

#### B5-9 与三兄弟的关系（别把方案当敌人）
一致性方案（先库后删）处理"主动更新"路径；三兄弟对策处理"读路径"的异常形态；降级门面处理"Redis 不可用"——**一条缓存路径上三个问题域，面试分开答显得系统**。
一句话口径：缓存面试三分区=更新一致性/读异常形态/可用性降级。

#### B5-10 一句话口径（浓缩背诵版）
"缓存更新我遵守 Cache-Aside：先更新数据库、再删除缓存。删而不是更——更新缓存在并发写下有乱序覆盖的问题，删除是幂等的、下次读自动重建；先库后缓存把不一致窗口压到毫秒级。删除失败在我们量级靠 TTL 兜底，生产级的延迟双删和 binlog 订阅（Canal）我知道是演进方向。核心判断是：什么数据值得缓存——读多写少且容忍秒级旧值的才进。"

## C. RabbitMQ 域

<a id="C1"></a>
### C1. 异步解耦（AI 审核）

#### C1-1 同步审核的体验账（为什么必须异步）
AI 审核一次 3~10 秒。同步做法：用户点"发布"→ HTTP 请求挂 10 秒 → 前端转圈 → 返回结果。用户点第三次发布就卸载 App 了。异步：发布秒回（状态=审核中），审核在后台队列完成回写——**用户 RT 与审核耗时彻底解耦**，体验由架构保证而非祈祷 AI 快。
一句话口径：异步解耦让"慢动作"离开请求路径——发布体验不再被审核耗时绑架。

#### C1-2 MQ 比线程池强在哪（别答"线程池也行啊"）
同是异步：线程池是**进程内存**——应用重启任务全丢；MQ 是独立进程+磁盘持久化——重启不丢。MQ 还提供：削峰（队列缓冲瞬时流量）、消费速率独立控制（consumer 数可调）、消息生命周期管理（TTL/[死信](V3支付与Redis详解.md#8.3%20TTL%20+%20DLX：让消息"睡"30%20分钟)）。本项目保留线程池作为**降级**（MQ 不可用时体验一致）——主路径 MQ、降级线程池，两层都是异步。
一句话口径：线程池=内存异步（重启即丢）；MQ=持久化异步（可靠+可管）——降级用线程池是取舍不是替代。

#### C1-3 一条消息从发布到消费的完整旅程（画出来）
```
发布接口 → 事务提交(afterCommit) → RabbitTemplate.convertAndSend(exchange,rk,noteId)
  → Broker 收到 → 持久化到磁盘 → 回 ACK(confirm) 给生产者
  → 路由到队列 → 推给消费者(AuditConsumer)
  → 消费者处理(调AI) → 手动 ACK → 消息出队
```
任一环断掉消息就丢或重投——可靠性要逐环核对（见 C1-4）。
一句话口径：完整链路=发送确认+持久化+消费确认，三段任缺一段都会丢。

#### C1-4 消息不丢的三段防线（高频追问）
①**发送端**：[publisher-confirm](V2内容社区与RAG详解.md#9.1%20为什么这个设计"企业味"足：先算一笔体验账)（Broker 收到回 ACK，没回执=没收到，可补发）；②**Broker**：队列 durable + 消息持久化（Broker 重启不丢）；③**消费端**：手动 ACK（处理成功才确认，没确认的重投）。面试问"消息怎么保证不丢"——**按链路答三段**，只说一个词是背八股。
一句话口径：发送 confirm + 存储持久化 + 消费手动 ACK——丢消息问题永远要按链排查。

#### C1-5 重复消费为什么必然发生（at-least-once）
消费者处理完、发送 ACK 前进程崩溃 → Broker 没收到 ACK → 重启后消息**重投** → 同一审核跑两次。MQ 的投递语义是 at-least-once（至少一次）——**重复是协议特性不是 bug**，所以消费端必须幂等（C2 详述）。
一句话口径：至少一次语义下重复是常态——幂等不是可选项是入场券。

#### C1-6 消费速率控制：prefetch 是什么
[prefetch](V2内容社区与RAG详解.md#9.2%20MQ%20拓扑与三个决策)=消费者一次预取多少条未确认消息。不设：Broker 狂推消息进客户端内存，消息全"在途"失去调度。本项目 prefetch=5：审核是秒级慢任务，预取多了积压客户端。经验：快任务可几百、慢任务个位数——**按任务耗时定预取**。
一句话口径：prefetch 是消费侧背压——慢任务少预取，别让消息堆在自己内存。

#### C1-7 为什么消费端方法要幂等还要"消息只带 id"
消息体只有 noteId，消费时回查最新笔记——为什么不是把审核内容塞进消息：①消息在队列躺几秒，期间用户可能编辑了笔记——带 id 消费时读的是**最新版**；②消息体小省 Broker 内存；③敏感内容不进 Broker。这是"消息是通知不是数据载体"的设计原则。
一句话口径：消息带 id 不带内容——消费时回查最新，防旧数据+省内存+少暴露。

#### C1-8 事务边界里发消息的坑（V2 踩过、V3 修成 afterCommit）
发布是 @Transactional：方法内直接 convertAndSend → 事务随后回滚（比如落库某步失败）→ **消息已发出** → 消费者回查"幽灵笔记"（null 防御救一命）。正确：**事务提交成功后才发**——Spring 的 `TransactionSynchronizationManager.registerSynchronization([afterCommit](V3支付与Redis详解.md#8.6%20发送侧：afterCommit（评审修正的反模式）)→send)`；或干脆把发消息放 Controller 层（事务边界外）。MQ 不参与 DB 事务，这是分布式事务的第一课。
一句话口径：消息必须在 DB 事务提交后发——afterCommit 或放事务外，事务内发=幽灵消息。

#### C1-9 面试连环追问脚本
①为什么用 MQ 不用线程池 → ②消息怎么不丢（三段） → ③重复消费怎么办 → ④prefetch 是什么 → ⑤消息带 id 不带内容为什么 → ⑥事务里能发消息吗。六问是异步化全貌。
一句话口径：必要性→可靠性链→幂等→背压→最小载体→事务边界，六问闭环。

#### C1-10 一句话口径（浓缩背诵版）
"AI 审核 3~10 秒，如果同步，用户点发布要干等十秒。我走异步：发布接口秒回（状态=审核中），笔记进 RabbitMQ 由消费者后台审核回写。可靠性是三层：发送端 confirm、Broker 持久化、消费端处理完才 ACK——丢消息问题我永远按这三段排查。MQ 不可用时降级到线程池，体验一致——这是可降级设计。注意消息在事务提交后才发，否则回滚会产生幽灵消息。"

<a id="C2"></a>
### C2. 消费端幂等三防线

#### C2-1 消费端幂等和接口幂等的区别（为什么消费端更难）
接口幂等：同一 HTTP 请求重复（前端重发/网关重试），相对好控制。消费端幂等：**MQ 的 at-least-once 语义天然重复投递**——同一消息可能因为 ACK 丢失/重启重投 N 次，且你无法控制 Broker 的行为，只能在消费逻辑里兜。所以消费端幂等的设计标准是"消息来十次，效果等于来一次"。
一句话口径：消费端幂等是被动防御——重复不可控，只能让处理逻辑可控。

#### C2-2 本项目三防线（每道挡什么，画出来）
①**流水表唯一键**：`uk(biz_type, biz_id, biz_version)` + INSERT IGNORE——重复消息第二次插返回 0 行→直接跳过（挡"串行重复"=重投）；②**笔记状态机 CAS**：`WHERE status='AUDITING'`——并发/重复下只有一个能改成功（挡"并发重复"）；③**AI 失败只记 ERROR 流水不动笔记**——服务挂了笔记停在审核中等人处理（挡"误杀"，价值观防线）。
一句话口径：三防线=插键挡重复、CAS 挡并发、停着不杀保正确——每道挡一种失败。

#### C2-3 为什么要流水表（多一层表的理由）
有人问"直接改笔记状态不就行？"——①状态字段只记录"当前"，不记录"经历过什么"；②AI 判定要留痕（ai_result_json）供申诉翻案；③重复消息要能区分"处理过"与"从没处理过"——流水表同时是幂等键和审计日志。**概率性系统（AI 判定）必须留判定证据**，这是 AI 工程与确定性工程的差异。
一句话口径：流水表=幂等键+审计日志二合一——AI 判定要能翻案，证据必留。

#### C2-4 INSERT IGNORE 的返回行数语义（实现细节）
INSERT IGNORE 撞键不抛异常而是**影响行数=0**——正好用 `rows==0` 判断"重复，跳过"。对比：纯 INSERT 会抛 DuplicateKeyException 要 catch。IGNORE 的好处：把"重复"变成可读的返回码而不是异常流。审核消费者据此：首次插入（rows=1）才继续走审核；重复（rows=0）直接 ACK 出队。
一句话口径：INSERT IGNORE 把"撞键"编码成 rows=0——重复检测从异常变返回码。

#### C2-5 防线②为什么 CAS 不依赖①（并发场景）
两条重复消息**同时**到达：都做 INSERT IGNORE——其中一条 rows=1（抢到流水）、一条 rows=0（重复，跳过）——其实①已经处理了并发？还有漏网：消息重投发生在"流水已插、笔记状态还没改完"的瞬间？此时①已挡住。真正需要②的场景：**非消息路径也在改同一笔记**（如用户重新编辑触发的再次审核、人工复核）——两类写入者竞争同一状态，只有状态机 CAS 能仲裁。
一句话口径：①管消息内部重复、②管"所有写入者之间的竞争"——两类并发两条防线。

#### C2-6 防线③的价值观（宁可不作为不可乱作为）
AI 挂了/超时 → 如果 catch 后把笔记标记为"拒绝"→ **误杀正常用户**（体验伤害+流失）；如果标记为"通过"→ 违规内容漏放（平台风险）。都不对——正确是**保持 AUDITING 不动+记 ERROR 流水**，靠补偿任务/人工处理。审核系统宁可不作为，不可乱作为——这条价值观面试讲出来是加分项。
一句话口径：AI 失败的正确动作是"停住等人工"而不是猜一个结果——误杀伤害大于晚发布。

#### C2-7 毒消息与幂等的交叉（引 C4）
必然失败的消息（AI 服务连续挂）若重入队 → 无限循环打爆日志。本项目消费端吞异常不重入队（default-requeue-rejected=false）——失败可发现（ERROR 流水+死信）优于失败无限重试。幂等防"重复成功"，[毒消息](V2内容社区与RAG详解.md#9.3%20消费端：幂等三防线（本章核心）)防"重复失败"——一个系统两个问题都要处理。
一句话口径：幂等管"成功重复"、毒消息管"失败重复"——消费端设计两道题都要答。

#### C2-8 补偿视角：丢消息怎么办（幂等的另一半）
幂等解决了"多处理"，还有"没处理"——消息丢了（Broker 崩溃且未持久化/发送端没发出去）。补偿：ERROR 流水+定时扫描（发现长期 AUDITING 的笔记重新送审或标记人工）。**幂等管重复、对账管丢失**——与支付回调和[主动查单](V3支付与Redis详解.md#7.3%20回调会丢：主动查单对账)是同一思想。

> **★ 2026-09-09 修缮更新**：这条从设计意图变成已落地——`CompensationTask` 定时三合一（订单超时兜底/审核堆积重送/支付查单对账），全部复用各自的状态机 CAS，"扫 100 次=扫 1 次"。配套还有：publisher-confirm/returns 回调真正注册（此前配置开了没人消费=摆设）、取消队列补挂死信。面试可讲"我评审时发现配置与实现脱节，把'承诺过的兜底'逐条兑现"——这比背八股更能证明工程判断力。
一句话口径：重复靠幂等、丢失靠补偿——可靠消息的"不重不漏"要两个机制，且都要真跑起来。

#### C2-9 面试连环追问脚本
①重复消费为什么必然发生 → ②三防线各挡什么 → ③为什么用流水表 → ④AI 失败为什么不标记拒绝 → ⑤丢了怎么办。五问是消费端设计全貌。
一句话口径：语义根源→三防线分工→审计留痕→失败价值观→补偿兜底，五问闭环。

#### C2-10 一句话口径（浓缩背诵版）
"审核消费端我做了三层幂等：流水表唯一键加 INSERT IGNORE 挡重复投递，返回 0 行就跳过；笔记状态机 CAS 挡并发——包括非消息路径的写入者；AI 服务失败时我只记 ERROR 流水、把笔记留在审核中等人处理——宁可晚发布也不误杀正常用户。重复靠这三道防，消息丢失靠 ERROR 流水加补偿扫描兜，'不重不漏'两边都要有机制。"

<a id="C3"></a>
### C3. TTL+DLX 延迟消息

#### C3-1 延迟消息解决什么（数字推演）
用户下单不付款 → 库存被占 30 分钟 → 要自动关单回补。轮询方案：定时任务每分钟扫"超时单"→ 平均延迟 30 秒 + 周期性全表扫描（索引也挡不住量级上去的扫描）。延迟消息：订单创建 30 分钟后**恰好**收到一条"检查它"的消息——到期触发、零扫描。
一句话口径：延迟消息让"到点该干嘛"从被动扫变成主动推——事件驱动优于轮询。

#### C3-2 死信的两个用途（本项目两个版本各用一次）
死信（dead letter）不只收容失败消息——V2 审核用它装"处理失败的消息"（第一用途）；V3 延迟消息用它的**第二用途**：把消息投进"没消费者+设 TTL"的队列，到期成死信被重路由到真正的工作队列——**死信是"到期转运"的机制**。同一个 DLX 概念两个用法，面试能讲清这个区分很加分。
一句话口径：死信队列两副面孔——失败收容与到期转运，V2/V3 各用一次。

#### C3-3 拓扑逐参数解读（本项目配置）
延迟队列的声明参数：`x-message-ttl=1800000`（队列内消息统一 30 分钟过期）+ `x-dead-letter-exchange=业务交换机` + `x-dead-letter-routing-key=order.cancel.dlx`。流程：下单 afterCommit 发 orderId 到 delay 队列（**没有任何消费者**）→ 30 分钟 TTL 到期 → Broker 按死信参数把消息投给业务交换机 → 落到 cancel 队列 → 消费者处理。
一句话口径：延迟=把消息关进"没人的 TTL 仓库"，到点由死信路由放出来——零插件。

#### C3-4 队列级 TTL vs 消息级 TTL（本项目为什么队列级）
消息级（每条自带 expiration）：可差异化但**[队头阻塞](V3支付与Redis详解.md#8.4%20队头阻塞：为什么用队列级%20TTL)**——RabbitMQ 只检查队首是否过期，队首 60 分钟的消息挡着后面 1 分钟的消息，后面实际延迟 60 分钟。队列级（x-message-ttl 设在队列）：全队统一时钟、无队头问题。本项目所有订单统一 30 分钟 → 队列级天然免疫。
一句话口径：延迟统一用队列级 TTL；差异化延迟才需要消息级并接受队头阻塞。

#### C3-5 消费端回查的幂等（消息只带 orderId）
延迟消息体只有 orderId，消费时 `selectById` 回查——30 分钟里订单可能：已支付/手动取消/已过期——**消息到点不代表订单该取消**，必须消费时判断当前状态。状态机 CAS（WHERE status='PENDING_PAY'）保证：取消成功者才回补库存、关支付单；0 行=已被处理（静默）。
一句话口径：延迟消息只是"提醒该检查了"，结论永远以消费时查到的状态为准。

#### C3-6 三方竞争（支付/取消/超时消息）全景
同一订单可能同时被：用户手动取消（HTTP）、延迟消息到点（MQ）、支付回调（第三方）操作——三方都想动 PENDING_PAY。解法就一个：**状态机 CAS**，先到先得，输家 0 行退出。这是本项目状态机模式最精彩的应用现场（A5 的展开）。
一句话口径：[三方竞争](V3支付与Redis详解.md#8.5%20消费端：三方竞争同一订单)一个状态=数据库行锁内先到先得，模式一处通吃。

#### C3-7 取消成功者的动作顺序（回补别乱）
只有 CAS 成功（rows=1）的线程才做后续：回补库存 + 关支付单。顺序错=状态没改却加了库存（虚增）。回补本身要幂等（回补 SQL 无状态依赖，天然安全）。失败分支：CAS 失败直接 return，什么都不做——**赢家负责全部副作用，输家袖手旁观**。
一句话口径：副作用只跟赢家走——CAS 返回 1 才有资格动库存和支付单。

#### C3-8 afterCommit 发消息（与 C1-8 同坑不同解法）
V3 的订单创建在 @Transactional 里，延迟消息必须在**事务提交后**发——否则回滚产生"幽灵订单消息"（消费者 30 分钟后查到不存在的订单，null 防御救一命）。注册 TransactionSynchronization.afterCommit。面试问"事务和消息一致性怎么办"——afterCommit（轻量）/本地消息表（强一致）两档答案。
一句话口径：延迟消息同样踩"事务内发消息"的坑——统一 afterCommit 解决。

#### C3-9 面试连环追问脚本
①为什么不用定时任务 → ②死信机制讲一下 → ③队头阻塞是什么 → ④为什么消息只带 id → ⑤消息到点订单一定取消吗 → ⑥事务里怎么发。六问是延迟消息全貌。
一句话口径：选型→死信机制→TTL 层级→回查语义→CAS 竞争→事务边界，六问闭环。

#### C3-10 一句话口径（浓缩背诵版）
"订单超时自动取消我用 RabbitMQ 的 TTL 加死信队列实现：下单后把 orderId 发进一个没有消费者的延迟队列，消息 TTL 30 分钟，到期后按死信配置自动路由到真正被消费的队列。用队列级 TTL 是因为统一超时，能避开消息级 TTL 的队头阻塞。消费端只收到一个 orderId，必须回查订单当前状态——30 分钟里可能已经支付或手动取消，用状态机 CAS 保证只有真的还待支付的才会被取消，取消成功者才回补库存。发送在事务提交后做，防止幽灵消息。"

<a id="C4"></a>
### C4. 毒消息处理

#### C4-1 毒消息的完整循环（默认配置下的灾难）
Spring AMQP 默认：消费抛异常 → 消息**重回队列** → 再次投递 → 再抛异常……循环往复：日志爆炸、CPU 空转、队列永远清不掉。默认 `default-requeue-rejected=true` 的行为在消息"必然失败"时是灾难。
一句话口径：默认重入队=必然失败消息的死循环——日志打爆、队列卡死。

#### C4-2 为什么吞异常而不是标记重试（两类失败的区分）
**瞬时失败**（网络抖动、死锁超时）值得重试（延迟重投几次可能就成功）；**确定性失败**（AI 服务连续挂、消息格式错、业务规则不允许）重试一万次也不会成功——只会把压力反复打到已挂的下游。判别式：失败原因会自己消失吗？会→重试；不会→别试。
一句话口径：瞬时失败重试有意义、确定性失败重试是自残——先判类型再定策略。

#### C4-3 本项目的不重试取舍（为什么敢）
审核链路：吞异常不重入队 → 消息 ACK 出队 → 失败证据留在 ERROR 流水 → 补偿任务扫描兜底。**"失败可被发现"优于"失败被无限重试"**——把失败交给日志+流水+人工/补偿，而不是让消费者空转。
一句话口径：吞掉不是掩盖——是转交：ACK 出队 + ERROR 流水 + 补偿兜底三件套。

#### C4-4 什么消息值得进死信队列（DLQ 的正确用法）
想让"失败"不止于日志：消费重试耗尽（如重试 3 次仍败）→ 显式 `basicReject(requeue=false)` 或 channelNack → 进 DLQ。DLQ 的意义：**失败有去处、可被监控**（DLQ 积压=告警触发器）、可人工/脚本重放。本项目审核的 DLQ 设计保留该通道（虽默认不重试，架构上给死信留了口子）。
一句话口径：DLQ 是失败的"收容所"——不丢不循环，积压即是告警信号。

#### C4-5 幂等与毒消息的分工（再次强调）
幂等处理"重复成功"，毒消息处理"重复失败"——一个消费端两个问题都答：处理逻辑幂等（C2）、失败逻辑不循环（本条）。面试把二者并列讲，体现的不只是背概念而是系统设计。
一句话口径：成功路径防重复=幂等；失败路径防循环=毒消息策略——两端都设计才是完整消费端。

#### C4-6 与熔断的关系（失败处理哲学贯通）
MQ 消费端"确定性失败不重试"与 V4 熔断"持续失败快速降级"是同一哲学：**不把请求打到明知会失败的资源上**。消费端视角没有熔断器时，"不重入队"就是最朴素的熔断。V2 到 V4 的失败处理思路一脉相承。
一句话口径：从 MQ 不重试到微服务熔断——"失败要有止损"是贯穿项目的价值观。

#### C4-7 补偿任务的设计（真正闭环）
吞掉的消息靠什么补：ERROR 流水 + 定时任务扫描（重试/人工处理）。补偿任务本身要幂等（重复扫描无副作用）、要有限制（每笔记最多补偿 N 次防死循环）。生产形如"死信队列 + 定时 re-drive"。面试答"消息失败后呢"，能接出补偿两个字就算过关。
一句话口径：吞消息 ≠ 放弃消息——ERROR 流水+补偿扫描让失败可恢复可追溯。

#### C4-8 面试连环追问脚本
①毒消息循环怎么产生 → ②为什么不重试 → ③吞了不影响可靠性吗 → ④重试耗尽进 DLQ 怎么做 → ⑤和幂等区别。五问是毒消息全貌。
一句话口径：循环成因→失败分类→转交设计→DLQ→与幂等分工，五问闭环。

#### C4-9 消费端设计完整清单（串联 C1-C4 的收尾）
一个"能上线"的消费者要有：①处理逻辑幂等（唯一键/CAS）；②消息带 id 回查最新；③事务边界正确（afterCommit）；④失败分类（瞬时重试/确定吞）；⑤失败去向（DLQ/流水/补偿）；⑥prefetch 背压。六条全有才是成熟消费端——面试直接照此清单自检。
一句话口径：消费端六件套=幂等/最小载体/事务/失败分类/去向/背压——背下即设计。

#### C4-10 一句话口径（浓缩背诵版）
"消费端我防毒消息：审核这种确定性失败（AI 挂了）重试一万次也没用，所以不重入队——异常被捕获、消息正常 ACK 出队，失败记录进 ERROR 流水，由补偿兜底。我区分两类失败：网络抖动这类瞬时失败值得延迟重试，服务挂这类确定性失败就该止损。默认配置是失败重回队列，那对必然失败的消息就是死循环——日志爆炸队列卡死，所以我显式关了 requeue，用'失败可被发现'替代'失败被无限重试'。"

## D. Spring 与工程域

<a id="D1"></a>
### D1. Spring 事务与 AOP 代理

#### D1-1 事务是怎么生效的（三层拆解）
①`@Transactional` 是 Spring AOP 的**切面通知**（around 增强）；②Spring 用[动态代理](V4服务化详解.md#3.2%20声明式客户端：像调用本地方法一样调远程)包一层目标对象——代理先开启事务→调真实方法→按结果提交/回滚；③注入到其它 Bean 里的其实是**代理对象**。所以"事务逻辑"活在代理里，不活在真实对象里——这一条是后面所有坑的根源。
一句话口径：事务=代理开、真实跑、代理收——一切自调用问题的根源都在"绕过了代理"。

#### D1-2 自调用为什么失效（this 调用的真相）
```java
class A {
  @Transactional public void outer() { this.inner(); }  // this 是真实对象！
  @Transactional public void inner() { ... }
}
```
`outer()` 内部 `this.inner()` 调的是**真实对象的方法**——不是注入进来的代理。事务逻辑（开事务/提交）在代理里，真实对象的 inner() 没有事务装饰 → inner 的注解白写。诊断线索：任何"方法加了 @Transactional 却不管用"先怀疑同类自调用。
一句话口径：this 是真实对象、注入的是代理——自调用绕代理，注解即摆设。

#### D1-3 为什么 @Configuration 的自调用又不失效（你研究过的深水区）
@Configuration 的 @Bean 方法自调用（一个 @Bean 调另一个 @Bean 方法）能拿到**代理后的单例**——因为 @Configuration 类是**容器级 CGLIB 增强**（非 AOP）：类被 CGLIB 子类化，方法内 this 指向增强子类，子类方法里 invokeSuper 之前会先查容器（保证单例+后处理器）。**与 AOP 代理是两个子系统**：AOP 是"方法调用加装饰"（面向业务 Bean），@Configuration 增强是"保证容器语义"（面向配置类）。能讲清这个区别的候选人极少——这是你比大多数人有优势的题。
一句话口径：@Transactional 走 Spring AOP 代理、@Configuration 走容器级 CGLIB——同是"自调用"，两个子系统两套结论。

#### D1-4 事务回滚规则（默认只滚 RuntimeException）
默认 `rollbackFor=RuntimeException`——受检异常（Exception 子类非 Runtime）**不回滚**。业务代码几乎只抛 Runtime（本项目自定义 BusinessException 继承 RuntimeException）所以没踩坑，但被问要答得上来。规范：事务方法要么 `rollbackFor=Exception.class` 显式声明，要么保证只抛运行时异常。
一句话口径：默认滚运行时异常——受检异常需 rollbackFor=Exception.class 显式声明。

#### D1-5 事务传播行为（被追问的核心传播项）
REQUIRED（默认）：有事务加入、无事务新建——嵌套调用合并进同一事务（本项目审核消费端外层一个事务全包）；REQUIRES_NEW：永远新开事务、外层回滚不影响它（适合"记录审计日志，主逻辑失败也要留日志"——但要用**代理对象**调用否则又是自调用失效）；NOT_SUPPORTED：挂起当前事务（长耗时操作别占事务）。能讲清这三个已超过多数候选人。
一句话口径：默认 REQUIRED 合并事务；要"独立成功/失败"用 REQUIRES_NEW（记得走代理）。

#### D1-6 事务与锁/并发的坑（嵌套与自增的连锁）
事务 + 行锁：加锁在事务内、释放随提交/回滚——所以"查了改"必须在一个事务里否则锁提前释放（A1 的 UPDATE 自带事务）。@Transactional 只对 public 方法生效（CGLIB 无法增强 private/protected 调用）。**事务里别做耗时操作**（调外部 API/大循环）——锁与连接占着，别人排队、连接池耗尽。
一句话口径：锁随事务走、事务别干慢活、注解只认 public——三条事务卫生守则。

#### D1-7 只读事务（@Transactional(readOnly=true)）
查询方法标注 readOnly：MySQL/连接池层面可走只读连接（优化），更重要的是**自我文档化**（告诉读者"这里是纯读"）。它不是强制的（MySQL 不阻止你写），是提示+轻优化。项目里的列表查询可标——面试提一句"我知道 readOnly 的语义边界"即可。
一句话口径：readOnly 是意图声明+轻优化，不是写保护——别神化别忽略。

#### D1-8 事务在 MQ/Redis 场景的边界（跨系统无事务）
@Transactional 只保护**同一个数据源**内的一致性。消息（C1-8）、缓存删除（B5）、Redis 计数（V3）都不参与 DB 事务——这是分布式一致性的起点认知。被追问"那怎么保证 DB 和 Redis 一致"→ 答：不追求原子，追求最终一致+窗口可控（B5 全套答案在这）。
一句话口径：本地事务管单库；跨系统没有事务，用 afterCommit/最终一致补。

#### D1-9 面试连环追问脚本
①事务怎么生效 → ②自调用为什么失效 → ③和 @Configuration 自调用的区别 → ④默认回滚什么 → ⑤传播行为有哪些 → ⑥事务里能干耗时活吗。六问是事务+代理全貌（此题是你的主场，答完即拉满）。
一句话口径：生效机制→自调用坑→配置类对照→回滚规则→传播→卫生守则。

#### D1-10 一句话口径（浓缩背诵版）
"事务是靠 Spring AOP 代理生效的：注入的是代理对象，代理负责开事务、提交或回滚。所以同类内 this 自调用会绕过代理——@Transactional 失效，我在项目里规避过这个问题。我还研究过 @Configuration 的自调用为什么不失效——那是容器级 CGLIB 增强、拦截的是 getBean 语义，和 AOP 代理是两个子系统。默认只回滚运行时异常，需要时显式 rollbackFor；传播默认 REQUIRED，审计这种要独立成败的场景用 REQUIRES_NEW 并且要走代理调用。"

<a id="D2"></a>
### D2. 有界线程池 + 反压

#### D2-1 Boot 默认线程池为什么是地雷
`@Async` 不配线程池时用 Boot 的 applicationTaskExecutor——**队列容量 Integer.MAX_VALUE（无界）**。提交速率>消费速率 → 队列无限增长 → 堆内存耗尽 OOM；且无界队列**永远触发不到拒绝策略**——没有报错没有日志，直到 OOM 前系统看起来一切正常。这是"无声故障"的典型。
一句话口径：无界队列=OOM 前夜无报警——有界+拒绝策略是生产底线。

#### D2-2 线程池参数逐个讲依据（本项目 4/16/200）
core=4（常驻线程）、max=16（峰值上限）、queue=200（有界缓冲）、namePrefix=aimall-async-（日志里认出池线程）、[TaskDecorator](V1.5工程基建详解.md#2.4%20坑一：异步线程里%20traceId%20丢了（本章最重）)（traceId 传递）、CallerRuns（反压）、优雅停机（等任务跑完再关）。**别背数字，讲推导**：任务画像=IO 密集（调 AI/查库/写文件，线程大部分在等），所以 core 可以小于 CPU 核数没关系，max 给到 4 倍缓冲突发。
一句话口径：参数是任务画像推出来的——IO 密集给足线程、有界队列留缓冲、拒绝策略定极端行为。

#### D2-3 执行顺序的语义（90% 的人记错）
不是"线程满了才排队"！真实顺序：先占满 core → **再进队列** → 队列满了才扩线程到 max → 再满触发拒绝策略。所以 core=4、queue=200 时：第 5~204 个任务**排队不扩线程**（不是马上有 16 线程）——只有队列也满了才用到 max。理解这个顺序才能解释"为什么我并发 50 才用了 4 个线程"。
一句话口径：先核心→再队列→满队列扩到最大→满拒绝——队列在扩线程之前，别记反。

#### D2-4 CallerRuns 反压的机制（为什么它能自动限速）
队列满时 [CallerRuns](V1.5工程基建详解.md#4.2%20AsyncConfig%20逐参数精读) 让**提交任务的线程自己执行任务**——Web 线程被任务占住 → 它没空处理新请求 → 上游速率自然下降。**不需要任何协调组件，压力沿调用链反向传导**——这就是反压（backpressure），与 TCP 滑动窗口是同一哲学。注意代价：提交线程被慢任务借用，极端时整个 Web 池都被拖进执行。
一句话口径：CallerRuns=把压力还给上游让它自己减速——不是丢弃是限速。

#### D2-5 四种拒绝策略的选型（别只说 CallerRuns）
Abort（默认，抛异常——适合能感知能重试的）；CallerRuns（提交线程跑——适合可延迟不可丢，本项目审核用它）；Discard（静默丢——几乎总是错的）；DiscardOldest（丢最老——有损）。**静默丢弃永不选**——要丢也要自定义 handler 先记日志。面试答拒绝策略时把四种说全并给结论。
一句话口径：能感知失败用 Abort、能减速用 CallerRuns、静默丢弃永不——拒绝也要有迹可查。

#### D2-6 MDC 与 TaskDecorator（traceId 跨线程）
[MDC](V1.5工程基建详解.md#2.1%20问题驱动：日志为什么"串不起来") 底层是 ThreadLocal——**线程隔离**：Web 线程存的 traceId 池线程看不见。TaskDecorator 在任务三时点做手脚：①提交时刻（还在 Web 线程）快照 MDC；②执行时刻（池线程）把快照 set 进来；③执行完恢复原状（池线程也复用，防串号）。三时点缺一不可。
一句话口径：TaskDecorator=提交快照/执行设置/完毕还原——池线程离开时还一个干净储物柜。

#### D2-7 线程串号（为什么 finally 清理是军规）
池线程复用：请求 A 在池线程 T 上没清理 MDC → T 归还池子 → 请求 B 分到 T → B 的日志带着 A 的 traceId → 排查 B 事故时 grep 到的是 A 的日志——**被误导比没有 traceId 更糟**。Filter 的 finally clear + Decorator 的恢复 previous 都是防它。
一句话口径：串号=日志张冠李戴——比没有 traceId 更危险，清理是军规不是礼貌。

#### D2-8 线程池的坑位（ThreadPoolExecutor 完整构造语义）
手动 new 时注意：core 设 0 的池会"来了任务才建线程"（有延迟毛刺）；`allowCoreThreadTimeOut(true)` 可让 core 也回收；拒绝策略里 getTaskCount/队列大小可以做监控（线程池监控是运维眼）。本项目用 ThreadPoolTaskExecutor（Spring 封装）简化配置，但底层语义全是 ThreadPoolExecutor 的。
一句话口径：底层是 ThreadPoolExecutor——core 预热/回收/监控都是它的语义，封装只是省配置。

#### D2-9 面试连环追问脚本
①Boot 默认有什么问题 → ②参数怎么定 → ③执行顺序 → ④CallerRuns 为什么能限速 → ⑤四种拒绝策略 → ⑥MDC 怎么跨线程。六问是线程池全貌（配合你的并发底子，此域也是主场）。
一句话口径：默认坑→参数推导→顺序→[反压](V1.5工程基建详解.md#4.2%20AsyncConfig%20逐参数精读)→拒绝→跨线程，六问闭环。

#### D2-10 一句话口径（浓缩背诵版）
"项目里的异步我不用 Boot 默认线程池——它的队列无界，提交快于消费就无声堆积到 OOM，而且永远不触发拒绝策略。我显式配了有界池：核心 4、最大 16、队列 200、拒绝策略 CallerRuns。选 CallerRuns 是因为审核任务可延迟不可丢，队列满时提交线程自己执行——这叫反压，让上游自然减速而不是静默丢弃。线程池上还挂了 TaskDecorator 把 traceId 从 Web 线程带进池线程，三时点快照设置还原，防止日志串号。"

<a id="D3"></a>
### D3. traceId 全链路日志

#### D3-1 没有 traceId 的现场（为什么必须做）
100 个并发请求的日志交织在一个文件。要查"这个下单请求经历了什么"——没有请求标识，只能靠时间戳猜。**traceId=请求的身份证**：同一请求的所有日志带同一个值，grep 一次还原完整现场。
一句话口径：traceId 让"大海捞针"变"按号取件"——可观测性的地基。

#### D3-2 MDC 为什么零侵入（对比传参方案）
0 基础方案：每个方法加参数 traceId 往下传——侵入所有方法签名（灾难）。MDC：往线程的"日志储物柜"put 一个值，同线程后续所有 log 语句的 pattern `%X{traceId}` 自动打出——**业务代码零改动**。本质是日志框架集成了 ThreadLocal。
一句话口径：MDC=线程级日志上下文——put 一次全线程受益，不污染方法签名。

#### D3-3 Filter 的三个设计点（为什么是 Filter/为什么 finally/为什么回写响应）
①Filter 不是 Interceptor：Filter 在 Servlet 层最早执行最晚退出，能覆盖静态资源；②finally 必须 clear：线程池复用（串号，D2-7）；③响应头回写 X-Trace-Id：用户报障时从前端 devtools 复制 traceId 给客服，精准定位——**可观测性是双向的：日志查得到、用户拿得到**。
一句话口径：Filter 最早覆盖最全、finally 清理防串号、响应头回传让用户能报号。

#### D3-4 异步线程丢 traceId（TaskDecorator 场景，D2-6 展开）
坑位记忆：MDC 是 ThreadLocal → 换线程即丢 → 池线程日志没有 traceId（只见 Web 线程有）→ 异步部分的排查断链。解法三时点 Decorator。面试追问"为什么不用 InheritableThreadLocal"——答：那只能传给**创建时**的子线程，池线程是复用的不适用；且池化下会污染，正确做法是任务提交时快照。
一句话口径：InheritableThreadLocal 治不了池线程——快照复制才是正解。

#### D3-5 logback pattern 与滚动（别小看配置）
pattern 打 `%X{traceId:-NONE}`（缺省 NONE 防列错位）；滚动策略 50MB/文件、30 天、总量上限——**不治理的日志会写满磁盘**（生产事故：磁盘满→写不进→应用挂）。traceId 在 pattern 里，滚动只是防爆——两者都要。
一句话口径：%X 打 traceId、滚动防爆盘——日志治理的两块基石。

#### D3-6 服务化后 traceId 怎么串（V4 网关透传）
V1.5 预留"上游传了 traceId 就沿用"的钩子 → V4 网关生成 `gw-xxxx` → 请求头透传给 mall-app → mall-app 调 ai-service 再透传 → 三个进程同一 traceId → `grep gw-xxxx` 跨进程捞出全链路。**设计时留钩子、演进时零成本**——V1.5 到 V4 的呼应讲出来是亮点。
一句话口径：网关是第一跳生成者，服务间请求头透传——traceId 设计跨越了单体到微服务。

#### D3-7 跨线程的进阶：TTL（知道边界即可）
TaskDecorator 单层池够用；**跨池传递**（A 池提交给 B 池）会失效（快照发生在 A 池，MDC 已空）——阿里 TransmittableThreadLocal（TTL）通过[注册中心](V4服务化详解.md#5.1%20零基础：没有注册中心的痛点)+包装 Runnable 解决。本项目单层池用 Decorator 够——**知道"什么时候不够"比背 API 重要**。
一句话口径：单层池 Decorator、跨池链 TTL——工具的边界和用法一样要记。

#### D3-8 响应头之外：日志规范（traceId 的配套纪律）
统一日志格式（时间/线程/traceId/级别/logger/消息）、统一 logger 命名（包名即定位）、敏感信息脱敏（token/密码不打日志）。traceId 让日志可串，规范让日志可信。
一句话口径：traceId 解决"串起来"，规范解决"不乱讲"——可观测的两半。

#### D3-9 面试连环追问脚本
①为什么用 MDC 不传参 → ②为什么 Filter → ③为什么 finally 清 → ④异步线程丢了怎么办 → ⑤网关怎么透传 → ⑥InheritableThreadLocal 为什么不行。六问是 traceId 全貌。
一句话口径：零侵入→选 Filter→串号→Decorator→跨服务→子线程陷阱，六问闭环。

#### D3-10 一句话口径（浓缩背诵版）
"全链路日志我做的是 Filter+MDC+TaskDecorator 三件套：请求进 Filter 生成 traceId 存进 MDC，日志 pattern 的 %X 自动打出，finally 必须清理否则线程池复用时日志串号；异步线程通过 TaskDecorator 在提交时快照、执行时设置、完成后还原，把 traceId 带进池线程。V4 服务化后网关负责生成透传，一次 grep 能跨三个进程还原整个请求链路——单体时代的钩子设计在微服务时代直接兑现。"

<a id="D4"></a>
### D4. 可插拔设计（策略模式家族）

#### D4-1 可插拔解决什么问题（现实的约束驱动）
项目引入过多个"有真实凭证才可用"的外部件：对象存储（MinIO）、AI embedding（DeepSeek 不提供）、支付（需要商户资质）、独立 AI 服务（要部署）。如果代码硬依赖这些——**没有凭证/没有部署的机器上项目起不来**。[可插拔](V1.5工程基建详解.md#0.3%20设计总原则：可降级、可插拔)=给每种能力一个接口+多个实现，默认实现零依赖。
一句话口径：可插拔是被"现实约束"逼出来的——没有凭证也要能跑能验。

#### D4-2 家族的四个成员（面试把它串成一条线）
| 抽象 | 默认实现 | 可切换 | 版本 |
|---|---|---|---|
| StorageService | local 文件 | MinIO | V1.5 |
| [EmbeddingClient](V2内容社区与RAG详解.md#6.4%20EmbeddingClient：一个现实约束催生的可插拔设计) | local 哈希 | spring-ai 真实模型 | V2 |
| [PayChannel](V3支付与Redis详解.md#4.4%20PayChannel：为什么是这三个方法) | MOCK | 支付宝沙箱 | V3 |
| AiService | local 进程内 | remote 独立服务 | V4 |
同一个"接口+多实现+配置切换"模式四次出现——**这是你的设计哲学主线**。
一句话口径：四代可插拔=storage/embedding/pay/ai——同一哲学贯穿全部版本演进。

#### D4-3 技术本质：策略模式+条件装配
策略模式（同一接口多实现按需选）+ 工厂/条件装配（@ConditionalOnProperty 或 @Qualifier+@Value 显式构造器）。两种装配姿势：①**配置驱动自动装配**（@ConditionalOnProperty 满足才建 Bean——AiServiceClient 只在 remote 模式存在）；②**运行前手动二选一**（ChatServiceImpl 构造器读配置选 localAi/remoteAi——显式、可日志打印）。
一句话口径：可插拔=策略模式的工程化——自动装配按配置、手动选择按构造器，两种我都会。

#### D4-4 为什么默认实现必须零依赖（铁律背后的面试价值）
面试官克隆仓库能一键跑起来=他能验证你的项目、愿意继续聊。若默认实现依赖 MinIO/Nacos/商户资质——他第一步就卡住。**"零依赖能启动"是演示型项目的生死线**，也是你项目做得像样（而非像教程）的原因。
一句话口径：默认零依赖让项目"开箱可验"——面试官能跑=沟通能继续。

#### D4-5 降级是另一个名字的"可插拔"
同一个机制两个视角：换实现叫可插拔、实现缺席的兜底叫可降级（storage 挂 MinIO→local；Redis 挂→DB；AI 服务挂→熔断降级文案；MQ 挂→线程池）。**可插拔解决"有多个选择"，可降级解决"选择没了"**——一体两面。
一句话口径：插拔是换件、降级是没件的兜底——一个抽象两手准备。

#### D4-6 怎么新增一个实现（演进路径清晰）
以 PayChannel 为例：写一个 AlipaySandboxChannel implements PayChannel → 实现 create/verifyCallback/query 三个方法 → 注册进 PayChannelRouter。**业务代码零改动**——这就是面向接口的回报：新增能力=加实现，不是改调用点（开闭原则的实践版）。
一句话口径：加能力=加一个实现类+注册——改的是扩展不是核心（开闭原则落地）。

#### D4-7 可插拔的边界（什么时候不该抽象）
别为"只可能有一个实现"的东西建接口——接口的意义在第二实现出现时才兑现（YAGNI）。判断：这个能力将来会换吗？会换才抽象。本项目的四次抽象都有"可替换的真实动机"（换供应商/换模型/换部署形态）——**抽象跟需求走，不跟潮流走**。
一句话口径：抽象要为"可预见的第二实现"而生——YAGNI 防过度设计。

#### D4-8 与设计模式的面试联系
被问"用过什么设计模式"——策略模式（四连）、模板方法（审核/索引流程骨架）、工厂（Router 按配置出实现）、门面（RedisOps 收敛底层）。**用项目实例讲模式比背定义强十倍**。
一句话口径：策略/模板/工厂/门面四个模式在项目里都有实例——讲实例不讲定义。

#### D4-9 面试连环追问脚本
①为什么项目里这么多抽象 → ②怎么实现切换 → ③默认实现为什么零依赖 → ④新增一个实现要改什么 → ⑤什么时候不该抽象。五问是可插拔全貌。
一句话口径：动机→机制→零依赖价值→开闭→YAGNI 边界，五问闭环。

#### D4-10 一句话口径（浓缩背诵版）
"项目从 V1.5 到 V4 贯穿一个设计主线：可插拔。对象存储抽象成 local/MinIO 双实现、AI embedding 可 local 可真实模型、支付渠道抽象成 MOCK/沙箱、V4 的 AI 能力抽象成本地/远程双模式——同一个接口加多个实现、配置切换、业务零改动。这么做的直接动机是现实约束：个人项目拿不到商户资质、不想强制别人装 MinIO，所以默认实现必须零依赖能跑。新增能力对我来说就是加一个实现类，这就是开闭原则在项目里的实践。"

<a id="D5"></a>
### D5. 单元测试

#### D5-1 测试优先级=事故代价（为什么先测钱和门）
测试资源有限，先测"错了是资损/安全/灾难"的：扣库存（超卖=资损）、越权（别人订单=安全）、支付状态机（重复回调）、上传安全（穿越=XSS）。页面样式错了是体验问题，排最后。**测试是风险管理不是覆盖率数字**。
一句话口径：先测钱、再测门、最后测皮——按事故代价分配测试精力。

#### D5-2 mockStatic 为什么能钉 StpUtil（静态调用的测试困境）
业务代码到处 `StpUtil.getLoginIdAsLong()`（静态方法）——单测没有登录态，普通 @Mock 注入不进去（静态调用没有"对象"可替换）。Mockito.[mockStatic](V1.5工程基建详解.md#5.2%20关键技巧：mockStatic%20钉住%20StpUtil) 在**字节码层**拦截类的静态调用：`when(StpUtil::getLoginIdAsLong).thenReturn(42L)`——钉死"当前用户=42"。
一句话口径：静态调用没有对象可注入——mockStatic 在字节码层拦类，钉住登录态。

#### D5-3 为什么必须 try-with-resources（mockStatic 的作用域军规）
mockStatic 改的是**类级别全局状态**——不限作用域会污染同 JVM 其它测试（并行测试随机挂）。try-with-resources 让 mock 只在代码块内生效、出域自动卸载。静态 mock 是全局副作用，作用域越窄越安全。
一句话口径：静态 mock 是全局副作用——try-with-resources 限定作用域是纪律。

#### D5-4 纯内存测试（为什么 CI 不需要数据库）
Mapper 全 mock、登录态 mockStatic、文件用 [@TempDir](V1.5工程基建详解.md#5.2%20关键技巧：mockStatic%20钉住%20StpUtil)（JUnit 自动建临时目录自动删）——单测不碰任何外部服务 → 秒级跑完 → **CI 容器里没有 MySQL 也能全绿**。测试依赖环境=测试不可靠（环境抖你也红，分不清代码错还是环境错）。
一句话口径：纯内存让测试只测代码——CI 零依赖跑单测是可靠性的前提。

#### D5-5 @TempDir 的意义（文件测试不留垃圾）
文件测试写死路径 → 残留垃圾文件+并行测试互相覆盖。@TempDir 每次注入独立临时目录（唯一）+测试完自动清理——**测试的隔离性**（每个测试不依赖不破坏其它测试的环境）。
一句话口径：TempDir=每人一间钟点房——隔离、自动清理、永不互扰。

#### D5-6 事务回滚测试（集成测试的干净回滚）
集成测试（连真库测 SQL）惯例：测试方法 @Transactional 让数据自动回滚（测完库还是原样）。注意：真实 MyBatis SQL/事务行为只能靠这类测试兜底（纯 mock 测不到 SQL 错）——**单测守逻辑、集成测守 SQL**，分工明确。
一句话口径：连库测试用事务回滚保持库干净——SQL 真伪只有真库能验。

#### D5-7 测试金字塔与"冰淇淋筒"反模式
金字塔：大量快的单测（毫秒）+少量关键集成（秒）+极少 E2E（分钟）。反模式"冰淇淋筒"：大量 E2E 少量单测——慢、脆、红了我不知道哪层错。本项目 **26 个单测**为主 + 冒烟清单（手工 9 项）当 E2E——量级匹配。
一句话口径：底层单测要厚、顶层 E2E 要薄——冰淇淋筒是反模式。

#### D5-8 断言的艺术（别只测"没抛异常"）
好[断言](V4服务化详解.md#6.2%20路由表（本项目的全部路由，面试画这张图）)测"行为契约"：扣库存返回 0 行时抛"库存不足"、越权订单返回 404、支付幂等重放后计数仍是 1。测"异常被抛+错误信息对+副作用没发生"三件事——**测试的价值=把需求变成可执行检查**。
一句话口径：断言测行为契约三件事：对错、信息、副作用——不是测"没崩"。

#### D5-9 面试连环追问脚本
①先测什么为什么 → ②登录态怎么处理 → ③为什么 CI 能零依赖跑 → ④文件测试怎么隔离 → ⑤SQL 谁测 → ⑥覆盖率多高。六问是单测全貌。
一句话口径：优先级→mockStatic→纯内存→TempDir→SQL集成→覆盖率哲学，六问闭环。

#### D5-10 一句话口径（浓缩背诵版）
"单测我先写钱和门：扣库存、越权、支付状态机这些错了是资损和安全的路径。登录态用 Mockito.mockStatic 钉住 StpUtil——静态方法普通 mock 注入不了，字节码拦截类的静态调用，try-with-resources 限定作用域防止污染其它测试。所有测试纯内存：Mapper mock、文件用 @TempDir，CI 容器里没数据库也能全绿。测试策略是按事故代价分级，不是追覆盖率数字。"

## E. 微服务域（★ 突击区：以下内容 V4 详解+词典全覆盖，是突击教材本体）

<a id="E1"></a>
### E1. 服务拆分决策

#### E1-1 先想清楚：微服务的本质是"决策"不是"技术"
拆服务前要答三问（三问宪法在架构层的应用）：①这个模块砍掉独立会怎样？②它挂了要不要连累别人？③它要不要独立伸缩/替换？**三条都"是"才值得拆**。绝大多数项目的错误是倒过来：先决定"我要微服务"，再硬凑边界。
一句话口径：微服务是决策的产物——先有三问的答案，才有拆分的动作。

#### E1-2 拆 AI 的五条理由（逐条能配"现场"）
①耗时差异：AI 秒级 vs 普通接口毫秒级，同进程 AI 请求会占满 Tomcat 线程池让下单排队；②故障隔离：模型供应商挂了，不能连累订单/支付不可用；③独立扩容：AI 是 token 成本密集，需要单独加资源/限流；④密钥隔离：DeepSeek key 只在 ai-service 存在，泄露半径最小；⑤模型可替换：换 GLM/换供应商只改一个服务。面试背五条是及格，逐条配一个"现场"是优秀。
一句话口径：耗时/故障/扩容/密钥/替换——五条全是业务现场，不是技术口号。

#### E1-3 为什么不拆用户/商品/订单（反向论证=高分点）
下单 = 同一事务锁库存+建订单+清购物车——拆开就要**分布式事务**（Seata 或本地消息表），引入协调者/一致性复杂度，且网络调用让链路多几个故障点。当前业务量（演示级）付不起这个复杂度。**"能讲出不做什么"比"堆五个服务"高级**——这句话本身就是面试答案。
一句话口径：事务耦合=不拆的理由；分布式事务的复杂度只有大流量才付得起。

#### E1-4 康威定律（为什么要会这个词）
系统架构会"长成"组织沟通结构的形状。单人项目的"组织"=脑内模块认知——你把 AI 当独立技能域认知，服务就沿这条缝裂开，天然贴合[康威定律](V4服务化详解【知识词典】.md#康威定律)；反过来为了简历堆五个服务=假装组织有五个团队，维护时会持续别扭。
一句话口径：拆分的缝要沿着"真实的职责边界"裂，不沿着简历关键词裂。

#### E1-5 数据归属先于建工程（拆分真正的设计工作）
建新 Spring Boot 工程是"动作"，划清每张表归谁是"设计"：t_knowledge_* 归 ai-service（mall 只能通过接口请求索引）；业务表归 mall-app（AI 只能回调查询）；API key 归 ai-service 独占。物理上可同库，**归属与访问权必须分离**——"逻辑边界先于物理边界"。
一句话口径：谁拥有数据谁就是主人——接口是访客的唯一通道。

#### E1-6 拆分的"顺手收益"与"顺手的坑"
收益：编译隔离（AI 改代码不重编全仓）、团队可并行（假想多团队）、部署独立（AI 热更新不动业务）。坑：跨服务排障（没 traceId 就完蛋——V1.5 已铺路）、网络故障面增加、测试要起多服务。**收益是渐进的、坑是立刻的**——这也是"先单体按需拆"的原因。
一句话口径：拆分收益是长线的、复杂度是即时的——所以起步要小、理由要硬。

#### E1-7 什么时候会拆更多（演进路线预演）
订单量上来了、下单变成独立瓶颈 → 拆订单服务，此时必须配套：本地消息表/Seata（一致性）+ 分布式事务预案（G1 有详版）。**被追问"以后会拆吗"**：会，但触发条件是性能与团队规模，不是"微服务先进"——给出触发条件比给"会/不会"高级。
一句话口径：拆更多=一致性复杂度+性能瓶颈同时到账——触发条件要能说出来。

#### E1-8 面试连环追问脚本
①为什么只拆 AI → ②（反向）[为什么不全拆](V4服务化详解.md#2.4%20反向论证（面试加分项原话）) → ③拆了数据怎么分 → ④多服务部署几个环境 → ⑤以后会拆更多吗。五问是拆分决策全貌。
一句话口径：正向五理由→反向事务耦合→[数据归属](V4服务化详解.md#2.3%20拆分的实质：数据归谁（比建模块重要十倍）)→运维成本→演进触发，五问闭环。

#### E1-9 配套知识点：服务间没有本地事务了（一句话铺垫）
拆开后 @Transactional 只能管自己库——服务A扣库存成功、服务B建单失败，两边各自提交了=数据不一致。解法方向：最终一致（本地消息表/对账）或强一致（分布式事务框架，昂贵）。本项目因为没拆业务域所以没这问题——**"不拆"就是为躲这个复杂度**（与 E1-3 闭环）。
一句话口径：拆开的账单是分布式事务——本项目选择不拆所以不付这张账单。

#### E1-10 一句话口径（浓缩背诵版）
"做服务化我第一步不是建工程，是决定拆谁不拆谁。拆 AI 有五条硬理由：它和业务接口的耗时差一个量级、它挂了不能让下单陪着挂、它的成本要独立伸缩、它的 key 要独立隔离、换模型只改它一个。反过来用户商品订单不拆——下单要同一事务锁库存建订单清购物车，拆开就得为分布式事务买单，当前量级不划算。能讲清'不做什么'和为什么，比堆五个服务更能体现架构判断。"

<a id="E2"></a>
### E2. 熔断（Resilience4j）

#### E2-1 不熔断会怎样（把事故推演到结局）
ai-service 挂 → mall-app 每个 AI 请求都等满读超时（120s）才失败 → 200 个 Tomcat 线程全在"等死" → 商品/订单接口没有线程可用 → 全站瘫痪。**[级联故障](V4服务化详解.md#7.1%20零基础：家里的保险丝)/服务雪崩**：一个下游挂了，把整条调用链拖死。熔断=在"每个请求傻等超时"和"立即失败"之间装一个自动开关。
一句话口径：雪崩的引擎是"等待"——熔断把等待变成即时失败，切断传导。

#### E2-2 三态机完整迁移表（能画能说）
```
CLOSED    正常转发+滑动窗口统计失败率（最近10次、至少5次起统计）
  │ 失败率≥50% 跳闸
  ▼
OPEN      一律快速失败走 fallback（根本不发请求），等 30 秒
  │ 30 秒后自动转
  ▼
HALF-OPEN 放 3 个试探请求
  │ 全成功→CLOSED；任一失败→回 OPEN
```
关键：**全程自动、无人值守**——服务恢复后自己探测回来。
一句话口径：CLOSED 统计→OPEN 止损→[HALF-OPEN](V4服务化详解.md#7.2%20三态机（面试必背，能画出来）) 试探→自动往返，无人值守。

#### E2-3 熔断 vs 重试（方向相反，别混）
重试="再试试"→给对端加压（适合偶发抖动：网络闪断）；熔断="先不试"→保护对端和自己（适合持续故障：服务挂了）。误用：对已挂的服务重试=雪上加霜（每次都占满超时）；对偶发抖动熔断=小题大做（误伤可用服务）。**判断依据：失败原因会不会自己消失**。
一句话口径：重试加压、熔断减压——按失败是否会自愈来选，方向相反别混用。

#### E2-4 fallback 的铁律（降级方法别变成二次事故）
降级方法三原则：①签名=原方法+尾部 Throwable 参数；②**绝不再调任何可能失败的东西**（[fallback](V4服务化详解.md#7.3%20代码接入（两行注解%20+%20一个降级方法）) 里调远程=降级了个寂寞，还二次超时）；③返回要"可接受"（友好文案/缓存旧值/功能收缩）。本项目 fallback 只 return 字符串+log——稳如老狗。
一句话口径：fallback 是最保守的兜底——只返回预设值，不发起任何新冒险。

#### E2-5 参数为什么这样配（能推导不背数字）
窗口 10（灵敏：10 次里有 5 次失败就跳）；阈值 50%（半数失败=下游确实不行）；minimum 5（样本太少不统计，防冷启动一次失败就跳）；half-open 3（试探要少量，3 个够了）；open 30s（给下游喘息+别让用户等太久）。**每个参数都能说出"为什么"**是被追问的护身符。
一句话口径：窗口管灵敏、阈值管判定、最少次数防误判、半开试探管恢复——参数皆可推导。

#### E2-6 与超时/限流/降级的分工（一次讲清四个词）
①**超时**：单次请求的上限（连接 2s/读取 120s）；②**限流**：入口管"来多少"（每用户 10 次/分）；③**熔断**：出口管"下游还能不能打"（失败率自动判定）；④**降级**：失败后给什么（fallback 文案）。四者是**一条链上的四个闸**，面试把分工说清=系统理解到位。
一句话口径：限流管入口、超时管单次、熔断管出口、降级管兜底——四个闸门一条链。

#### E2-7 配置层级坑（本项目真实踩过，讲出来最可信）
Resilience4j 配置前缀是**顶层 `[resilience4j](V4服务化详解.md#7.4%20配置（以及那个真实踩过的坑）):`**——写进 spring: 块会被**静默忽略**（不报错、熔断器照常用默认参数：窗口 100 不是配的 10）。你演示"6 次失败就跳闸"会失败因为默认要 100 次统计。**验证方法**：启动日志里 Resilience4j 打印的实例参数 / actuator/circuitbreakers 端点。讲"我踩过、我怎么发现的、以后怎么防"是面试最稀缺的真实性。
一句话口径：resilience4j 必须顶层配置，错层级静默失效——启动日志核对参数是防坑姿势。

#### E2-8 熔断在 AOP 下的限制（自调用失效，与 D1 呼应）
@CircuitBreaker 靠 AOP 代理拦截——同类内 this.chat() 调用不走代理=熔断失效（与 @Transactional 自调用同源）。本项目熔断注解打在 RemoteAiService 的 public 方法上、由外部注入调用——代理生效。**面试主动说这个坑**（你在 V3/V4 都撞过代理自调用）能展示教训的迁移能力。
一句话口径：熔断注解和事务注解一样怕自调用——代理失效坑全家族通用。

#### E2-9 面试连环追问脚本
①不熔断会怎样 → ②三态迁移 → ③和重试区别 → ④fallback 注意什么 → ⑤参数怎么定的 → ⑥踩过什么坑。六问是熔断全貌。
一句话口径：雪崩→三态→重试对照→fallback 铁律→参数推导→配置层级坑，六问闭环。

#### E2-10 一句话口径（浓缩背诵版）
"给 ai-service 的调用我加了 Resilience4j 熔断：最近 10 次里失败率到 50% 就跳闸，之后请求毫秒级走 fallback 返回'AI 助手休息中'的文案，30 秒后进半开放 3 个请求试探，服务恢复了自动闭合。不熔断的后果我推演过：每个请求傻等 120 秒超时，线程池被占满，下单跟着瘫痪——级联故障。熔断是'先别试'保护双方，和重试方向相反。配置上踩过一个坑：resilience4j 必须顶层配置，挂 spring 下会静默失效，全靠启动日志核对参数才发现。"

<a id="E3"></a>
### E3. 注册发现

#### E3-1 没有注册中心会怎样（问题驱动）
mall-app 部署 3 个实例：地址列表写死在 gateway 配置里？实例挂了/加了，要人工改配置重启——**错、慢、等于没弹性**。注册中心=服务电话簿：实例启动自动登记、心跳失效自动销号、调用方按名字查号——实例上下线对调用方透明。
一句话口径：注册中心让"实例增删"对调用方透明——写死地址就没有弹性。

#### E3-2 三个动作（注册/心跳/查询）与参数链
①注册：启动时把 IP:port 报给注册中心；②心跳：默认 5 秒一次报平安，15 秒(3次)标记不健康、30 秒(6次)剔除；③查询：调用方按服务名拿实例列表。**心跳≠业务健康**：进程活着但数据库连不上时心跳照报——生产要配 actuator 健康检查做主动探测。
一句话口径：注册+心跳+查询三动作；心跳只证明"进程在"，业务健康要主动探测。

#### E3-3 lb:// 的完整解码（面试把它拆开）
`lb://mall-app` 不是 URL 是**指令**：①向 [DiscoveryClient](V4服务化详解.md#5.3%20本项目的三层实现（面试最有讲究的部分）) 要 mall-app 实例列表 → ②负载均衡器（轮询）挑一个 → ③用真实地址（http://192.168.6.1:8080）发请求。**服务名是逻辑地址、实例是物理地址**——注册发现=逻辑到物理的自动翻译。
一句话口径：[lb://](V4服务化详解.md#5.2%20`lb://`%20的完整含义（拆开讲）) = 查表+挑实例+真实转发三连——逻辑地址与物理地址解耦。

#### E3-4 负载均衡的前提（无状态）
挑哪个实例结果都一样的前提是**服务无状态**——用户的登录态、会话不能只存在某个实例的内存里。本项目单实例没暴露这个问题，被追问"多实例怎么办"：状态外置 Redis（会话/限流计数本来就外置了——V3 基建恰好是答案）。
一句话口径：负载均衡的前提是无状态——状态外置是分布式的基本功。

#### E3-5 两种实现的选择（SimpleDiscoveryClient vs Nacos——本项目双保险）
[SimpleDiscoveryClient](V4服务化详解.md#5.3%20本项目的三层实现（面试最有讲究的部分）)：实例写在 yml 的静态表（零额外服务、适合演示/兜底）；NacosDiscoveryClient：真注册中心（动态增删、控制台可视）。**同一个 DiscoveryClient 接口**，本项目两个都配：Nacos 主（体验真注册发现）+ Simple 兜底（Nacos 挂了 lb:// 仍可解析）——注册中心自身故障也不拖垮转发（可降级铁律延伸到基础设施）。
一句话口径：同一抽象两实现并存=体验真实注册发现+基础设施故障兜底。

#### E3-6 多网卡陷阱（Windows 开发真实事故）
Nacos 自动选 IP 会选中**第一块非回环网卡**——装了 VMware 的 Windows，192.168.49.x 虚拟网卡常排最前 → 注册地址跨机器不可达。修复：yml 显式 `nacos.discovery.ip`。**元教训**：自动检测（IP/时区/编码）在复杂环境是隐形炸弹——环境相关的东西显式声明。
一句话口径：自动选 IP 会选到虚拟网卡——[多网卡](V4服务化详解.md#5.4%20真实踩坑：多网卡机器的注册%20IP（本机实测）)机器显式声明注册 IP 是纪律。

#### E3-7 fail-fast=false 的降级哲学（注册中心挂了怎么办）
fail-fast=true：连不上注册中心直接启动失败（适合强依赖：每个请求都要查发现）；本项目=false：连不上打 WARN 照常启动，客户端后台自动重连、恢复后自动补注册。**注册中心是基础设施，基础设施的故障不该拖垮业务启动**。
一句话口径：fail-fast 选择=注册中心是不是生死依赖——本项目不是，所以降级。

#### E3-8 与网关的配合（lb:// 出现在哪）
gateway 路由 `uri: lb://mall-app`——网关就是最大的"发现客户端"。也正因为网关代查，后端服务之间直连（mall→ai 内部调用走静态 URL+Hmac，不经网关，避免"所有流量过网关"的单点）。**两种调用模式并存**：外部经网关（发现+过滤）、内部直连（签名+快）——面试讲这个分层很显架构感。
一句话口径：外部流量走网关+发现、内部调用直连+签名——流量分层不是全挤一条道。

#### E3-9 面试连环追问脚本
①没有注册中心会怎样 → ②心跳和健康检查区别 → ③lb:// 是什么 → ④多实例怎么办（无状态） → ⑤两种实现为什么并存 → ⑥注册中心挂了会怎样。六问是注册发现全貌。
一句话口径：问题→机制→解码→无状态→双实现→降级，六问闭环。

#### E3-10 一句话口径（浓缩背诵版）
"[服务发现](V4服务化详解.md#5.1%20零基础：没有注册中心的痛点)我用的 DiscoveryClient 抽象——mall-app 启动自动注册到 Nacos，gateway 路由写 lb://mall-app 按服务名查实例列表、负载均衡挑一个转发。演示环境我额外配了 SimpleDiscoveryClient 静态实例兜底：注册中心挂了 lb:// 还有解析目标，转发不断——这是把可降级哲学用到了基础设施上。还踩过 Windows 多网卡的坑：Nacos 自动选了 VMware 虚拟网卡注册，跨机器不可达，显式指定注册 IP 解决。"

<a id="E4"></a>
### E4. 网关

#### E4-1 网关的三个存在理由（逐条能配现场）
①一个门牌：前端只认识 9000，后端怎么拆前端无感；②安全边界：`/internal/**` 在门口拒绝——没网关时外部直连 8081 就绕过了内部鉴权设计（虽然内部接口还有签名，但网关是纵深第一层）；③横切集中：traceId/跨域/访问日志一处实现，服务各自专注业务。
一句话口径：一个入口+一层边界+集中横切——网关不是转发器是治理层。

#### E4-2 路由/断言/过滤器三概念（Gateway 配置阅读能力）
断言（Predicate）="这请求归哪条路由"（Path=/api/** 等）；过滤器（Filter）=转发前后加工（加头/改状态/记日志）；路由（Route）=断言+目标+过滤器组合。**会读任何 Gateway 配置**=三概念+格式。
一句话口径：断言分流、路由定向、过滤器加工——三个概念读遍网关配置。

#### E4-3 为什么 /internal/** 拒 404 不是 403（最小信息披露）
403="端点存在但你没权限"→ 告诉探测者这里有事可挖；404="没这东西"→ 让探测者失去方向。**拒绝对手时别免费给情报**——与支付验签失败不透露细节同一思想族。本项目还有第二层：即便有人绕过网关直连 8081，内部接口还有 HMAC 签名（纵深防御：网关挡 404+服务层验签 401）。
一句话口径：404 装傻优于 403 摆谱——安全细节不给对手免费信息。

#### E4-4 traceId 过滤器（V1.5 钩子的兑现）
网关是无状态转发层，但它是**链路第一跳**——请求进来没有 traceId 就生成（gw-xxxx），透传给 mall-app（请求头），mall-app 调 ai-service 再透传：一次 grep 跨三个进程。**单体时代 Filter 里的"上游传了沿用否则生成"在微服务时代由网关扮演上游**——设计留钩子的回报。
一句话口径：网关生成 traceId 透传全链路——单体预留的钩子被微服务兑现。

#### E4-5 网关自身的单点问题（知道就有加分）
网关挂了=全站入口没了——所以网关必须无状态+多实例+负载均衡前置（Nginx/LB 挡在网关前）。本项目演示环境单网关可接受，被追问"网关挂了"→ 答：网关无状态可快速重启；生产会多实例+前置 LB，K8s 下天然多副本。
一句话口径：网关是单点但要让它"可丢"——无状态+多副本+前置负载。

#### E4-6 鉴权放哪层（网关 vs 业务服务的经典讨论）
本项目：Sa-Token 鉴权在 mall-app（业务侧）。为什么不上移网关：①Sa-Token 的会话语义耦合业务；②网关要做的事越少越快。生产的常见形态：网关做**认证**（验 token 有效性）→ 服务做**授权**（验权限）——知道这个分层，被问"鉴权为什么不在网关"就有答案。
一句话口径：认证可上移到网关、授权留在业务——本项目规模认证在业务侧即可。

#### E4-7 网关与跨域（前后端分离的日常）
前后端分离：前端 dev server（5173）调网关（9000）是跨域 → 网关统一配置 CORS（允许来源/方法/头/凭证）——一个地方解决所有服务的跨域，而不是每个服务各自配。**"一个门牌"的附带红利：跨域也只配一处**。
一句话口径：跨域集中网关配——服务不用各自背 CORS 配置。

#### E4-8 面试连环追问脚本
①为什么需要网关 → ②三概念讲一下 → ③内部端点为什么拒 404 → ④traceId 怎么跨服务 → ⑤网关挂了会怎样 → ⑥鉴权放哪。六问是网关全貌。
一句话口径：必要性→三概念→安全边界→链路→单点→鉴权分层，六问闭环。

#### E4-9 演进方向（别只说"做了网关"）
可加的：网关限流（RequestRateLimiter+Redis）、网关重试/超时策略、灰度路由（按 header 分流到不同版本）、静态资源缓存。**知道方向与触发条件**——"网关限流在生产会和 Redis 联用；灰度在版本发布频率上来后值得做"。
一句话口径：限流/灰度是网关的成熟功能——触发条件是流量与发布频率，不是标配就上。

#### E4-10 一句话口径（浓缩背诵版）
"网关我做了三层价值：统一入口让前端只认 9000；安全边界——外部访问 /internal/** 直接返 404，不确认端点存在，防止探测；横切集中——traceId 在这里生成透传、跨域在这里配。路由是断言加过滤器的组合，lb:// 目标配合注册发现。网关本身无状态，生产会多实例加前置负载均衡。用户鉴权我放在业务服务（Sa-Token），认证授权分层的概念清楚——规模上来认证可以上移到网关。"

<a id="E5"></a>
### E5. 服务间鉴权（HMAC）

#### E5-1 为什么"内网就不用鉴权"是错的
风险路径：①docker -p 误把内部端口映射公网；②跳板机/办公网被横向渗透，内网接口裸奔=免费 AI 额度+数据泄露；③误配置/安全组开放。**"内网"是会变的假设**——安全设计不建立在会变的假设上。这是[零信任](V4服务化详解【知识词典】.md#内网裸奔错觉)（Zero Trust）的核心思想（不信任任何网络位置，只验证身份）。
一句话口径：鉴权不赌网络位置——内网是会变的假设，身份验证才是不变的。

#### E5-2 三件套头的协议设计（为什么这三样）
X-Internal-Client（我是谁——签名输入）；X-Internal-Ts（何时——参与签名防重放）；X-Internal-Sign=HMAC-SHA256("client=..&ts=..", secret)（证明+防篡改）。校验三步：头齐全→时间窗（5 分钟）→[constant-time](V3支付与Redis详解.md#5.4%20时序攻击：为什么不用%20equals) 比签名。**时间戳为什么参与签名**：改 ts 签名就失效；过期 ts 直接拒——防"截获合法请求原样重发"。
一句话口径：身份+时间+签名三件套；时间戳进签名=重放攻击在协议层被掐死。

#### E5-3 防重放的窗口设计（5 分钟怎么来的）
窗口太小：机器时钟漂移误杀正常请求；太大：重放攻击窗口变宽。5 分钟在"容忍时钟漂移"与"限制重放"间取平衡。**注意它是"新鲜度检查"不是"精确[防重放](V4服务化详解.md#4.3%20为什么是%20HMAC%20+%20时间戳（与%20V3%20支付验签对照着记）)"**——5 分钟内的重放仍有效；要绝对防重放需 nonce+服务端记录（每 nonce 只收一次）。本项目量级：时间窗足够+幂等兜底。
一句话口径：时间窗管漂移容忍、防重放管新鲜度——绝对防重放要 nonce，量级没到。

#### E5-4 为什么用 HMAC 对称而不是 JWT/非对称（选型逻辑）
对称 HMAC：收发共享一个 secret——适合"同一部署体内的服务"（信任关系简单）；JWT/RSA 非对称：私钥签名公钥验签——适合"跨组织多方"（支付宝验签就是 [RSA2](V3支付与Redis详解.md#5.5%20HMAC%20vs%20RSA2：对称与非对称)）。**按信任结构选认证方案**：内部服务信任模型简单 → 共享密钥最省事。
一句话口径：内部共享密钥、跨组织非对称——按信任结构选，不是按流行度。

#### E5-5 客户端怎么自动带签名（声明式客户端的红利）
签名头放在 RestClient 的 defaultRequest——**每个通过该客户端发出的请求自动带**，业务代码零感知、漏带不可能。对比手动在每个调用点拼签名：漏一个调用点=安全洞。这就是"声明式收敛横切"的价值（与网关集中 traceId 同理）。
一句话口径：签名头收敛到客户端配置——安全语义一次配齐，调用点漏不掉。

#### E5-6 双侧必须同步（协议一致性）
服务端 InternalAuthFilter 与客户端 InternalSignature 是**同一协议的左右手**：拼接串一字不差（client=xxx&ts=xxx）、secret 同值、算法同款。改一侧不改另一侧=全部 401。**协议=先约定后实现，两侧同步演进**——这是"接口即契约"的服务端版本。
一句话口径：签名协议双侧是镜子关系——改一侧必须同步改另一侧，否则全量 401。

#### E5-7 与支付验签的对照（同一算法家族第三次复用）
V3 支付回调验签（HMAC 校验第三方通知）与 V4 内部签名是同一算法家族——差别在信任对象与附加机制（支付另有[金额核对](V3支付与Redis详解.md#6.3%20handleCallback%20逐段精读（六步全贴）)+幂等，内部另有时间戳防重放）。面试把"同一个 HMAC 我用了三次（Mock 支付/内部调用）"讲出来，比堆十个框架更显功力。
一句话口径：HMAC 从支付回调用到服务间调用——一个算法家族贯穿业务与架构。

#### E5-8 和用户鉴权（Sa-Token）的分层（别混淆两种身份）
用户 token（Sa-Token）：证明"哪个用户在操作"（业务身份）；内部签名：证明"哪个服务在调用"（服务身份）。**两个身份层不互替**：用户请求到 mall-app 用 token 验人；mall-app 到 ai-service 用签名验服务。面试画一下"两层身份"就清晰了。
一句话口径：业务身份用 token、服务身份用签名——两层身份各管一段，别混。

#### E5-9 面试连环追问脚本
①为什么内网也要鉴权 → ②协议怎么设计的 → ③防重放怎么做的 → ④为什么 HMAC 不用 JWT → ⑤客户端怎么自动带 → ⑥和支付验签什么关系。六问是内部鉴权全貌。
一句话口径：动机→协议→防重放→选型→收敛→家族复用，六问闭环。

#### E5-10 一句话口径（浓缩背诵版）
"服务间调用我做了 HMAC 签名鉴权：每个请求带 client、时间戳和签名三个头，签名是 client 加时间戳拼起来用共享密钥算 [HMAC-SHA256](V3支付与Redis详解.md#5.2%20从哈希到%20HMAC：三步演进（0%20基础）)。服务端校验三步——头齐全、时间戳在 5 分钟窗口内（防截获重放）、constant-time 比对签名。选 HMAC 对称是因为这是同一部署体内的服务，信任模型简单，不用背 JWT 的密钥分发复杂度。签名头放在客户端配置的 defaultRequest 里自动带，调用点不会漏。'内网就裸奔'是错觉——端口误映射、横向渗透都真实存在。"

## F. AI/RAG 域（★ 你的文档是护城河，此处只提炼，细读在 AI 模块详解）

<a id="F1"></a>
### F1. Function Calling 双工具路由

#### F1-1 Function Calling 是什么（先讲清楚，很多人只会用不会讲）
模型本身不知道你的数据库——Function Calling 让你把"一个方法"声明成工具给模型：模型判断需要时**自己决定调用、自己填参数**，拿到结果再组织回答。本质是"把数据查询能力借给模型"，而不是"把数据塞给模型"（后者=token 爆炸+数据过期+模型看漏）。
一句话口径：不把数据喂给 AI，给 AI 一个能查数据的工具——AI 按需自取。

#### F1-2 为什么用双工具而不是一个"万能搜索"
问题分两类：问价格库存（要实时 SQL 精确值）vs 问使用体验（要语料模糊检索）——一个工具塞两种逻辑=模型不知道何时用哪个（或瞎猜）。双工具让模型按问题性质分流（工具描述的引导，见 F1-3）。**两个工具互斥边界清楚**，比"一个工具内部 [if-else](V2内容社区与RAG详解.md#8.2%20为什么不写%20if-else（核心思想）)"更符合 Agentic 思路。
一句话口径：工具按"数据域"切，模型按问题性质选——边界越清楚路由越准。

#### F1-3 工具描述工程（description 是给模型看的接口文档）
模型看不到 Java 代码，只靠 @Tool 的 description 决定"何时调/怎么传参"。写法：**正向说清**返回什么、何时该调（"当用户问商品价格时"）+**负向说清**何时别调（"价格库存不要用 searchDocs，用 searchProduct"）。正负结合压制工具抢跑/错跑。
一句话口径：description=给 AI 看的说明书——正向说适用、负向说禁区，两句都要。

#### F1-4 为什么不用规则路由（if-else 的穷举陷阱）
"问题含'价格'→走 A，含'防水'→走 B"的规则路由：自然语言意图**不可枚举**（"这玩意儿贵吗""预算内有没有替代"都是价格意图但无"价格"二字）。模型的语义理解就是为这种开放集而生的——**Agentic 路由=把分流交给模型，代码只负责把工具描述清楚**。
一句话口径：规则路由永远挂一漏万，模型路由吃下长尾——分流发生在模型侧代码零判断。

#### F1-5 参数防御（模型传参不老实是真实常态）
模型可能传 null/0/负数/超长——方法体必须有兜底：keyword 去空转 null、page<1 用 1、pageSize 上限截断（别让模型一次要 100 条撑爆上下文）。**把模型的参数当外部输入防御**（与 HTTP 参数校验同一心态）。
一句话口径：模型传参等同不可信输入——方法体参数防御是标配不是可选。

#### F1-6 工具失败怎么办（工具异常绝不上抛）
工具调用异常直接抛出=整段对话失败。正确：catch 后返回结构化"查不到/服务不可用"→ 模型据此如实回答"暂时查不到"——**绝不编造**（编造比查不到更伤信任）。这是工具层降级（与 V2 检索 safe()、V4 熔断 fallback 同一失败哲学）。
一句话口径：工具失败降级成"如实说查不到"，绝不让模型进入编造模式。

#### F1-7 工具与 system prompt 的分工（两级引导）
system prompt 定总纲（"你是导购，问价格先调 searchProduct…"）+ 工具 description 定分工——两级引导双保险。总纲说"大方向"，工具描述说"边界细节"。本项目两条链路（文本/视觉）各挂[双工具](V2内容社区与RAG详解.md#8.1%20从"单手"到"双手")，模型在两个工具间按需切换甚至都调（综合问题）。
一句话口径：system 定战略、description 定战术——两级引导是 Agent 稳定性的关键。

#### F1-8 与"伪代码思维"的区别（你真理解 Agent 吗）
传统编程：我规定每一步（命令式）；Agentic：我提供能力+边界，模型编排（声明式）——**控制点从"执行序列"移到"能力边界与引导词"**。面试被问"Agent 和普通接口调 AI 的区别"→ 答：普通调用是单轮问答，Agent 是多轮工具编排（模型自主决定"查一下→基于结果再答"），Function Calling 是 Agent 的最小形态。
一句话口径：Agent=能力声明+边界引导+模型自编排——Function Calling 是它的最小切片。

#### F1-9 面试连环追问脚本
①FC 是什么 → ②为什么双工具 → ③模型怎么知道何时调哪个 → ④为什么不规则路由 → ⑤参数怎么防 → ⑥工具失败呢。六问是 FC 全貌。
一句话口径：本质→工具切分→描述工程→Agentic 理由→防御→降级，六问闭环。

#### F1-10 一句话口径（浓缩背诵版）
"导购里我用了 Function Calling 双工具：searchProduct 查实时价格库存（走 SQL），searchDocs 检索说明书和用户笔记（走 RAG）。模型读工具描述自己选——'多少钱'走前者、'戴着舒服吗'走后者，代码里没有 if-else 意图判断，分流发生在模型侧。关键在工具描述工程：正向写清何时调、负向写清何时别调，防工具抢跑。模型传参不可信，方法体全做了兜底；工具异常绝不外抛，降级成'查不到'让模型如实说，绝不让它编。"

<a id="F2"></a>
### F2. Hybrid RAG

#### F2-1 RAG 的一句话与边界（最优先立住）
RAG=先查资料再回答：检索语料里最相关的 3~5 段 → 塞进 prompt 当参考资料 → 模型基于资料回答。**边界红线**：价格/库存这类精确实时数据走 SQL（RAG 语料是快照会过期+检索是模糊的会答错）；说明书/体验这类非结构化文档走 RAG。说反=原理性错误。
一句话口径：SQL 管精确数据、RAG 管非结构化文档——边界立错全盘皆输。

#### F2-2 为什么"几百份文档"不能全塞长上下文（四连理由）
①语料动态增长（笔记每天发，全塞不可持续）；②首字延迟（Prefill 耗时随长度线性涨，打字机第一字等数秒）；③权限隔离（检索层可按来源过滤，全塞无边界）；④成本（全量 token×对话次数失控）。**面试答全四条**是"想清楚过"的信号。
一句话口径：动态/延迟/权限/成本四条——长上下文只适合一次性长文档分析。

#### F2-3 为什么要 Hybrid 双路（语义鸿沟）
关键词检索（BM25/ngram）抓精确词（"蓝牙5.3""IPX7"）强、抓换说法（"续航久"vs"电池耐用"）弱；向量检索反之。电商两类问题都高频 → 单路必有一类答不上 → **双路召回+融合**是 2026 生产基线（业界共识：比单路召回率高 10-20%）。
一句话口径：精确词与模糊语义是两座山——Hybrid 不绕山，是两路各上一座。

#### F2-4 向量是什么（0 基础也能讲）
embedding 把文本映射成几百维的浮点数组：语义相近的文本在向量空间里距离近（"续航久"和"电池耐用"靠近）。算相似度=算向量夹角余弦。**工程铁律**：全库向量必须来自同一模型（不同模型的"坐标系"不同，不可比）；查询与语料同一模型。
一句话口径：向量=文本的地图坐标，语义近则坐标近——前提是同一张地图（同一模型）。

#### F2-5 RRF 融合（为什么用排名不用分数）
BM25 分可上百、余弦∈[0,1]——量纲不同不能直接加权（厘米加斤）。[RRF](V2内容社区与RAG详解.md#7.3%20RRF：一行公式与三个为什么) 只看排名：score=Σ 1/(k+rank)。三个推论：①免量纲（只看名次）；②双路都命中的片段自动加分（双确认，无需显式编码）；③k=60 压平第 1 名与第 2 名差距。
一句话口径：RRF 用名次不用分数——双确认自动浮现，k=60 平滑头部权重。

#### F2-6 UGC 语料的价值（为什么社区+RAG 是黄金组合）
官方说明书只说"IPX5 防水"（参数话术）；用户笔记才有"戴着跑步出汗没坏"（真实体验）。**体验类问题只有 UGC 语料能答**——这是"种草社区+RAG"互相成就的业务逻辑，纯电商项目抄不走（没有语料来源）。内容飞轮：UGC→语料→更好的 AI→帮用户写更好笔记→更多 UGC。
一句话口径：参数看说明书、体验看 UGC——社区供语料、RAG 还价值，飞轮闭环。

#### F2-7 切片的细节（为什么 300 字+50 重叠）
检索按"片段"做，不整篇塞——所以要切。300 字是检索粒度与上下文效率的平衡；50 字[重叠](V2内容社区与RAG详解.md#6.3%20切片：固定长度%20+%20重叠（逐行读%20split）)保证**关键句不横跨切片边界**（一句被切断两边都语义残缺）。工程上有语义切分等进阶，固定长度+重叠是性价比最高的起步。
一句话口径：切片防"整篇塞"的低效；重叠防"切一刀断一句话"——300+50 是工程平衡点。

#### F2-8 embedding 的实现与"穷人版"（诚实定位）
DeepSeek 官方不提供 embedding 接口——本项目默认 local 哈希 embedding（文本切 2-gram 哈希到 256 维桶）：**能捕获字面重叠、不能捕获语义**（"续航久"与"电池耐用"字面零重叠=不相似）。它是降级实现保链路可跑，接真实模型可切换（可插拔）。**面试主动说"我的默认 embedding 只能匹配字面"**——诚实+知道边界，比假装用了真模型可信。
一句话口径：local 哈希=链路可用但只认字面；真实语义向量是配置切换项——诚实标注边界。

#### F2-9 面试连环追问脚本
①RAG 是什么/边界 → ②为什么不塞上下文（四条） → ③为什么 Hybrid → ④RRF 为什么用排名 → ⑤语料哪来的 → ⑥向量怎么来的/embedding 局限。六问是 RAG 全貌（AI 岗几乎是必考）。
一句话口径：定位→四连理由→双路→RRF→语料→embedding 诚实版，六问闭环。

#### F2-10 一句话口径（浓缩背诵版）
"RAG 我做的是 Hybrid：BM25 关键词通道加向量语义通道并行召回、RRF 排名融合——精确型号词和模糊体验问法都能答上。语料是两个来源：官方说明书（答参数）和真实用户笔记（答体验），体验类问题只有 UGC 语料能答，这是社区和 AI 互相成就的闭环。为什么不全[塞长上下文](V2内容社区与RAG详解.md#5.3%20必考题："几百份文档为什么不塞长上下文")：语料动态、首字延迟、权限、成本四条。embedding 默认是 local 哈希的降级实现，只匹配字面不匹配语义，这是 DeepSeek 不提供 embedding 接口逼出来的诚实方案，接真实模型只需换实现。"

<a id="F3"></a>
### F3. SSE 流式输出

> ⚠️ **★ 先立住一条诚实边界（被追问拆服务后最容易翻车）**：
> **local 模式（`aimall.ai.mode` 默认）= 真流式**（token 级，`Flux` 来自 WebClient 的 `bodyToFlux`）；
> **remote 模式 = 伪流式**——`RemoteAiService.stream()` 实际是 `Flux.just(chat(message, null))`，**先拿到完整结果再包装成 Flux**，不是跨进程逐 token 透传。
> **答法**：被问"拆了服务之后流式怎么还通"→ 答"**当前 remote 是整段返回，真流式透传要把 SSE 帧跨进程转发（WebClient 的 `bodyToFlux` 或 RSocket），这是已知的下一步**"。**别把 remote 说成 token 级流式**——被要求现场看代码就穿了。

#### F3-1 为什么需要流式（体验账）
模型生成 300 字约 5-30 秒。非流式：用户盯着转圈 30 秒才看到全量结果——**首字延迟是 AI 体验的生死线**。流式：token 边生成边推给前端逐字显示——第一字 1 秒内出现，30 秒的等待变成"打字机"体验。
一句话口径：流式把"等 30 秒看结果"变成"1 秒开始逐字看"——首字延迟决定体验生死。

#### F3-2 SSE 协议（别和 WebSocket 混）
SSE（Server-Sent Events）= 单向 HTTP 长连接，服务端可反复推 `data: xxx\n\n` 事件——**HTTP 之上、纯文本、浏览器原生支持**（EventSource）。WebSocket 是双向全双工（适合聊天互动）。AI 打字机是"服务器→客户端单向流"——SSE 是更轻的正解。
一句话口径：AI 流式是单向推送——SSE 恰是为此设计的；WebSocket 的双向能力用不上就别背复杂度。

#### F3-3 后端实现（Flux 是什么）
Spring AI 的 `.stream()` 返回 Reactor 的 Flux<String>（响应式流）——每个模型 token 是一个元素，Flux 按序发射，Spring MVC 自动包装成 SSE 帧。**不用自己管理线程/连接**——框架把"事件流"转成"HTTP 流"。
一句话口径：Flux 是"按需产出的事件流"——框架层完成 SSE 包装，业务只管发射 token。

#### F3-4 流式与幂等的矛盾（前端重复点击）
流式请求不能简单重放（用户点两次=两段话）。解法：前端按钮禁用+节流（发送中不可再发）；后端幂等键（可选）。本项目前端"发送中禁用按钮"——简单可靠。面试被问"流式中断/重试"→ 答：断流不落半截话（攒齐才存），用户可重新发起（会新起一轮）。
一句话口径：流式的重试语义=重新来一轮，不是续传——按钮禁用+攒齐落库是两端配合。

#### F3-5 会话历史怎么持久化（多轮上下文的载体）
模型无记忆，每轮要带历史。两表：t_conversation（会话）+ t_message（消息，role+content+图片）——请求时把该会话消息转成 Spring AI 的 Message 列表塞回 prompt。**存储的是"历史文本"不是"向量状态"**——上下文每次重新组装。
一句话口径：历史存库、每次重组上下文——模型无记忆，仓库就是它的外置大脑。

#### F3-6 上下文膨胀治理（历史太长怎么办）
历史无限增长 → token 成本涨+超上下文窗口。治理：只带最近 N 轮（滑动窗口）；更早的摘要压缩。本项目演示级会话不长未做截断，**知道这个演进方向**（回答"以后呢"用）。
一句话口径：历史要设上限——最近 N 轮+更早摘要，token 预算与回忆深度权衡。

#### F3-7 流式下的错误处理（流中途断怎么让用户知道）
流开始前校验（限流/鉴权）失败=返回 JSON 错误；流中途失败=SSE 里推一个特殊事件/结束事件带 error 标记，前端据此显示"生成中断"。**错误要能穿过流**——这是流式服务比普通接口多的一个设计点。
一句话口径：错误要能进 SSE 流——前端按事件类型区分"正常结束"与"异常中断"。

#### F3-8 与工具调用的流式（进阶：FC+SSE 一起）
模型调工具时前端可能看到"停顿"（模型在想/在等工具结果）——进阶产品会推"正在搜索…"的中间事件。Spring AI 流式+工具是支持的（token 流里可能夹工具调用）。本项目流式主链路纯问答，知道工具调用可以进流即可。
一句话口径：FC+SSE 要处理"工具调用期的静默"——中间状态事件是进阶产品体验。

#### F3-9 面试连环追问脚本
①为什么流式 → ②SSE 和 WebSocket 区别 → ③后端怎么实现的 → ④历史怎么存 → ⑤重复点击 → ⑥流中途失败。六问是流式全貌。
一句话口径：体验账→协议选型→Flux→持久化→幂等→错误穿流，六问闭环。

#### F3-10 一句话口径（浓缩背诵版）
"AI 问答我做成了 SSE 打字机：Spring AI 的 stream() 返回 Flux，每个模型 token 是一个事件，Spring MVC 自动包装成 SSE 帧推给前端，前端手撕 EventSource 逐字渲染。选 SSE 不选 WebSocket 是因为它就是单向服务器推送的协议，比双工通道轻。会话历史存两表，请求时重组上下文——模型没记忆，仓库是外置大脑。断流不会落半截话，攒齐才存库。"

<a id="F4"></a>
### F4. AI 内容安全（审核与文案红线）

#### F4-1 为什么 UGC 平台必须有审核（法律责任前置）
用户发布的内容平台承担法律责任（色情/广告/违禁品）——没有审核的 UGC=风险全裸奔。AI 审核用模型做初筛：几秒判定，把 99% 明确内容分流掉，灰区留人工。**AI 审核的定位是"初筛"不是"终审"**——机审明确违规拒、明确安全放、不确定留人工。
一句话口径：审核分层=机审清大头、人工兜灰区——AI 是初筛不是法官。

#### F4-2 先发后审 vs 先审后发（本项目取折中）
纯先审后发：体验差（发布要等审核完）；纯先发后审：违规内容已公开过（撤回也传播过）。本项目折中：**发布即入队（状态=审核中，对外不可见）+ 秒级 AI 审**——用户感觉"发出去了"（立即有反馈），系统实际"过审才公开"——两头的优点都拿到，靠异步（C1）。
一句话口径：对外不可见但用户秒回反馈=先审后发的体验改良版。

#### F4-3 审核提示词的设计（分类显式+JSON+倾向）
①违规类目显式列出（色情/广告引流/辱骂/政治/违禁品）——不让模型自由发挥"什么是违规"；②强制 JSON 输出 {pass, reason, categories}——解析做防御（模型可能包 markdown 围栏）；③**拿不准倾向 pass**（[宁漏勿杀](V2内容社区与RAG详解.md#9.5%20审核提示词：三个温度的第三副面孔)：误杀正常用户伤害>漏一条软广）。
一句话口径：类目白名单+JSON 契约+宁漏勿杀——审核提示词的三根支柱。

#### F4-4 温度的工程意义（同一个模型三个温度）
审核 0.1（判定要确定）、导购 0.7（平衡）、文案 0.8（要发散）——**temperature 是按任务性质选的**：判定类低（稳定答案）、创作类高（多样性）。面试聊 temperature 别背定义，用项目三处实例讲。
一句话口径：低温度买稳定、高温度买创意——判定与创作是温度轴的两端。

#### F4-5 为什么 AI 文案只出草稿（不直写业务数据）
AI 生成的笔记直接公开：幻觉编造的"真实体验"=虚假宣传（法律风险）；且责任主体不能是 AI。红线：AI 产出永远经人确认与流程把关——文案接口返回草稿（editable），用户编辑后走正常发布+审核。**"AI 辅助、流程把关"是内容平台底线**。
一句话口径：AI 可以起草、不能盖章——草稿+人确认+审核流程三件套。

#### F4-6 审核错误的两种代价（为什么宁漏勿杀）
漏放一条软广：平台或有小损失（可巡查补删）；误杀正常用户：个体确定性伤害（体验挫伤+流失+投诉）——**伤害不对称**，所以方向选"宁可漏，不可杀"。加"拿不准倾向 pass"就是为这个不对称买单。
一句话口径：漏是可逆的小损失、杀是不可逆的伤害——不对称决策选保守方。

#### F4-7 审核留痕（为什么流水表重要）
AI 判定是概率性的——会错、要翻案。每次判定原始结果（ai_result_json）入库：用户申诉时可查当时依据、可统计误杀率优化提示词、可人工复核。**概率性系统必须留判定证据**（与 C2-3 呼应）。
一句话口径：判定是概率、翻案要证据——ai_result_json 是审核系统的审计日志。

#### F4-8 面试连环追问脚本
①为什么 UGC 要审核 → ②先审还是先发 → ③提示词怎么设计 → ④AI 判错了怎么办 → ⑤为什么文案只出草稿 → ⑥温度怎么选。六问是 AI 内容安全全貌。
一句话口径：责任→时序→提示词→容错→红线→温度，六问闭环。

#### F4-9 合规意识的加分表达（有分寸地讲）
可以主动提一句"AI 内容安全是 2025-2026 的监管重点，个人项目做这个能体现内容平台的合规意识"——不展开法规细节（容易露怯），点到即止。
一句话口径：点到"UGC 平台责任+AI 辅助初筛"即可——合规意识加分不背法条。

#### F4-10 一句话口径（浓缩背诵版）
"内容平台要有审核，我用 AI 做初筛：发布后状态立即变审核中（对外不可见），异步调模型几秒判定——明确违规拒、明确安全放、不确定留人工。提示词把违规类目显式列出、要求强制 JSON 输出、并且拿不准倾向放行——因为误杀正常用户的伤害大于漏一条软广，伤害不对称决策就选保守方向。每次判定结果留痕可翻案。AI 文案只出草稿绝不自动发布——AI 可以辅助不能盖章，产出必须走人的确认和审核流程。"

---

## G. 支付域

<a id="G1"></a>
### G1. 支付回调全链路

#### G1-1 支付在技术上的本质（破除黑盒恐惧）
支付=四件事：①创建支付单（插自己库一行）；②跳收银台（拿个 URL）；③用户付款（发生在第三方，与你无关）；④异步回调（第三方 POST 你一个接口）。**真正新的只有第④步**——你的接口挂公网，任何人能 curl，怎么证明"这请求真来自支付宝、没被改"=验签。支付全部复杂度集中在"可靠地、恰好一次地"处理回调。
一句话口径：支付的技术难点=回调的验签与幂等——其余都是你写过的 CRUD+URL。

#### G1-2 回调六步（背下来，每步一条红线）
①验签（不过=sign-error 直接拒）；②定位支付单（查不到=拒绝）；③幂等快路径（已支付=回 success）；④金额核对（缺失/非法/不一致全拒）；⑤支付单状态机 CAS（PAYING→PAID）；⑥订单状态机 CAS（PENDING_PAY→PAID）。每步返回明确错误码，且**方法永不抛异常**（抛了=给对方 500=它重发）。
一句话口径：验签→定位→幂等→金额→支付单→订单，六步六红线，回调永不抛异常。

#### G1-3 验签为什么必须（回调接口的处境）
你的回调接口 = 向全世界开放的"改单入口"——不验签，任何人 curl 一下就能把订单改成已支付（白嫖）。验签回答"消息真是第三方发的吗？有没有被改一个字节"——用密钥重算签名比对（HMAC/RSA2，本项目 MOCK 渠道 HMAC）。
一句话口径：回调不验签=把改单权限开放给全世界——验签是回调的第一行代码。

#### G1-4 金额核对为什么"缺失即拒绝"（P0 级教训）
"回调带 amount 字段才核对"的写法=攻击者构造**不带 amount**的回调绕过核对。金额是回调与支付单的必配字段，缺失本身就是异常——三个分支全拒：缺失 amount-missing、非法 amount-invalid、不一致 amount-mismatch。**可空校验=给漏洞留门**（本项目评审揪出修掉的 P0）。
一句话口径：必配字段缺失即异常——校验的省略号就是攻击者的突破口。

#### G1-5 幂等三层（为什么重复必然发生）
第三方没收到 success 会重试（支付宝 8 次+，间隔递增）→ 重复回调是常态。三层：①支付单号唯一索引（物理：重复创建不可能）；②已支付→回 success（快路径：挡串行重复）；③状态机 CAS（慢路径：挡并发重复——两个回调同时穿过快路径）。**并发下只有 CAS 让一个落空**。
一句话口径：唯一索引+快路径+CAS 三层——串行重复与并发重复分别有防。

#### G1-6 为什么已支付的回调必须回 success
对方逻辑："收到 success 才认为通知成功、停止重发"。你回其它内容（包括 500）→ 它持续重试→ 每次重试都空跑一遍链路→ 浪费且日志爆炸。**成功处理的重复=礼貌地告诉对方"我知道了，别再发"**。
一句话口径：回 success 是协议动作——成功要回对字面量，机器只认字面量。

#### G1-7 与超时取消的竞争（状态机 CAS 的应用现场）
订单同时被支付回调（要 PAID）和延迟消息（要 CANCELLED）命中：闸门=WHERE status='PENDING_PAY'，行锁先到先得。**取消赢了支付才到**：钱已收（第三方扣款）→ 支付单保持 PAID（资金事实）+订单 CANCELLED（业务事实）→ 记 ERROR 走退款。**资金与业务分开记账**（A5 展开详版）。
一句话口径：竞争输赢由 CAS 裁决；资金事实与业务事实分开记，差异走退款。

#### G1-8 回调不用的鉴权（为什么白名单放行）
第三方服务器没有你用户的 token——回调加登录校验=必然 401=永远收不到钱。所以回调进 Sa-Token 白名单，身份由**验签**保证。**两套身份模型**：业务接口答"你是谁"（认证）、回调答"消息谁发的/有没有被改"（完整性）——混用必出事。
一句话口径：第三方没你的 token——回调靠验签不靠登录，两套身份模型别混。

#### G1-9 面试连环追问脚本
①支付本质 → ②六步链路 → ③验签为什么 → ④金额缺失为什么拒 → ⑤重复回调怎么防（三层） → ⑥和取消竞争怎么办。六问是支付全貌。
一句话口径：本质→链路→安全→校验→幂等→竞争，六问闭环。

#### G1-10 一句话口径（浓缩背诵版）
"支付我做的重点是回调处理，链路是六步：验签（HMAC 重算比对，不过直接拒）、定位支付单、幂等快路径（已付就回 success——不回对方会无限重发）、金额核对（缺失非法不一致全拒）、支付单状态机 CAS、订单状态机 CAS。为什么不加登录校验——回调的是第三方服务器不是用户，它没你的 token，身份由验签保证。重复回调用唯一索引加快路径加 CAS 三层防，和超时取消的竞争由 CAS 闸门裁决——取消赢了钱又到了就支付单保持已付、订单保持取消，差异走退款。"

---

> 全部 30 条 × 10 问完成。**用法提醒**：这是"深挖弹药库"不是"背诵清单"——每条读通后用你自己的话复述一遍；★★★ 的逐题读通，★★ 的至少读前 5 问。被追问卡住时，回这本来找 30 秒口径，再去对应详解章节精读原理。
> 对应详解：A/C/D → V1.5/V2/V3 详解；B → V3 详解；E → V4 详解；F → AI 模块详解+V2 详解；G → V3 详解支付章。
