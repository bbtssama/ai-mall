# AGENTS.md — ai-mall 工作区指令

## 求职工作区（每次会话必读）

本仓库的求职工作目录是 **`后端求职/`**。

- **会话开始时**：静默读取 `后端求职/dsh.memory` —— 这是 DSH agent 的长期记忆（用户画像 / 求职红线 / 项目与文档地图 / 技术口径红线 / 环境事实 / 待办）。
- **会话结束前**：静默把本次新增事实回写到 `后端求职/dsh.memory` 的对应区块；**第 9 节会话日志只追加、最新在最上**，不覆盖历史。
- `后端求职/dsh.memory` 含个人隐私（电话/邮箱/生日），已在 `.gitignore` 忽略 —— **禁止提交、禁止外泄**。

### 服务约定

- 全程中文（含思考）；角色是**专家顾问 + 主动型助手**，主动提问给建议，不等派活。
- 用户是**初级 Java 后端**，正在把 ai-mall 这个超大项目**收敛成"传统后端主线叙事"**。
  优先**做减法**：把已有内容收成主线，**不要新增技术点**；每个技术点必须给"为什么这么做 / 为什么不那么做"。
- 讲不清的东西宁可删掉——用户是"讲不出道理就没底气吹"的类型。

## 文档位置约定（2026-09-19 归类，找文件先看这张表）

> **根目录只保留 4 份 md**：`README.md`（项目说明）· `AGENTS.md`（本文件）·
> `项目设计文档.md`（总纲蓝图 V1~V5）· `AI模块详解-SpringAI从零到实战.md`（AI 教学专题）。
> 其余一律归类，**不要再往根目录放文档**。

| 想找什么 | 去哪 |
|---|---|
| ★ **快速吃透整个项目（面试用）** | `docs/项目手册/index.html`（多页面 HTML 手册，9 页 + `_assets/`；双击即开、零依赖、可离线） |
| 各版本工程实现讲解 / 概念词典 | `docs/版本详解/`（V1.5~V4 详解 + 词典，共 8 份） |
| 全项目文档怎么读、哪些过时 | `docs/00-项目文档地图与存废裁决.md`（唯一权威索引） |
| 求职主线（八股试卷 / 面试弹药库 / 版本叙事） | `后端求职/` |
| 简历 C 与超链学习目录 | `后端求职/简历/`（**含隐私，已 gitignore**） |
| 简历深挖笔记 / 面试准备 | `后端求职/`（随求职材料一起） |
| 历史审核产出 | `docs/归档/` 与 `_archive/2026-09-19-文档审计/` |

> **维护 `docs/项目手册/` 必读 `_assets/编写规范.md`**：它规定了 9 个页面共用的组件类名、
> 三层渐进披露结构、口径红线与代码事实基线。**新增/修改页面前先读它，否则风格会走样。**
> 目录结构：`index.html`（驾驶舱）+ `01`~`08` 专题页 + `_assets/{style.css, app.js, _template.html, 编写规范.md}`。
> 所有页面零外部依赖（无 CDN / 无字体 / 无图片），共用一套 CSS/JS，导航在 `app.js` 的 `HANDBOOK_NAV` 里统一维护。

> ⚠️ 移动文档时**必须同步重写引用路径**：跨目录引用有 341 条
> （超链目录用 `../../../../docs/版本详解/V3支付与Redis详解.md` 这种深度）。
> 改完跑 `后端求职/简历/简历C-超链学习目录/_链接校验.py` 验证（应为 1180/1180 通过）。

## 项目铁律（沿用既有约定）

- **业务驱动演进，绝不塞技术**：每个技术决策都要能答出"砍掉它会怎样"。
- **表结构唯一权威** = `backend/src/main/resources/db/migration/`（Flyway，只增不改）。历史上曾有一份 `sql/init.sql` 便利脚本，因与 Flyway 严重脱节（只建 8/24 表、用 `DROP TABLE`、注释口径相反）已于 2026-09-19 删除。
- **零依赖能启动是硬约束**：所有基建可降级、可插拔（storage/voice/AI 皆如此）。
- **口径纪律**：扣库存说"行锁 + `WHERE stock>=?` 条件更新（CAS 思想）"，**不说"乐观锁"**；价格库存走 SQL，RAG 只答说明书与 UGC。
- ⚠️ **验证编译必须带 `clean`**：本机的 `mvn test` / `mvn compile` 会打印
  `Nothing to compile - all classes are up to date.` 并**静默跳过编译**（增量编译判定不可靠），
  于是改动过的源码根本没被编译却报 BUILD SUCCESS —— **假绿**。
  这条曾导致一个真实的编译错误（lambda 引用非 final 变量）骗过本地验证、只在 CI 上暴露。
  **验收命令统一用** `mvn -B clean verify`（与 `.github/workflows/ci.yml` 完全一致），
  或先 `rm -rf backend/target`。看到 `Compiling N source files` 才算真的编译过。
- 本机**无 Docker**；MySQL / Nacos 在 `192.168.6.102`；Maven 可用（`D:\maven\apache-maven-3.9.14`，
  JDK17 在 `D:\JetBrains\JDK\JDK17`）。
