<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aimall.order.mapper.OrderItemMapper">

    <select id="selectByOrderId" resultType="com.aimall.order.bean.OrderItem">
        SELECT id, order_id, sku_id, product_name, sku_name, price, quantity
        FROM t_order_item
        WHERE order_id = #{orderId}
    </select>

    <insert id="batchInsert">
        INSERT INTO t_order_item (order_id, sku_id, product_name, sku_name, price, quantity)
        VALUES
        <foreach collection="items" item="it" separator=",">
            (#{it.orderId}, #{it.skuId}, #{it.productName}, #{it.skuName}, #{it.price}, #{it.quantity})
        </foreach>
    </insert>

</mapper>