# 简历项目经历与面试准备：ai-mall 技术语句库（挑菜指南）

> 用途：① 从技术语句库挑选你能 hold 住的句子填进简历；② 每条句子自带"必被追问+30秒答案"，写一条就会一条。
> 使用原则：**写进简历的每一行 = 邀请面试官提问的请柬**。答不上来的技术点宁可删掉——空白不减分，被戳穿减大分。
> 数据事实：本项目全部语句都锚定真实代码（V1~V4 已全部落地），**没有一句是编的**——这比市面模板简历的核心优势：任何一条被追问，项目里都有真实现场可讲。

---

## 【使用指南】（先读这个，3 分钟）

### 一、推荐度分级（按你的掌握底细标定）

| 级别 | 含义 | 决策 |
|---|---|---|
| ★★★ 已会 | 你深入学过（MySQL/并发/Spring/Redis基础/RabbitMQ基础），项目里真实用过 | **放心写**，只需过一遍追问答案 |
| ★★ 突击 | 项目里有、你没系统学过，但**半天能背下 30 秒答案+一个现场** | 想写就今天突击；不写零损失 |
| ★ 谨慎 | 深挖必露怯（要数学/源码级/大厂生产细节） | **不建议写**，除非面试官明确问起 |

### 二、低耦合设计（本库的组织方式）

每条语句**只围绕一组同类技术**（如"Redis+Lua"一条、"MQ+[死信](V3支付与Redis详解.md#8.3%20TTL%20+%20DLX：让消息"睡"30%20分钟)"一条）——删掉任何一条不影响其它条的真实性。挑选时整组拿或整组扔，不会出现"一条语句三个坑"。

### 三、三套成品模板（⛔ 已废弃——简历已定稿）

> ⛔ **2026-09-19 更新**：简历**已定稿为「简历C-后天投递.md」**（含超链学习目录），**只用这一版投递**。
> 以下 A/B/C 三套模板是"定稿前的选材器"，**已完成历史使命**，不要再从里面挑版本。
> 保留原因仅为追溯"当初为什么这么选"。
>
> ⚠️ 尤其注意：~~模板C AI 应用版（面向 AI Agent/应用开发岗）~~ **与当前策略直接冲突**——简历 C 已把 AI 明确定位为**"了解层"**（技术栈主线是传统后端），**不要走 AI 岗叙事**。

- ~~**模板A 后端基础版**（面向初级 Java 后端，全部 ★★★，零风险）~~ → 已被简历 C 承接并升级
- ~~**模板B 微服务版**（模板A + 突击半天微服务，适合中大厂）~~ → ⚠️ 简历 C **不含微服务组件**在技术栈行
- ~~**模板C AI 应用版**（面向 AI Agent/应用开发岗，你的文档储备反而是王牌）~~ → ❌ **与当前策略冲突，废弃**

### 四、配套详解与二级深挖笔记（对照阅读法）

**两级材料对照**（推荐阅读顺序：选句 → 卡片看 30 秒答案 → 被问住点卡片下方「深挖入口」）：

```
《简历项目经历与面试准备.md》   ← 本文件：挑菜 + 30 秒答案 + 追问
      │ 每张卡片标题下有"深挖入口"
      ▼
《简历语句技术深挖笔记.md》     ← 二级笔记：同编号语句的 10 个深挖点（#A1…#G1 锚点直达）
      │ 展开点里仍不懂的原理
      ▼
四套详解（V1.5/V2/V3/V4）     ← 系统学习 + 词典
```

每条语句更深的展开教学在四套详解里：[V1.5工程基建详解](V1.5工程基建详解.md) / [V2内容社区与RAG详解](V2内容社区与RAG详解.md) / [V3支付与Redis详解](V3支付与Redis详解.md) / [V4服务化详解](V4服务化详解.md)——被某条追问卡住，就去对应章节精读。

---

# 第一部分：项目介绍段

> ⛔ **2026-09-19 更新：简历已定稿为「简历C-后天投递.md」，以下三个版本是定稿前的对比稿，仅供追溯。**
> 当前采用：**版本A（电商后端，最稳）** 的叙事故事情节 + 简历 C 的措辞。

> 项目介绍段写"做了什么"，不写技术（技术放亮点里）——但**定位决定面试官用什么滤镜看你**。

**版本A（电商后端，最稳）★ 当前采用**：
> AI 种草商城——对标小红书+得物的内容电商平台。围绕"种草-拔草"构建完整业务闭环：内容社区（发布/审核/Feed流）、电商交易（购物车/订单/支付/限量发售）、AI 导购助手三大板块。

**版本B（微服务向）⛔ 未采用**：
> 同上 + 末尾追加：采用"单体优先、按需拆分"策略演进至服务化架构：业务主体保持单体，AI 能力独立成服务（耗时隔离/密钥隔离），网关统一入口。

**版本C（AI 应用向）❌ 已废弃（与当前策略冲突）**：
> ~~同 A + 末尾追加：核心特色是 AI 与业务的深度融合——基于 Function Calling 的智能导购、Hybrid RAG 检索增强问答（官方说明书+UGC 笔记双语料）、AI 内容审核与种草文案生成。~~

**核心技术栈行**（★ 以简历 C 为准，见 `简历C-后天投递.md:66`）：
```
SpringBoot3 + MyBatis（XML 手写 SQL）+ MySQL 8 + Redis + RabbitMQ + Sa-Token + Flyway + Vue3
```
> ⚠️ **不含** Spring Cloud Gateway / Resilience4j / Nacos——微服务只在技能栏写"了解 Spring Cloud"，技术栈行不列（写了就是主动请战）。

---

# 第二部分：技术语句库（按域分组，挑菜开始）

## A. 数据库与事务域【★★★ 全部已会——你的主场】

> 你的 MySQL 是强项（联合索引字典序、行锁这些你比大多数候选人熟），**这个域要全拿**——它是初级后端面试的必考区，而且项目里全是真实实现。

### A1. 行锁 CAS 扣库存（必写，项目的看家技术）
> 📖 深挖 10 问：[跳转到「A1」](简历语句技术深挖笔记.md#A1) ← 被追问答不上就点这里
**简历语句**：订单库存扣减采用"行锁 + 条件更新（CAS 思想）"方案，`UPDATE ... WHERE stock >= ?` 由数据库原子性保证并发安全，杜绝超卖。
**项目锚点**：`t_product_sku` 的 deductStock；限量发售第 2 层同一套。
**必被追问**：
1. *为什么不用乐观锁（version 字段）？* —— ✅ 答："version 是预留字段从未参与比对；库存扣减的本质是'条件判断+扣减'一步完成，WHERE stock>=? 已经是 CAS，加 version 反而多一次无意义的比对。口径上我不说乐观锁，说条件更新。"
2. *高并发下行锁会不会成瓶颈？* —— ✅ 答："会——单行热点确实串行。所以我在限量发售场景前面加了 Redis Lua 预减挡 99% 流量（如果写了 B 组），DB 只接少数真实请求。锁是正确性兜底，不是性能手段。"
3. *为什么不先 SELECT 再 UPDATE？* —— ✅ 答："先查后改两步之间会被并发插队（[TOCTOU](V2内容社区与RAG详解.md#4.1%20先撞南墙：狂点红心会发生什么)），两个请求都查到 stock=1 都往下走。条件更新把判断焊进一条 SQL，输家拿 0 行。"
**30 秒答案**：扣库存 = 一条 `UPDATE sku SET stock=stock-? WHERE id=? AND stock>=?`，返回 0 行即失败。并发裁决交给数据库原子性；热点场景前面挡 Redis。

### A2. 唯一索引幂等（点赞/限购/防重复三连）
> 📖 深挖 10 问：[跳转到「A2」](简历语句技术深挖笔记.md#A2) ← 被追问答不上就点这里
**简历语句**：点赞/收藏采用[唯一索引幂等](V2内容社区与RAG详解.md#4.2%20对策：唯一索引兜底，不靠代码)方案（uk(note_id,user_id)），并发重复请求由数据库唯一约束兜底，捕获 DuplicateKeyException 静默处理；限量发售"一人一单"同理由 uk(activity_id,user_id) 硬保证。
**项目锚点**：`NoteServiceImpl.like` 的 try-catch；`t_drop_record`。
**必被追问**：
1. *为什么不用"先查后插"？* —— 同 [A1](简历语句技术深挖笔记.md#A1) 的 TOCTOU，两次插入都通过了检查。
2. *捕获异常后返回什么？* —— 点赞返回成功（幂等=重复即已达成）；限购返回业务失败"每人限购一次"。**同一个异常两种业务语义**——这个对比主动讲是加分项。
**30 秒答案**：唯一索引是成本最低的幂等——并发第二名的 INSERT 直接撞约束，catch 当"已处理"。语义分两种：状态确认型静默、业务限制型报错。

### A3. 游标分页（深分页标准答案）
> 📖 深挖 10 问：[跳转到「A3」](简历语句技术深挖笔记.md#A3) ← 被追问答不上就点这里
**简历语句**：社区 Feed 流采用游标分页替代 OFFSET 深分页，WHERE id < cursor 走索引定位，翻页开销与深度无关；热度排序使用（hot_score, id）[二元组游标](V2内容社区与RAG详解.md#2.3%20二元组游标：本项目最容易讲错/写错的细节)解决排序键并列的漏数据问题。
**必被追问**：
1. *LIMIT 100000,20 为什么慢？* —— 扫描并丢弃前 10 万行，惩罚深度用户。
2. *为什么要二元组？* —— hot_score 会并列，只比 score 漏掉同分笔记；补唯一键 id 保证全序，"下一个"才有定义。
3. *[游标分页](V2内容社区与RAG详解.md#2.2%20游标分页：把"第几页"换成"从哪继续")的代价？* —— 不能跳页。Feed 连续翻无所谓，后台管理要跳页用 offset——按场景选型，主动说这个边界加分。
**30 秒答案**：OFFSET 是"数到第 N 页"，游标是"从上次的位置继续"；排序键可能并列时游标补唯一键成二元组；代价是不能跳页。

### A4. 索引设计（你已会的知识挂上项目现场）
> 📖 深挖 10 问：[跳转到「A4」](简历语句技术深挖笔记.md#A4) ← 被追问答不上就点这里
**简历语句**：核心查询路径的联合索引设计与验证（最左前缀、覆盖索引），如订单列表 (user_id, status, created_at)、笔记全文索引采用 [ngram](V2内容社区与RAG详解.md#3.2%20中文全文检索：ngram%20一站讲透) 分词。
**必被追问**：最左前缀失效场景 / 为什么这个顺序（等值在前、范围在后）——你的八股直接对接现场。
**30 秒答案**：以 (user_id,status,created_at) 为例：先按用户等值过滤、再按状态、最后时间范围+排序——列序由查询模式决定，等值列在前。

### A5. 状态机 CAS 状态流转（一套模式吃四处）
> 📖 深挖 10 问：[跳转到「A5」](简历语句技术深挖笔记.md#A5) ← 被追问答不上就点这里
**简历语句**：订单/支付/审核的状态流转统一采用状态机条件更新（UPDATE ... WHERE status=期望态），与扣库存同一 CAS 模式解决并发竞争（如"支付回调 vs 超时取消"竞争同一订单）。
**必被追问**：*支付和取消同时到达怎么办？* —— 两边闸门都是 PENDING_PAY，只放一个过；取消赢支付来 → 支付单 PAID 但订单 CANCELLED → 记 ERROR 走退款——**资金状态与业务状态分开记账**。
**30 秒答案**：所有状态流转都是带 WHERE status=旧值的条件更新，输家 0 行。一个模式解决扣库存/审核/取消/支付四类并发——这是我对 CAS 的项目级理解。

### A6. Flyway（低风险加分，随便问不深）
> 📖 深挖 10 问：[跳转到「A6」](简历语句技术深挖笔记.md#A6) ← 被追问答不上就点这里
**简历语句**：数据库结构纳入 [Flyway](V1.5工程基建详解.md#1.2%20Flyway%20怎么工作（5%20分钟原理）) 版本管理（含存量库 baseline 接入），迁移只增不改保证环境一致性，克隆即得同构库。
**必被追问**：*改了已执行的迁移会怎样？* —— [checksum](V1.5工程基建详解.md#1.4%20迁移纪律（团队约定）) 校验失败拒绝启动，保护历史不可篡改；改结构=新增迁移。
**30 秒答案**：SQL 按版本有序执行并记录 checksum，启动即校验；存量库用 [baseline-on-migrate](V1.5工程基建详解.md#1.3%20本项目的接入：一个关键决策（面试高频）)+幂等首迁移接管。

---

## B. Redis 域【★★★ 缓存三兄弟已会；Lua/锁突击半小时】

> 你有 Redis 基础+项目全套真实实现，V3 详解就是你的复习资料。**注意：每条都要能回答"Redis 挂了怎么办"**——这是本项目最独特的答案（可降级门面）。

### B1. 缓存三兄弟（必写，八股对接项目）
> 📖 深挖 10 问：[跳转到「B1」](简历语句技术深挖笔记.md#B1) ← 被追问答不上就点这里
**简历语句**：商品详情缓存完整落地缓存三大问题：穿透用空值缓存（短 TTL）、击穿用 SET NX [互斥锁重建](V3支付与Redis详解.md#3.4%20老二·击穿：热点%20key%20过期的瞬间)、雪崩用 TTL 随机抖动；读路径命中率 95%+ 时 DB 压力降为 1/20。
**必被追问**（三兄弟是追问重灾区，逐个备好）：
1. *空值 TTL 为什么短？* —— 商品可能上架，[空值缓存](V3支付与Redis详解.md#3.3%20老大·穿透：查一个根本不存在的%20id) 30 分钟=上架后半小时查不到；2 分钟是挡刷与防脏读的平衡。
2. *互斥锁 TTL 多少？锁着的人挂了？* —— 3 秒防死锁；赢家超时锁自动释放，最坏退化成无锁不会错。
3. *没抢到锁的请求怎么办？* —— sleep 50ms 重读缓存，仍读不到直接查库兜底——缓存不能决定可用性。
4. *Redis 挂了这些全废？* —— ✅ 王牌答案："所有 Redis 调用收敛在一个可降级门面（safe 包装），异常→WARN→兜底值→业务自然走 DB——挂了变慢不挂掉。"**这是别人简历没有的**。
**30 秒答案**：穿透=不存在也缓存（短TTL）；击穿=热点过期瞬间只放一个线程重建（SET NX+TTL，输家等待后兜底查库）；雪崩=TTL 加随机抖动打散失效点。

### B2. Redis 预减 + MQ 异步下单削峰（并发性能的招牌，★★突击半小时）
> 📖 深挖 10 问：[跳转到「B2」](简历语句技术深挖笔记.md#B2) ← 被追问答不上就点这里
**简历语句**：限量发售采用经典「Redis 预减 + MQ 异步下单削峰」：HTTP 线程 Lua 原子预减（判重+扣减同一脚本）后投递 RabbitMQ、毫秒级返回受理，消费端事务建单（DB 行锁 CAS 防超卖 + uk 一人一单），失败路径对称回补；前端轮询结果接口收敛异步体验。
**必被追问**：
1. *预减和削峰不是一回事？* —— ✅ 王牌答案："预减挡的是'量'（准入闸门，放行消息数≤库存，抢完洪峰在 Redis 纯内存被拒绝）；MQ 摊的是'峰'（节奏器，DB 行锁不被万级请求争抢）——**两件事缺一不可**。"
2. *为什么 buy 不返回订单号？* —— 订单由消费者异步创建还不存在——返回 QUEUED，前端轮询 result 拿 SUCCESS(orderId)——秒杀类系统的标准接口形态。
3. *为什么 Lua 不用分布式锁？* —— 判断+扣减是简单原子计数，Lua 一次网络往返完成；锁要两次往返还全局串行化——锁适合复杂临界区，计数用 Lua。
4. *消费失败怎么办？* —— 三分流：撞 uk 幂等跳过+回补、业务失败回补+失败标记、未知异常进死信队列；消息彻底丢失由定时补偿任务兜底。
5. *回补失败怎么办？* —— Redis 恰好挂了则靠 warmUp 重灌+活动后对账，**最终一致**。
**30 秒答案**：HTTP 线程只做 Lua 原子预减（判重+扣减一发入魂）+投 MQ；消费端匀速建单（CAS+uk）；预减是准入闸门挡量，MQ 是节奏器削峰；失败对称回补，死信+补偿兜底。

### B3. 分布式锁（★★突击半小时，与 B2 同族）
> 📖 深挖 10 问：[跳转到「B3」](简历语句技术深挖笔记.md#B3) ← 被追问答不上就点这里
**简历语句**：分布式锁采用 [SET NX PX](V3支付与Redis详解.md#3.4%20老二·击穿：热点%20key%20过期的瞬间) 原子加锁（UUID 标识持有者）+ Lua 校验 value 释放，防止锁误删与死锁。
**必被追问**：
1. *为什么 value 存 UUID？* —— A 的锁超时释放后 A 执行 finally 会删掉 B 的锁——释放前校验"是我的"。
2. *为什么释放也要 Lua？* —— GET 比对+DEL 两步之间锁可能易主。
3. *锁过期业务没跑完？* —— Redisson [看门狗](V3支付与Redis详解【知识词典】.md#看门狗)自动续期（知道名字与原理即可）。
**30 秒答案**：加锁 SET NX PX 一条命令（互斥+防死锁）；释放 Lua 校验 value（防误删）；续期上 Redisson 看门狗。

### B4. 限流（★★，小而美）
> 📖 深挖 10 问：[跳转到「B4」](简历语句技术深挖笔记.md#B4) ← 被追问答不上就点这里
**简历语句**：AI 接口基于 Redis 固定窗口限流（Lua 原子 INCR+EXPIRE），按 token 成本与线程占用设计阈值；Redis 不可用时放行（[fail-open](V3支付与Redis详解.md#2.3%20RedisOps%20门面：把"降级"收敛到一处（本章核心）)——限流是成本保护非可用性核心）。
**必被追问**：*[固定窗口](V3支付与Redis详解.md#10.2%20实现：固定窗口%20+%20Lua%20原子化)的缺陷？* —— 窗口边界 2 倍突刺，防刷够用；严格上滑动窗口/令牌桶。*为什么挂了放行不拒绝？* —— 拒绝所有用户=自己制造全站故障，多花钱优于不可用。
**30 秒答案**：INCR 首次设 EXPIRE 的 Lua 原子窗口计数；缺陷是边界突刺；降级方向按组件语义选——限流 fail-open。

### B4.5 热门榜 zset（★★，简历 C 有，必被问"一致性"）
> 📖 深挖：[跳转到「P13 热门榜与限流」](简历/简历C-超链学习目录/二级文件/P13-热门榜与限流.md) ← 被追问答不上就点这里
**简历语句**：热门笔记榜用 Redis zset 维护综合热度（点赞×3 + 收藏×5 + 浏览×1），**增量刷新 + 定时全量重建双轨**；榜单只存 id 与分数，展示时 **IN 批量回查明细，避免 N+1**。
**必被追问**：
1. *为什么用 zset 不用数据库 ORDER BY？* —— 榜单是"高频读、按分数取 Top N"——zset 的 `ZREVRANGE` 是 O(log N + M)，数据库每次都要全表算分再排序，读一次贵一次。
2. *为什么要有"定时全量重建"？增量刷新不够吗？* —— ✅ 王牌答案："增量只保证'变化那一刻'正确——**帖子被删、被审核下架、点赞被取消，这些'减法'增量容易漏**；全量重建是每天一次的对账，把增量累积的偏差抹平。"
3. *两个轨道不会打架吗？* —— 重建时写临时 key，`RENAME` 原子替换，读方永远看到一个完整榜单、不会读到半成品。
4. *为什么展示还要回查数据库？* —— zset 只有 id+分数，标题/封面/作者都在 MySQL——`ZREVRANGE` 拿到 id 列表后**一次 `IN` 查回**（而不是循环单查，否则 20 条就是 20 次往返 = 典型 N+1）。
5. *量大了怎么办？* —— 分页用 `ZREVRANGE start end`；再大就按小时分桶、或榜单只做人气 Top 200 剩下的走搜索。
**⚠️ 诚实边界（被追问到必须承认）**：当前实现有个**已知缺陷**——增量刷新不限 1000 条、全量重建却限 1000 条，两者口径不一致会导致**榜单边缘抖动**（第 1000 名附近的条目时而出现时而消失）。这是我已知待修的项，不要硬吹"完全正确"。
**30 秒答案**：zset 存 id+分数，ZREVRANGE 取 Top N；增量刷"变化"、定时全量重建抹平减法漏算；只存 id 展示时 IN 批查明细避免 N+1；已知缺陷是增量/全量的 1000 条口径不一致会抖动。

### B5. 缓存一致性（★★★，八股+项目双对接）
> 📖 深挖 10 问：[跳转到「B5」](简历语句技术深挖笔记.md#B5) ← 被追问答不上就点这里
**简历语句**：缓存更新采用"先更新数据库、再删除缓存"（[Cache-Aside](V3支付与Redis详解.md#3.6%20缓存与%20DB%20的一致性：先更库再删缓存)）策略，接受秒级不一致换取最终一致。
**必被追问**：*为什么删不更？为什么先库后缓存？* —— 更新缓存在并发写下顺序会颠倒（旧值复活）；先删缓存两步之间读请求会载回旧值。*怎么处理删除失败？* —— 我项目量级用 TTL 兜底；生产级延迟双删/Canal 订阅 binlog。
**30 秒答案**：删是幂等的、更不是；先库后缓存的不一致窗口最小；追求强一致就别用缓存。

---

## C. RabbitMQ 域【★★★ 你有 AMQP 基础，DLX 背半小时】

### C1. 异步解耦（你有基础，直接写）
> 📖 深挖 10 问：[跳转到「C1」](简历语句技术深挖笔记.md#C1) ← 被追问答不上就点这里
**简历语句**：AI 内容审核异步化：发布接口秒回（状态=审核中），审核经 RabbitMQ 异步处理——发布 RT 与审核耗时彻底解耦；MQ 不可用时自动降级线程池执行，体验一致。
**必被追问**：
1. *为什么用 MQ 不用线程池？* —— 线程池丢进程内存（重启即丢），MQ 持久化+确认机制保证不丢；且消费速率可独立于生产。
2. *消息怎么不丢？* —— 发送端 [publisher-confirm](V2内容社区与RAG详解.md#9.1%20为什么这个设计"企业味"足：先算一笔体验账) + 队列/消息持久化 + 消费端处理完才 ACK——三段缺一段都可能丢。
3. *消费者挂了消息呢？* —— 未 ACK 消息重投（at-least-once）——所以消费端必须幂等（接[C2](简历语句技术深挖笔记.md#C2)）。
**30 秒答案**：秒级耗时的审核挂起发布请求是体验灾难；MQ 的持久化+确认换来可靠性，降级路径线程池保可用。

### C2. 消费端幂等（★★★ 与 A2 呼应，模式复用）
> 📖 深挖 10 问：[跳转到「C2」](简历语句技术深挖笔记.md#C2) ← 被追问答不上就点这里
**简历语句**：消费端[幂等三防线](V2内容社区与RAG详解.md#9.3%20消费端：幂等三防线（本章核心）)：流水表版本唯一键（INSERT IGNORE 挡重投）+ 笔记状态机条件更新（挡并发）+ AI 失败只记 ERROR 不误杀（宁可不作为，不可乱作为）。
**必被追问**：*为什么 MQ 会重复投递？* —— 消费者处理完、ACK 前崩溃，重启后重投——重复是协议特性不是 bug。
**30 秒答案**：at-least-once 语义下重复是常态；幂等与点赞/限购同一套"唯一索引+条件更新"，一个模式全项目复用。

### C3. TTL+DLX 延迟消息（★★ 背半小时，高频考点）
> 📖 深挖 10 问：[跳转到「C3」](简历语句技术深挖笔记.md#C3) ← 被追问答不上就点这里
**简历语句**：订单 30 分钟未支付自动关闭：TTL+死信队列零插件实现延迟消息（无消费者的延迟队列→过期死信→重路由到消费队列），消费端状态机 CAS 保证与支付回调竞争安全。
**必被追问**：
1. *为什么不用定时任务扫表？* —— 平均延迟 30 秒+周期性全表扫描；延迟消息到期即触发零扫描。
2. *[队头阻塞](V3支付与Redis详解.md#8.4%20队头阻塞：为什么用队列级%20TTL)知道吗？* —— RabbitMQ 只检查队首过期——消息级 TTL 各不同会阻塞；我用队列级 TTL（统一 30 分钟）天然免疫。
3. *在事务里发消息行吗？* —— 不行（MQ 不参与 DB 事务，回滚产生幽灵消息）；用 [afterCommit](V3支付与Redis详解.md#8.6%20发送侧：afterCommit（评审修正的反模式）) 提交后发。
**30 秒答案**：消息进无消费者的 TTL 队列"睡觉"，到期成死信被路由到工作队列；队列级 TTL 免疫队头阻塞；afterCommit 防幽灵消息。

### C4. 毒消息处理（★★，体现深度）
> 📖 深挖 10 问：[跳转到「C4」](简历语句技术深挖笔记.md#C4) ← 被追问答不上就点这里
**简历语句**：消费端防[毒消息](V2内容社区与RAG详解.md#9.3%20消费端：幂等三防线（本章核心）)：必然失败的消息吞异常 ACK 不重入队（无限重试只会打爆日志），失败由 ERROR 流水+死信队列收容，"失败可被发现"优于"失败无限重试"。
**30 秒答案**：确定性失败（服务挂了）重试一万次也不会成功；能重试的是瞬时故障，分清这两类是消费端设计的成熟度。

---

## D. Spring 与工程域【★★★ 你的强项区】

### D1. 事务与 AOP（你研究过 CGLIB 三模式，降维打击）
> 📖 深挖 10 问：[跳转到「D1」](简历语句技术深挖笔记.md#D1) ← 被追问答不上就点这里
**简历语句**：深入理解 Spring 事务的代理机制——项目中规避自调用失效（@Transactional 同类自调用不走代理），审核消费端、延迟消息发送等场景均验证过代理边界。
**必被追问**（这是你纠正过我的领域，放心迎战）：
1. *@Transactional 自调用为什么失效？* —— this 调用绕过代理对象，增强逻辑（开事务）没机会执行。
2. *怎么救？* —— 注入自身代理/拆到另一个 Bean/AopContext。
3. *@Configuration 自调用为什么又不失效？* —— 容器级 CGLIB 增强（invokeSuper+getBean 保证单例），与 AOP 代理是两个子系统——能讲清这个区别的候选人极少。
**30 秒答案**：事务靠 AOP 代理，自调用 this 绕过代理；@Configuration 是容器级 CGLIB，拦截的是 getBean 语义——同是"自调用"，两个子系统两套结论。

### D2. 线程池（你深入过并发，直接写）
> 📖 深挖 10 问：[跳转到「D2」](简历语句技术深挖笔记.md#D2) ← 被追问答不上就点这里
**简历语句**：显式配置有界线程池（核心4/最大16/队列200）+ CallerRuns 反压策略 + 优雅停机，拒绝 Boot 默认无界队列的 OOM 隐患；[TaskDecorator](V1.5工程基建详解.md#2.4%20坑一：异步线程里%20traceId%20丢了（本章最重）) 解决 MDC 跨线程传递。
**必被追问**：
1. *为什么 [CallerRuns](V1.5工程基建详解.md#4.2%20AsyncConfig%20逐参数精读)？* —— 队列满让提交线程自己跑=反压：上游自然减速；不是失败是限速。
2. *参数怎么定？* —— IO 密集核数×2~4 起步压测调；4/16 的依据是任务画像（调AI/查库都在等 IO）。
3. *Boot 默认有什么问题？* —— 队列 Integer.MAX_VALUE 无界——永不拒绝、无声堆积、OOM 前夜。
4. *[MDC](V1.5工程基建详解.md#2.1%20问题驱动：日志为什么"串不起来") 为什么要 TaskDecorator？* —— ThreadLocal 线程隔离，池线程看不见主线程的 traceId；提交时快照、执行时设置、完成时恢复。
**30 秒答案**：有界+[反压](V1.5工程基建详解.md#4.2%20AsyncConfig%20逐参数精读)+优雅停机三件套；TaskDecorator 三时点传递 traceId；池化线程离开时要还干净的储物柜（防串号）。

### D3. 全链路日志（MDC 实战，好讲好懂）
> 📖 深挖 10 问：[跳转到「D3」](简历语句技术深挖笔记.md#D3) ← 被追问答不上就点这里
**简历语句**：基于 Filter+MDC+TaskDecorator 构建 traceId 全链路日志（三时点传递防串号），服务化后网关生成透传，一次 grep 跨三进程还原完整请求链路。
**30 秒答案**：Filter 入口生成存 MDC（finally 清理防线程复用串号）→ pattern %X 输出 → TaskDecorator 带进池线程 → 网关透传跨服务。

### D4. 声明式 HTTP 与可插拔设计（★突击半天=微服务敲门砖，也可只当 Spring 用法写）
> 📖 深挖 10 问：[跳转到「D4」](简历语句技术深挖笔记.md#D4) ← 被追问答不上就点这里
**简历语句**：统一抽象 + 多实现按配置装配的[可插拔](V1.5工程基建详解.md#0.3%20设计总原则：可降级、可插拔)设计贯穿项目（存储 local/MinIO、AI 本地/远程、支付 MOCK/沙箱），架构演进时改配置不改代码。
**必被追问**：*什么模式？* —— 策略模式+条件装配（@Qualifier 按配置二选一注入）；价值是"新环境零依赖可跑、升级零业务改动"。
**30 秒答案**：三处同款设计（storage/embedding/pay）串成我的设计哲学：抽象先行，实现可换，降级有路。

### D5. 单元测试（低风险）
> 📖 深挖 10 问：[跳转到「D5」](简历语句技术深挖笔记.md#D5) ← 被追问答不上就点这里
**简历语句**：核心资损路径（扣库存/越权/支付）单元测试覆盖，[mockStatic](V1.5工程基建详解.md#5.2%20关键技巧：mockStatic%20钉住%20StpUtil) 钉住登录态实现纯内存测试（CI 无需数据库），测试按"事故代价"分级而非追覆盖率。
**30 秒答案**：先测钱和门再测其它；mockStatic 限定 try-with-resources 作用域（静态 mock 是全局副作用）。

---

## E. 微服务域【★★ 全部突击半天可写——V4 详解就是突击教材】

> 提示：这组的语句**每一分钟都被 V4 详解+词典覆盖**，突击效率极高。建议至少拿下 E1+E2+E3（面试问微服务 90% 从"为什么拆/熔断/注册发现"开始）。

### E1. 服务拆分决策（微服务面试第一问）
> 📖 深挖 10 问：[跳转到「E1」](简历语句技术深挖笔记.md#E1) ← 被追问答不上就点这里
**简历语句**：主导服务化拆分决策：仅将 AI 能力独立成服务（耗时差异/故障隔离/密钥隔离/独立扩容/模型可替换五条业务理由），用户/商品/订单因事务耦合保留单体——避免为拆而拆引入[分布式事务](V4服务化详解【知识词典】.md#分布式事务为什么不做)。
**必被追问**：*[为什么不全拆](V4服务化详解.md#2.4%20反向论证（面试加分项原话）)？* —— 下单=同事务锁库存+建订单+清购物车，拆开要 Seata/本地消息表，当前量级不划算；**能讲"不做什么"比堆五个服务高级**。
**30 秒答案**：拆分按"耗时/故障/伸缩/安全属性是否与众不同"判断——AI 五条全占所以拆；业务域事务紧所以留。

### E2. 熔断（★突击 40 分钟，面试必问词）
> 📖 深挖 10 问：[跳转到「E2」](简历语句技术深挖笔记.md#E2) ← 被追问答不上就点这里
**简历语句**：AI 服务调用接入 Resilience4j 熔断（滑动窗口10/失败率50%跳闸/30s后半开放行试探），故障时毫秒级降级返回兜底文案，兑现"AI 故障不影响商城主体"的隔离承诺。
**必被追问**：
1. *三态怎么流转？* —— CLOSED 统计→超阈值 OPEN 快速失败→30s 后 [HALF-OPEN](V4服务化详解.md#7.2%20三态机（面试必背，能画出来）) 放 3 个试探→全过则闭合。
2. *熔断和重试区别？* —— 重试加压对端、熔断保护双方；偶发抖动重试、持续故障熔断。
3. *不熔断会怎样？* —— 每请求等满 120s 超时，线程池被"等死"的请求占满，[级联故障](V4服务化详解.md#7.1%20零基础：家里的保险丝)全站瘫痪。
4. *（真实踩坑）配置踩过什么坑？* —— Resilience4j 配置必须顶层 [resilience4j](V4服务化详解.md#7.4%20配置（以及那个真实踩过的坑）):，我实际踩过挂 spring 下静默失效（不报错参数全默认）——**讲自己踩的坑永远比背八股可信**。
**30 秒答案**：三态机+自动恢复；熔断是"先别试"保护双方；[fallback](V4服务化详解.md#7.3%20代码接入（两行注解%20+%20一个降级方法）) 返回文案必须稳如老狗（不再调任何可能失败的东西）。

### E3. 注册发现（★突击 40 分钟）
> 📖 深挖 10 问：[跳转到「E3」](简历语句技术深挖笔记.md#E3) ← 被追问答不上就点这里
**简历语句**：基于 DiscoveryClient 抽象实现服务发现——演示环境 [SimpleDiscoveryClient](V4服务化详解.md#5.3%20本项目的三层实现（面试最有讲究的部分）) 静态实例零依赖，生产切 Nacos 仅改配置；多网卡环境显式指定注册 IP 规避虚拟网卡陷阱。
**必被追问**：*[lb://](V4服务化详解.md#5.2%20`lb://`%20的完整含义（拆开讲）) 是什么？* —— 按服务名查实例列表→负载均衡挑一个→真实地址转发；逻辑地址与物理地址解耦。*为什么两个实现？* —— 抽象与实现分离，注册中心挂了静态兜底照常转发。
**30 秒答案**：服务名是逻辑地址；[DiscoveryClient](V4服务化详解.md#5.3%20本项目的三层实现（面试最有讲究的部分）) 是标准抽象，Nacos/静态只是实现——换实现零改代码。

### E4. 网关（★突击 30 分钟）
> 📖 深挖 10 问：[跳转到「E4」](简历语句技术深挖笔记.md#E4) ← 被追问答不上就点这里
**简历语句**：Spring Cloud Gateway 统一入口：路由转发、内部端点（/internal/**）网关层拒绝（404 最小信息披露）、traceId 生成透传、跨域集中处理。
**30 秒答案**：网关三价值=一个门牌+安全边界+横切集中；拒 404 不拒 403 是不给探测者确认端点存在。

### E5. 服务间鉴权（★突击 30 分钟，安全意识加分）
> 📖 深挖 10 问：[跳转到「E5」](简历语句技术深挖笔记.md#E5) ← 被追问答不上就点这里
**简历语句**：服务间调用 HMAC 签名鉴权（client+timestamp+secret）+ 5 分钟时间窗防重放 + [constant-time](V3支付与Redis详解.md#5.4%20时序攻击：为什么不用%20equals) 比较——"内网裸奔"是错觉，鉴权不建立在环境假设上。
**必被追问**：*为什么时间戳参与签名？* —— 防截获重放：改 ts 签名失效，过期 ts 拒绝。*为什么内网也要鉴权？* —— 端口误映射/横向渗透即裸奔；API 额度即资损。
**30 秒答案**：三件套头+时间窗；与支付回调验签同算法家族——对称密钥按信任结构选（内部共享密钥 vs 跨组织 [RSA2](V3支付与Redis详解.md#5.5%20HMAC%20vs%20RSA2：对称与非对称)）。

---

## F. AI/RAG 域【★★★ 有自写 109KB 文档加持——AI 岗位必写，普通后端岗选 1-2 条】

### F1. Function Calling 双工具（AI 岗王牌）
> 📖 深挖 10 问：[跳转到「F1」](简历语句技术深挖笔记.md#F1) ← 被追问答不上就点这里
**简历语句**：基于 Spring AI Function Calling 实现智能导购：[双工具](V2内容社区与RAG详解.md#8.1%20从"单手"到"双手")（商品 SQL 检索/知识库 RAG 检索）由模型按问题语义自选路由，代码零意图判断；工具描述正向（何时调）+负向（何时不调）双重引导压制幻觉。
**必被追问**：*为什么不用 [if-else](V2内容社区与RAG详解.md#8.2%20为什么不写%20if-else（核心思想）) 规则路由？* —— 自然语言意图无法枚举（"贵吗/预算内/值不值"都是价格意图）；模型语义理解就是路由器。*模型乱调工具？* —— description 写清边界+参数全防御（null/0/负数兜底）。
**30 秒答案**：把两个工具的"说明书"发给模型让它自己选——分流发生在模型侧；规则路由永远挂一漏万。

### F2. Hybrid RAG（AI 岗王牌）
> 📖 深挖 10 问：[跳转到「F2」](简历语句技术深挖笔记.md#F2) ← 被追问答不上就点这里
**简历语句**：Hybrid RAG 检索架构：BM25 关键词通道+向量语义通道并行召回、[RRF](V2内容社区与RAG详解.md#7.3%20RRF：一行公式与三个为什么) 排名融合（免量纲+双确认加分）；语料=官方说明书+UGC 笔记双语料，UGC 答官方答不了的体验类问题。
**必被追问**：*为什么要两路？* —— 精确词（型号/IPX7）向量抓不住、模糊问法（"戴久了舒服吗"）关键词搜不到——电商两类都高频。*RRF 为什么用排名不用分数？* —— BM25 可上百、余弦∈[0,1]，量纲不可加；排名天然免疫。*为什么不[塞长上下文](V2内容社区与RAG详解.md#5.3%20必考题："几百份文档为什么不塞长上下文")？* —— 语料动态/首字延迟/权限隔离/成本，四条背熟。
**30 秒答案**：先查资料再回答（开卷考试）；双通道互补+RRF 融合；边界清晰——价格库存走 SQL 绝不走 RAG。

### F3. SSE 流式（AI 岗标配）
> 📖 深挖 10 问：[跳转到「F3」](简历语句技术深挖笔记.md#F3) ← 被追问答不上就点这里
**简历语句**：AI 问答 SSE 流式输出（打字机效果）：后端 Flux 流式转发模型 token，前端 fetch 手撕 SSE 解析；会话历史持久化（两表）支持多轮上下文。
**30 秒答案**：C 端首字延迟决定体验——流式把 30 秒等待变成逐字出现；SSE 是单向长连接上的 data: 事件流。

### F4. AI 内容安全（体现工程判断）
> 📖 深挖 10 问：[跳转到「F4」](简历语句技术深挖笔记.md#F4) ← 被追问答不上就点这里
**简历语句**：AI 审核采用"先发后审"（发布即入队、过审才公开）+[宁漏勿杀](V2内容社区与RAG详解.md#9.5%20审核提示词：三个温度的第三副面孔)（拿不准放行、AI 失败仅记 ERROR 停留人工）+流水留痕可翻案；AI 文案仅出草稿绝不直写业务数据。
**30 秒答案**：审核系统宁可不作为不可乱作为——误杀正常用户的伤害大于漏一条软广。

---

## G. 支付域【★★ HMAC 突击半小时；幂等部分你会】

### G1. 支付回调全链路（挑会的部分写）
> 📖 深挖 10 问：[跳转到「G1」](简历语句技术深挖笔记.md#G1) ← 被追问答不上就点这里
**简历语句**（可裁剪版）：支付回调处理链路：验签（[HMAC-SHA256](V3支付与Redis详解.md#5.2%20从哈希到%20HMAC：三步演进（0%20基础）) 签名校验+constant-time 比较）→幂等三层（支付单唯一索引/已付快路径/状态机 CAS）→金额核对（缺失即拒）→订单状态机流转，与超时取消竞争场景由 CAS 闸门裁决。
**必被追问**：*验签失败为什么必须拒？* —— 回调是公网端点，不验签=向全世界开放改单入口。*重复回调？* —— 必然发生（对方重试 8 次+），已付必须回 success 否则永远重发。*为什么回调不用 token？* —— 第三方没有你的用户 token；验签答"消息是谁发的"，认证答"你是谁"——两套身份模型。
**30 秒答案**：六步链路每步一条红线；幂等三层=物理唯一+快路径+CAS；重复是常态幂等是入场券。

---

# 第三部分：三套成品模板（直接抄，再按掌握度删）

## 模板A：后端基础版（全部★★★，零风险，适合投递面广）

**项目介绍**：版本A介绍段。
**核心技术栈**：SpringBoot 3 / MyBatis / MySQL 8 / Redis / RabbitMQ / Sa-Token
**项目亮点**（6 条）：
1. A1 行锁 CAS 扣库存
2. A2 唯一索引幂等（+A5 状态机 CAS 合并：状态流转统一条件更新模式）
3. B1 缓存三兄弟（+B5 [先更库再删缓存](V3支付与Redis详解.md#3.6%20缓存与%20DB%20的一致性：先更库再删缓存)合并）
4. C1 审核异步化（+C2 消费端幂等合并）
5. C3 [TTL+DLX](V3支付与Redis详解.md#8.3%20TTL%20+%20DLX：让消息"睡"30%20分钟) 延迟取消
6. D2 有界线程池+反压（+D3 traceId 合并）

## 模板B：微服务版（A + E1/E2/E3 突击半天，适合中大厂）

**在模板A基础上追加**：
7. [E1](简历语句技术深挖笔记.md#E1) 服务拆分决策（只拆 AI、[五条理由](V4服务化详解.md#2.2%20拆%20AI%20的五条理由（面试逐条背）)）
8. [E2](简历语句技术深挖笔记.md#E2) Resilience4j 熔断三态+降级
9. [E3](简历语句技术深挖笔记.md#E3) 注册发现抽象+[E4](简历语句技术深挖笔记.md#E4) 网关安全边界（合并）

## 模板C：AI 应用版（面向 AI Agent/应用岗，你的文档是护城河）

**项目介绍**：版本C介绍段。
**核心技术栈**：SpringBoot 3 / Spring AI / MySQL / Redis / RabbitMQ / Vue3
**项目亮点**：
1. F1 Function Calling 双工具路由
2. F2 Hybrid RAG+RRF+双语料
3. F3 SSE 流式+会话持久化
4. C1+C2 AI 审核异步化+幂等
5. F4 AI 内容安全价值观
6. A1 或 B1 挑一条证明后端功底

---

# 第四部分：面试战术（考前看）

## 4.1 万能开场（项目自述 60 秒，背熟）

> "这是一个 AI 种草商城，对标小红书加得物：内容社区负责种草（发布/审核/Feed 流），电商负责拔草（购物车/订单/支付/限量发售），AI 导购衔接两端。我按业务演进做技术：V1 单体闭环、V2 内容社区+RAG、V3 支付+Redis、V4 只拆了 AI 服务——每一步都有'为什么这么做'和'为什么不那么做'。"

**最后一句是钩子**——面试官大概率顺势问"为什么不拆其它"，你就进了自己最熟的领域。

## 4.2 "你遇到最难的问题"（用真实踩坑回答）

排序（按可信度与深度）：
1. **熔断配置静默失效**——配置挂错层级不报错全默认，启动日志核对才发现（讲完加一句"从此我改完配置必做两件事：parse 校验+启动日志验证参数"——展示的是方法论）
2. **写了没生效三连**——缓存服务没接线/抽象没调用方，"评审第一动作永远是 grep 调用点"
3. **[多网卡](V4服务化详解.md#5.4%20真实踩坑：多网卡机器的注册%20IP（本机实测）)注册错 IP**——VMware 网卡被自动选中，"自动检测在复杂环境是隐形炸弹"

## 4.3 反问环节备弹药

- "团队的服务化处于什么阶段？拆分的痛点主要在哪？"（微服务版）
- "AI 能力是自建还是接平台？RAG 语料治理谁负责？"（AI 版）

## 4.4 红线自查（口径纪律，来自项目真实约定）

- ❌ 不说"乐观锁扣库存"——说**条件更新/CAS 思想**（version 从未参与比对）
- ❌ 不说"RAG 查价格"——**价格库存走 SQL，说明书与体验走 RAG**
- ❌ 微服务被问"为什么不全拆"时**不支吾**——事务耦合+分布式事务不划算，一句话顶回去
- ❌ 不会的技术点当场承认+给学习路径——"这块我没深入，我的理解是…计划…"远好于编造

---

> 最后提醒：**挑完语句后，把每条的"30秒答案"连读三遍**——写进简历的每一条，都应该能在被点名时 30 秒内给出锚定真实代码的回答。这套材料的全部价值就在于此：**每句话都有现场**。
