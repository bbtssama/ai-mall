# V2 内容社区 + Hybrid RAG 详解：让项目名副其实

> ai-mall 项目升级讲解文档 · V2（内容社区 + AI 审核 + Hybrid RAG + 种草文案）
> 配套词典：`V2内容社区与RAG详解【知识词典】.md`
> 前置阅读：`V1.5工程基建详解.md`（Flyway/traceId/对象存储/线程池——V2 全程复用）

---

## 【易错关键点】（先背这个）

1. **RAG 只管"文档"不管"数据"**——价格/库存走 SQL（searchProduct），说明书/用户体验走 RAG（searchDocs）。说反了就是原理性错误。
2. **"文档才几百份为什么不塞长上下文"必考**——四条标准答案：语料动态增长 / C 端首字延迟 / 权限隔离 / 成本。
3. **Hybrid 不是噱头是刚需**——"蓝牙5.3""IPX7"这类精确词向量检索抓不住（BM25 的主场）；"戴久了舒服吗"这种说法 BM25 搜不到（向量的主场）。电商两类问题都高频，必须双路。
4. **RRF 用"排名"不用"分数"**——BM25 分可以上百、余弦 ∈ [0,1]，量纲不同不能直接加权；RRF 天然免疫量纲，且双路命中的片段自动加分（双确认）。
5. **点赞幂等靠唯一索引，不是先查后插**——`uk(note_id,user_id)` 撞键抛 DuplicateKeyException 当"已点过"处理；先查后插在并发下会插两条。
6. **发布秒回、审核异步**——AI 审核要几秒，用户点发布必须立刻看到结果；这是 MQ 的第一个真实业务理由，不是"为了用 MQ"。
7. **消费端防"毒消息循环"**——必然失败的消息无限重试只会打爆日志；本项目消费端吞异常 ACK，失败由 ERROR 流水+死信队列兜底。
8. **游标分页与排序键必须配套**——按热度排时游标是 `(hot_score, id)` 二元组，只用 id 会漏数据。
9. **MySQL 中文全文检索必须 `WITH PARSER ngram`**——默认解析器按空格分词，中文整句是一个词，检索全失效。
10. **AI 文案只出草稿绝不自动发布**——"AI 不直写业务数据"是项目红线；未经审核的 AI 内容直接公开是 UGC 平台事故。

---

## 全书地图

```
第 0 章 V2 为什么长这样 —— 补齐"种草"的灵魂
第 1 章 数据库设计 —— 内容域 10 张表的取舍
第 2 章 Feed 流 —— 游标分页、N+1 攻防、热度公式
第 3 章 点赞收藏 —— 唯一索引幂等、计数不丢失
第 4 章 Hybrid RAG —— 分块、双通道、RRF、Agent 分流（V2 最重章）
第 5 章 AI 审核 + MQ —— 发布秒回、异步回写、幂等三防线、毒消息
第 6 章 AI 种草文案 —— RAG 参与创作的内容飞轮
第 7 章 模拟数据 —— 让 100 商品/500 笔记长得像真的
第 8 章 联调验收 —— 全链路走一遍
```

---

# 第 0 章 V2 为什么长这样

## 0.1 V1 留下的三个"名不副实"

V1 结束时项目叫"AI 种草商城"，但：
- **没有任何种草内容**——"种草"二字无实体承载；
- **MQ 无处落地**——设计文档写了审核异步，但没有内容可审；
- **RAG 无处落地**——没有说明书语料，更没有 UGC 笔记语料。

V2 一句话：**把"种草"变成数据库里的表、页面上的流、AI 嘴里的语料。**

## 0.2 V2 的四件事与它们的互相成就

```
        ┌───────────────────────────────────────────┐
        │            内容社区（地基）                 │
        │   笔记 CRUD / Feed / 点赞收藏 / 搜索        │
        └──────┬──────────────────┬─────────────────┘
               │ 提供审核对象       │ 提供 UGC 语料
               ▼                  ▼
        ┌─────────────┐    ┌──────────────────┐
        │ AI 审核+MQ  │    │  Hybrid RAG       │
        │ （第5章）    │    │ 说明书+笔记双语料  │
        └─────────────┘    └────────┬─────────┘
                                   │ 检索真实体验
                                   ▼
                          ┌──────────────────┐
                          │ AI 种草文案（第6章）│
                          │ UGC→语料→更好UGC   │
                          └──────────────────┘
```

**内容飞轮**（面试可以画在白板上）：
用户发笔记 → AI 审核通过 → 成为 RAG 语料 → AI 导购能答体验类问题 / AI 文案能引用真实体验 → 帮新用户写出更好的笔记 → 更多语料。**每一步都在喂养下一步。**

---

# 第 1 章 数据库设计：内容域 10 张表

> 迁移文件：`V2__content_and_rag.sql`（Flyway 只增不改原则的第一个实践）

## 1.1 表清单与设计理由

| 表 | 作用 | 关键设计 |
|---|---|---|
| `t_note` | 笔记主表 | 状态机 AUDITING→PUBLISHED/REJECTED/OFFLINE；冗余计数；**FULLTEXT ngram** |
| `t_note_image` | 图集 | URL 存 V1.5 对象存储的返回值，sort 排序 |
| `t_note_tag` | 标签 | 独立表（而非 JSON 列）：可索引、可聚合出热门话题 |
| `t_note_product` | 种草清单 | **内容域与电商域的接缝**，笔记变现的载体 |
| `t_note_like` | 点赞 | `uk(note_id,user_id)` **唯一索引=幂等** |
| `t_note_collect` | 收藏 | 与点赞同构；分表是因为业务语义不同（收藏夹/排行榜） |
| `t_user_follow` | 关注 | V3 Feed 关注流用；V2 先建表不建接口（按需演进） |
| `t_audit_record` | 审核流水 | `uk(biz_type,biz_id,biz_version)` 幂等 + AI 结果留痕 |
| `t_knowledge_doc` | RAG 文档头 | `uk(source_type,ref_id)` 同一来源只有一份 |
| `t_knowledge_chunk` | RAG 切片 | embedding 存 JSON；**content 加 ngram 全文索引** |

## 1.2 三个值得讲的决策

**① 冗余计数（like_count 等）为什么放主表**
列表页要展示，每次 `count(*)` 聚合太贵。V2 用同事务 `UPDATE ... SET like_count=like_count+1`（原子，不丢更新）；V3 升级为 Redis 计数+定时落库（热点写的正解）——**先简单正确，再按压力演进**。

**② 审核为什么单独建流水表而不只在笔记上打状态**
AI 判定是概率性的：出错要能翻案（留 ai_result_json）、要能人工复核（reviewer 字段）、要幂等（版本唯一键）。只在主表打状态，出问题就是黑箱。

**③ 向量为什么敢存 MySQL**
当前量级（500 笔记+100 商品 ≈ 数千切片）全量加载内存算余弦是毫秒级。上 pgvector/Milvus 的时机是"切片数上万"——检索层已抽象，届时只换实现类。**在 100 商品上吹 Milvus 才是玩具行为。**

---

# 第 2 章 Feed 流：游标、N+1、热度

## 2.1 游标分页（对比 OFFSET 深分页）

`LIMIT 100000,20` 要先扫描丢弃 10 万行。游标分页：`WHERE ... ORDER BY hot_score DESC, id DESC LIMIT 20`，靠索引直接定位，翻多深都一样快。

**★ 本项目最容易讲错的细节**：游标必须与排序键配套。

```
按最新排：游标 = id            → WHERE id < #{cursorId}
按热度排：游标 = (hot_score, id) → WHERE hot_score < #{h}
                                    OR (hot_score = #{h} AND id < #{cursorId})
```
只用 id 会漏"热度更高但 id 更小"的笔记。**SQL 里这个 OR 条件**（NoteMapper.xml `pageWhere`）就是二元组游标的落地。

**代价**：游标分页不能跳页，只能连续翻——Feed 场景天然如此（一直往下滑），后台管理要跳页就用 offset。**按场景选型，不是谁替代谁。**

## 2.2 N+1 攻防（列表组装）

20 篇笔记 ×（作者+封面+标签+商品+互动态）= 100+ SQL → 全部改批量：

| 数据 | 一次批量 SQL | 组装方式 |
|---|---|---|
| 作者 | 循环 selectById 但 computeIfAbsent 去重（≤20 个用户） | Map<userId, User> |
| 封面 | `selectCoversByNotes`（子查询取每篇 MIN(sort)） | Map<noteId, url> |
| 标签 | `selectTagsByNotes` IN | groupingBy |
| 商品 | `selectProductsByNotes` IN | groupingBy |
| 互动态 | `listLikedNoteIds` / `listCollectedNoteIds` IN | Set contains |

结果：**无论页大小，固定 5 条 SQL**。这是所有列表接口的通用功力，面试常考"列表接口怎么优化"的第一答。

## 2.3 热度公式

```
hot_score = like_count × 3 + collect_count × 5 + view_count × 1
```
收藏权重最高的理由：收藏=强意图（要买/要再看），点赞=弱认可，浏览=免费。互动时重算即时回写；V3 改 Redis zset 排行（`ZINCRBY`+定时同步）。

---

# 第 3 章 点赞收藏：幂等与计数不丢失

## 3.1 幂等：唯一索引兜底，不是先查后插

```java
try {
    likeMapper.insert(noteId, userId);      // 直接 INSERT
    noteMapper.incrLikeCount(noteId);
} catch (DuplicateKeyException e) {
    // 已点过：静默成功（幂等）
}
```

并发下两个"点赞"同时到达：先查后插会插两条（查的时候都还没插）；直接 INSERT 撞 `uk_note_user` 必有一个失败。**把并发正确性交给数据库唯一约束，是成本最低、最可靠的做法。**

取消点赞对称处理：`delete` 返回 0（本来没点）就不减计数。

## 3.2 计数不丢失：原子 UPDATE

```sql
UPDATE t_note SET like_count = like_count + 1 WHERE id = ?
```
而不是 Java 里 `setLikeCount(note.getLikeCount()+1)`——两个并发请求都读到 100，各自写 101，丢一次更新。自减加 `AND like_count > 0` 防负数。

---

# 第 4 章 Hybrid RAG（V2 最重章）

## 4.1 先立边界：什么问题走 RAG

| 用户问题 | 正确通道 | 理由 |
|---|---|---|
| "这耳机多少钱" | searchProduct → SQL | 价格是实时结构化数据 |
| "有货吗" | searchProduct → SQL | 库存同理 |
| "防水吗/续航多久" | searchDocs → RAG | 说明书文本 |
| "戴久了耳朵疼吗" | searchDocs → RAG | **只有别的用户的笔记能答** |

第 4 行是本项目 RAG 的灵魂：**UGC 笔记是最优质的语料**，官方说明书答不了主观体验。

## 4.2 索引链路：入库 → 切片 → 向量化

```
笔记审核通过（或商品上架）
  → RagIndexService.index()
     ├─ upsert 文档头（uk(source_type,ref_id)，同来源只有一份）
     ├─ 删旧切片（替换语义，防脏语料残留）
     ├─ 切片：300字/片，重叠50字
     ├─ embed：EmbeddingClient（local 哈希 / spring-ai 真实模型）
     └─ 批量写 t_knowledge_chunk
```

**为什么重叠 50 字**：一句话被从中间切断，两半都语义残缺；重叠保证任何一句话完整出现在至少一个切片里。

**EmbeddingClient 双实现（可插拔）**：
- `LocalHashEmbeddingClient`（默认）：字符 2-gram 哈希分桶——**诚实定位：只能捕获字面重叠，捕获不了"续航久≈电池耐用"这种语义**。价值是零依赖跑通全链路+验证工程正确性。
- `SpringAiEmbeddingClient`：配了真实 embedding 服务时启用（DeepSeek 不提供 embedding 接口，这是现实约束），语义质量显著提升，**业务代码零改动**。

> 面试话术："我的 embedding 层是可插拔的，默认用本地哈希向量保证任何环境能跑；接入真实模型后检索质量升级，这层切换不改任何业务代码。" ——这比硬吹"我用了向量检索"诚实且高级。

## 4.3 检索链路：双通道 + RRF

```
问题 q ──┬── ① 关键词通道：MATCH(content) AGAINST(q) ngram → Top-20
          └── ② 向量通道：embed(q) 与全量切片算余弦 → Top-20
                        │
                ③ RRF 融合：score(d) = Σ 1/(k + rank_i(d))，k=60
                        │
                ④ 截断 Top-5 →（带来源 MANUAL/NOTE）返回
```

**为什么要两路（背下来）**：

| | BM25/全文 | 向量 |
|---|---|---|
| 擅长 | 精确词：型号"蓝牙5.3""IPX7" | 语义模糊："戴久了舒服吗"≈"长时间佩戴体验" |
| 短板 | 换说法就失配 | 专有名词易失配 |
| 失配后果 | 答不上型号类问题（电商高频） | 答不上体验类问题（种草高频） |

**RRF 为什么用排名不用分数**：BM25 分可以上百，余弦 ∈[0,1]——"拿厘米和斤相加"。RRF 只看名次，天然免疫量纲；且**两路都命中的片段得分自动叠加**（字面+语义双确认，最可信的排最前）。k=60 压平第一名与第二名的差距，是原论文推荐值。

**单通道故障降级**：任一通道抛异常，`safe()` 包装让它返回空列表，另一通道托底——检索链路的"可降级"传统。

## 4.4 Agent 分流：双工具，模型自己选

V1 只有 searchProduct 时，"防水吗"这类问题模型只能硬编。V2 注册第二个工具：

```java
@Tool(description = "检索商品知识库（官方说明书+真实用户种草笔记）…"
    + "价格、库存、是否有货等实时信息不要用本工具（用 searchProduct）。")
public String searchDocs(String question) { ... }
```

**关键设计：分流靠工具 description + system prompt 的"总-分"引导，不是代码 if-else**。自然语言意图无法枚举，规则路由必然挂一漏万；让模型读"两只手"的说明书自己决策——这就是 2026 年 Agentic RAG 的方向。

system prompt 定调（ChatServiceImpl.textSystemPrompt）：
> "价格库存先调 searchProduct（实时数据）；使用体验、佩戴感受、功能细节先调 searchDocs（说明书+真实用户笔记）；回答注明来源。"

**回复带来源标注**：searchDocs 返回的每条片段带 `source: 官方说明书/用户种草笔记`，AI 回答时注明——这是 RAG 可信度的产品化表达（用户可验证），不是黑盒。

## 4.5 必考追问："几百份文档为什么不用长上下文"

1. **语料动态增长**：笔记每天在发，全塞 prompt 不可持续；
2. **C 端首字延迟**：长上下文 Prefill 随长度线性涨，打字机体验崩掉；
3. **权限隔离**：RAG 检索层可以按来源过滤（如只检公开笔记），prompt 全塞无权限边界；
4. **成本**：每次全量灌入，调用量上来后 token 费用失控。

（这四条是 2026 年业界共识决策树，源自我做评审时核实的多份生产实践材料。）

---

# 第 5 章 AI 审核 + MQ：异步、幂等、毒消息

## 5.1 链路全景

```
用户点"发布"
  → 笔记落库 status=AUDITING ──秒回──→ 前端显示"审核中"
  → AuditService.submit()
      ├─ MQ 可用：发消息(note.audit) → Broker(durable) → AuditConsumer
      └─ MQ 不可用：本地线程池 @Async（降级，体验一致）
              │
              ▼
      AuditAiClient.review(低温度 0.1, JSON 判定)
              │
      ┌───────┴────────┐
   pass=true          pass=false / AI 挂了
      │                 │
  → INSERT IGNORE 流水(PASS)   → 流水(REJECT/ERROR)
  → updateStatus 条件更新       → REJECT 带原因 / 停在 AUDITING
  → RAG 索引该笔记                (不误杀，人工兜底)
```

## 5.2 为什么这个设计"企业味"足

**① 发布接口的 RT 与审核耗时长短无关**——用户体验由架构保证，不靠祈祷 AI 快。

**② 消息只带 id 不带内容**——消费者按 id 回查最新数据：消息在队列里躺了 5 分钟、笔记已被编辑，审核的永远是最新版；且消息体小不占 Broker。

**③ 幂等三防线（面试划重点）**：
1. `t_audit_record` 的 `uk(biz_type,biz_id,biz_version)` + INSERT IGNORE——MQ 重投第二次直接被数据库挡住；
2. `updateStatus` 带 `WHERE status='AUDITING'`——重复回写自然失效（状态机 CAS）；
3. AI 失败只记 ERROR 不动笔记状态——**审核系统宁可不作为，不可乱作为**（误杀内容比晚发布伤害大得多）。

**④ 毒消息防护**：消费端 catch 全部异常、不重入队（`default-requeue-rejected: false`）。必然失败的消息（AI 服务挂了）重试一万次也不会成功，只会打爆日志。兜底=ERROR 流水 + 死信队列（DLX 已声明）+ 未来的定时补偿任务。**"失败可被发现"优于"失败被无限重试"。**

**⑤ 发布者确认**（publisher-confirm-type: correlated）：消息到 Broker 才算成功——"不丢"的发送端一半。

## 5.3 三重降级（本项目可插拔传统的延续）

| 故障 | 行为 |
|---|---|
| RabbitMQ 没启动 | 发布照常，审核走线程池（AuditServiceImpl.submit 的 catch AmqpException） |
| AI 服务挂 | 笔记停在 AUDITING，流水记 ERROR，绝不误杀 |
| RAG 索引失败 | 发布不受影响，索引可重跑（upsert 幂等） |

## 5.4 审核提示词的工程要点

分类标准显式给出（色情/广告/辱骂/敏感/违禁品）+ **强制 JSON 输出** + **低温度 0.1**（对比导购 0.7、文案 0.8——同一个项目三个温度，各有理由）+ **拿不准倾向 pass**（宁漏勿杀，误杀正常用户的伤害 > 漏过一条软广）。解析做防御：模型可能带 markdown 围栏，抠 `{...}` 再解析。

---

# 第 6 章 AI 种草文案：RAG 参与创作

## 6.1 与普通"帮我写文案"的区别

```
普通做法：商品参数 → prompt → AI → 干巴巴的说明书复读
本项目：  用户 brief + 商品信息 + RAG 检索的真实用户反馈 → 有细节的文案
```

生成前先 `ragRetrievalService.retrieve(brief)` 拿 3 条相关笔记片段当参考素材——产出带真实使用细节（"通勤地铁上开降噪，世界瞬间安静"）。

**这就是内容飞轮的闭环**：UGC 笔记 → 语料 → 帮新用户写出更好的笔记 → 更多 UGC。每一步都喂养下一步，这是"种草社区+RAG"独有的化学反应，纯电商项目抄不来。

## 6.2 红线：AI 只出草稿

返回 `editable: true` 提醒前端；用户编辑后走**正常发布+审核流程**。"AI 不直写业务数据"——未经审核的 AI 内容直接公开是 UGC 平台事故。

## 6.3 高温度创作

文案生成 temperature=0.8（要发散有灵气），审核 0.1（要确定），导购 0.7（平衡）——**同一项目三个温度各司其职**，面试聊 temperature 时这是现成的纵深。

---

# 第 7 章 模拟数据：让数据长得像真的

> `scripts/gen_mock_data.py` → `sql/mock_data.sql`（固定种子可复现）

分布设计（拒绝均匀假数据）：

| 维度 | 做法 | 为什么 |
|---|---|---|
| 品类 | 8 类不等量（数码 22、文具 8） | 真实 mall 品类天然不均 |
| 笔记口碑 | 55% 含缺点段（"戴久了耳道有点胀"） | 全是好评的语料没有信息量，RAG 检索不出"真实感" |
| 互动数 | 幂律：10% 爆款(800~5000 赞) + 90% 长尾(3~300) | 热门榜要有梯度才像真的 |
| 时间 | 三角分布：越近越密 | 社区有"生长感" |
| 正文 | 场景开头+双优点+可选缺点+结尾 模板组合 | 500 篇不重样且有真实感 |

导入后调 `POST /api/v1/ai/index-all`（RagIndexBuildService，@Async 后台批量建索引，游标分批防内存爆）。

---

# 第 8 章 联调验收

```bash
# 0. 环境：可选起 MQ（不起也能跑，自动降级）
docker compose up -d rabbitmq

# 1. 建表：启动后端，Flyway 自动执行 V2__content_and_rag.sql
#    （存量库自动接着 V1 往后跑；日志出现 "Successfully applied ... V2"）

# 2. 灌模拟数据
mysql -h192.168.6.102 -uroot -proot123 ai_mall < sql/mock_data.sql

# 3. 建索引（登录后）
curl -X POST http://localhost:8080/api/v1/ai/index-all -H "Authorization: <token>"

# 4. 前端验收清单
#    [ ] 导航出现"种草"，/notes 瀑布流有 500 篇
#    [ ] 搜索"降噪"能命中（ngram 全文检索）
#    [ ] 热门/最新切换，加载更多（游标翻页）
#    [ ] 笔记详情：点赞红心/收藏/浏览数、右侧"文中好物"跳商品页
#    [ ] 发布：AI 起草 → 编辑 → 发布 → 显示"审核中" → 秒级变已发布（看 MQ 模式日志）
#    [ ] AI 助手问"XX 戴久了耳朵疼吗" → 模型调 searchDocs → 回答注明来源
#    [ ] AI 助手问"XX 多少钱" → 模型调 searchProduct（分流正确）
#    [ ] traceId：响应头 X-Trace-Id，日志可 grep 全链路

# 5. 幂等演示（面试可现场讲）
#    重复点赞 → 计数不变（DuplicateKey 吞掉）
#    重发审核消息 → 流水唯一键挡住，日志"重复审核消息（幂等跳过）"
```

---

## 附：V2 面试口径速查

| 问题 | 一句话答案 |
|---|---|
| 为什么用 MQ | AI 审核秒级耗时 vs 发布必须秒回；异步解耦后审核慢/挂都不影响发布体验 |
| 消息丢了怎么办 | 发送端 publisher confirm + 队列/消息持久化 + （失败）流水留痕可补偿 |
| 重复消费怎么办 | 流水表版本唯一键 INSERT IGNORE + 笔记状态机条件更新，双防线 |
| RAG 语料是什么 | 商品说明书（官方）+ 种草笔记（UGC 双眼：参数+体验） |
| 为什么不塞长上下文 | 语料动态/首字延迟/权限隔离/成本（四条背熟） |
| 为什么 Hybrid | 精确词（BM25）与语义模糊（向量）电商都高频，RRF 免量纲融合+双确认 |
| 向量存 MySQL？ | 数千切片内存余弦毫秒级；检索层已抽象，量级上来换实现不改代码 |
| 点赞怎么做 | 唯一索引幂等 + 原子自增计数；V3 升 Redis 计数+定时落库 |
| AI 审核出错怎么办 | ERROR 流水+停留审核中+死信/补偿，宁可不作为不可乱作为 |
