# V1.5 工程基建详解：从"能跑"到"能上线"

> ai-mall 项目升级讲解文档 · V1.5（工程基建补全）
> 配套词典：`V1.5工程基建详解【知识词典】.md`（查概念用那本）

---

## 【易错关键点】（先背这个，再看正文）

1. **扣库存是"行锁 + `WHERE stock>=?` 条件更新"，不是乐观锁**——`version` 字段是预留的、从未参与比对。面试说"乐观锁"会被追问"版本号在哪比对"而当场卡壳。
2. **MDC 是 ThreadLocal，切线程就丢**——线程池场景必须用 `TaskDecorator` 手动传递；`InheritableThreadLocal` 救不了线程池（线程创建时父线程还没有 traceId）。
3. **线程池 finally 里必须 `MDC.clear()`**——线程是池化复用的，不清理会"串号"：下一个请求打印出上一个请求的 traceId，比丢失更糟（误导排查方向）。
4. **Flyway 已执行的迁移文件永远不要改**——checksum 会被校验，改了启动就报 Validation 失败。要改表就新增 `V2__xxx.sql`。
5. **上传文件名永远不要信**——UUID 重命名防三件事：路径穿越（`../../etc/passwd`）、覆盖攻击、文件名乱码/XSS。
6. **文件类型用白名单不是黑名单**——黑名单永远列不全，漏一个 `.html` 就是存储型 XSS。
7. **默认线程池的队列是无界的（`Integer.MAX_VALUE`）**——意味着永远触发不到拒绝策略，请求堆积最终 OOM。生产必须显式配"有界队列 + 拒绝策略"。
8. **本地磁盘存图片在多实例下必炸**——用户传到实例 A、读请求打到实例 B → 404。所以要有 `StorageService` 抽象层。

---

## 全书地图

```
第 0 章 为什么要 V1.5 —— 玩具项目和能上线项目的分界线在哪
第 1 章 Flyway —— 数据库版本管理（表结构变更纳入版本控制）
第 2 章 traceId 与可观测性 —— 让每条日志都能串回"同一次请求"
第 3 章 对象存储抽象 —— 本地/MinIO 双实现，可插拔可降级
第 4 章 单元测试 —— 先守住"钱和库存"相关的五类事故
第 5 章 线程池配置 —— 为什么默认线程池是隐患
第 6 章 Docker Compose 与 CI —— 环境一键化 + 每次提交自动验证
第 7 章 before/after 总览 —— 这次升级到底改了什么
第 8 章 验收清单 —— 怎么亲手验证 V1.5 全部生效
```

---

# 第 0 章 为什么要 V1.5：玩具项目与能上线项目的分界线

## 0.1 先看三个"真实事故场景"

V1 的 ai-mall 功能是完整的：能注册下单、能 AI 问答、冒烟测试 9/9 全过。但换一个视角——假设明天就要把它部署到一台服务器上给真用户用，会发生什么？

**事故一：改表改出事故**
> 同事 A 在他本地给 `t_user` 加了个字段，你本地的库没有这个字段。代码一合并，你的环境起不来了。于是群里开始互发 SQL 截图，每个人手动在自己的库上执行一遍——有人执行漏了，有人执行错了，排查了一下午。

**事故二：线上日志查不了**
> 用户说"下单失败了"。你登服务器 `tail -f` 日志，发现几百个请求的日志**交错混在一起**——这条是张三的 SQL、下一条是李四的异常、再下一条不知道是谁的入参。你根本分不清哪几条日志属于这个用户的这一次请求。

**事故三：图片全丢**
> 用户上传的头像存本地磁盘 `./uploads/`。容器一重启——没挂载卷，全没了。或者流量上来加了第二个实例——用户图片在实例 A 上，读请求打到实例 B，404。

## 0.2 V1.5 的答案：六块拼图

```
┌──────────────────────────────────────────────────────┐
│              V1.5 工程基建（不引入新中间件）            │
├──────────┬──────────┬──────────┬─────────┬───────────┤
│ ① Flyway │ ②traceId │ ③ 对象存储│ ④ 测试  │ ⑤Compose  │
│ 表结构版本│ 全链路日志│ 可插拔存储│ 守住钱  │  + ⑥ CI   │
│  管理    │  可排查  │ 双实现   │ 和库存  │ 环境一键化 │
└──────────┴──────────┴──────────┴─────────┴───────────┘
```

**为什么说这是"分界线"**：这六项没有一项是"新功能"，用户完全感知不到。但它们决定了这个项目是"我一个人在我电脑上能跑的 demo"，还是"一个团队在任何环境都能跑、出了问题能排查的项目"。

面试官问"你的项目有什么工程化实践"，答案就在这六章里。

## 0.3 设计总原则：可降级、可插拔

延续本项目 voice 模块的思路（缺 VITS 资产自动降级 Edge-TTS，但应用照常启动），V1.5 的所有基建都遵守：

| 组件 | 有依赖时 | 没依赖时 |
|---|---|---|
| MinIO | 用 MinIO（生产级） | **自动回退本地磁盘存储**，照常能跑 |
| RabbitMQ/Redis（Compose 备好） | V2/V3 直接用 | 不启动也完全不影响当前代码 |
| CI（GitHub Actions） | push 自动跑 | 本地 `mvn verify` 等价 |

**任何环境 clone 下来都能直接启动**——这是硬约束。

---

# 第 1 章 Flyway：数据库版本管理

## 1.1 问题驱动：没有它会怎样

回忆 0.1 的事故一。根源是：**表结构变更靠"人"传播**（发 SQL 截图、手动执行）。人是最不可靠的传播介质——会漏、会错、会忘。

数据库版本管理工具的思路一句话：**把"改表"写成带版本号的 SQL 文件放进代码仓库，让应用启动时自己核对"我执行到哪个版本了"，缺的自动补上。**

```
改表的传统方式：                      Flyway 的方式：
                                    
张三改了表                           张三提交 V3__add_phone.sql
  ↓                                    ↓
群里发 SQL 截图                      git push（代码+SQL 一起走）
  ↓                                    ↓
每个人手动执行（漏/错/忘）             每个人重启应用 → Flyway 自动比对
  ↓                                    flyway_schema_history
有人环境挂了，查一下午                  ↓
                                    缺 V3？自动执行。齐了？什么都不做。
```

## 1.2 Flyway 怎么工作（原理）

核心就一张表：`flyway_schema_history`。Flyway 第一次在库里运行时自动创建它，长这样：

| installed_rank | version | description | script | checksum | success | installed_on |
|---|---|---|---|---|---|---|
| 1 | 1 | init | V1__init.sql | 1804289383 | 1 | 2026-09-06 ... |

应用每次启动，Flyway 做三件事：

1. 扫描 `classpath:db/migration/` 下所有 `V版本号__描述.sql` 文件；
2. 和 `flyway_schema_history` 里已记录的对比；
3. **找出还没执行的，按版本号顺序依次执行**，每执行一个记录一行（含 checksum 校验和）。

启动日志里会看到：

```
Migrating schema `ai_mall` to version "1 - init"
Successfully applied 1 script to schema `ai_mall`
```

**checksum 的作用**：Flyway 会校验"已执行文件现在的内容"和"当时执行时的内容"是否一致。你改了已经执行过的 `V1__init.sql`？启动直接报 `Validate failed: Migration checksum mismatch`。这是**故意的**——强制你"向前迁移，不改历史"。

## 1.3 本项目的接入（含一个关键决策）

文件：`backend/src/main/resources/db/migration/V1__init.sql` + `application.yml`：

```yaml
spring:
  flyway:
    enabled: true
    baseline-on-migrate: true   # ★ 关键
    baseline-version: 1
    locations: classpath:db/migration
    validate-on-migrate: true
```

**★ `baseline-on-migrate: true` 是什么、为什么要它**：

本项目是"V1 已经跑了一段时间、库里已经有 10 张表"的**存量项目**。存量项目接 Flyway 有个鸡生蛋问题：

> 库里已经有表了，但 `flyway_schema_history` 不存在。如果直接跑，Flyway 会执行 `V1__init.sql` → `CREATE TABLE IF NOT EXISTS` 虽然不报错，但万一历史表和 V1 里有细微差异，就埋雷了。

`baseline-on-migrate: true` 的语义：**"发现非空库且没有 flyway 历史时，自动打一条基线记录（版本=baseline-version=1），声明'版本 1 及之前的状态我认了'，从 V2 开始管理"**。

两种环境的实际表现：

| 环境 | 库状态 | Flyway 行为 |
|---|---|---|
| 全新机器（空库） | 无表 | 正常执行 `V1__init.sql` 建全量表 + 种子数据 |
| 你的开发库（已有 10 张表） | 非空 | 自动 baseline 到 1，跳过 V1，安静地待命 |

> 这就是"把 Flyway 引入已有项目"的标准做法，面试聊到 Flyway 时主动讲这一点，非常加分——因为大部分人只会在新项目里用 Flyway。

## 1.4 迁移纪律（团队约定）

```
┌────────────────────────────────────────────────┐
│  Flyway 三条铁律                                 │
│                                                │
│  1. 只增不改：已执行的迁移文件是"历史"，永不变更    │
│     改表 = 新增 V2__add_note.sql，V3__xxx.sql   │
│                                                │
│  2. 迁移内不写 DROP/删数据列（除非刻意为之），     │
│     删列会丢生产数据——真要删，先停写、备份、再删   │
│                                                │
│  3. 命名必须 V<数字>__<下划线描述>.sql，          │
│     版本号全团队递增，不回填（V2 之后不插 V1.5）   │
└────────────────────────────────────────────────┘
```

## 1.5 顺带修正的一处过时注释

迁移文件里有一处**故意改掉的注释**——旧 `sql/init.sql` 里 `t_product_sku.version` 写着"乐观锁版本号（V3 秒杀/防超卖使用）"，这是**错误说法**（代码从未用它做过版本比对）。`V1__init.sql` 里已改为如实描述：

```sql
version INT NOT NULL DEFAULT 0 COMMENT
  '★预留字段，当前【未用作乐观锁】。防超卖靠 UPDATE ... WHERE stock>=? 的行锁 CAS'
```

`sql/init.sql` 保留为"全新环境手工初始化的便利脚本"，但**权威来源已变为迁移目录**。

## 1.6 面试怎么讲

> "我们用 Flyway 做数据库版本管理。它是启动时比对 `flyway_schema_history` 和迁移目录，缺的按序执行，并用 checksum 防止有人篡改历史脚本。我这个项目比较特殊是**存量库接入**——用 `baseline-on-migrate` 把已有表结构基线化到版本 1，从 V2 开始纳管，这样新老环境都兼容。"

---

# 第 2 章 traceId 与可观测性

## 2.1 问题驱动：日志为什么"串不起来"

回忆 0.1 事故二。本质：**并发下日志是交错的，缺一个"把同一次请求的所有日志归到一起"的钥匙**。

这把钥匙就是 traceId（链路追踪 ID）：每次请求进来生成一个随机 ID，这个请求打的所有日志都带上它。

```
没有 traceId：                        有 traceId：
                                                     
12:00:01 INFO  Controller 入参 ...    12:00:01 INFO [a1b2c3...] Controller 入参 ...
12:00:01 INFO  SQL: select ...        12:00:01 INFO [d4e5f6...] SQL: select ...
12:00:01 ERROR 下单失败!              12:00:01 INFO [a1b2c3...] SQL: select ...
12:00:02 INFO  SQL: select ...        12:00:01 ERROR [a1b2c3...] 下单失败!
（谁的？不知道）                       12:00:02 INFO [d4e5f6...] SQL: select ...
                                      grep a1b2c3 → 这一次请求的完整链路
```

## 2.2 MDC：日志框架自带的"线程储物柜"

一句话核心概念：**MDC（Mapped Diagnostic Context）= 日志框架给每个线程配的一个 Map，往里放的东西会在打日志时自动被 pattern 取出来，业务代码零侵入。**

```java
// 放（一个请求只放一次）
MDC.put("traceId", "a1b2c3d4e5f67890");

// 日志 pattern 里这样取（%X 是 MDC 取值语法）
// pattern: ... [%X{traceId}] %m%n
// 输出:    ... [a1b2c3d4e5f67890] 下单失败!
```

**为什么不用方法参数传**：要在几百个方法签名里加 `String traceId` 参数？侵入性爆炸。MDC 底层是 `ThreadLocal`——"随线程走"，天然无侵入。

代价马上来：**ThreadLocal 换线程就取不到了**（2.4 节）。

## 2.3 入口：TraceIdFilter（为什么是 Filter 不是 Interceptor）

`com/aimall/common/trace/TraceIdFilter.java`，三个设计点：

**设计点一：用 Filter（Servlet 层），比 Spring MVC 的 Interceptor 更早。**
被拦截器拦掉的 401 请求、过滤器阶段就出错的请求，也都有 traceId。排查鉴权问题时恰恰最需要它。

**设计点二：支持上游透传。**
请求头带 `X-Trace-Id` 就沿用，没有才生成：

```java
String incoming = request.getHeader("X-Trace-Id");
String traceId = StringUtils.hasText(incoming) ? incoming : TraceIdContext.generate();
```

为什么？**为 V4 微服务做准备**：网关生成 traceId → 调 ai-service 时放进请求头 → ai-service 沿用同一个 ID。一条链路跨多个服务，traceId 一致才能串起来。这就是"分布式链路追踪"的种子（生产级方案是 OpenTelemetry/SkyWalking，原理相同：入口生成、全程透传）。

**设计点三：finally 必须 clear（初学者最常踩的坑）。**

```java
try {
    filterChain.doFilter(request, response);
} finally {
    TraceIdContext.clear();   // ← 没有这行会怎样？
}
```

Tomcat 线程是**池化复用**的：处理完你这个请求的线程会回到池里，服务下一个请求。如果不清理，下一个请求复用这个线程时，MDC 里还挂着**上一个人的 traceId**——日志串号，比丢 traceId 更可怕（它会把你的排查方向带偏）。

另外把 traceId **回写到响应头**：

```java
response.setHeader("X-Trace-Id", traceId);
```

用户报障时，让他按 F12 把响应头里的 `X-Trace-Id` 抄给你，你拿它在日志里精确 grep。

## 2.4 坑一：异步线程里 traceId 丢了

场景：V2 的 AI 审核要异步做（发布笔记的接口立刻返回，审核在线程池里慢慢跑）。写法：

```java
@Async
public void auditNote(Long noteId) {
    log.info("开始审核笔记 {}", noteId);   // ← 这行日志没有 traceId！
}
```

为什么？MDC 是 ThreadLocal，请求线程 A 放的值，线程池的线程 B 里**取不到**。一次请求的日志被切成两半：同步部分有 traceId、异步部分没有。

**为什么 InheritableThreadLocal 也救不了**：ITL 的继承发生在**子线程创建那一刻**。线程池的线程是应用启动时就创建好、反复复用的——创建那一刻请求还没来，继承了个寂寞。

**唯一可靠的解法**：提交任务时"拍照"，执行任务时"还原"。这就是 `MdcTaskDecorator`：

```
提交任务（线程A，有traceId）         执行任务（线程B，本来没有）
┌──────────────────────┐          ┌──────────────────────────┐
│ MDC.getCopyOf-       │  装饰器   │ ① MDC.setContextMap(快照) │
│ ContextMap()         │ ───────► │ ② 跑真正的任务(日志有ID了) │
│  → 拍下 Map 快照      │          │ ③ finally: MDC.clear()    │
└──────────────────────┘          └──────────────────────────┘
```

```java
public class MdcTaskDecorator implements TaskDecorator {
    @Override
    public Runnable decorate(Runnable runnable) {
        Map<String, String> snapshot = MDC.getCopyOfContextMap(); // 提交方线程拍照
        return () -> {
            try {
                if (snapshot != null) MDC.setContextMap(snapshot); // 执行方线程还原
                runnable.run();
            } finally {
                MDC.clear();   // 归还线程池前清理，防串号
            }
        };
    }
}
```

这个坑有专门的单测钉死（`MdcTaskDecoratorTest` 第二个用例：任务跑完后再往同一线程提交"未装饰"任务，断言读不到残留 traceId）。

## 2.5 logback-spring.xml：让 pattern 真的打出 traceId

文件：`backend/src/main/resources/logback-spring.xml`。关键行：

```xml
<property name="FILE_PATTERN"
          value="%d{...} %5p [%15.15t] %-40.40logger{39} : [%X{traceId:-N/A}] %m%n%wEx"/>
```

- `%X{traceId}` —— 从 MDC 取 traceId；
- `:-N/A` —— 取不到时显示 N/A（一眼就能看出"这条日志丢了上下文"）；
- **文件 pattern 不能用 `%clr`**——颜色转义符写进文件就是乱码，所以控制台和文件两条 pattern 分开写。

滚动策略是防"磁盘写满"这类低级线上事故：

```xml
<maxFileSize>50MB</maxFileSize>      <!-- 单文件超 50MB 切分 -->
<maxHistory>30</maxHistory>          <!-- 保留 30 天 -->
<totalSizeCap>2GB</totalSizeCap>     <!-- 总量 2GB 封顶，旧的最先删 -->
```

日志目录可通过 `aimall.logging.dir` 配置（`springProperty` 把 yml 值接进来）。

## 2.6 Actuator：给运维一双眼睛

`spring-boot-starter-actuator` + `application.yml`：

| 端点 | 作用 | 本项目暴露 |
|---|---|---|
| `/actuator/health` | 活着吗？DB 连得上吗？ | ✅ |
| `/actuator/health/liveness` `/readiness` | K8s 存活/就绪探针 | ✅（probes 开启） |
| `/actuator/metrics` | JVM 内存、线程、HTTP 耗时等指标 | ✅（本地演示） |
| `/actuator/beans` | 所有 Bean 列表 | ✅（仅本地，**生产要收窄**） |

价值一句话：**没有它，运维只能用"进程在不在"判断服务状态；有了它，能判断"活着还是假死、依赖是否正常"**——假死（进程在但 DB 连接池耗尽不响应）是 ping 不出来的。

## 2.7 面试怎么讲

> "我们用 MDC + Filter 做了全链路 traceId：入口 Filter 生成并回写响应头，日志 pattern 用 `%X{traceId}` 无侵入输出，异步线程池用 TaskDecorator 传递快照、finally 清理防串号。生产级的下一代是 Micrometer Tracing / SkyWalking，原理一样是'入口生成、沿途透传、集中收集'，我这套是它的手动版，V4 拆服务后升级。"

---

# 第 3 章 对象存储抽象：本地/MinIO 双实现

## 3.1 问题驱动：本地存图片的三宗罪

```
① 多实例必炸：用户图片传到实例A ──读请求打到实例B──► 404
② 容器化即丢：docker restart，没挂卷 ──► 全没了
③ 没法做 CDN / 权限控制 / 生命周期清理
```

所以生产用对象存储（S3/OSS/MinIO）。但本项目硬约束是"任何环境零依赖可启动"——于是采用和 voice 模块同款思路：**接口抽象 + 双实现 + 配置切换**。

## 3.2 抽象层设计（一句话核心）

**业务代码只依赖 `StorageService` 接口，不关心文件最终落在磁盘还是 MinIO；换存储 = 改一行配置，业务零改动。**

```
                        ┌─ type=local（默认）─► LocalStorageService ─► ./uploads/ + /files/** 映射
业务代码 ─► StorageService 接口
   upload(file, "notes") └─ type=minio ───────► MinioStorageService ─► MinIO bucket: aimall
   delete(key)
```

`StorageConfig` 里一个工厂方法显式选择（比两个 `@ConditionalOnProperty` 更直白，"未配置=本地"的兜底语义一眼可见）：

```java
@Bean
public StorageService storageService(StorageProperties properties) {
    if ("minio".equalsIgnoreCase(properties.getType())) {
        return new MinioStorageService(properties);
    }
    return new LocalStorageService(properties);   // 默认/未知取值 → 本地，保证能跑
}
```

## 3.3 安全三件套（上传接口的红线）

`FileRestController.upload()` 的校验 + `newObjectKey()` 的命名策略：

| 威胁 | 防线 |
|---|---|
| **路径穿越**：文件名 `../../etc/passwd.png` | UUID 重命名，objectKey 形如 `notes/20260906/a1b2c3....png`，原始文件名完全不参与存储路径 |
| **覆盖攻击**：两人都传 `avatar.jpg` 互相覆盖 | UUID 唯一，永不覆盖（有单测验证） |
| **存储型 XSS**：上传恶意 `.html`，借你的域名访问偷 Cookie | MIME **白名单**（jpeg/png/webp/gif），黑名单永远列不全 |
| **磁盘打满（DoS）**：传个 10GB 文件 | 服务端大小上限 10MB（`max-size-bytes`） |

**为什么白名单优于黑名单**：黑名单要枚举"所有危险类型"——`.jsp .php .html .svg .exe ...`，漏一个就破防；白名单只允许"明确安全的几种"，未知的一律拒绝。安全设计默认原则：**默认拒绝**。

## 3.4 本地实现的关键点

`LocalStorageService`：

- `Files.createDirectories(parent)` —— 等价 `mkdir -p`，已存在不报错；
- try-with-resources 拿流再 `Files.copy(REPLACE_EXISTING)`；
- `delete` 失败只 warn 不抛——删除是"尽力而为"，不能因为删不掉文件把主流程炸了（对比：上传失败必须抛）。

**必须配套的静态资源映射**（`StorageConfig` 里注册）：文件落盘了，但 Spring Boot 默认只认 `classpath:/static/`，不知道你的 `./uploads` 目录——不映射的话"上传成功但访问 404"。映射规则：URL `/files/**` → 磁盘 `file:./uploads/`，带 7 天缓存（文件名含 UUID 不会变，可放心缓存）。

## 3.5 MinIO 实现的两个讲究

`MinioStorageService`：

**① 懒初始化，不阻断启动。** 客户端和 bucket 检查都推迟到**第一次真正上传**时才做（双重检查锁保证并发下只建一次）。效果：MinIO 没起，应用照样启动，只有"上传"这一个动作会报错。这正是"缺资产降级但应用照常"的家族传统。

**② URL 两种策略。**
- 配了 `url-prefix`（如 CDN 域名）→ 直接拼 URL，桶公开只读；
- 没配 → **预签名 URL**（presigned URL，7 天有效）：桶可以完全私有，访问靠临时签名授权。生产更推荐——公开桶被人遍历拖库是经典事故。

## 3.6 配置速查

```yaml
aimall:
  storage:
    type: local          # local | minio
    local:
      root: ./uploads    # 落盘目录
      url-prefix: /files # 访问前缀（与静态映射一致）
    minio:
      endpoint: http://localhost:9000   # docker compose 里的 minio
      access-key: aimall
      secret-key: aimall123
      bucket: aimall
      url-prefix:        # 空 = 预签名 URL
```

`docker compose up -d minio` 后把 type 改成 minio 即完成切换，**业务代码一行不动**——这就是抽象层的价值。

## 3.7 面试怎么讲

> "图片存储我们做了 `StorageService` 接口抽象，本地实现保证零依赖可启动、MinIO 实现用于生产，配置切换业务零改动。安全上：UUID 重命名防路径穿越和覆盖，MIME 白名单防存储型 XSS，服务端限大小防 DoS。MinIO 桶走预签名 URL 而不是公开桶，避免被遍历拖库。"

---

# 第 4 章 单元测试：先守住"钱和库存"

## 4.1 为什么第一批测试是它们

V1 原本 0 测试。补测试不能撒胡椒面，先测**出事故代价最大**的地方：

```
下单扣库存 ──► 错了 = 超卖/丢单 = 资损        ──► 最优先
订单状态机 ──► 错了 = 重复取消/重复回补库存    ──► 最优先
越权访问   ──► 错了 = 看到别人的订单 = 事故    ──► 最优先
上传安全   ──► 错了 = XSS/穿越                ──► 高
MDC 传递   ──► 错了 = 线上日志串不起来         ──► 高（隐性bug，运行时才暴露）
分页 offset──► 错了 = 漏数据（不报错！）       ──► 中
```

这些逻辑用手工点页面**测不出来**：并发超卖要点几千次同一秒下单；状态机并发流转要两个请求赛跑。单测是唯一 economical 的方式。

## 4.2 关键技巧：mock 静态方法 StpUtil

`OrderServiceImpl` 开头就是 `StpUtil.getLoginIdAsLong()`（Sa-Token 静态方法）。单测没有登录上下文，直接调会炸。解法是 Mockito 的 `mockStatic`：

```java
try (MockedStatic<StpUtil> stp = Mockito.mockStatic(StpUtil.class)) {
    stp.when(StpUtil::getLoginIdAsLong).thenReturn(1L);   // "假装我已登录，userId=1"
    // ... 在这个作用域里跑业务逻辑
}
```

这样测试是**纯单元测试**：不起 Spring 容器、不连数据库、不依赖任何环境，毫秒级跑完。这就是为什么 CI 里不用配数据库也能 `mvn verify`。

## 4.3 每个测试守护哪类事故（速查）

`OrderServiceImplTest`（5 个用例）：

| 用例 | 守护的事故 |
|---|---|
| 扣库存返回 0 → 抛库存不足，**never insert 订单** | 并发抢库存时"库存没扣成、订单却生成了"的资损 |
| 库存充足 → 落订单，金额=单价×数量、状态=PENDING_PAY | 金额算错、初始状态错 |
| 商品下架 → 抛错且 **never deductStock** | 下架商品还在被扣库存 |
| 取消已支付订单 → 状态机 CAS 未命中，**never addStock** | "重复取消→重复回补库存"造成库存虚增 |
| 查他人订单 → 抛 NOT_FOUND（不是 FORBIDDEN） | 越权 + 顺带讲一个安全细节：用 404 不用 403，**不向试探者泄露"这个订单ID存在"** |

`LocalStorageServiceTest`（5 个用例）：恶意文件名不进 objectKey、两次上传不互相覆盖、删除幂等、URL 拼接规整。

`MdcTaskDecoratorTest`（2 个用例）：异步能读到 traceId；任务结束 MDC 无残留（防串号）。

`PageQueryTest`（3 个用例）：offset 差一错误（漏数据型 bug）。

## 4.4 @TempDir：不留垃圾的文件测试

`LocalStorageServiceTest` 用 JUnit5 的 `@TempDir`：每个测试自动分配临时目录、测试完自动删。既真实落了盘，又不在仓库留垃圾。

## 4.5 面试怎么讲

> "测试我优先覆盖钱和库存：下单的 CAS 扣减、状态机并发流转、越权访问。Sa-Token 的静态方法用 Mockito mockStatic 处理，测试是纯单元级、毫秒级、CI 无需数据库。有一个细节：查他人订单我返回 404 而不是 403，避免向攻击者确认订单号存在。"

---

# 第 5 章 线程池配置：默认线程池是颗雷

## 5.1 问题

Spring Boot 默认的 `applicationTaskExecutor` 队列容量是 `Integer.MAX_VALUE`——**无界队列**。后果：

```
流量洪峰 ─► 任务提交速度 > 消费速度
        └► 队列无限堆积（不触发拒绝策略！）
            └► 堆内存耗尽 ─► Full GC 频繁 ─► OOM
```

它"温柔"地把故障从"快速失败"变成"慢慢死透"，后者排查难度大一个数量级。

## 5.2 AsyncConfig 的每个参数都有理由

```java
executor.setCorePoolSize(4);          // IO 密集（查库/调AI），可大于核数
executor.setMaxPoolSize(16);
executor.setQueueCapacity(200);       // ★有界队列：宁可快速失败
executor.setThreadNamePrefix("aimall-async-");   // 日志一眼识别来源
executor.setTaskDecorator(new MdcTaskDecorator()); // traceId 跨线程（第2章）
executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
executor.setWaitForTasksToCompleteOnShutdown(true);  // 优雅停机
executor.setAwaitTerminationSeconds(10);
```

**拒绝策略对比**（面试高频）：

| 策略 | 行为 | 适用 |
|---|---|---|
| `AbortPolicy`（默认） | 抛异常 | 需要显式感知失败并处理时 |
| `DiscardPolicy` | 静默丢弃 | **几乎不能用**——丢了你都不知道 |
| `DiscardOldestPolicy` | 丢队头最老的 | 允许"只保最新"的场景（如行情推送） |
| `CallerRunsPolicy` ★ | 提交线程自己跑 | **本项目选择**：天然背压/限流——提交方被占用就无法继续提交，压力向上游传导而不是撑爆内存 |

Bean 名叫 `taskExecutor`（@Async 的默认查找名），V2 的 AI 审核异步直接 `@Async` 就能用到这套配置。

## 5.3 面试怎么讲

> "我们显式配置了异步线程池：有界队列 200 防止默认无界队列 OOM，拒绝策略选 CallerRuns 把压力回传给上游形成天然背压，TaskDecorator 传递 MDC traceId，开启优雅停机防发版丢任务。"

---

# 第 6 章 Docker Compose 与 CI

## 6.1 docker-compose.yml（环境一键化）

一条命令拉起 MySQL8 + Redis7 + RabbitMQ3.13 + MinIO：

```bash
docker compose up -d          # 全部启动
docker compose ps             # 状态
docker compose logs -f minio  # 日志
docker compose down           # 停止（数据卷保留）
```

两个细节：

- **healthcheck**：MySQL 用 `mysqladmin ping` 做健康检查。价值：依赖方可以等它"真正可用"而不是"容器起来"——这两者之间差着 MySQL 初始化的十几秒。
- **数据卷**：`volumes: mysql-data:/var/lib/mysql` —— `down` 不丢数据，`down -v` 才清空（慎用）。

> 注意：本项目 `application.yml` 默认仍连开发机 `192.168.6.102` 的 MySQL（保持现状不动）。想切到本地容器，把 url 改成 `jdbc:mysql://localhost:3306/ai_mall` 即可。Redis/RabbitMQ/MinIO 是为 V2/V3 预置的环境。

## 6.2 GitHub Actions（CI）

`.github/workflows/ci.yml`：push / PR 自动跑 `mvn -B clean verify`（编译 + 全部单测）。

```
push ─► Checkout ─► JDK17(setup-java, maven缓存) ─► mvn clean verify ─► 失败?上传测试报告artifact
```

价值：**"我本地能跑" → "任何人 clone 下来都能跑"**；代码合入前自动拦回归。因为测试是纯单元级（4.2 节的设计），CI 不需要数据库和真实 API Key，一个占位值即可。

---

# 第 7 章 before/after 总览

| 维度 | V1（before） | V1.5（after） |
|---|---|---|
| 表结构变更 | 手动执行 `sql/init.sql`，靠人传播 | **Flyway** 版本化管理，存量库 baseline 接入 |
| 日志排查 | 无 traceId，并发日志交错不可读 | **MDC + Filter + TaskDecorator**，全链路（含异步）可 grep，响应头回传 X-Trace-Id |
| 日志文件 | 只有控制台 | **滚动文件**：按天+50MB 切分，30天/2GB 封顶 |
| 健康检查 | 无 | **Actuator** health/metrics + K8s 探针 |
| 文件存储 | 无上传能力 | **StorageService 抽象**：本地（默认）/MinIO（预签名URL），UUID重命名+MIME白名单 |
| 异步任务 | 未配置线程池（默认无界队列） | **有界队列 + CallerRuns + MDC 传递 + 优雅停机** |
| 测试 | 0 | **15 个用例**守住库存/状态机/越权/上传安全/MDC/分页 |
| 环境 | 手动装中间件 | **docker compose 一键起** 四件套 |
| 质量门禁 | 无 | **CI** push 自动 build+test |

新增文件清单：

```
backend/src/main/resources/db/migration/V1__init.sql          # Flyway 迁移
backend/src/main/resources/logback-spring.xml                 # 日志(含traceId)
backend/src/main/java/com/aimall/common/trace/                # TraceIdContext / TraceIdFilter / MdcTaskDecorator
backend/src/main/java/com/aimall/config/AsyncConfig.java      # 线程池
backend/src/main/java/com/aimall/common/storage/              # 7 个类：接口/配置/双实现/上传端点
backend/src/test/java/com/aimall/...                          # 4 个测试类
docker-compose.yml                                            # 环境一键化
.github/workflows/ci.yml                                      # CI
```

改动文件：`pom.xml`（+Flyway/Actuator/MinIO）、`application.yml`（+flyway/storage/actuator/multipart）、`.gitignore`（+uploads/）。

---

# 第 8 章 验收清单（亲手验证 V1.5 生效）

启动后端前，`docker compose up -d`（可选，V1.5 代码不依赖它）。

1. **Flyway**：启动后端，控制台看到 `Successfully applied 1 script`（新库）或无报错安静启动（存量库，已 baseline）。
2. **traceId**：随便调一个接口，看响应头有 `X-Trace-Id`；控制台日志出现 `[xxxxxxxxxxxxxxxx]` 段。
3. **日志文件**：`logs/aimall.log` 生成且带 traceId。
4. **上传**：登录后 `POST /api/v1/files/upload`（form-data：file + dir=notes）→ 返回 url；浏览器开该 url 能看到图（走 `/files/**` 映射）。
5. **Actuator**：`GET /actuator/health` 返回 `{"status":"UP"}`。
6. **测试**：`mvn test` → 15 个用例全绿。
7. **CI**：push 到 GitHub 后 Actions 页出现绿色 ✓。
8. **降级验证**：不启动 MinIO 时一切正常（type=local）；切 `type: minio` 但不启动 MinIO，应用**照常启动**、仅上传报错——懒初始化生效。

---

## 附：与设计文档的对应关系

- `项目设计文档.md` §8.2（V1.5 工程基建补全）——本章是其完整实现
- §11（六块拼图）——六项全部落地
- §2（三问筛伪需求）——每一项的"砍掉会怎样"都在各章 1.1/2.1/3.1 的问题驱动里
