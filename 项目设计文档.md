# AI 种草商城（AI 内容电商平台）项目设计文档

> 定位：面向初级 Java 求职的核心实战项目，以电商交易为主干、内容社区为流量层、AI 为智能引擎，三者构成完整商业闭环。所有技术栈均为业务真实需要，按版本逐步自然引入，杜绝"为加技术栈而加"的空壳设计。
>
> 📌 **本文档已于 2026-09-06 合并《项目设计评审与优化》全文**，并基于"全量代码扫描 + 2026 年技术现状核实"做了修正。凡文档与代码冲突处，**以代码为准**，文中用 ✅ / ⚠️ / ★ 标注。

---

## 1. 项目一句话定位

一个带 AI 智能导购、AI 内容审核、AI 种草助手、智能客服与数据分析能力的**内容电商平台**（对标小红书 + 得物 + 淘宝逛逛）。

### 核心闭环

```
用户创作种草笔记 → AI 审核 → 内容发布 → AI 推荐/Feed 流 → 兴趣点击
→ AI 导购问答 → 加入购物车 → 下单支付 → 口碑沉淀 → 反哺推荐
```

四个角色互相成就：**内容社区是流量入口，电商是变现主干，AI 是引擎**，三者缺一不可，不存在"两张皮"。

---

## 2. 【设计评审方法论】三问筛掉伪需求

> 这是本项目的**设计宪法**。每引入一个技术栈之前，先过这三关。任一项答不上来，就不做。

| # | 问题 | 答不上来的后果 |
|---|---|---|
| 1 | **砍掉它，系统会出什么真实业务问题？** | 无业务驱动 = 硬塞 |
| 2 | **真实企业里它怎么用？我这是不是"玩具用法"？** | 用法失真 = 一问就露馅 |
| 3 | **2026 年了，这方案还是主流吗？** | 过时 = 主动暴露短板 |

### 2.1 数据规模（是"验证手段"，不是"选型依据"）

本项目为求职实战项目，**无真实用户**，通过 AI 生成模拟数据。规模如下——**但请注意：规模只是"能把多少条真实业务路径跑通、验证到"的标尺，不是"所以简单方案就够了"的理由**：

| 数据 | 规模 | 它的真实作用 |
|---|---|---|
| 商品 + SKU | ~100 商品 / 数百 SKU | 足以跑通"多维筛选"这条业务路径的**数据形态**（关键词/分类/价格区间的组合） |
| 种草笔记 | ~500 篇 | 语料量足够让 **RAG 的检索质量可被感知**（否则 RAG 与直连 LLM 无差别） |
| 用户行为 | 模拟点赞/收藏/浏览/购买 | 排行榜有真实热度分布、能验证**排名锯齿/重建口径**这类问题 |

> ⚠️ **口径纪律（重要，面试最容易在这里翻车）**：
> **数据量是"触发条件的标尺"，不是"选型的理由"。** 决定选型的是**数据形态**（长文本？结构化多维度？）与**失效机制**（它会在什么条件下塌）。
>
> ❌ 错误说法："商品才 100 个，所以 LIKE 够用 / 不用上 ES。"——被问"那有 50 万商品呢"就崩，且等于自认玩具。
> ✅ 正确说法："商品搜索目前是最简实现，**成熟方案还没做**；它会在数据量上升、前缀通配导致全表扫时塌，触发条件是十万级或 P99>200ms，目标方案是 ES。"
>
> 📌 "量级"只允许出现在**触发条件**里，不允许出现在**理由位**里。

---

## 3. 为什么选这个项目（选型理由）

### 3.1 覆盖岗位要点

| 岗位要点 | 本项目落地场景 |
|---|---|
| Java 基础/并发/JVM | 限量发售防超卖、限流器、线程池处理异步任务、OOM 排查 |
| Spring 全家桶 | 整个项目的基础框架，IOC/AOP 用于日志、缓存、事务埋点 |
| MySQL | 用户/商品/订单/笔记/审核流水，索引与慢 SQL 优化有真实案例 |
| Redis | 热点内容缓存、点赞计数、排行榜、购物车、会话、分布式锁、限流 |
| 消息队列 | 内容审核异步流、订单异步处理、支付回调、粉丝通知推送 |
| 微服务 | 按业务域拆分（V4 **仅拆 AI 服务**，见 8.6） |
| 分布式高并发 | 限量发售防超卖、热点 Key、Feed 流高并发、支付幂等 |
| AI 工程化 | Spring AI 接入 DeepSeek：Hybrid RAG 导购、内容审核、Agent 客服 |

### 3.2 与常见竞品项目的区别

| 项目类型 | 问题 | 本项目解法 |
|---|---|---|
| 纯电商商城 | 简历同质化严重，AI 只能外挂"智能推荐" | 内容社区 + AI 是业务刚需，AI 不是外挂 |
| 纯 AI 问答/知识库 | 缺高并发、事务、支付等硬场景 | 电商主干天然提供支付、防超卖、一致性 |
| 博客/论坛 + AI | 商业故事弱，技术深度浅 | 种草→转化→口碑是完整商业闭环，有真实产品对标 |

### 3.3 为什么技术栈都是"自然融入"的

判断标准：**砍掉某技术栈，系统是否会有真实问题**。

- 砍掉 Redis：热点笔记被刷爆 DB、点赞计数扛不住、AI 调用成本飙升——有问题，需要 Redis。
- 砍掉 MQ：笔记上传后审核要等几秒才能发布、订单要同步等支付回调——体验崩坏，需要 MQ。
- 砍掉 RAG：商品说明书与种草笔记是**非结构化长文本且持续新增**，塞不进 prompt——需要 RAG。
- 砍掉 AI 审核：UGC 内容人工审核成本极高、时效差——需要 AI 审核。
- 砍掉防超卖：限量发售一开库存就超卖——需要预减 + 行锁 CAS。
- ⚠️ **不做 ES（注意：这是"还没做"，不是"不需要"）**：搜索能力目前是**缺口**，不是已完成的选型结论。前置的"关键词命中"这一环，商品侧甚至还是 `LIKE '%kw%'`（全表扫，索引失效）；笔记侧用 MySQL ngram 全文索引**只覆盖了"关键词"一环**，多维筛选（分类/价格区间/聚合面板）与拼音同义词全是缺的。**换 ES 的触发条件是商品量到十万级或 `LIKE` 查询 P99>200ms**，而目标方案 ES 解决的是**多维筛选**这件事（倒排索引 + filter 上下文 + 聚合），不只是"比 MySQL 快"。

---

## 4. 总体架构

### 4.1 演进原则（重要）

**第一阶段永远是单体**，一个 Spring Boot 应用跑通全流程。微服务不是目标，是规模倒逼的结果。每个版本的代码都在上一个版本上增量演化，不推倒重来。

### 4.2 业务域划分（从 V1 起就按域分包）

```
┌─────────────────────────────────────────────┐
│                 前端（Vue3）                 │
│  商城页 / 内容Feed / 笔记创作 / AI对话窗      │
└────────────────────┬────────────────────────┘
                     │ HTTP / SSE
┌────────────────────▼────────────────────────┐
│             网关入口层（V4 引入 Gateway）     │
├─────────────────────────────────────────────┤
│  用户域      │  内容域      │  电商域         │
│  auth/用户   │  笔记/Feed   │  商品/购物车    │
│  关注/粉丝   │  点赞/收藏   │  订单/库存/支付 │
├──────────────┴──────┬──────┴────────────────┤
│         AI 域（智能层，V4 独立成服务）        │
│  导购 Hybrid RAG / 审核 / 种草助手 / Agent   │
├──────────────┬──────┬──────┬────────────────┤
│   MySQL      │ Redis │ RabbitMQ │ 向量检索    │
└──────────────┴──────┴──────┴────────────────┘
```

### 4.3 技术栈覆盖矩阵（含版本与业务理由）

| 技术栈 | 业务场景（砍掉它会怎样） | 版本 |
|---|---|---|
| Spring Boot 3 / Java 17 | 基础框架 | V1 |
| **MyBatis**（XML 手写 SQL）+ MySQL 8 | 慢 SQL 要 `explain` 调优，SQL 必须可见可控 | V1 |
| Sa-Token | 前后端分离需要无状态登录态 | V1 |
| Vue 3 + Vite + Element Plus | 前端 | V1 |
| Spring AI（DeepSeek） | 简单问答 | V1 |
| **Agent Function Calling** | 不查库 AI 就会**编造商品和价格** | V1 |
| 多模态图片识别 | 用户拍图搜同款 | V1 |
| **Flyway** | 多人协作改表没有版本管理必炸 | V1.5 |
| **traceId + 结构化日志 + Actuator** | 请求跨多模块，没链路 ID 无法排查 | V1.5 |
| **对象存储（MinIO，可降级本地）** | 图片存本地，重启/扩容就丢 | V1.5 |
| **JUnit5 + Mockito + Testcontainers** | 没测试的项目没人敢改 | V1.5 |
| **Docker Compose / CI** | "我本地跑不起来"是协作杀手 | V1.5 |
| **Spring AI Hybrid RAG** | 说明书/笔记是非结构化长文本且持续更新 | V2 |
| Apache POI / PDFBox | 商品说明书解析入库 | V2 |
| 支付宝/微信沙箱支付 | 补齐商业闭环最后一环 | V3 |
| Redis | 商品详情读多写少；笔记点赞是热点写 | V3 |
| RabbitMQ | AI 审核要几秒不能让用户等；订单超时取消 | V3 |
| 分布式锁 | 限量发售瞬间流量百倍 | V3 |
| 限流熔断 | AI 调用按 token 计费，不限流成本失控 | V3 |
| Nacos / Gateway / OpenFeign | **AI 服务必须独立**（见 8.6） | V4 |
| 向量检索（Redis Stack / pgvector） | RAG 检索 | V2 |
| 相似推荐 + 热门榜 | 猜你喜欢、种草转化 | V5（可选） |

> ✏️ **ORM 选型变更**：`MyBatis-Plus` **弃用**，改用原生 **MyBatis**（mapper 接口 + XML 手写 SQL）。✅ 已核实代码中确实无 MyBatis-Plus。
> 理由：① SQL 可见可控、便于 `explain` 与慢 SQL 优化；② 分页手写 `LIMIT`，**不引分页插件（真实约束：可控性与零额外依赖，不是"数据少"）**；③ 深分页优化点保留（见 9.5）。

> ✏️ **统一返回体 `R.ok` 重载取舍**：早期 `R.ok(String msg)` 与 `R.ok(T data)` 并存，传入 `String` 时 Java 会解析到 `ok(String)`，导致「AI 回答这类纯字符串 data 丢失」。本项目去掉单参 `ok(String)`，仅保留 `ok(T data)` / `ok(String msg, T data)`。

---

## 5. 核心业务设计

### 5.1 用户域

- 注册/登录：Sa-Token 无状态认证，BCrypt 加密。✅ V1 已实现
- 用户资料：昵称、头像、简介、身份（普通用户/商家）。
- 收货地址：增删改查 + 默认地址。✅ V1 已实现
- 关注/粉丝：关注关系表 + Redis 计数器（V3）。⛔ **未实现**——`t_user_follow` 表已建（`V2__content_and_rag.sql`）但**至今无任何 Java/XML 代码使用**，属"建表未做接口"的休眠规划。

### 5.2 内容域（V2，**项目地基，非可选项**）

> ⚠️ **逻辑断裂警告（评审发现）**：项目叫"种草商城"，但 V1 没有任何种草内容。
> 更要命的是：V3 的 **AI 审核异步（MQ）** 审核什么？**Redis 点赞计数/排行榜** 给谁点赞？—— **全都依赖内容社区**。
> 没有内容社区，V3 的 Redis 和 MQ 都是空中楼阁。**内容社区必须前置。**

- **笔记**：图文内容（标题、封面、正文、图片列表、标签、关联商品）。
- **点赞/收藏**：Redis 计数 + 异步落库（V3）。
- **热门榜**：Redis zset，按热度（点赞+收藏+浏览加权）倒序。模拟数据下**有真实热度分布**。
- **关注 Feed**：⛔ **未实现（规划）**。拉模式（按时间倒序，游标分页）；大 V 可升级推模式（V4）。当前 Feed 流**不做关注过滤**，就是全站最新/最热。
- **搜索**：分场景，成熟度不同——**笔记**走 MySQL 全文索引（`FULLTEXT + WITH PARSER ngram`，已落地）；**商品**目前是最简 `LIKE` 模糊匹配，**属已知技术债**（前缀通配符使 B+ 树索引失效、必然全表扫）。商品的成熟解法是 ES（倒排索引 + 多维 filter + 聚合筛选面板），**触发条件：商品十万级 或 查询 P99 > 200ms**。
  > ⚠️ **口径纪律**：不要说"商品才几百条所以 LIKE 够用"——那是规模借口，被追问"50 万商品怎么办"就崩，且等于自贬项目是玩具。正确说法是"**还没做成熟的搜索方案**"，并说清它何时会塌、换它的触发条件与目标方案。
- **审核链路**：发布 → MQ 异步 → AI 审核 → 结果回写 → 状态变更。**MQ 第一个真实理由。**

### 5.3 电商域

- **商品**：SPU/SKU 两级，价格、库存、销量、上下架、规格属性。✅ V1 已实现
- **商品多图**：`t_product_image` SKU 图集/规格图，封面=首 SKU 首图。✅ V1 已实现
- **购物车**：**MySQL `t_cart`（京东模式）** —— 宽松收集 + 实时库存校验 + 勾选结算 + 失效分组 + 显式库存 + 原子 UPSERT。
  > **为什么 V1 用 MySQL 而不是 Redis**：京东式购物车要「多端同步 + 持久化 + 失效商品分组」，这些是 MySQL 的强项。✅ 已实现
- **订单**：订单主表 + 明细，状态机（待支付/已支付/已发货/已完成/已取消）。✅ V1 已实现
- **支付**：沙箱；下单→预支付单→收银台→异步回调→验签→**幂等**→状态流转（V3）。渠道选型见 5.3.2。
- **限量发售（Limited Drop）**：**替换原"秒杀"设计**，见 5.3.1。
- **售后**：退款申请 → 审核 → 退款（可选扩展）。

#### 5.3.1 ★ 为什么是"限量发售"而不是"秒杀"

> ⚠️ **评审判定：原设计的"秒杀"是八股堆砌。**
> 本项目对标小红书 + 得物。**得物什么时候搞过"秒杀"？** 凭空长出一个秒杀模块，面试官一问"你们业务为什么需要秒杀"，只能答"为了练习分布式锁"——**当场暴露是堆的**。

**修正为「限量发售（Limited Drop）」**：球鞋、潮玩、联名款发售——这是得物/潮玩电商的**真实场景**，且与"种草"主题天然吻合（发售前先种草）。

**防超卖的技术含量一点没少**，但业务上说得通：

| 层 | 方案 | 作用 |
|---|---|---|
| 1 | 接口限流 + Redis 原子预减库存 | 挡住 99% 无效请求 |
| 2 | 数据库 `UPDATE ... WHERE id=? AND stock>=?` | 行锁 CAS 兜底（见 9.1） |
| 3 | MQ 异步扣减 + 消费端幂等（订单号唯一索引） | 削峰 + 不丢单 |

> **面试口径对比**：
> ❌ "我做了秒杀，用了分布式锁"（八股复读机）
> ✅ "我们有限量发售场景，发售瞬间流量是平时百倍。我用 Redis 原子预减挡住 99% 流量，剩余落到数据库靠行锁 CAS 兜底，下单进 MQ 异步消费保证幂等。之所以不全程用分布式锁，是因为 Redis 预减已经足够，加锁反而成为瓶颈。"

#### 5.3.2 ★ 支付渠道：个人沙箱要不要接（选型论证，V3 落地后确认）

> **先破一个常见误判**："个人接入支付和小公司接入路径不相干，接了也没用"——不成立。
> 把"接入支付"拆三层，差异立刻清楚：

| 层 | 个人沙箱 | 小公司 | 面试考不考 |
|---|---|---|---|
| **资质开通**（营业执照、APPID、密钥配置） | 沙箱一键给 | 服务商/直连申请，运营事务 | ❌ 不考（本来就不是工程能力） |
| **接入工程**（验签、回调、幂等、状态机、对账、掉单处理） | **同一套** | **同一套** | ✅ **全考这里** |
| **渠道决策**（直连 vs 聚合、费率、分账） | 无 | 业务决策 | 偶尔闲谈 |

**工程层 100% 相干**——小公司后端接支付写的正是这套代码。真正不同的两块在面试里没有考察点。

**决策标准（三问宪法的应用）**：
- **接的唯一真实理由**：把"换渠道业务零改动"从**主张变成实证**——PayChannel 抽象下 Mock 与支付宝沙箱是两个平行实现，切换只改配置。这句话证明的是**抽象设计能力**，含金量高于"我接入了支付宝"这个集成事实。
- **不接的理由**：时间 ROI。MockPayChannel 已覆盖可考内容的 ~95%（验签/回调六步/幂等三层/金额核对/查单对账全是真代码），真实沙箱多出的只有 RSA2 密钥管理 + SDK 胶水 + 真实网络回调体验。

**结论（按时间预算决策）**：
- 面试近（两周内）→ **不接**。优先跑通 V3 验收 + 讲透设计点；简历写"支付渠道抽象 + 完整回调链路（Mock 渠道全真实现）"。
- 面试远（一个月+）→ **花半天接沙箱**：开放平台注册 → RSA2 密钥对 → 实现 `AlipaySandboxChannel` 的三个接口方法。**到此为止**。
  > ⛔ **截至 2026-09-19 未实现**：该类**不存在于代码中**（`find -name "Alipay*"` 无结果），仅出现在 `MockPayChannel.java:84` 与 `PayChannel.java:15` 两处注释里作为"可扩展行"举例。**面试别说"我接过沙箱"**——说"渠道做了抽象，Mock 全真实现，换真实渠道业务代码零改动"。
- **千万别做**：真实收款（个人无资质）、微信支付（商户号门槛更高，性价比低）、退款/分账/转账全流程（过度延伸）、把沙箱当简历主卖点。

> **面试口径对比**：
> ❌ "我接入了支付宝沙箱"（集成事实，含金量有限，还可能被追问资质细节）
> ✅ "我做了一层支付渠道抽象，Mock 渠道完整跑通验签/幂等/对账全链路；支付宝沙箱是同一抽象下的另一个实现，切换只改配置——这验证了抽象本身。"（抽象设计能力，且诚实说明模拟边界）

### 5.4 AI 域（智能层）

#### 5.4.1 ★★ RAG 的正确定位（评审修正，全篇最重要）

> ⚠️ **原设计的问题**：写"RAG 检索商品知识库（说明书/评测笔记/**规格**）"，但同时已有结构化 SKU 表 + Function Calling 查库。**边界模糊，且把"规格"塞进 RAG 是原理性错误。**

**原理性错误在哪**：

| 数据类型 | 例子 | 正确方式 | 用 RAG 会怎样 |
|---|---|---|---|
| **结构化** | 价格、库存、SKU、销量 | **SQL** | ❌ 价格一变就要重新向量化；库存不实时；无法做"低于500元"过滤；答错就是事故 |
| **非结构化** | 说明书、评测文章、用户真实体验 | **RAG** | ✅ 这才是 RAG 的主场 |

> **一句话：RAG 只管"文档"，不管"数据"。价格库存归 SQL，说明书归 RAG。**

**本项目 RAG 的正确用法 —— 语料 = 商品说明书（商家上传）+ 种草笔记（UGC）双源**：

> 💡 **关键洞察：种草笔记（UGC）本身就是最优质的 RAG 语料。**
> 用户问"这款耳机戴久了耳朵疼吗？"——官方说明书答不了，但**别的用户的种草笔记里有真实体验**。
> 这正是小红书式问答的灵魂，也是本项目 RAG **唯一的、不可替代的**价值落点。**不是硬塞，是契合定位。**

**Agent 决策分工（Agentic RAG）**：

```
用户提问
   │
   ▼
Agent 判断意图
   ├─ "多少钱 / 有货吗 / 比XX便宜"  → searchProduct 工具 → SQL
   ├─ "防水吗 / 戴久舒服吗 / 说明书怎么说" → searchDocs 工具 → Hybrid RAG
   └─ "我的订单到哪了"              → queryOrder 工具 → SQL
   │
   ▼
LLM 基于结构化结果 / 检索片段回答 → SSE 流式 + 引用来源
```

> **这个分工讲出来，面试官立刻知道你思考过边界。** 原设计的模糊写法，一问"为什么不用 SQL"就崩。

#### 5.4.2 ★ 2026 时效修正：别做"朴素 RAG"

2026 年的生产基线已经不是"分块 + 向量检索"了：

| 原设计写法 | 2026 正确做法 | 为什么 |
|---|---|---|
| 纯向量检索 | **Hybrid RAG：向量 + BM25 关键词，RRF 融合** | 商品查询大量是型号/规格词（"蓝牙5.3""IPX7""Type-C"），**这类精确词向量检索效果差，BM25 是主场**。业界公认低成本提升召回 10~20% |
| 切完就完 | **Rerank 重排序**（Top-40 粗筛 → Rerank → Top-7 精读） | 2026 黄金组合：RAG 粗筛 + 长上下文精读 |
| 检索一次生成 | **Agentic RAG**：Agent 决定是否检索、检索几次 | 见 5.4.1 决策图 |
| MySQL 存向量 | **pgvector 或 Redis Stack** | MySQL 存向量没索引，数据量上千就废 |
| 无评估 | **至少建 50 条评测集** | "没有评测的 RAG 是盲飞"——生产与 demo 的分水岭 |

> ⚠️ **必背追问**："你文档才几百份，直接全塞进长上下文不就行了？"
> **标准答案**：① 笔记和说明书**持续新增**（语料动态），全塞不可持续；② 面向 **C 端要求首字延迟**（长上下文 Prefill 延迟不可控）；③ 涉及**权限隔离**（商家只能检到自己商品的文档）；④ **成本**——每次全量灌入，调用量上来成本失控。
> 这四条正是 2026 年业界"RAG vs 长上下文"决策树的结论。

#### 5.4.3 AI 能力清单

| 能力 | 版本 | 说明 |
|---|---|---|
| AI 种草助手（对话式导购） | ✅ V1 | Function Calling + 多模态识图 + SSE 流式 + 会话持久化 |
| AI 内容审核 | V2 | 发布 → MQ 异步 → AI 审核 → 回写状态（违规分类） |
| Hybrid RAG 导购 | V2 | 见 5.4.1 / 5.4.2 |
| AI 种草文案生成 | V2 | 用户给素材 → 生成笔记文案 → 编辑发布 |
| 相似推荐 + 热门榜 | 热门榜 ✅ V2 / 相似推荐 ⛔ V5 未开始 | 热门榜见 5.2（zset 已落地）；**相似推荐与 ItemCF 未实现**，见 5.5 |
| 商家经营看板 + 受限 NL2SQL | ⛔ V5 未开始 | 见 9.6（**纯规划，无任何代码**） |

### 5.5 ★ 推荐系统：为什么"基于内容"而不是"协同过滤"

> 📌 **基于模拟数据的可行性重估**：100 商品 + 500 笔记 + 可模拟的用户行为，**技术上跑 ItemCF 完全可行**。
> 但 —— **100×500 的用户-物品矩阵极度稀疏**，协同过滤的效果会非常差。

**正确做法（有取舍，才高级）**：

| 方案 | 定位 | 实现 |
|---|---|---|
| **基于内容相似推荐** | **主方案** | 同品类 / 同价格带 / 同标签；笔记关联商品推荐 |
| **热门榜** | 主方案 | Redis zset（点赞+收藏+浏览加权），模拟数据下有真实热度分布 |
| **ItemCF** | **做对比实验** | 实现出来，用**离线指标（准确率/召回率/覆盖率）** 说明为什么当前规模选前者 |

> **面试口径**："我实现了 ItemCF，但在 100 商品、500 笔记的稀疏矩阵下，它的召回率明显低于基于内容的方案，所以我主用后者，并把 CF 留作数据量上来后的升级路径。"
> —— 这比"我不会 CF"或"我瞎用了 CF"都强得多。**能讲出"我做过 A、但有理由选了 B"，才是工程判断力。**

### 5.6 衔接层（项目灵魂，面试深挖点）

| 衔接 | 设计思路 |
|---|---|
| AI 导购 → 购物车 | 导购回复的商品卡片带"加入购物车"按钮，走标准接口，AI 不直接改交易数据 |
| AI 客服 → 订单 | Agent 通过工具调用订单查询接口，**只读** + 权限校验 |
| 内容 → 商品 | 笔记关联商品（种草清单），笔记详情页"去购买"跳商品页 |
| 数据分析 → 报表 | NL2SQL 仅允许 SELECT，白名单表 + 行级权限过滤（见 9.6） |

**设计原则：AI 永远不直接写交易核心数据，只通过受控接口间接影响，保证安全与可追溯。**

---

## 6. 数据库设计

### 6.1 ✅ V1 实际表（10 张，以 Flyway 迁移 `V1__init.sql` 为唯一权威）

```
-- 用户域
t_user            id, username, password_hash, nickname, avatar, role, status, created_at  [uk_username]
t_user_address    id, user_id, receiver, phone, province, city, detail, is_default        [idx_user]

-- 电商域
t_product         id, spu_name, sub_title, category_id, main_img, detail, status, created_at  [idx_status]
t_product_sku     id, product_id, sku_name, price, stock, sales, image,
                  version(★预留字段，当前【未用作乐观锁】——扣库存靠行锁 CAS，见 9.1)          [idx_product]
t_product_image   id, product_id, sku_id(可空), url, sort                                  [idx_product,sku_id]
t_cart            id, user_id, sku_id, quantity, checked, created_at                       [uk_user_sku]
t_order           id, order_no, user_id, total_amount, status, pay_type,
                  pay_time, cancel_time, version, created_at                [uk_order_no, idx_user_created]
t_order_item      id, order_id, sku_id, product_name, sku_name, price, quantity            [idx_order]

-- AI 域
t_conversation    id, user_id, biz_type, title, created_at                                 [idx_user]
t_message         id, conversation_id, role, content, extra_json, created_at                [idx_conversation]
```

> ⚠️ 早期版本规划的 28+ 张表，**仅上述 10 张为 V1 实际落地**。内容域、支付、秒杀、RAG 向量表等均为 V2+ 规划。
> ⚠️ 表名注意：收货地址是 **`t_user_address`**（非 `t_address`）。

### 6.2 V2 新增（内容域 + RAG）

```
t_note               id, user_id, title, cover, content, status(审核中/已发布/下架),
                     audit_result, hot_score, like_count, collect_count, view_count, created_at
t_note_image         id, note_id, url, sort
t_note_tag           id, note_id, tag
t_note_product       id, note_id, product_id, remark        (种草关联商品)
t_note_like          id, note_id, user_id, created_at       [uk(note_id,user_id)]
t_note_collect       id, note_id, user_id, created_at       [uk(note_id,user_id)]
t_user_follow        id, user_id, follow_user_id, created_at [uk(user_id,follow_user_id)]
t_audit_record       id, biz_type, biz_id, ai_result_json, status, reviewer, reviewed_at
t_knowledge_doc      id, title, source_type(PDF/笔记), file_url, chunk_count, status
t_knowledge_chunk    id, doc_id, chunk_index, content, embedding
```

### 6.3 V3 新增（支付 / 限量发售）

```
t_payment            id, payment_no, order_id, amount, channel, status, third_trade_no,
                     callback_body, created_at             (回调幂等依据)
t_drop_activity      id, title, product_id, drop_price, drop_stock, start_time, end_time, status
t_refund             id, order_id, amount, reason, status, handled_at
```

### 6.4 关键索引与优化点

- `t_order`：`uk(order_no)` 幂等兜底；`idx(user_id, created_at)` 分页查询。
- `t_note_like` / `t_note_collect`：**联合唯一索引，天然幂等**（防重复点赞）。
- `t_cart`：`uk(user_id, sku_id)` → 原子 UPSERT 的基础。
- ⚠️ **审核幂等键（2026-09-20 修订，见 `V5__note_audit_version.sql`）**：`t_audit_record` 唯一键为 **`uk_biz(biz_type, biz_id, biz_version, status)`**（含 `status`）——ERROR 表示"本次审核没有结论"，不该占用终态幂等位，否则一次 AI 超时就会堵死后续补审；配套 `t_note` 新增 **`audit_version`** 列（发布=1，每次重新送审/编辑后重审 +1），让幂等键能区分"同一次审核的 MQ 重复投递"与"改完内容后的新一次审核"。
- **慢 SQL 优化素材**：Feed 深分页 → 游标（`created_at < 上页末条`）替代 `LIMIT offset`；点赞数 Redis 累加、定时批量落库。

---

## 7. 目录结构

### 7.1 V1 单体结构（✅ 当前实际）

```
ai-mall/
├── frontend/                    # Vue3 + Vite（src 共 18 文件）
│   └── src/
│       ├── views/               # 9 页：Login/Home/ProductDetail/Cart/Orders/Chat/Mine/AddressManage
│       ├── stores/              # Pinia: auth.js / cart.js
│       ├── api/                 # request.js(拦截器) + index.js(API 汇总)
│       ├── router/              # 8 条路由 + 登录守卫
│       └── voice/               # voiceStage.vue(Live2D) + useAudioQueue.js(TTS 队列)
├── backend/src/main/java/com/aimall/
│   ├── common/                  # R/ResultCode/BusinessException/GlobalExceptionHandler/PageQuery/PageResult
│   ├── config/                  # AiConfig/SaTokenConfig/PasswordConfig/WebConfig
│   ├── user/                    # 注册登录/当前用户/收货地址
│   ├── goods/                   # 商品/SKU/商品图集/购物车(京东模式)
│   ├── order/                   # 订单(状态筛选/Drawer明细)
│   ├── ai/                      # 会话管理/AI问答(搜索工具+识图+SSE)
│   └── voice/                   # 派蒙 TTS 引擎（自包含 VITS:9944 + 降级 Edge-TTS）
├── sql/
├── scripts/smoke-test.ps1       # 9 项冒烟
└── voice-tts/                   # 派蒙 VITS Python sidecar
```

### 7.2 V4 服务化结构（仅拆 AI）

```
ai-mall/
├── gateway/          # Spring Cloud Gateway
├── ai-service/       # ★ 唯一独立的服务：ChatClient / RAG / 向量检索 / API Key
├── mall-app/         # 仍为单体：user / content / goods / order
└── common/           # DTO、工具、Feign 接口
```

---

## 8. 演进路线（重构版）

### 8.1 V1 —— MVP：单体电商闭环 + AI 导购 ✅ 已完成

- 后端：Spring Boot 3.4.5 + 原生 MyBatis（XML）+ MySQL 8 + Sa-Token + 统一返回/全局异常。
- 前端：Vue3 + Element Plus + Pinia，商城/详情/购物车/订单/AI 助手。
- 购物车（京东模式）、商品多图、订单筛选、AI 助手（Function Calling + 识图 + SSE + 会话持久化）。
- 派蒙语音动效助手（可选，资产受限）。
- 表：10 张（见 6.1）。冒烟 9/9 通过。

### 8.2 V1.5 —— 工程基建补全（约 1 周）★ 最高性价比

> **理由**：不学新中间件，但让项目从"能跑"变成"能维护"。这一步做完，**玩具感基本消失**。
> 它同时是 V2 的地基（先有 Flyway，建表才有版本管理；先有对象存储，笔记图片才有地方放；先有测试，V2 代码才有保障）。

| # | 项 | 内容 |
|---|---|---|
| 1 | **Flyway** | 原手工 `sql/init.sql` 改造为 `V1__init.sql`，后续变更走版本化迁移 |
| 2 | **可观测性** | `traceId` 全链路透传（MDC + 拦截器 + 异步线程池传递）+ 结构化日志 + Actuator 健康检查/指标 |
| 3 | **对象存储** | `StorageService` 抽象 + MinIO 实现 + **本地文件系统降级实现**（未配 MinIO 时自动回退） |
| 4 | **测试** | JUnit5 + Mockito 单测；Testcontainers 集成测试（可选） |
| 5 | **Docker Compose** | mysql / redis / rabbitmq / minio 一键起 |
| 6 | **CI** | GitHub Actions：build → test → package |

### 8.3 V2 —— 内容社区 + AI 审核 + Hybrid RAG（3~4 周）★ 补齐项目灵魂

- **内容社区**：笔记发布/列表/详情/点赞/收藏，MySQL 全文索引搜索（不上 ES）。
- **AI 内容审核（MQ 异步）**：发布 → MQ → AI 审核 → 回写。**MQ 的第一个真实理由。**
- **Hybrid RAG 导购**：语料 = 说明书 + 种草笔记；向量 + BM25 混合；Agent 决定调 SQL 工具还是文档检索。
- **AI 种草文案生成**：给素材 → 生成文案 → 编辑发布。
- 面试可讲：RAG 全流程与**结构化/非结构化分工**、Hybrid 检索原理、MQ 异步审核的幂等与可靠。

### 8.4 V3 —— 支付 + Redis + 高并发治理（3~4 周）

- **支付闭环**：沙箱支付 → 回调验签 → **幂等** → 状态机流转（补齐商业闭环）。渠道选型（个人沙箱要不要接）见 5.3.2。
- **Redis**：商品详情缓存（缓存三兄弟真实案例）+ 点赞计数 + 热门榜 zset + 分布式锁。
- **RabbitMQ 延迟消息**：订单 30 分钟未支付自动取消（比定时轮询优雅，可讲对比）。
- **限量发售防超卖**：Redis 原子预减 + 行锁 CAS 兜底 + MQ 异步。
- **AI 成本治理**：接口限流 + token 统计 + 降级文案。

### 8.5 V5 —— 可选加分（量力而行）⛔ **未开始**

> ⛔ **截至 2026-09-19，V5 全部未实现**（项目已暂停演进）。以下两条**只作面试时的"下一步规划"讲**，绝不能说得像做过。

- 商家经营看板 + **带护栏的 NL2SQL**（见 9.6）。
- 相似推荐 + 热门榜，并**实现 ItemCF 做离线对比**（见 5.5）。—— 注：**热门榜已单独在 V2 落地**（zset 增量刷新 + 定时全量重建），此处指的是"相似推荐"部分。

### 8.6 V4 —— 服务化：AI 服务独立（2~3 周）★ 只做基础微服务

> ⚠️ **原设计"拆 5 个服务"是缺乏业务驱动的。** 2026 年业界早已过了"为拆分而拆分"的阶段。

- Gateway + Nacos + OpenFeign；**仅拆出 `ai-service`**，主体保持单体。
- **唯一站得住的拆分理由 —— AI 服务必须独立**：

| 理由 | 说明 |
|---|---|
| **耗时差异** | AI 调用是秒级~几十秒，普通接口毫秒级。放一起会**拖垮 Tomcat 线程池** |
| **故障隔离** | 模型超时/限流/供应商挂了，不能让下单也不可用 |
| **独立扩容** | AI 是调用成本密集，需独立扩缩 |
| **密钥隔离** | API Key 只存在于 AI 服务，泄露半径最小 |
| **模型可替换** | 换模型/供应商只改这一个服务 |

- ★ **必须能讲"为什么不拆其余"**："用户/商品/订单事务耦合紧密，拆开要引入分布式事务，当前业务量不划算。这正是我把它们留在单体里的原因。"
  **能讲出"不做什么"和"为什么不做"，比堆五个服务高级得多。**

---

## 9. 关键技术方案细节（面试深挖预案）

### 9.1 ★ 防超卖：术语修正（务必按此口径讲）

> ⚠️ **原文档写"乐观锁"是错的。** 已核实代码：`t_product_sku.version` **只是预留字段，未参与版本比对**。

```sql
UPDATE t_product_sku
   SET stock = stock - #{n}, sales = sales + #{n}
 WHERE id = #{id} AND stock >= #{n}
```

**原理**：InnoDB 对 `UPDATE` 命中的行自动加**排他锁**（行锁），配合 `stock >= ?` 谓词做**条件原子更新**——
这是 **CAS 思想**，由数据库保证原子性，**无需应用层重试，也无需 version 字段**。

| 说法 | 正误 | 风险 |
|---|---|---|
| "行锁 + `stock>=?` 条件更新（CAS 思想）" | ✅ 正确 | — |
| "乐观锁" | ❌ 错 | 被追问"版本号在哪比对"答不上来 |
| "CAS 式乐观扣减" | ⚠️ 含糊 | "乐观"二字易引误会，建议去掉 |

### 9.2 支付流程（沙箱，V3）

```
用户下单 → 本地创建订单(待支付) + payment 记录(待支付)
→ 调沙箱创建预支付 → 返回支付参数 → 前端收银台
→ 用户支付 → 平台异步回调 → 验签(密钥/证书)
→ 按 payment_no 幂等处理(状态机校验) → 更新支付单 + 订单
→ 发 MQ 事件 → 后续业务(发券/通知)
```

要点：回调必须**验签 + 幂等**，并以"主动查单对账"兜底（防通知丢失）。

### 9.3 缓存三兄弟（V3 真实案例）

- **穿透**：恶意刷不存在的笔记 ID → 空值缓存 + 布隆过滤器。
- **击穿**：热门商品缓存过期瞬间 → 互斥锁重建（Redisson `tryLock`）。
- **雪崩**：大量 key 同时过期 → 随机过期时间 + 热点数据永不过期 + 异步刷新。

### 9.4 消息可靠性（V3）

- **不丢**：生产者确认（publisher confirm）+ 持久化 + 消费端手动 ACK。
- **不重**：消费端幂等（唯一键 + 状态校验），如 `uk(note_id, user_id)` 天然幂等。
- **延迟消息**：订单超时取消用 **RabbitMQ 延迟消息 / 死信队列**，对比定时轮询的优劣。

### 9.5 Feed 流与深分页

- **拉模式（默认）**：查关注表 → in 查询笔记时间线 → **游标分页**（`created_at < 上次位置`）。
- **推模式对比**：发笔记时写粉丝信箱（Redis List）；热点大 V 用拉模式防"写放大"。
- 双模式结合 + 冷热用户分治，说清取舍即可。

### 9.6 受限 NL2SQL 的安全护栏（V5 可选）

> ⚠️ 裸奔的 Text2SQL 是**减分项**，不是加分项。必须加护栏。

| 护栏层 | 措施 |
|---|---|
| 身份 | 仅**商家端**可用，需角色鉴权 |
| 范围 | **白名单表** + 行级过滤（强制注入 `shop_id = 当前商家`） |
| 语句 | 仅 `SELECT`；执行前 **AST 解析校验**（JSqlParser），禁止跨表子查询/系统表 |
| 资源 | **只读数据库账号** + 超时 + 结果行数上限 |
| 兜底 | 失败降级为"预设看板指标"，不暴露错误 |

### 9.7 分布式事务选型（若 V4 后确需）

- 推荐**本地消息表（最终一致性）**起步：下单事务内写消息表 → 定时投递 → 下游消费幂等。简单、可控、好讲。
- Seata AT 作对比：适用短事务、侵入低，但性能损失与全局锁需权衡。
- **面试答案模板**：资金类用强一致，通知类用最终一致。

---

## 10. 【2026 技术时效体检】

| 技术 | 本项目 | 2026 现状 | 建议 |
|---|---|---|---|
| Spring Boot | 3.4.5 | **3.5+ / 4.0**（4.0 于 2025-11 发布，Spring Framework 7，支持 Java 25） | 3.4.5 **不算过时**（稳、资料多）。有余力升 3.5.x；**不要为追新在求职前大改** |
| Java | 17 | **21 是标配**（虚拟线程已普及），25 已支持 | 17 是最低底线，够用。**会讲虚拟线程是加分项**，非必须升级 |
| MyBatis 手写 XML | ✅ | 企业实际 MP 更多，但手写完全正当 | ✅ 保持。理由：SQL 可见可控、便于 `explain` 与慢 SQL 优化 |
| Sa-Token | ✅ | 企业主流仍是 Spring Security；Sa-Token 在国内中小项目常见 | ⚠️ **必须准备"为什么不用 Spring Security"**：轻量、API 简洁、登录鉴权场景够用；并说明"Security 我也了解，OAuth2/方法级鉴权是它的强项" |
| Sentinel | 计划 V3 | **Resilience4j 是 Spring Cloud 官方标准**，Sentinel 是阿里系 | 走 Spring Cloud Alibaba（Nacos+Sentinel+Seata）在国内完全正当。**知道 Resilience4j 存在即可**，别被问住 |
| RabbitMQ | 计划 V3 | 仍是主流，国内 RocketMQ/Kafka 也多 | ✅ 保持 |
| Elasticsearch | 早期写"进阶可选" | 商品搜索的事实标准 | ★ **笔记**已用 MySQL 全文索引（ngram）落地；**商品**的 LIKE 是**未完成的缺口，不是选型结论**——ES 是商品搜索（多维筛选/聚合/高亮/拼音）的正解，触发条件：十万级商品或 P99 > 200ms。**别拿"数据少"当理由**，要说"还没做，演进路径是 ES" |
| 向量库 | 早期写"MySQL 存向量" | **pgvector / Redis Stack** 为主，大规模才 Milvus | 按 5.4.2 修正 |
| 熔断限流 | 计划 V3 | 2026 已成标配 | ✅ 保留，**限流对 AI 接口是刚需**（成本） |
| RAG | 早期写"朴素 RAG" | **Hybrid RAG + Rerank + Agentic** 是生产基线 | 按 5.4.2 修正 |

---

## 11. 【完整度差距】让它"像企业级项目"的六块拼图

> 这些**不是新技术栈**，但正是"玩具项目"与"能上线的项目"的分水岭。
> 面试官看简历时，这些往往比"又多会一个中间件"更有说服力。**成本低、收益极高 → 归入 V1.5。**

| # | 缺失项 | 现在的问题 | 补法 | 为什么像企业级 |
|---|---|---|---|---|
| 1 | **数据库版本管理** | 只有手工 `sql/init.sql`，改表靠手动 | **Flyway** | 没有迁移工具 = 多人协作必炸 |
| 2 | **可观测性** | 完全没有 | `traceId` + 结构化日志 + Actuator + Micrometer | 线上排查全靠猜 = 玩具 |
| 3 | **对象存储** | 图片存本地裸路径 | **MinIO**（本地可跑，S3 兼容）**+ 本地降级** | 本地存图片，重启/扩容就全丢 |
| 4 | **测试** | 0 测试 | JUnit5 + Mockito；Testcontainers 集成测试 | 没测试的项目，没人敢让你改 |
| 5 | **环境一键化** | 手动装 MySQL/Redis | **Docker Compose** | "我本地跑不起来"是协作杀手 |
| 6 | **CI** | 无 | GitHub Actions：build → test → package | 每次提交自动验证 |

> 💡 设计原则（与 voice 模块一脉相承）：**这些基建都应"可降级、可插拔"**——
> 没配 MinIO 就自动回退本地存储，没跑 Docker 也能正常启动。**保证任何环境下项目都能跑起来。**

---

## 12. 风险与边界控制（防膨胀守则）

1. **主线唯一**：电商是主干，内容 + AI 是引擎。新增需求先问"是否服务闭环"。
2. **三问过关**（§2）：砍掉会不会出问题？是不是玩具用法？2026 还主流吗？
3. **一次只演进一层**：每版验收跑通再进下一版。
4. **AI 不直写交易数据**：只通过受控接口影响业务。
5. **伪需求清单（明确不做）**：直播、复杂优惠券/拼团、多级分销、推荐算法论文级调参、**ES 全文检索（当前数据量不需要）**、区块链类噱头、**秒杀（已改为更贴合业务的限量发售）**。
6. **版本与时间**：V1~V3 是简历底线（务必完成），V4 视时间，V5 加分项。宁可一个版本做扎实，不要五个版本全烂尾。

---

## 13. 面试口径速查：每项技术的"一句话业务理由"

> 背这个，而不是背技术清单。**每个都要能答出"不这么做会怎样"**。

| 技术 | 一句话业务理由（砍掉它会怎样） |
|---|---|
| MyBatis 手写 XML | 慢 SQL 要 `explain` 调优，SQL 必须可见可控 |
| Sa-Token | 前后端分离需要无状态登录态 |
| **行锁 CAS 扣库存** | 不加条件会超卖；注意不是乐观锁 |
| **Function Calling** | 不查库 AI 就会**编造商品和价格**——电商里这是事故 |
| **RAG（Hybrid）** | 说明书/笔记是非结构化长文本，塞不进 prompt 且持续更新；**但价格库存坚决不走 RAG** |
| **Redis** | 商品详情读多写少；笔记点赞是热点写，直接打 DB 扛不住 |
| **RabbitMQ** | AI 审核要几秒不能让用户干等；订单超时取消用延迟消息比定时任务实时得多 |
| **分布式锁** | 限量发售瞬间流量百倍，需 Redis 预减挡在前面 |
| **微服务（只拆 AI）** | AI 调用是秒级，会和毫秒级下单接口**抢线程池**；且 API Key 需隔离 |
| **Flyway** | 多人协作改表没有版本管理必炸 |
| **traceId / 可观测** | 请求跨多模块，没链路 ID 无法排查 |
| **对象存储** | 图片存本地，重启/扩容就丢 |
| **限流** | AI 调用按 token 计费，不限流成本失控 |
| **测试** | 没测试的代码没人敢重构 |

---

## 附：面试准备提纲

1. **一句话项目介绍（30 秒）**：种草社区 + 电商交易 + AI 引擎的完整闭环。
2. **三个必讲深挖点**：Hybrid RAG 导购（结构化/非结构化分工）、限量发售防超卖、支付回调幂等。
3. **对比题预案**：
   - 单体 vs 微服务（为什么 V4 **只拆 AI 服务**，其余为什么不拆）
   - 本地消息表 vs Seata
   - RAG vs 长上下文（语料动态 / C 端延迟 / 权限隔离 / 成本）
   - 拉模式 vs 推模式
   - **基于内容推荐 vs ItemCF**（为什么当前数据规模选前者）
4. **两个"最难问题"素材**：
   - 笔记量增长后审核队列堵塞怎么排查（MQ 堆积监控 + 消费端扩容 + 死信）
   - AI 调用成本飙升怎么限流降级（接口限流 + token 统计 + 兜底文案）
5. **★ 一句定心丸**：「**我做的每个技术决策都能说出"为什么做"和"为什么不这么做"。**」
